# invoice-fe Test Coverage Map

Live record of what's actually automated vs. manually verified, and when. Maintained by `functional-tester` per `.claude/CONVENTIONS.md`. Not the same as a `feature_N_*.md`'s Verification Plan (stable design intent) — this is the running log of real test execution.

Automated suite lives at `apps/invoice-fe/e2e/` (Playwright — `dashboard-outbound-split.spec.ts`, `group-a-layout-overflow.spec.ts`; a separate, larger planned suite in `docs/feature_12_fe_test_suite.md` was never built, don't confuse the two).

| Gap / Feature | Test type | Automated or manual | Last verified | Evidence |
|---|---|---|---|---|
| Gap 89 (dashboard/outbound split-screen) | E2E | Automated — `e2e/dashboard-outbound-split.spec.ts` | 2026-07-29 | 7/7 passing, see spec file |
| Gap 76 (Trainer Commit button clipped) | E2E | Automated — `e2e/group-a-layout-overflow.spec.ts` | 2026-07-31 | 15 tests, see spec file |
| Gap 81 (outbound upload never leaves UPLOADED) | Manual, real backend (worker killed/restarted mid-test) | Manual | 2026-08-02 | `test_evidence/gap81_outbound_worker_liveness.log` — root cause: no worker process supervision, not an enqueue/delivery bug |
| Gap 84 (inbound/outbound stuck at PROCESSING) | Manual, real backend (corrupted-file OCR failure induced deliberately) | Manual | 2026-08-02 | `test_evidence/gap84_stuck_processing_root_cause.log` — root cause: `handle_process_invoice`/`handle_process_outbound_invoice` never persist a terminal DB status on exception |
| Gap 72 (PDF viewer: wrong PDF / zoom / rotate) | Manual, real non-headless Chromium (Playwright `headless:false`) | Manual | 2026-08-02 | `test_evidence/gap72_pdf_viewer/` (4 screenshots + README) — wrong-PDF ruled out, zoom works, rotate confirmed broken with root cause |
| Gap 82 (Auditor Review click-to-edit) | Manual, real non-headless Chromium, all invoice states (AUDIT_REQUIRED/COMPLETED/REJECTED) | Manual | 2026-08-03 | `test_evidence/gap82_click_to_edit/` (4 screenshots + README) — does NOT reproduce as a broken handler; click-to-edit works correctly on every editable state, correctly disabled on resolved invoices |
| Gap 83 (Outbound Audit redundant tab row) | Manual, real non-headless Chromium | Manual | 2026-08-03 | `test_evidence/gap83_outbound_review_double_shell/` (4 screenshots + README) — root cause: `app/invoices/outbound-review/[id]/page.tsx` still double-wraps in `<Shell>`, the Gap 71 bug class, never fixed on this file |
| Gap 96 (Connectors "Configure" button) | Manual, real non-headless Chromium, real Google OAuth client | Manual | 2026-08-03 | `test_evidence/gap96_connectors_flow/` (4 screenshots + README) — does NOT reproduce; Configure link and Connect/OAuth flow both work correctly. Recommend closing as described |
| Gap 105 (new — Connectors swallows broken-connection errors as "empty folder") | Manual, real backend + DB fault injection (fake-but-valid-format token) | Manual | 2026-08-03 | `test_evidence/gap96_connectors_flow/` (screenshot 4 + `be_502_log_excerpt.txt`) — `FolderTreeExplorer.tsx::fetchFiles()` discards fetch errors, real 502 renders identically to a genuinely empty folder |
| Gap 85 (Dashboard Command Center title crowding) | E2E width sweep (1024-1600px) | Automated — `e2e/group-a-layout-overflow.spec.ts` | 2026-07-31 | Not reproducible across sweep; blocked on reporter's actual viewport/screenshot. Not re-swept 2026-08-03 (no new angle available) |
| Gap 179 (Connectors OAuth "not working" — likely = Gap 131) | Manual, real backend, real dev Google/Salesforce OAuth client IDs, Playwright headless nav to real authorize URLs | Manual | 2026-08-11 | `apps/invoice-be/docs/test_evidence/gap131_179_oauth_dev_verification/` (shared with BE Gap 131 — 2 screenshots + README + log) — CONFIRMED-FIXED: no redirect_uri_mismatch on either provider in dev; full token exchange not completable by an automated agent (same limitation as Gap 96) |
| Gap 152 (Settings → Security route 404?) | Manual, Playwright headless, nav-link click + direct URL | Manual | 2026-08-11 | `test_evidence/gap152_settings_security/` (3 screenshots + README) — CONFIRMED-FIXED: route resolves cleanly both ways, no 404 |
| Gap 180 (Chat "Find Thread" inconsistent) | Manual, real backend, Playwright headless, real create/rename/delete/search on live chat sessions | Manual | 2026-08-11 (Checkpoint 2 retest) | `test_evidence/gap177_checkpoint2_retest/` (12 screenshots + README + network/console logs + DB query output) -- CONFIRMED-CLOSED: Checkpoint 1 (`test_evidence/gap180_chat_find_thread/`) root-caused this as a Gap 177 symptom; Checkpoint 2 retested live against the shipped Gap 177 fix -- delete now returns 204, thread leaves the sidebar immediately, and an identical search performed right after a delete (no reload) correctly returns zero matches. Tracker entry closed `[x]` on this evidence. |
| Gap 177 fix retest (chat thread delete + webhook delete, `proxyJson()` null-body 204/205/304 fix) | Manual, real backend, Playwright headless, real create/delete on live chat sessions and webhook subscriptions | Manual | 2026-08-11 (Checkpoint 2) | `test_evidence/gap177_checkpoint2_retest/` -- CONFIRMED-FIXED: all `DELETE /chat/sessions/{id}` and `DELETE /webhooks/{id}` calls return 204 (previously 500), UI updates immediately without a reload in every case, direct Postgres queries confirm the rows are actually gone. "Delete Chat" button rename (was "Clear Chat") also confirmed live. Incidental unrelated finding filed in the evidence README, not fixed here: chat thread rename (`PUT /api/chat/sessions/{id}`) 405s -- no such route handler exists, `useChatSession.ts::renameSession` silently falls back to a client-state-only rename that never persists. |
| Gap 143 (Subscriptions nav entry + real usage tracker) | Manual, real backend, real Postgres, Playwright headless, mock-auth role/tenant-state switching (test_viewer bearer token + direct SQL tenant plan flips) | Manual | 2026-08-12 (Checkpoint 4) | `test_evidence/gap143_checkpoint4_retest/` (6 screenshots + README + network_responses.txt) -- CONFIRMED-FIXED: Admin sidebar Subscriptions item present and navigates with correct single-row highlighting; Viewer role sees neither Settings nor Subscriptions; free-tier usage bar (13/50 used, 37 remaining) reconciles exactly against a live DB row and `DEFAULT_FREE_INVOICES_LIMIT`; paid tier renders "Not metered on this plan"; simulated-500 error state renders "Usage unavailable", not a silent zero; `e2e/rbac-sidebar.spec.ts` passes 19/19 once the real backend is not sharing the default port with the spec's own isolated dev server (documented as an environmental gotcha for future runs, not a code defect). |
| FE Gap 218 (Approve Invoice button label) | E2E | Automated — `e2e/audit-review-console.spec.ts` | 2026-08-12 | 10/10 passed in gap-resolution Playwright run |
| FE Gap 219 (Autopilot folder browser) | E2E | Automated — `e2e/autopilot-folder-browser.spec.ts` | 2026-08-12 | Browse → Select This Folder on Autopilot tab; mocked connector APIs |
| FE Gaps 220–221 (Trainer restructure + Response Style) | Unit (tsc) | Automated — `npx tsc --noEmit` | 2026-08-12 | UI wired in `TrainerControlBar.tsx`, `ChatResponseStylePanel.tsx`, `app/trainer/page.tsx`; dedicated trainer e2e for style save deferred |
| FE Gaps 232–238 (Feature 14 — alert-anchored Trainer + chat correction lane) | E2E + type check + live | Automated (`e2e/trainer-alert-anchored.spec.ts`, `trainer-loading-state.spec.ts`, `group-a-layout-overflow.spec.ts`, `rbac-sidebar.spec.ts`) + **Manual, real unstubbed backend** | 2026-08-17 | `npx tsc --noEmit` clean. Full suite 49/64 passed on first (parallel) run — re-run single-worker: 21/22 passed on Trainer-scoped specs (only failure = pre-existing unrelated Gap 86), 17/19 then 1/1 clean on rbac-sidebar's Feature-14-specific test. Live (unstubbed) checks: `can_train=false` permission gate and the real vendor-invoice picker (real alert counts) both confirmed against the real running backend. `test_evidence/feature14_trainer_redesign_2026-08-17/README.md` |
| FE Gap 86 (Ingestion receive-only toggle) — re-confirmed pre-existing/unrelated to Feature 14 | E2E | Automated, independently re-checked | 2026-08-17 | `git diff --stat` against the uncommitted Feature 14 changeset shows zero files under `app/ingestion` or any `PageHeader` component — confirms the developer's "pre-existing, unrelated" claim rather than accepting it on trust |
| Feature 15 (Help Center — Knowledge Base Guides tab, search, tab switcher) — **unmerged branch `feature/contact-us-and-support-tickets` @ `fc48ef0`** | E2E | Automated — `e2e/help-support.spec.ts` (tests 1-3 of 6) | 2026-08-18 (documented this pass; not independently re-run) | `/help` opens on Guides tab by default (`#tab-btn-guides` `aria-selected=true`, `#help-guides-container` + `#help-guide-search` visible); typing in the search box filters visible guide topics (`#guide-item-overview` still visible for "trainer"); clicking the "AI Support Assistant" tab switches `aria-selected` and renders `#support-chat-window`, `#direct-raise-ticket-btn`, `#prompt-chip-0`. These 3 cases make no backend calls at all (guides content is static/client-side) |
| FE Gap 243 / BE Gap 254 — live low-confidence vs incident cards | Manual live | Local invoice-fe `/help` + invoice-be, tenant `test 413 co.` | 2026-08-19 | Miss `how do I wash my car?` → *I couldn't find a specific help article* + neutral *Didn't find an answer? / Raise a Ticket*; modal subject pre-filled, priority Normal. 504 chip → red *Issue Diagnosis* + ticket subject *System Timeout / Batch Sync Failure (Error 504)*, priority Urgent. Plan example "bulk upload 200 invoices" is **not** a miss (matches `ingestion_upload`). |
| Feature 15 (AI Support Assistant chat, smart escalation, direct-raise-ticket modal) — same branch | E2E, **UI-level only — see caveat** | Automated — `e2e/help-support.spec.ts` (tests 4-6 of 6) | 2026-08-18 (documented) | Test 4: typed chat message -> assistant response text rendered in `#support-chat-messages`. Test 5: clicking the "504 gateway timeout" prompt chip renders `#smart-escalation-card`; clicking Escalate opens `#support-ticket-modal-overlay` with `#ticket-subject` pre-filled from `escalation_context.subject`; submitting shows `#ticket-success-card` containing the returned `ticket_number`. Test 6: `#direct-raise-ticket-btn` in the chat header opens a blank modal; filling subject/description and submitting shows the success card with the returned `ticket_number`. **Caveat -- confirmed by reading the spec: every one of these 6 tests stubs the backend via Playwright `page.route()` (`/api/auth/me`, `/api/settings/service-flow`, and for tests 4-6 also `/api/support/chat` and/or `/api/support/ticket`). The `POST /api/support/ticket` mock in tests 5 and 6 returns hardcoded literal ticket numbers (`TICK-2026-9812`, `TICK-2026-1122`) that are never generated by `routers/support.py` and never written to a real database. This is UI-level coverage only -- no FE-to-BE integration coverage exists for this flow. Do not treat this suite as evidence that the real backend, the real ticket-number generator, or a real DB-persisted `SupportTicket` row work end-to-end.** |
| FE Gap 277 (Record Inbound Invoice as Paid from Queue Table) | E2E | Automated — `e2e/inbound-mark-paid.spec.ts` | 2026-08-20 | 2/2 passed. Verifies "Mark as Paid" action appears on COMPLETED/active invoices, is hidden on PAID invoices, and clicking sends `PUT /api/audit/resolve/{id}` with `status: PAID` after confirmation. |
| *(remaining gaps — populate as functional-tester runs scenarios)* | | | | |
