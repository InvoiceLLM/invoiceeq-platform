# Frontend Features Progress Tracker

This document tracks the implementation progress of all frontend features for the `invoice-fe` Next.js client, including theme, layouts, and page routing. Feature spec files (`feature_1..7_*.md`) describe the target design only — every open item and pending build task is tracked here instead, so status doesn't drift out of sync across files.

**Current Status (reconciled 2026-07-31):** 13 of 14 tracked features complete (Features 1–8, 10, 11, 2.1, 3.1, and 4.1); Feature 9 (Webhooks Settings, FE) is the only remaining spec-only feature — note this is distinct from the BE's Feature 15 (Outbound Webhooks), which is fully built; the FE settings screen for managing subscriptions hasn't been built yet. Feature 2.1 (Outbound Dashboard split-screen) was built 2026-07-29 on a separate branch (`new-changes-29/07`, merged 2026-07-30) — `OutboundMetricsGrid.tsx`, the metrics proxy route, conditional split-layout wiring, and the direction-agnostic `NeedsAttentionWidget` — and brought this repo's first real Playwright suite with it (`npm run test:e2e`, 7/7 passing, API-mocked page tests). Gaps 28/67/59 are confirmed closed. **29 gap items (68–97) logged from two independent live-testing passes 2026-07-29** — 68 and 71 fixed same day; 73 and 98 fixed 2026-07-30. **Group A (layout/viewport overflow) closed 2026-07-31: Gaps 86, 69, and 76 fixed and regression-tested; Gap 85 investigated and NOT reproduced across a 1024–1600px width sweep, so it stays open pending the reporter's actual viewport size rather than receiving a speculative fix.** That leaves **23 open** — 70, 72, 74, 75, 77, 78, 79, 80, 81, 82, 83, 84, 85, 87, 88, 90, 91, 92, 93, 94, 95, 96, 97 — (**Gap 99 completed `[x]` on 2026-08-03**: Gated app page routes in `apps/invoice-fe/middleware.ts` using Clerk's `auth().protect()`, excluding the public `/flows` simulator. FE RBAC is fully code-complete, automated-test-verified, and page-level routes are now secured. **Gap 87's Help-button finding also shipped in the same batch** — the search box and notification bell are untouched, so Gap 87 itself stays open.) plus Gap 89 partial and Feature 9 unbuilt, all catalogued with root causes below. **New Gap 99 (2026-07-31): FE role-based access control doesn't exist — `useAuth()` is fully mocked (`localStorage`, defaults everyone to Admin) and `Sidebar.tsx` shows every nav item to every user regardless of role.** Per-group implementation plans for every remaining gap were planned to live in `docs/guides/fe_gap_plan_group_{a..h}_*.md` — never actually committed to source control, dead reference, noted here rather than left silently broken. A second, broader Playwright test plan (`feature_12_fe_test_suite.md`, real-backend E2E rather than API-mocked) is drafted but not yet built — complementary to, not redundant with, the mocked suites. Feature 5 (Chat) completed 2026-07-22; Feature 6 (AI Trainer) frontend completed 2026-07-22, merged alongside its backend (Feature 10) 2026-07-24. **This summary paragraph is stale past this point — it predates Gaps 100–109; see the gap entries themselves below for current status, not this line.** **Gap 109 (2026-08-04, fixed same day): live Azure dev environment showed only 3 Sidebar items for every user regardless of real role** — six independently-stacked bugs across invoice-website's proxy config, invoice-fe's token minting, and a broken Clerk Dashboard JWT Template shortcode; see the full entry below for the complete root-cause chain.

---

## Feature Tracker

- `[x]` [Feature 1: Global Theme & Core Shell Layout](feature_1_layout_theme.md)
- `[x]` [Feature 2: Dashboard Analytics Command Center](feature_2_dashboard.md)
- `[x]` [Feature 3: File Ingestion Portal & Active Tagging — **NOVA**](feature_3_ingestion.md)
- `[x]` [Feature 13: Tenant Autopilot — Ingestion & Scheduled Sync](feature_13_autopilot.md) — completed 2026-08-12. Unified manual invoice upload + scheduled folder sync (Google Drive or Salesforce), Autopilot tab + config form in `/ingestion`, `AutopilotHistoryTable` component with polling, API endpoints, backend sync engine, ACA Job script, and 18/18 pytest test suite. **Correction 2026-08-21 — read the "18/18 pytest suite" above with care**: this feature was marked complete and green while it had **never once worked from a browser**. Two defects dating from this original build were found in live testing on 2026-08-21 and are filed as **FE Gap 278** (the three `/api/autopilot/*` proxy routes were never built, so every call 404'd at Next.js) and **BE Gap 288** (`run_sync()` looked up the OAuth connection under the wrong provider name, so every Google Drive sync failed). The suite passed throughout because it drives FastAPI directly via `TestClient`, bypassing Next.js entirely — so it could not see Gap 274 — and because its own fixture stored the connection under the same wrong name the buggy query used, so it could not see Gap 282 either. A green suite is evidence about the code it actually exercises, not about the feature being usable end-to-end.
- `[x]` [Feature 4: Split-Screen Auditor Review Console — **SENTINEL**](feature_4_auditor.md)
- `[x]` [Feature 5: Semantic Chat Assistant & SQL Audit Drawer — **SAGE**](feature_5_chat.md)
- `[x]` [Feature 6: AI Trainer Interactive Sandbox — **EVOLVE**](feature_6_trainer.md)
- `[x]` [Feature 7: Third-Party Connectors & Explorer View](feature_7_connectors.md) — implemented 2026-07-28: API proxy routes under `/api/connectors/*`; `IntegrationCard.tsx` (provider states, connect/disconnect OAuth redirects, folder mappings); `FolderTreeExplorer.tsx` (expandable mock paths, multiselect, background queue bulk import triggers); sub-page at `/settings/connectors`; settings main page links successfully. TypeScript: 0 errors.
- `[x]` [Feature 8: Email Setup](feature_8_email_ingestion.md) — implemented 2026-07-28; **redesigned 2026-08-10**: global mailbox + inbound/outbound authorized sets; Gap 147 superseded. **Gap 125 FE**: staff-notify multi-select on review screens (never customer email).
- `[x]` [Feature 9: Webhooks Settings](feature_9_webhooks.md) — implemented 2026-08-04: Next.js API proxy routes under `/api/webhooks/*`; `/settings/webhooks` configuration subpage; CRUD support, active status toggling, error display, and secure signature key reveal; Settings page linked successfully. TypeScript: 0 errors.
- `[x]` [Feature 10: Settings Screen](feature_10_settings.md) — implemented 2026-07-28: `app/api/settings/service-flow/route.ts` (GET+PUT proxy → BE `/settings/vendor-flow`); `components/settings/ServiceFlowToggles.tsx` (Receive/Send switches, outbound sender email, Admin-only enforcement, Combined Pro upgrade modal, client-side email guard); `app/settings/page.tsx` (Service Flow toggles + Connectors/Email Ingestion & Delivery sections, each now linking to their own real sub-pages once Features 7/8 landed same day, below; only Webhooks remains a "coming soon" chip); Sidebar `/settings` link was already present. Manual tests: `tests/manual/test_settings_service_flow.md` (8 cases). TypeScript: 0 errors.
- `[x]` [Feature 3.1: Service Flow — Send Invoices Tab — **NOVA**](feature_3.1_vendor_flow_ingestion.md) — built 2026-07-29; extends Feature 3. All 4 tasks done (2 deviations from the original plan, noted in the doc); `tsc --noEmit` clean; full manual click-through against a live backend still outstanding.
- `[x]` [Feature 4.1: Service Flow — Outbound Auditor Tab — **SENTINEL**](feature_4.1_vendor_flow_auditor.md) — built 2026-07-29; extends Feature 4. All 5 tasks done (1 scope note re: overdue banner placement); `tsc --noEmit` clean; full manual click-through against a live backend still outstanding.
- `[x]` [Feature 2.1: Service Flow — Outbound Dashboard (Split-Screen)](feature_2.1_vendor_flow_dashboard.md) — built 2026-07-29; extends Feature 2. All tasks done: new `OutboundMetricsGrid.tsx`, new `app/api/dashboard/outbound-metrics/route.ts` proxy, conditional split-layout in `app/dashboard/page.tsx` gated on `/api/settings/service-flow` (receive-only unchanged, send-only single grid, both = two-column split stacking on narrow viewports), `NeedsAttentionWidget.tsx` extended to merge outbound `NEEDS_REVIEW` rows with inbound `AUDIT_REQUIRED` ones, and `top_customers` wired into `ClientPerformanceChart.tsx`. `next build` clean. **First Playwright suite in this repo** (`@playwright/test` 1.62.0, `npm run test:e2e`) — 7/7 passing in `e2e/dashboard-outbound-split.spec.ts`, covering all three toggle branches, the narrow-viewport stack, the per-direction review links, and the no-combined/net-figure rule. Two deviations noted in the doc: `ClientPerformanceChart.tsx` gained optional `title`/`subtitle` props (its heading was hardcoded, so the doc's "imported-not-edited" premise didn't hold, and two identically-titled ranking panels would be ambiguous to a screen reader), and Task 2.1.6's call to remove `FilterBar` was corrected — it contradicted the same doc's Functionality section and FilterBar is intentionally kept. Playwright specs stub all `/api/**` calls, so a full manual click-through against a live backend is still outstanding.
- `[x]` [Feature 11: System Flow Visualization](feature_11_flows_visualization.md) — backfilled 2026-07-27; found already built and undocumented at `app/flows/page.tsx`, a standalone animated demo/explainer diagram, not a functional screen or part of Service Flow's actual implementation (two of its four tabs visualize Service Flow's design, clearly marked spec-only)
- `[x]` [Feature 15: Help Center Knowledge Base & AI Support Assistant with Direct Ticket Escalation](feature_15_help_center_support_bot_and_tickets.md) — built 2026-08-17. `/help` is now a dual-tab hub: the pre-existing illustrated Knowledge Base guides open by default and are searchable, plus a new SAGE AI support assistant tab (`SupportChatWindow.tsx`) with escalation cards and a direct ticket modal (`SupportTicketModal.tsx`) routing alerts to `Application@infinevocloud.com` via BE Feature 19 — see Gaps 246/247/248 below. **One deviation from the spec**: the assistant does **not** stream and is not RAG-backed — `SupportChatWindow.tsx` makes a single non-streaming `POST /api/support/chat`, and the backend answers from a hardcoded keyword-matched knowledge base.
- `[x]` [Feature 20: Enterprise Observability & Client-Side RUM](../../invoice-be/docs/feature_20_observability_monitoring_alerts.md) — implemented 2026-08-17: `@microsoft/applicationinsights-web` integrated via `AppInsightsProvider.tsx` wrapping `RootLayout` in `app/layout.tsx`. Automatic route tracking, exception capture, and distributed correlation headers (`X-Request-ID`, `X-Trace-ID`). The connection string is `NEXT_PUBLIC_APPINSIGHTS_CONNECTION_STRING`, inlined at image build time via a `Dockerfile.fe` build-arg (a runtime container env var would be invisible to the browser bundle — same constraint as Gaps 6/172).


- `[x]` [Feature 16: Admin Console & Organization Debug Tooling](feature_16_admin_console.md) — spec doc written and task closed on 2026-08-20. Landed via commit `99c395b` ("Cherry-pick auth-feature-4: forgot-password, admin console, website deploy infra") with full user administration, optimistic permission toggling, inbound email drops auditing, and Clerk debug tool integration. File coordinates:
  - `app/admin/page.tsx` (846 lines) — Admin console page, role-honesty checks, and permission controls.
  - `app/api/admin/create-user/route.js` (174 lines) — Next.js POST endpoint to provision users into the active Clerk organisation.
  - `app/api/admin/users/[userRef]/route.ts` (111 lines) — Next.js DELETE endpoint executing the two-step backend detach + Clerk delete revocation flow.
  - `app/debug-org/page.tsx` (81 lines) — debug page for inspecting Clerk memberships (debug-only).

---

## Feature 6 Completion Summary (2026-07-22)

Feature 6 frontend was fully implemented and design-refined in session 2026-07-22.
All 6 open gaps are now closed. Below is a summary of every file delivered:

| File | Task | Status |
|---|---|---|
| `components/layout/Sidebar.tsx` | Added `/trainer` nav link (GraduationCap icon) | ✅ Done |
| `lib/trainer-service.ts` | Service layer: data models + 6 methods (live API calls via `/api/trainer/*` as of 2026-07-23) | ✅ Done |
| `components/trainer/ScopeSelector.tsx` | 3-way Global / Existing Vendor / New Vendor tab selector | ✅ Done |
| `components/trainer/TrainerUploader.tsx` | Scope-conditioned vendor dropdown + drag-and-drop PDF uploader | ✅ Done |
| `components/trainer/PdfViewerPanel.tsx` | PDF viewer canvas + Global empty state card + session-loading progress panel (three modes, Gap 139) | ✅ Done |
| `components/trainer/QnAPanel.tsx` | Chat Assistant + Variables & Rules Inspector (2-tab panel) | ✅ Done |
| `components/trainer/CommitModal.tsx` | Scope-aware registry commit modal with re-audit notice | ✅ Done |
| `components/trainer/RuleHistoryDrawer.tsx` | Rule version history drawer + rollback confirmation | ✅ Done |
| `app/trainer/page.tsx` | Main page orchestrator: state mgmt, toast notifications, audit deep-link | ✅ Done |

**Design Refinements Applied (2026-07-22):**
- Premium glassmorphism surfaces with `backdrop-blur` on all panels
- Per-scope color glow rings on `ScopeSelector` (blue / emerald / purple)
- Animated drag-pulse overlay on `TrainerUploader` dropzone
- Dual ambient radial glow orbs on `PdfViewerPanel` Global empty state
- Gradient message bubbles (blue gradient for user, glass dark for AI) in `QnAPanel`
- Three-dot `animate-bounce` typing indicator replacing plain pulse dots
- Empty chat state with `Zap` icon prompt in `QnAPanel`
- Scope-adaptive confirm button colors in `CommitModal` (blue / emerald / purple)
- Active bottom indicator line on `ScopeSelector` active tab

**Backend Integration Completed (2026-07-23):** `trainer-service.ts` now calls the live backend through same-origin proxy Route Handlers added under `app/api/trainer/**`; the mock datasets were removed. Wiring:
- `GET /api/trainer/vendors` → `getTenantVendors()`
- `POST /api/trainer/sessions/global` → `startSession("global")` (multipart, PDF optional)
- `POST /api/trainer/sessions/from-production?vendor_name=X` → `startSession("existing_vendor", ...)`
- `POST /api/trainer/upload` → `startSession("new_vendor", ..., file)`
- `POST /api/trainer/sessions/{id}/chat` → `sendChatMessage()`
- `POST /api/trainer/sessions/{id}/commit` → `commitSession()` (wired into `CommitModal` `onConfirm` / `handleConfirmCommit`)
- `GET /api/trainer/templates/history?scope=&vendor_name=` → `getRuleHistory()`
- `POST /api/trainer/templates/{id}/rollback/{version}` → `rollbackTemplate()` (wired into `RuleHistoryDrawer` `onRollback`)

`page.tsx`'s `handleConfirmCommit` and `handleRollback` now perform real network calls with success/error toasts, and New-Vendor sessions start empty until a PDF is uploaded. Matching backend: `be_features_tracker.md` Gaps 1b, 5, 6, 8, 29 (Feature 10).

---

## Open Items / Gaps

Gaps below are grouped by the feature file whose target design they still need to catch up to.

**Ingestion portal** ([feature_3_ingestion.md](feature_3_ingestion.md)):
- `[x]` **Gap 1: Directory Watcher (Bulk Processing)** — Fixed Jul 27, 2026. Added a "Bulk Directory Scan" card to `app/ingestion/page.tsx` — a folder-path text input + Scan button, calling the new `POST /api/invoices/watcher` proxy route (`be_features_tracker.md` Gap 12). Successful scans feed the found job_ids into the existing `StatusTable`, same as a normal upload; a `501` response (watcher not configured) and path-traversal `400` both surface clear inline error messages rather than a silent failure.
- `[x]` **Gap 2: Live Terminal Feed** — Fixed Jul 27, 2026. New `components/ingestion/LogTerminal.tsx` — a scrolling console subscribing to the same SSE stream `StatusTable` uses, opening its own `EventSource` regardless of batch size (log visibility isn't tied to StatusTable's polling/SSE transport choice), filtering for the new `type === "log_line"` event (backend: `be_features_tracker.md` Gap 57) and rendering real per-stage lines (OCR, classify, dynamic_qa, extract, verify, chunking, embedding) instead of just status transitions. Wired into `app/ingestion/page.tsx` alongside `StatusTable`. Verified end-to-end with a real upload — log lines rendered in order, including a genuine retry-loop repeat.
- `[x]` **Gap 14: Live Statistics Counters** — Fixed Jul 27, 2026. `StatusTable.tsx` now computes and shows Found/Processed/Duplicates/Failed counts in its header, derived from the same `items` state driving the table rows. Found and fixed a real bug along the way: `pollJobStatus()` (used for batches under 6 files) had no branch for a `DUPLICATE` status at all — a duplicate upload silently stayed on "Processing" forever instead of stopping and showing a Duplicate badge, since none of the existing `if`/`else if` branches matched it. Added the missing branch, a `DUPLICATE` badge (matching `RecentInvoicesTable.tsx`'s existing amber styling), and extended the `StatusItem` status union to include it.

**Dashboard** ([feature_2_dashboard.md](feature_2_dashboard.md)):
- `[x]` **Gap 4: Actionable Insights Panel** — Fixed Jul 27, 2026. New `components/dashboard/ActionableInsightsPanel.tsx`, fetching its own `GET /dashboard/insights` (backend: `be_features_tracker.md` Gap 30) independently of the main metrics call, same pattern as the Trainer Impact Panel. Renders each AI-generated recommendation as a severity-colored card (critical/warning/info). Wired into `app/dashboard/page.tsx` below `ClientPerformanceChart`. Verified live against real Azure OpenAI + real invoice data.
- `[x]` **Gap 5: Status-Based Sub-Tabs** — Fixed Jul 27, 2026. Added All/Paid/Pending/Rejected tabs above the table in `RecentInvoicesTable.tsx`, client-side filtered against the already-fetched invoice list. "Pending" covers everything not yet finalized (Processing, Completed, Audit Required, Duplicate) rather than mapping 1:1 to a raw status enum, matching the AP mental model of "still in the pipeline" vs. closed out.
- `[x]` **Gap 11: Scroll-Lock Container** — Fixed Jul 27, 2026. Wrapped the table in a `max-height: 320px`, `overflow-y-auto` container with a `sticky` header row, so a long invoice list scrolls internally instead of pushing the rest of the dashboard down.
- `[x]` **Gap 12: Client-Side Pagination** — Fixed Jul 27, 2026. Added Previous/Next controls (8 rows/page) below the table, operating on the tab-filtered list; resets to page 1 whenever the active tab or the underlying invoice count changes so a stale page number can't point past the end of a newly-filtered/shrunk list.
- `[x]` **Gap 29: Dashboard Invoice-List Scalability** — Fixed Jul 27, 2026. `routers/dashboard.py::get_dashboard_metrics` was pulling every matching Invoice row into Python to sum/count/group by hand; dollar totals, status breakdown, top vendors, and the spend-over-time series are now real SQL `SUM`/`COUNT`/`GROUP BY` aggregates (`average_processing_time`/`extraction_accuracy` still need a per-row pass for completed_at/created_at deltas and sa_alerts list lengths, which have no portable cross-dialect SQL form here, so that pass now only fetches the 4 narrow columns it needs instead of full ORM rows). `routers/invoices.py::list_invoices` gained a `vendor_name` filter, deterministic `created_at desc` ordering, and an `X-Total-Count` response header (forwarded through `backendProxy.ts`) reporting the full matching count independent of `limit`/`offset`. The dashboard's Recent Invoices table (`RecentInvoicesTable.tsx`) is now driven by real backend `limit`/`offset` paging from `app/dashboard/page.tsx` instead of fetching one fixed 20-row batch and re-slicing/re-filtering it client-side — Previous/Next now reach every invoice, not just the first fetched window. The status sub-tabs (Gap 5) became real server-side filters too (`status`/new `status_in` for the multi-status "Pending" grouping) so they compose correctly with paging instead of filtering only within whatever page happened to be in memory. Composite indexes `(tenant_id, status)`/`(tenant_id, invoice_date)`/`(tenant_id, vendor_name)` added via migration `c7d8e9f0a1b2`. A dedicated full Invoices Queue/list page (Gap 28, still open) should consume this same paginated `/invoices` API.
- `[x]` **Gap 183: Money is always displayed as "$"/USD regardless of the invoice's actual currency** — opened 2026-08-10, reported by user ("saw an INR invoice shown as $ in the extracted field"). Root cause confirmed by code inspection, not yet fixed: `lib/utils.ts::formatCurrency(amount)` takes no currency argument at all and hardcodes `new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" })` — every dashboard component that uses it (`MetricsGrid`, `RecentInvoicesTable`, `OutboundMetricsGrid`, `OutboundInvoicesTable`, `NeedsAttentionWidget`, `ClientPerformanceChart`) always renders "$", ignoring `Invoice.currency` entirely. Two more standalone copies of the same hardcoded pattern exist in `app/invoices/review/[id]/page.tsx:80` and `app/invoices/outbound-review/[id]/page.tsx:37`. Backend storage path looks structurally correct — `queue_worker/handlers.py:677` writes `invoice.currency = extracted_data.get("currency")` straight from the LLM's ISO-4217 extraction with no hardcoded default — so this reads as a pure display bug, not a data bug, but that hasn't been verified against a real INR invoice yet. **Fix (not yet coded, awaiting approval to proceed)**: thread each invoice's real `currency` (default `"USD"` only when the field is null) into `formatCurrency()` and both local copies. **Follow-up, not yet scoped**: user separately asked whether the extraction pipeline itself should add a currency confidence/validation check (same category as `be_features_tracker.md`'s existing per-field confidence checks, Gap 3) to catch cases where the LLM's extracted `currency` value itself might be wrong, not just mis-displayed — tracked here as a pointer, to be opened as its own BE gap once this display bug is confirmed/ruled out as the sole cause.
  - **Scope grew 2026-08-10**: user asked the deeper question — what happens when the *same* tenant has invoices in 2+ currencies. Confirmed by reading `routers/dashboard.py`: the KPI totals (`total_invoiced`/`paid_amount`/`outstanding_amount`/`at_risk_amount`, lines 86-97), the top-vendors breakdown (lines 116-119), and the spend-over-time series (lines 129+) — plus the mirrored outbound dashboard aggregate (lines ~388-422) — all run a raw `func.sum(Invoice.grand_total)` with **zero currency grouping**. A tenant with both $500 and ₹40,000 invoices gets a single blended `40500` today, mislabeled as one currency. **Decision (user-approved): break totals out per-currency** rather than FX-converting to one blended number (avoids needing an FX rate data source, and avoids the totals becoming an approximation that drifts from the real printed invoice amounts) or restricting the dashboard to one currency at a time via a filter. This is now a cross-cutting BE (aggregate SQL grouping in `dashboard.py` + the outbound mirror) + FE (KPI cards, vendor table, time-series chart all need a per-currency dimension, not just a single formatted number) change — larger than the original per-invoice display-bug scope. Routing through the architect agent for a proper scope brief before coding, per this project's normal workflow for cross-cutting work.
  - **Fixed 2026-08-11** (both slices shipped together — the per-invoice display bug and the per-currency aggregate rework — since the second is meaningless without the first). Backend half is cross-referenced at `be_features_tracker.md` Gap 185.
    - **Shared formatter**: `lib/utils.ts::formatCurrency(amount, currency?)` now takes the code as an optional second argument (backwards compatible — untouched call sites still render USD) and `normalizeCurrencyCode()` folds `null`/`""`/whitespace/lower-case into a single uppercase code. An unrecognised code degrades to `"<CODE> 1,234.56"` instead of letting `Intl`'s `RangeError` blank the panel. `"USD"` is a **display** default only — nothing is ever written back to the row, and no migration/backfill was done.
    - **API shape changed, old keys deleted not deprecated**: `GET /dashboard/metrics` and `GET /outbound-dashboard/metrics` now return `totals_by_currency: [{currency, ...}]`; the flat blended `total_invoiced`/`paid_amount`/`outstanding_amount`/`at_risk_amount` (and the outbound `total_invoiced_out`/`amount_collected`/`outstanding_receivables`/`at_risk_receivables`) are **gone**. A blended figure has no correct label, so keeping it "for compatibility" would only invite something to render it again. Every consumer was grepped and migrated in the same change; a regression test in each BE suite asserts the old keys are absent.
    - **KPI cards**: `KpiCard` takes `value: string | number | string[]` and renders one line per currency. New `useFitText` hook shrinks the font (24px → 9px floor) by measuring the real rendered box against its content and re-measuring on resize — the limiting dimension is often *width* (`₹40,00,000.00` overflows a 200px card long before line count matters), so it is measured, not guessed from a line count. Deliberately **not** truncated (would hide a currency's total outright — the exact bug), **not** overflowed, and the card is bounded `min-h-[84px] max-h-[124px]` rather than growing without limit; the KPI strip's default `align-items:stretch` keeps all four cards level.
    - **Cross-currency ratios removed**: `MetricsGrid`'s `paidPercent = paidAmount / totalInvoiced` and `OutboundMetricsGrid`'s `collectedPercent` were ratios of two blended sums. Both are now computed *within* each currency; the trend badge is shown only when there is exactly one currency, since one badge cannot honestly summarise several.
    - **Charts**: the spend/revenue trendlines draw one polyline per currency, each scaled to its **own** min/max with a shared date x-axis and a legend marked "(scaled separately)" — a shared y-scale drew a ₹40,000 day as an order-of-magnitude spike beside a $500 day. The area fill is only drawn for single-currency tenants (overlapping translucent fills read as a stacked total). `ClientPerformanceChart` groups vendors/customers by currency and scales bars within each group; previously one global `maxAmount` made a ₹40,000 bar ~80x a $500 bar purely from the exchange rate.
    - **Per-row call sites threaded**: `RecentInvoicesTable`, `OutboundInvoicesTable`, `NeedsAttentionWidget`, both review consoles (`app/invoices/review/[id]` and `outbound-review/[id]` — their local hardcoded-USD `fmt()` copies now delegate to the shared helper, and their `InvoiceDetail` types finally declare `currency`), and — found by sweep, beyond the original brief — `components/ingestion/StatusTable.tsx` (2 sites) and `SendInvoiceStatusTable.tsx` (1 site), which were building `` `$${n.toFixed(2)}` `` by hand on the live ingestion ledger.
    - **Verified**: `npx tsc --noEmit` clean (exit 0). BE `pytest` 48/48 across the four affected suites. Live Playwright run against the Next dev server on the three affected specs: **44/44 on a warm re-run, 42/44 on a cold run** — the two cold-run failures are `dashboard-outbound-split.spec.ts:339` (narrow-viewport stacking assertion times out waiting on its testid) and `rbac-sidebar.spec.ts:218` (Help nav). Both are cold-start timing flakes, not currency-logic failures: the Next **dev** server compiles each route on first request, and with `fullyParallel: true` several specs hit uncompiled routes simultaneously, blowing the 5s assertion timeout under the local `retries: 0` config. Both pass consistently once the routes are compiled. Three of the passing specs are new Gap 183 specs (per-currency figures rendered and no blended `40,500` anywhere; KPI text measurably shrinks below 24px and `scrollHeight <= clientHeight`; an INR row renders `₹`/`INR` and not `$`). Not verified against a real seeded multi-currency tenant end-to-end — the multi-currency data was fixture/mock-based, not a real INR PDF through the extraction pipeline.
  - **Still open, unchanged by this fix**: the follow-up question above about validating the *extracted* `currency` value itself (a wrong ISO code from the LLM would now be displayed faithfully-but-wrongly). Still needs its own BE gap.
- `[x]` **Gap 21: Trainer Impact Panel** — Task 2.5; Fixed Jul 27, 2026. New `components/dashboard/TrainerImpactPanel.tsx`, fetching its own `GET /dashboard/trainer-impact` (backend: `be_features_tracker.md` Gap 28) independently of the main metrics call. Renders 3 rule-count tiles (Global/Vendor/Total), a hand-built weekly audit-rate bar trend (no chart library, matching the rest of this dashboard), and a "Vendors Needing a Rule" list with deep-links straight into Trainer (`/trainer?from=audit&scope=existing_vendor&vendor_name=X`, reusing the existing Task 6.8 deep-link handler). Wired into `app/dashboard/page.tsx` below `ClientPerformanceChart`. Verified live: real data from this session's actual Trainer commits rendered correctly.

> Note: `RecentInvoicesTable.tsx`'s duplicate badge, vendor-name fallback, and hover-only tag row CSS (`docs/feature_3.1_fix_ftr2_3.md` Task 3.1.3) are already implemented — verified directly against the component source 2026-07-13, not just tracker bookkeeping.

**Auditor console** ([feature_4_auditor.md](feature_4_auditor.md)):
- `[x]` **Gap 10: Line Items Table** — Fixed Jul 27, 2026. The metadata inspector's line items section only ever showed Description + Total in a plain list — `quantity`/`unit_price` were already present on the `LineItem` type and returned by the backend, just never rendered. Converted to a real `<table>` with Description/Qty/Unit Price/Total columns plus a subtotal footer row.
- `[x]` **Gap 15: Confidence-based field highlighting** — Fixed Jul 25, 2026. Added an amber warning icon to fields below 60% OCR confidence in the `AlertConsole` UI. *(Task 4.5)*
- `[x]` **Gap 19: Editable Metadata Inspector & Correction Capture** — Fixed Jul 25, 2026. Built `EditableField` component allowing metadata fields to be click-to-edit. The `AlertConsole` now tracks a `corrections` diff and sends it on save. *(Task 4.6)*
- `[x]` **Gap 20: Rule Suggestion Prompt** — Fixed Jul 25, 2026. Added a purple "Want to save this as a rule?" banner to `AlertConsole` when `suggested_rule` is present. Clicking it navigates to `/trainer?from=audit&scope=...` using the existing Trainer deep-link handler. *(Task 4.7)*
- `[x]` **Gap 26: "Report an issue" action on any invoice** — Fixed Jul 27, 2026, two parts: (1) the review page (`app/invoices/review/[id]/page.tsx`) was unreachable from anywhere in the app — every link to it (Sidebar nav, Dashboard row actions, Ingestion row actions, chat citation pills) pointed at `/audit`, a route that has never existed; fixed all four to point at the real path. (2) added a "Save Correction" button for the case where an invoice has zero alerts (e.g. `COMPLETED` but factually wrong) — previously the only way to submit `corrections` was via `Mark Paid & Finalize`/`Reject Invoice`, both of which forced a status change. Backend: `be_features_tracker.md` Gap 53 (no backend change needed — the endpoint already supported this). See `feature_4_auditor.md` for the full writeup. Verified live against the real running app + a real invoice.
- `[x]` **Gap 28: No dedicated Invoices Queue / list page — closed 2026-07-29, superseded by `feature_4_auditor.md` Task 4.9.** Found while fixing Gap 26 (Sidebar's "Invoices" nav item and `RecentInvoicesTable`'s "View all ledger" link both pointed at the same nonexistent `/audit` route, removed rather than repointed since there was nowhere real to land). Fully designed as part of the Dashboard/Audit split (2026-07-29): Dashboard becomes overview-only (metrics + `NeedsAttentionWidget`), the full invoice table/filters relocate to a new `/invoices` queue screen, Sidebar's "Invoices" item re-added pointing there. Closing this Gap entry to avoid double-tracking — actual build status now lives on Task 4.9. **Built 2026-07-29** — see Task 4.9 in `feature_4_auditor.md` and Task 2.7 in `feature_2_dashboard.md` for what shipped and what's still unverified (real backend data pass).

> Note: `PdfViewerCanvas.tsx`'s bounding-box overlay (Task 4.2) is fully built but has no backend data source yet — see `be_features_tracker.md` Gap 16.

**Chat assistant** ([feature_5_chat.md](feature_5_chat.md)):

> ✅ **Core Feature 5 implemented 2026-07-22** — 10 new files created, 0 existing files modified, TypeScript compile: **0 errors**.

**Completed Tasks (from `feature_5_chat.md`):**

- `[x]` **Task 5.1: Build Conversational Message Thread Interface** — scroll-to-bottom chat bubble container with markdown formatting, thread selector sidebar with `+ New Chat` action, active thread highlighting, and empty state guidance.
  - Files: `components/chat/ChatWindow.tsx`, `components/chat/MessageBubble.tsx` (includes `MessageStream` with auto-scroll)
- `[x]` **Task 5.2: Integrate Interactive Citations** — parse `citations[]` from the backend RAG response and render as clickable pills. Each pill navigates to `/invoices/review/{id}?page={n}` (originally `/audit?invoice_id=...`, a route that never existed — fixed 2026-07-27 alongside Gap 26).
  - File: `components/chat/CitationPill.tsx`
- `[x]` **Task 5.3: Build Expandable SQL Drawer** — collapsible accordion titled "Executed SQL Query & Data Sources" with emerald-green formatted code block and one-click copy-to-clipboard.
  - File: `components/chat/SqlAuditDrawer.tsx`
- `[x]` **Task 5.4: Bind Chat Queries to Backend** — async submit hook (`useChatSession`) with optimistic user bubble, error rollback, and session title auto-update. Three Next.js API proxy routes forward to the FastAPI backend.
  - Files: `hooks/useChatSession.ts`, `app/api/chat/sessions/route.ts`, `app/api/chat/sessions/[sessionId]/route.ts`, `app/api/chat/sessions/[sessionId]/message/route.ts`

**Supporting files created:**

- `[x]` `types/chat.ts` — TypeScript interfaces for `ChatSession`, `ChatMessage`, `Citation`, and all request/response shapes matching the backend API contract.
- `[x]` `app/chat/page.tsx` — `/chat` page entry point wiring `useChatSession` hook to `ChatWindow`. Uses `-m-8` to fill the full viewport edge-to-edge.

**Completed Gaps:**

- `[x]` **Gap 13: Typing Indicators** — three bouncing dots animation in `MessageBubble.tsx` `MessageStream` component while `isSending` is true, matching WhatsApp/Slack UX convention.

**Remaining Gaps:**

- `[x]` **Gap 6: Suggestion Chips** — Fixed Jul 27, 2026. New `SuggestionChips` component inside `ChatWindow.tsx`, shown only when an active session has zero messages (a fresh new chat) — clicking a chip calls `onSendMessage` directly, submitting immediately rather than just filling the input box. Not part of `feature_5_chat.md`'s original task list — tracked as a separate enhancement.
- `[x]` **Gap 27: Per-answer thumbs up/down** — Fixed Jul 27, 2026. New `FeedbackVote` component inside `MessageBubble.tsx`, shown on assistant messages next to the timestamp. Optimistic update with rollback on failure; clicking the active thumb again clears the vote instead of re-sending it. Calls the new `PUT`/`DELETE /api/chat/messages/{id}/feedback` proxy route. Backend: `be_features_tracker.md` Gap 54. Verified live through the real proxy route and real backend (vote → reload persists → change vote → clear → reload shows null).

- `[x]` **Gap 22: Chat never actually worked through the real UI — FE/BE contract mismatch on every endpoint** — found + fixed Jul 24, 2026 during live end-to-end testing (the benchmark harness that previously reported "95.2% RAG chat passed" calls the backend directly, bypassing the FE entirely — this exact class of bug was invisible to it). Every message send returned a `422` because `hooks/useChatSession.ts` sent `{message: text}` while the backend's `MessageCreate` requires `{content: text}`; `types/chat.ts::SendMessageRequest` had the wrong field name too, so it wasn't caught at the type level. Three more mismatches found in the same pass, all silent (wrong data or `undefined`, not errors): `GET /chat/sessions` returns a bare array, not `{sessions: [...]}`; `GET /chat/sessions/{id}` returns a bare message array, not `{session, messages}`; `POST .../message`'s response is the flat `ChatMessage`, not `{message: ...}`; and `Citation.page_number` didn't match the backend's `page` field (would have shown "p.undefined" on every RAG citation). Fixed all of them in `useChatSession.ts`, `types/chat.ts`, and `CitationPill.tsx`. Verified live: asked 3 real questions against a real ingested invoice, got correct SQL-routed answers with no console errors.

**Trainer sandbox** ([feature_6_trainer.md](feature_6_trainer.md)):
- `[x]` **Gap 3: Rule Scope Selector** — Tasks 6.1–6.4; ✅ 3-way Global / Existing Vendor / New Vendor entry point implemented in `ScopeSelector.tsx` + `TrainerUploader.tsx` (2026-07-22)
- `[x]` **Gap 7: Active Rules Registry** — ✅ Active rule candidates list with `Active` status badges rendered in `QnAPanel.tsx` Variables & Rules Inspector tab (2026-07-22)
- `[x]` **Gap 8: Always-Enabled Chat Input** — ✅ Chat input bar always active; Global-scope sessions with no seed PDF start chat-only with empty variables list (2026-07-22)
- `[x]` **Gap 9: Active Validation Alerts Panel** — ✅ Low-confidence field warnings (< 80%) shown with amber `AlertCircle` icons in `QnAPanel.tsx` Variables Inspector; corrected fields shown with emerald `CheckCircle2` (2026-07-22)
- `[x]` **Gap 16: Rule History & Rollback UI** — Task 6.7; ✅ Full `RuleHistoryDrawer.tsx` implemented with version timeline, `isCurrent` badge, rollback confirmation bar, and loading state (2026-07-22)
- `[x]` **Gap 17: Audit-Seeded Trainer Session Entry** — Task 6.8; ✅ URL parameter parsing (`?from=audit&scope=...&vendor_name=...&correction=...`) handled in `page.tsx` `useEffect` — pre-seeds scope, vendor, and sends an initial chat correction automatically (2026-07-22)
- `[x]` **Gap 23: Screen not cleared after commit, left pointing at a dead session** — found + fixed Jul 25, 2026 during live UI testing (this trainer code was only merged from a feature branch the same day and had never run end-to-end before). The backend deletes the session immediately on commit, but `page.tsx::handleConfirmCommit()` never reset FE state — the same chat/PDF/variables stayed on screen looking live. Fixed: clear state per scope after commit (Global auto-starts a fresh session; Existing/New Vendor reset to their empty picker state). See `be_features_tracker.md` Gap 50.
- `[x]` **Gap 24: Document viewer panel was a hardcoded mock, not the real document** — found + fixed Jul 25, 2026. `PdfViewerPanel.tsx` showed literal sample data (`"Acme Logistics Corp"`) no matter what was actually uploaded — its own comment called it a "simulated invoice body." Fixed: real `<iframe src={pdfUrl}>` render plus a live summary strip from the session's actual `variables`. See `be_features_tracker.md` Gap 51.
- `[x]` **Gap 25: Chat correction had no progress feedback during its ~25-30s round-trip** — found + fixed Jul 25, 2026; initially looked like a hung/stuck bug (a message sat on "Refining rules..." well past a 20s+6s test wait) until a network-level check confirmed the backend genuinely takes that long (two sequential real LLM calls: refine constraints, then re-extract) and the UI updates correctly the moment the response lands — not a correctness bug, just no indication of how long to expect. Added a client-side elapsed-time-estimated progress bar + stage text ("Analyzing correction..." → "Re-extracting with updated rules..." → "Finalizing...") in `QnAPanel.tsx`, capped short of 100% until the real response arrives so it never falsely claims completion early.

**Connectors** ([feature_7_connectors.md](feature_7_connectors.md)):
- **Note (2026-07-22):** SAP and QuickBooks were deferred from Task 7.1's integration grid, not forgotten — no confirmed customer/system needs them today. Re-add if real demand shows up.

**Flow Visualization** ([feature_11_flows_visualization.md](feature_11_flows_visualization.md)):
- `[x]` **Gap 58: Canvas didn't auto-scroll to the active node during playback** — Fixed Jul 27, 2026. Autoplay walked the active node off-screen with nothing telling the viewer to scroll down; added a `useEffect` that scrolls the canvas wrapper to keep the active node centered, using its known coordinates directly.
- `[x]` **Gap 60: Viewer had to manually scroll down after pressing Play** — Fixed Jul 27, 2026. The canvas container itself started partially below the fold under the page's sticky header; `handlePlay()` now calls `scrollIntoView({ block: "nearest" })` to bring it fully into view immediately.
- `[x]` **Gap 61: No guidance for a first-time visitor** — Fixed Jul 27, 2026. Added a hint banner under the flow tabs ("Pick one of the 4 flows above, then press Play...") shown until the current flow has been played at least once.
- `[x]` **Gap 62: Page rendered inside the full authenticated app Shell (Sidebar + Header)** — Fixed Jul 27, 2026. `/flows` is a standalone, no-login public demo, but shared the same `Shell` as every real tenant screen — a visitor could click Dashboard/Ingest/Chat straight into internal screens out of context. `components/layout/Shell.tsx` now skips Sidebar/Header for a small allowlist of standalone routes (currently just `/flows`); every other route is unaffected.
- `[x]` **Gap 59: Outbound tab's autoplay silently freezes partway through** — Fixed 2026-07-29. Root cause: `OUTBOUND`'s `sequence` array included `{ nodeId: "ob_rules", edgeIds: [] }` as its 4th of 8 steps, and `runStep()` treats an empty `edgeIds` as "flow finished" regardless of remaining steps. Fixed by dropping `ob_rules` from `sequence` (`app/flows/page.tsx`) — its edge already fires from `ob_ocr`'s own step, so the node still visually activates, autoplay now runs the full 8 steps. See `feature_11_flows_visualization.md` Task 11.4 for the original root-cause writeup.
- `[x]` **Gap 63: Gap 58's auto-scroll undershot on later nodes — last 1-2 nodes of every flow never scrolled into view** — Fixed Jul 27, 2026, found while re-verifying Gap 58 live per user report ("not going to the last boxes without moving the scroll button"). The fix computed scroll position from raw viewBox coordinates, but the SVG auto-scales its rendered height (~1.9x) since it's `width="100%"` with no explicit `height` — coordinate math undershot proportionally more the further down the flow. Fixed by reading each node's real rendered position via `getBoundingClientRect()` (tagged with `data-node-id`) instead. Verified: both Inbound and Chat now reach `scrollTop === maxScroll` with their final node fully visible at the end of a full autoplay run.
- `[x]` **Gap 64: No plain-language narration of user/agent moments** — Fixed Jul 27, 2026. Added blinking explainer bubbles above human-facing nodes only (upload/question = user action, reply/outcome = agent result); internal technical nodes are untouched since they already have the activity log.
- `[x]` **Gap 65: Gap 61's hint banner was easy to skim past** — Fixed Jul 27, 2026, per user report ("should blink or something else, not able to understand that"). Added a box-shadow glow-pulse to both the hint banner and the Play button. First pass used `transform:scale` on the button, which broke Playwright's click-stability check (a real signal it was a jumpier hit-box than intended) — switched to box-shadow-only, same technique as the banner.

**Auth** (see `apps/invoice-website/website_features/feature_4_auth_gateway.md` for the full Clerk auth gateway feature, which is website-tracked; this gap is the `invoice-fe`-side piece of that same reconciliation):
- `[x]` **Gap 66: `invoice-fe` had zero Clerk wiring — no middleware, no provider, sign-out button did nothing real** — Reconciled Jul 28, 2026 from the `auth-feature-4` branch onto current master. Added `middleware.ts` (bare `clerkMiddleware()`, no `.protect()` calls — doesn't gate any existing route), wrapped `app/layout.tsx` in `ClerkProvider`, and wired `components/layout/Header.tsx`'s Sign Out button to a real `signOut()` + backend logout call. Fixed two things found during the port: the branch's `Header.tsx` hit a hardcoded `http://localhost:8000/auth/logout` directly from a client component — replaced with a new `app/api/auth/logout/route.ts` proxy route (backend's `auth.router` is mounted unprefixed, unlike every other router, so it can't reuse `backendProxy.ts`'s `proxyJson`, which always appends `/api/v1`); and the post-logout redirect pointed at `invoice-fe`'s own `localhost:3000/admin/login` (a route that doesn't exist on this app) — now points at `invoice-website`'s `/login` via `NEXT_PUBLIC_WEBSITE_URL`. Verified: dashboard/chat/ingestion/trainer/flows/settings all still return 200 with the middleware in place. **Blocked on real Clerk keys** — tracked in `apps/invoice-website/website_features/website_features_tracker.md` Gap 2 (the auth gateway originates from the website, so that's where this is owned) — currently runs on a placeholder key that lets Clerk initialize but can't complete real sign-in/sign-out calls.
- `[x]` **Gap 67: "Apply as standing rule" checkbox on the inbound Audit/correction screen — closed 2026-07-29, superseded by `feature_4_auditor.md` Task 4.10.** FE piece of `be_features_tracker.md` Gap 62 — add a checkbox to the invoice correction UI (wherever `PUT /audit/resolve/{id}` is called from) offering "apply this correction as a standing rule for this vendor?", wired to the new backend param that gap introduces. Backend enforces the safety re-extraction check before committing; FE just needs to surface the checkbox and show the result (rule applied / rule rejected because the safety check failed). Closing this Gap entry to avoid double-tracking — actual build status now lives on Task 4.10. **Built 2026-07-29** — see Task 4.10 in `feature_4_auditor.md` for what shipped.

---

## Live Testing Gaps (2026-07-29)

Found during local E2E run (Website :3000 → FE :3001 → BE :8000 against real Azure OpenAI + Doc Intelligence).

- `[x]` **Gap 68: Dashboard content overflows viewport — all sections don't fit on one screen** — Fixed 2026-07-29 (second pass). Merged title+filters into one `PageHeader` row (no subtitle), compacted `MetricsGrid` (smaller KPI cards, shorter Spend Trend chart, combined Accuracy+Processing-Time card), and replaced the 3-col NeedsAttentionWidget/ClientPerformanceChart/ActionableInsightsPanel/TrainerImpactPanel squeeze with one full-width tabbed panel. Verified live at 1280×720 — whole dashboard fits with room to spare.

- `[x]` **Gap 69: File Ingestion page content overflows viewport — left column cards require scrolling** — Fixed 2026-07-31 (Group A — the planning doc this cites, `docs/guides/fe_gap_plan_group_a_layout_overflow.md`, was never committed; see `feature_3_ingestion.md` instead for the surviving writeup). Two changes in `app/ingestion/page.tsx`: `space-y-6` → `space-y-4` on both the Receiving and Sending left columns, and the `Bulk Directory Scan` card converted to a collapsed-by-default disclosure (`aria-expanded`/`aria-controls`, chevron rotation, matching `RuleHistoryDrawer.tsx`'s affordance). Bulk Directory Scan was the right card to fold — it's the least-used control in that column (a shared-drop-folder path, not the normal drag-and-drop route) and its header row stays visible, making it *more* discoverable collapsed than it was below the fold. Measured at 1280×720 before → after: Shell `<main>` overflow **178px → 1px**, left column height **718px → 541px**, and the Directory Scan card's bottom edge went from **146px below the fold to 31px above it**. Landed together with Gap 86 since that fix reclaims the header row this one needed. Right column (`StatusTable`/`LogTerminal`) untouched, as planned. See `feature_3_ingestion.md` for the full writeup.

- `[x]` **Gap 70: Outbound ingestion only allows selecting a single PDF — should support multi-file batch** — Fixed 2026-08-03. Changed outbound file selector to accept multiple files, allowing batch uploads and parallel processing of multiple outbound invoices at once.

- `[x]` **Gap 71: Auditor Review Console renders double Shell (Sidebar + Header appear twice)** — Fixed 2026-07-29. Removed all three `<Shell>` wrapper usages from `app/invoices/review/[id]/page.tsx` (loading state, error state, main return) plus the now-unused `Shell` import. The page is already inside the shell from `app/layout.tsx`.

- `[x]` **Gap 72: PDF viewer in Auditor Review Console — Rotate control breaks the layout** — Fixed 2026-08-03. Programmed `PdfViewerCanvas` to swap dimensions and apply centered scaling rotation on 90°/270° transform angles, ensuring boundaries stay scrollable.
  1. **"Loads wrong/broken PDF" — NOT reproduced, ruled out.** `GET /api/invoices/{id}/pdf` returns a real, valid `%PDF-1.3` document with the correct `Content-Type`, and a real browser renders it correctly via its native PDF plugin, matching the invoice's actual metadata exactly. Confirms the prior headless-Chromium blank-iframe finding was purely a test artifact (headless Chromium ships no PDF viewer plugin by default) — not a real bug, as suspected.
  2. **Zoom In / Zoom Out — NOT broken, works correctly**, contrary to the reported complaint. Measured the iframe wrapper's real rendered width via `getBoundingClientRect()` across 3x Zoom In + 5x Zoom Out clicks: it scaled exactly proportionally (100%→130%→80%, verified to 2 decimal places). One real cosmetic nuance, not a functional break: `PdfViewerCanvas.tsx`'s "zoom" resizes the CSS width of the iframe's wrapper `<div>` rather than calling any real PDF-level zoom API, so widening the iframe incidentally makes Chrome's own native PDF-viewer toolbar expand and show its own separate zoom/page-nav controls — two zoom UIs stacked on top of each other, confusing but not dead.
  3. **Rotate — CONFIRMED BROKEN, root-caused.** A single Rotate click (0→90°) makes almost the entire PDF disappear with no way to scroll back to it — screenshot shows only a ~50px sliver of content remaining, the rest of the panel empty. Root cause: `PdfViewerCanvas.tsx` (lines 91–103) applies `transform: rotate(90deg)` directly to the iframe's sizing wrapper with no compensating width/height swap or re-centering. Measured DOM geometry after rotation: the rotated box's `left` becomes **negative** (-264px) relative to its scroll container, and the container's `scrollHeight === clientHeight` (832 == 832) — i.e. the browser's layout engine never recognizes the transformed content as overflowing, so no scrollbar is ever offered to reach it. This is exactly the two live complaints ("displays only half", "rotate doesn't work at all") — rotate doesn't no-op, it actively breaks the layout.
  - **Fix implication for senior-dev** (diagnosis only, not built here): either (a) swap the wrapper's width/height and re-center on 90°/270° rotation, or (b) rotate a fixed-size outer frame around the iframe instead of the iframe's own sizing wrapper, so the rotated bounding box stays within the visible/scrollable container at every angle. Also worth deciding whether "zoom" should keep resizing the iframe box (current behavior, causes the native-toolbar-doubling nuance above) or drive a real embedded PDF viewer's zoom API instead.
  - Evidence: `apps/invoice-fe/docs/test_evidence/gap72_pdf_viewer/` (4 screenshots — baseline/zoomed-in/zoomed-out/rotated — plus `README.md` with the full measured geometry and reasoning).

- `[x]` **Gap 73: Dashboard split-screen (Receiving | Sending) is entirely unbuilt on the FE** — **Fixed 2026-07-30**, merged in from the independently-built `new-changes-29/07` branch (see `feature_2.1_vendor_flow_dashboard.md` for the full build writeup). `OutboundMetricsGrid.tsx` + conditional split-layout in `app/dashboard/page.tsx`, gated on the same `receive_invoices_enabled`/`send_invoices_enabled` flags `app/ingestion/page.tsx` already uses. Merge reconciled this against the same night's independent `PageHeader`/tabbed-panel Dashboard redesign (Gap 68) — the split-screen metrics logic and the tabbed Needs Attention/Top Vendors/Insights/Trainer Impact panel below it both kept, with `NeedsAttentionWidget` and the Top Vendors tab made direction-aware (merges inbound+outbound rows, shows Top Customers alongside Top Vendors when both flows are enabled).

- `[x]` **Gap 74: Auditor Review Console has no `flow_direction` awareness — outbound invoices show the wrong action buttons** — Fixed 2026-08-03. Check `flow_direction` upon mounting `/invoices/review/[id]` and automatically redirect outbound invoices to `/invoices/outbound-review/[id]`.

- `[x]` **Gap 75: Auditor Review Console gives no explanation when status is `AUDIT_REQUIRED` but `sa_alerts` is empty** — Fixed 2026-08-03. Show an explicit amber alert explaining that warnings were previously dismissed and the invoice is awaiting finalization when status is `AUDIT_REQUIRED` but alerts list is empty.

- `[x]` **Gap 76: Trainer's "Commit to Template Registry" button appears clipped/not visible** — Fixed 2026-07-31 (Group A). The original guess in this entry (a `min-h-0` flex issue like Gap 71) was wrong — the inner panels already had `min-h-0`. Real root cause: `app/trainer/page.tsx`'s root was `h-screen` (100vh), but the page renders inside `Shell.tsx`'s `<main className="flex-1 overflow-y-auto p-8">`, which has already spent the global Header's 64px plus 32px of padding top and bottom. A 100vh child in a container ~128px shorter than the viewport is taller than the space it has, so its own content gets pushed past the bottom edge. Fixed: root `h-screen` → `h-full`; plus `min-w-0`/`truncate` on the header's title side and `shrink-0` on its actions side, closing a second independent path to the same symptom (the `whitespace-nowrap` EVOLVE badge could push the buttons out *horizontally* at narrow widths); EVOLVE badge is now `hidden sm:flex` so decoration doesn't compete with an action button for space. Measured: `<main>` scroll overflow on `/trainer` **178px → 1px** at 1280×720. Worth recording how this symptom presents, since the obvious test misses it: at `scrollTop=0` the Commit button is on screen *even pre-fix*, so "is the button visible" passes on the bug. The real defect is that the page was taller than its container, making Shell's `<main>` scrollable on a screen designed not to scroll — so any scroll at all carries the header off the top, which fits it being reported as "appears clipped" rather than as a hard reproducible break. Regression cover is therefore scroll-based (scroll `<main>` to its end, then assert the button is still in the viewport) plus a `scrollHeight`-vs-`clientHeight` check; both deterministically fail pre-fix. The horizontal `min-w-0`/`shrink-0` part is defensive hardening — the buttons were not measured overflowing the right edge at either tested width. **Deliberately left open:** this page still renders its own `<header>` below Shell's global `Header` (two stacked header bars). That's plausibly intentional — page-specific actions like Commit/Rule History arguably don't belong in a global header — so it wasn't removed as a side effect of a layout fix; flagged for a product decision, and related to Gap 88's broader IA critique. See `feature_6_trainer.md`.

- `[x]` **Gap 77: Trainer screen's scope selector and "Browse PDF" grounding control are two disconnected UI blocks** — closed 2026-08-05. Unified scope selectors, vendor pickers, upload buttons, and step indicators into a single compact `TrainerControlBar` component.

- `[x]` **Gap 78: Trainer screen — uploaded PDF doesn't display well, extracted fields poorly placed** — closed 2026-08-05. Wrapped PDF viewer inside a constrained box with internal scrollbars, resolving zoom/scroll overflow.

- `[x]` **Gap 79: Rename "Extraction Accuracy" (inbound) and "Verification Accuracy" (outbound) to "Auto-Verification Rate (Invoice Quality)"** — **Closed 2026-08-04.** Renamed both metric components to "Auto-Verification Rate" on the dashboard grid and added clarifying tooltips pointing out that lower rates indicate poor document layout quality/completeness rather than AI inaccuracy. Updated Playwright E2E tests to match the new heading selectors.


- `[x]` **Gap 80: Dashboard "Weekly Audit Rate" mini-chart is too small/unclear to read** — **Closed 2026-08-04.** Improved the chart in `TrainerImpactPanel.tsx` by doubling its height to 112px, rendering visible percentage labels above each bar, adding a clean bottom border/grid line, and adding hover transition effects.

- `[x]` **Gap 81: Outbound invoice upload can succeed (201) but the invoice gets stuck at `UPLOADED` forever, never picked up by the worker** — **Fixed 2026-08-04.** Built all three of the fix implications the 2026-08-02 investigation left for senior-dev, except that (1) was resolved differently on purpose:
  - **(2) Stuck-invoice reconciliation sweep — built.** `services/invoice_reconciliation.py` (`find_stuck_invoices()`, `reconcile_stuck_invoices()`, `force_requeue()`) + `scripts/reconcile_stuck_invoices.py` (with `--dry-run`). Ages out anything in `PROCESSING`/`PROCESSING_OCR`/`EXTRACTING_DATA`/`UPLOADED` past `INVOICE_STUCK_AFTER_MINUTES` (default 15), re-enqueues it on the same queue with the same task name and kwargs the upload routers use (so a recovered invoice takes the identical code path, not a parallel one that could drift), and marks it `FAILED` with a `processing_timeout` alert once `INVOICE_MAX_REPROCESS_ATTEMPTS` (default 2) is exhausted. Two new `Invoice` columns back it (migration `b5c6d7e8f9a0`): `last_enqueued_at` — staleness has to be measured from the last enqueue, not `created_at`, or a just-re-enqueued invoice looks permanently overdue and gets re-enqueued every pass — and `processing_attempts`.
  - **(3) The swallowed enqueue-failure signal — fixed.** Both upload routers' `except` around `send_message` was `logger.warning`; both are now `logger.error` and the message names the actual consequence ("will remain at PROCESSING/UPLOADED until the reconciliation sweep re-enqueues it"). The missing-`AZURE_STORAGE_CONNECTION_STRING` branch got the same treatment.
  - **(1) Process supervision — deliberately not built here, and the sweep is not a substitute for a different reason than it looks.** Supervision belongs to whatever runs the worker (`run-local.ps1` today, Container Apps later), not to application code. More importantly, supervision alone would **not** have recovered this gap's own motivating incident: the 2026-07-29 invoice was still frozen four days later *because its queue message was gone*, and several worker restarts in between drained everything else without touching it. Only something that reads the database and re-enqueues can recover a row whose message no longer exists — which is what (2) does.
  - **The specific still-stuck invoice**: `force_requeue()` / `--invoice-id` exists exactly for this, ignoring age and attempt count and reviving a `FAILED` row to its own direction's in-flight status. **It has not actually been run against `d5fb23dc-fc5d-491a-a287-1aba39e7f2eb`** — the local stack (Postgres :5433, Azurite, worker) was down for this whole session, so the DB was unreachable. The command is `uv run python scripts/reconcile_stuck_invoices.py --invoice-id d5fb23dc-fc5d-491a-a287-1aba39e7f2eb`, and it needs `alembic upgrade head` (for `b5c6d7e8f9a0`) applied first. That record is therefore **still stuck** as of this entry.
  - **Verified**: `tests/test_invoice_reconciliation.py` 19/19 (shared with Gap 84 — see there for the breakdown). Full BE suite green. **Not verified**: nothing run against the live stack; nothing schedules the sweep yet.
  - *Original 2026-08-02 investigation, kept for context:* **Re-investigated 2026-08-02 with the worker confirmed alive/dead by direct process inspection throughout, not just tracker-level.** Root cause found: **neither** of the two suspects named in the original entry. It's a worker-liveness/supervision gap, not an enqueue bug and not an Azurite delivery bug:
  - **Scenario A (worker alive throughout)**: uploaded a real PDF via `POST /outbound-invoices/upload` — 201, then watched the worker log live. Enqueue → `Received task process_outbound_invoice` → `PROCESSING_OCR` → `EXTRACTING_DATA` → `VERIFIED` in ~20s, no gaps. `routers/outbound_invoices.py::upload_outbound_invoice`'s enqueue path is correct.
  - **Scenario B (worker killed *before* upload, confirmed 0 worker processes via `Get-CimInstance Win32_Process`)**: upload still returned 201 and the message was still accepted by Azurite silently (no error in BE logs — the `except Exception` around the `queue_client.send_message` call in `upload_outbound_invoice` only fires on a real send failure, and Azurite accepting a message with zero consumers isn't one). Invoice sat at `UPLOADED`, exactly matching the reported symptom.
  - **Same invoice, worker restarted ~40s later**: the message was still there — durably queued — and got picked up and fully processed to `NEEDS_REVIEW` within seconds of the worker coming back up, no manual intervention. This rules out message loss: Azurite's queue delivery is reliable across a worker outage in this stack.
  - **Conclusion**: the actual gap is that nothing supervises/auto-restarts the queue worker (`run-local.ps1` just launches it once in a plain window) and nothing alerts on an invoice sitting at `UPLOADED`/`PROCESSING_OCR`/`EXTRACTING_DATA` past a reasonable SLA — so a worker crash between an upload and the next restart produces exactly this silent-forever symptom, recoverable only by someone noticing and restarting the worker. Also found live during this session (worth flagging, not a code bug): multiple redundant `queue_worker.main_worker` processes (4 stray instances) had accumulated from prior sessions all polling the same queue concurrently — harmless per-message (each message only delivered once) but wasteful and a plausible contributor to the *original* 2026-07-29 incident if two workers raced on the same message and one was killed mid-processing.
  - **The original 2026-07-29 invoice (`d5fb23dc-fc5d-491a-a287-1aba39e7f2eb`) is still stuck at `UPLOADED` today, 4 days later** — checked live, still there, all fields still null. Unlike this session's controlled repro, its queue message never reappeared despite several fresh worker restarts today successfully draining everything else in the queue — so whatever happened to it that night is not simply "the worker restarted and drained it," it's genuinely gone (see evidence file for the detailed reasoning). Not re-diagnosed further since it predates this session and reproducing the exact 4-day-old failure mode isn't practical; flagging as a real, still-open, unrecovered stuck invoice a fix should also account for (e.g. a reconciliation job that re-enqueues invoices stuck past N minutes).
  - **Fix implication for senior-dev** (diagnosis only, not built here): (1) process supervision for the worker (restart-on-crash — even a simple watchdog loop, since this is still a single-VM dev/early-prod setup, not AKS/Container Apps yet); (2) a stuck-invoice reconciliation sweep (age-out `UPLOADED`/`PROCESSING_*` past N minutes → re-enqueue or surface to the user); (3) making the currently-swallowed `logger.warning` on a real enqueue failure louder (metric/alert, not just a log line nobody watches).
  - Evidence: `apps/invoice-fe/docs/test_evidence/gap81_outbound_worker_liveness.log` (both scenarios' worker log excerpts, timestamps, and the still-stuck 2026-07-29 invoice's current state).

- `[x]` **Gap 82: Auditor Review Console — "Click a field to correct it" does nothing** — Fixed 2026-08-03. Removed `pointer-events-none` from the disabled field styles and added `cursor-not-allowed` so hover cursors are styled correctly on non-editable fields.

- `[x]` **Gap 83: Outbound Audit screen still shows a redundant internal tab row** — **Fixed 2026-08-03.** Removed all three `<Shell>` wrapper usages (and the now-unused `Shell` import) from `app/invoices/outbound-review/[id]/page.tsx` to resolve the double-rendering of layout chrome.
  - Evidence: `apps/invoice-fe/docs/test_evidence/gap83_outbound_review_double_shell/` (4 screenshots + README).

- `[x]` **Gap 84: Some inbound and outbound uploads get stuck showing "PROCESSING" indefinitely** — **Fixed 2026-08-04**, exactly as the 2026-08-02 root-cause analysis prescribed: both handlers' `except` blocks now write `FAILED` to the `Invoice` row before re-raising, so the REST poll path converges with what SSE already showed live.
  - **`_persist_processing_failure()`** (`queue_worker/handlers.py`, called from both the inbound and outbound handlers) delegates to `services/invoice_reconciliation.py::mark_invoice_failed()`. It is wrapped in its **own** `try/except` on purpose: if recording the failure itself fails (DB down, row gone), that must not replace the original exception being re-raised — losing the real cause to a bookkeeping error would make this strictly harder to debug than the bug it fixes. `mark_invoice_failed()` **appends** its alert instead of replacing `sa_alerts`, since an invoice can fail after verification alerts were already raised and those are the auditor's only diagnostic.
  - **On the status value**: the gap entry warned `FAILED` "doesn't currently exist in the `Invoice.status` enum/lifecycle at all". Checked directly rather than assumed — `Invoice.status` is a plain `str` with no validation, `routers/invoices.py`'s SSE generator already treats `FAILED` as terminal, `list_invoices`'s `status`/`status_in` filters are free-form strings, and `StatusTable.tsx`, `FilterBar.tsx` and `RecentInvoicesTable.tsx` all already render it. So no enum/filter work was needed on the inbound side; the only reader genuinely missing it was outbound's `SendInvoiceStatusTable.tsx`, now updated (see below). Dashboard aggregates deliberately left alone: they classify by explicit status lists (`COMPLETED`/`PAID`/`AUDIT_REQUIRED`/`REJECTED`), so a `FAILED` invoice is correctly excluded from "processed" counts and accuracy rates rather than silently inflating them.
  - **`SendInvoiceStatusTable.tsx`**: `FAILED` added to `OutboundStatus`, a red/`XCircle` badge matching `StatusTable.tsx`'s existing inbound one, and a red block surfacing the backend's alert message. **Real bug found in the same file**: its poll's stop condition read `status` out of an effect closure keyed only on `invoiceId`, so it was pinned to `"UPLOADED"` forever and the 2-second poll never stopped even on `VERIFIED`/`SENT`. Fixed to check the value each pass actually fetched — adding `FAILED` to a stop list that never fired would have been cosmetic.
  - **Verified**: `tests/test_invoice_reconciliation.py` 19/19 — for this gap specifically: `mark_invoice_failed` writing `FAILED` + `completed_at`, alert preservation, the **inbound** handler's failure path persisting to the DB (not just SSE), the **outbound** handler's doing the same, and the persist-failure path swallowing its own error without masking the original exception. Also `npx tsc --noEmit` clean. Full BE suite green.
  - **Not verified**: no live repro against a running stack — Postgres/Azurite/worker were all down this session, so migration `b5c6d7e8f9a0` has not been applied anywhere and the pre-existing unprompted stuck invoice `1c2b8027-92c7-4b9f-9282-c55e2a446af6` has not been re-checked. It will not self-heal: the fix only applies to *future* failures, and that row needs the Gap 81 sweep to age it out (which will mark it `FAILED` after its retries).
  - *Original 2026-08-02 root cause, kept for context:* **Root-caused 2026-08-02, reproduced live for both directions with the worker confirmed alive and its log watched in real time throughout.** Neither of the two suspects named in the original entry — it's a real backend bug, and it's distinct from Gap 81:
  - **Repro (inbound)**: uploaded a deliberately corrupted file (garbage bytes with a `.pdf` extension) via `POST /invoices/upload`. Worker picked it up immediately, `PROCESSING_OCR` fired, then Azure Document Intelligence genuinely rejected it (`InvalidContent: The file is corrupted or format is unsupported`). The worker logged the error and published an SSE `FAILED` event — but `GET /invoices/status/{id}` (the DB-backed endpoint the FE actually polls) kept returning `PROCESSING` indefinitely, checked repeatedly after the failure.
  - **Repro (outbound)**: same corrupted file via `POST /outbound-invoices/upload`. Identical pattern — SSE says `FAILED`, DB status frozen at `UPLOADED` forever (outbound's initial status, since outbound never persists an intermediate DB status at all, only via SSE).
  - **Root cause, confirmed by reading the code that produced the above**: `queue_worker/handlers.py::handle_process_invoice` and `queue_worker/outbound_handlers.py::handle_process_outbound_invoice` both wrap their extraction pipeline in one `try/except Exception`. The `except` block (lines 590–594 inbound, 148–151 outbound) publishes `{"status": "FAILED", ...}` to the **ephemeral Redis pub/sub SSE channel only** and re-raises — it never writes anything back to the `Invoice` row in Postgres. Since the DB status was only ever set once, at upload time (`PROCESSING` for inbound, `UPLOADED` for outbound), a real, permanent, already-logged processing failure leaves the persisted record frozen at that pre-failure status forever.
  - **This is not a FE polling bug** — checked `components/ingestion/StatusTable.tsx::pollJobStatus()` directly: it correctly polls `GET /invoices/status/{jobId}` every 2s and already has a correct `FAILED` branch (badge, stops polling, etc.) if the backend ever reports it. It just never does, over REST. The **only** way a user ever sees "Failed" today is if their browser happens to have the SSE `EventSource` open at the exact moment the exception fires; a page reload, a second tab, or simply not having been on the ingestion page at that instant all just see "Processing" with no error, forever, on every subsequent poll — exactly the reported symptom.
  - **Confirms this is a real, pre-existing production issue, not a test artifact**: found a genuine, unprompted instance of the exact same failure from earlier the same day (`invoice 1c2b8027-92c7-4b9f-9282-c55e2a446af6`, created 04:47 UTC 2026-08-02, hit the identical Doc Intelligence "file is corrupted" error, still `PROCESSING` when checked live during this session) — this wasn't caused by this test run, it was already sitting there.
  - **Relationship to Gap 81**: different root cause, same visible symptom class. Gap 81 = worker never touches the message at all (silent, zero log trace). Gap 84 = worker *does* process it, hits a real terminal failure, logs it clearly and broadcasts it over SSE — but the failure is never durably persisted, so a fresh page load can't see it either. Worth noting: on outbound specifically, a Gap-84 failure and a genuine Gap-81 worker-down case are **indistinguishable from the DB alone** (both leave the invoice at `UPLOADED`) — only the worker log tells them apart.
  - **Fix implication for senior-dev** (diagnosis only, not built here): in both handlers' `except` blocks, add a DB write setting `invoice.status = "FAILED"` (a status value that doesn't currently exist in the `Invoice.status` enum/lifecycle at all today — needs to be added everywhere status is read: `StatusTable`/`SendInvoiceStatusTable` already handle the string, but dashboard/audit-queue filters and the `Invoice` model's status validation, if any, need to accept it too) before re-raising, so the REST poll path converges with what SSE already (correctly) shows live.
  - Evidence: `apps/invoice-fe/docs/test_evidence/gap84_stuck_processing_root_cause.log` (both directions' worker log excerpts, DB status checks, and the pre-existing unprompted repro).

- `[x]` **Gap 85: Dashboard "Command Center" title crowds its row, other info (filters/metrics) doesn't display properly** — Fixed 2026-08-04. Refactored `components/layout/PageHeader.tsx` to handle flex-wrapping and dynamic font sizing (scaling down to `text-lg` and stacking components vertically on narrow viewports/high zoom levels) to ensure filter select tags are never truncated or crowded.

- `[x]` **Gap 86: Ingestion page — title and Receiving/Sending toggle are stacked on separate lines instead of sharing one row** — Fixed 2026-07-31 (Group A). Turned out to need no new component API at all: `components/layout/PageHeader.tsx` already had an `actions` slot on the title's flex row (added for the Dashboard's FilterBar merge, Gap 68), and Ingestion simply never used it — it rendered the toggle as its own sibling block below `<PageHeader>`. Fixed by passing the toggle `<div>` into `actions`, still gated on `showTabs` so a single-service tenant's view is byte-for-byte unchanged (that non-regression has its own test). Reclaimed the row that Gap 69's overflow fix needed.

- `[x]` **Gap 87: Global Header (top bar) search box + Help/Notification icons are dead weight** — Fixed. The Help button has been wired to `/help`, the non-functional search box has been removed entirely from the header row, and the notification bell displays the active "Needs Attention" count (inbound `AUDIT_REQUIRED` + outbound `NEEDS_REVIEW`) and links to the `/invoices` queue screen.
  - **Closed 2026-08-04**: Search box removed, help link functional, bell displays needs attention count.

- `[x]` **Gap 88: Trainer screen's overall information architecture is confusing, not just individual clipping bugs** — Fixed 2026-08-04 (P1 Redesign). Merged scope selection and uploader into a unified control bar, added step progress indicator (`Scope → Ground → Teach`), compressed empty-state canvas prose, and added variables tab placeholder helper hints.

- `[x]` **Gap 90: Trainer "Existing Vendor" scope throws a real 500 loading a production sample invoice, and the raw error JSON renders directly in the PDF pane** — **Closed 2026-08-04.** Both prescribed fixes turned out to be already in the code; a third, latent one was found while verifying that and was fixed here.
  - **(1) BE 404-instead-of-500 — already done, entry was stale.** `routers/invoices.py::get_invoice_pdf` already catches `(FileNotFoundError, ResourceNotFoundError)` and returns 404 (landed in commit `6ad3ac4`, after this gap was filed). Verified by reading the code, not by trusting the tracker. Now has a **regression test**: `tests/test_queries.py::test_stream_pdf_missing_blob_is_404_not_500` — the Azure SDK's `ResourceNotFoundError` is deliberately *not* a Python `FileNotFoundError`, which is the exact reason the original catch clause missed it, so the test raises the real SDK exception type rather than a stand-in.
  - **(2) FE friendly message — already done, entry was stale.** `components/trainer/PdfViewerPanel.tsx` already had a `pdfError` state, a HEAD probe, and a "Document Unavailable" card instead of dumping raw JSON into the pane.
  - **(3) New finding: the probe was one proxy edit away from failing on *every* PDF.** The probe was `if (!res.ok) → "Document Unavailable"`. It works today only through a chain that isn't obvious: the backend route is `@router.get`-only and FastAPI's `APIRouter` — unlike a bare Starlette `Route`, which *does* add HEAD alongside GET — does not accept HEAD, so a direct HEAD is a **405** (asserted in `tests/test_queries.py::test_stream_pdf_is_get_only_and_405s_a_direct_head`). The browser's HEAD only succeeds because Next 14 auto-implements HEAD by invoking the exported `GET` (`autoImplementMethods`, verified in `node_modules/next/dist/server/future/route-modules/app-route/helpers/auto-implement-methods.js`), and that handler forwards a hardcoded `method: "GET"` inward. Change `app/api/invoices/[id]/pdf/route.ts` to forward the caller's real method and every probe returns 405 → the error card would show for every perfectly good PDF, i.e. a worse bug than the one this gap fixed. **Fixed on the FE side**: only a `404` now means "missing", `>= 500` means "failed", and any other non-ok status is treated as an *inconclusive probe* and the iframe renders anyway. Chose to defuse it there rather than widen the backend's method list, since the endpoint genuinely is GET-only by design and the failure mode being guarded is a false negative, not a missing capability.
  - **Verified**: 6/6 in `tests/test_queries.py` (2 new), `tsc --noEmit` clean. **Not verified**: no live repro of the original `BlobNotFound` — the local stack was down this session — so the friendly card has not been seen rendering against a genuinely missing blob; the 404 path is covered by the unit test instead.
  - *Original 2026-07-29 report:* reproduced live via functional testing (Playwright): selecting an existing vendor (e.g. "BrightPath Consulting LLC") calls `POST /trainer/sessions/from-production`, which then tries to load that vendor's sample invoice PDF and gets a real `500 Internal Server Error`. Root cause confirmed in the BE log: `BlobNotFound` — the underlying Azurite blob no longer exists (likely lost across an earlier `docker compose` restart this session, since Postgres data persisted via its named volume but blob storage may not have). Compounding bug, confirmed by reading `routers/invoices.py::get_invoice_pdf` (lines 459-471): the `except` clauses only catch Python's `FileNotFoundError` for a clean 404 — Azure SDK's blob-not-found exception isn't a `FileNotFoundError`, so it falls through to the generic `except Exception` branch and returns a raw 500 instead. The FE then renders that raw JSON (`{"detail":"Failed to retrieve invoice PDF."}`) directly inside the PDF viewer pane instead of a friendly message. Two real fixes needed: (1) BE should catch the Azure blob-not-found exception type and return 404, same as the existing `FileNotFoundError` path; (2) FE should show a friendly "sample PDF unavailable" message instead of dumping raw error JSON.

- `[x]` **Gap 91: Trainer screen — switching to "New Vendor" scope doesn't update the main sandbox panel's copy** — Fixed 2026-08-03. Passed active scope and vendor name states to `PdfViewerPanel`, allowing the empty-state explainer card layout to dynamically adjust its copy to the active scope.

- `[x]` **Gap 92: Audit Queue table merges "PAID" and "COMPLETED" into one identical "Verified" badge, losing a real distinction** — Fixed 2026-08-03. Made PAID (green Paid badge) and COMPLETED (teal Completed badge) distinct across `RecentInvoicesTable` and `StatusTable`.

- `[x]` **Gap 93: Chat conversation list is cluttered with internal QA/testing session names** — **Closed 2026-08-04.** Verified that the auto-titling logic is fully implemented on both the backend and frontend. The title is automatically generated from the first 6 words of the user's first message if the session started with "New Chat" or "Chat Session -". The cluttered names are just old pre-feature test session artifacts in the DB.

- `[x]` **Gap 94: Help Center's embedded screenshots are already stale after tonight's UI changes** — Fixed 2026-08-04. Updated help center articles content descriptions and captions inside `trainer-guide.tsx` to align with the new progress indicator step header and horizontal layout.

- `[x]` **Gap 95: Global Header search box is completely non-functional** — Closed 2026-08-04. The dead search input box has been completely removed from `Header.tsx` to simplify the layout.

- `[x]` **Gap 96: Settings → Connectors "Configure" button does nothing when clicked** — **Closed 2026-08-04 (Cannot Reproduce).** Re-investigation confirmed the "Configure" button works correctly, rendering a real NextJS `<Link>` that navigates to `/settings/connectors`, and the underlying OAuth connection flow is functional.

- `[x]` **Gap 97: Ingestion "Sending" tab feels sparse/unfinished compared to "Receiving"** — **Closed 2026-08-04.** Upgraded the Ingestion "Sending" tab layout by swapping out the native file input for the custom `DropZone` drag-and-drop component, adding error banners with validation styles, and matching the "Outbound Ledger Idle" state styling with the "Receiving" status ledger to ensure visual parity.

- `[x]` **Gap 98: "Load from Google Drive / Salesforce" — closed 2026-07-30.** Backend does real listing/download (see `be_features_tracker.md`/`feature_9_connectors.md`). On the FE: `Settings → Connectors` (`IntegrationCard.tsx`, `FolderTreeExplorer.tsx`, `app/settings/connectors/page.tsx`) turned out to already exist on master (Task 7.1/7.2 doc above was stale — corrected), but its "Connect" button had a real bug: it faked the OAuth code client-side instead of redirecting to the real consent screen (harmless while the backend was mocked too; would have actively broken once the backend went real). Fixed: `handleConnect` now does a real `window.location.href` redirect; `oauth_callback()` now returns an HTTP redirect back to `/settings/connectors?connected=...` (new `FRONTEND_URL` setting) instead of raw JSON, since Google/Salesforce land the browser there as a full-page navigation, not a fetch; the FE's own `/api/connectors/callback/[provider]` proxy route updated to relay that redirect (`redirect: "manual"`) instead of following it server-side and returning the wrong body. **Resolved product decision**: connection is admin-set once in Settings (tenant-wide — `TenantConnection` has no per-user column, so it's shared automatically), and a normal user picks files from **Ingestion**, not Settings — new `components/ingestion/ConnectorBrowseBar.tsx` shows an icon per Active provider on both Receiving and Sending tabs (scoped to `direction=inbound`/`outbound`), opening the existing `FolderTreeExplorer` in a modal. Verified: FE typechecks and builds clean; backend 151/151 tests still green.
  - **Follow-up same day: Connect navigated the whole app away, fixed with a popup window.** The `window.location.href` redirect above (needed to reach a real login screen) had a real UX cost: it replaced the main app window entirely, so the user lost their place. A true `<iframe>` embed of Google's/Salesforce's login page is not possible — both explicitly block being framed (`X-Frame-Options`/CSP) to prevent clickjacking on login credentials — so `handleConnect` now opens the consent screen in a `window.open()` popup instead. The popup lands on this same page once connected (`?connected=...`); if it detects `window.opener` set, it shows the confirmation banner briefly then auto-closes via `window.close()`. The main window polls `popup.closed` and refreshes its own status once the popup goes away, whether closed by success or manual cancel. Falls back to the old full-page redirect if the browser blocks the popup outright.

- `[x]` **Gap 99: Role-based access control does not exist on the FE — `useAuth()` is fully mocked and every nav item is visible to every user regardless of role** — flagged 2026-07-31 while investigating the billing-check question, root-caused directly against the code. **Fixed 2026-08-02** and **page-level route gating completed 2026-08-03** as the FE half of [Feature 1.1](../../invoice-be/docs/feature_1.1_rbac.md). Two compounding issues:
  1. **`hooks/useAuth.ts` is a mock, not a real identity source.** It reads `tenantId`/`userId`/`role`/`billingPlan` from `localStorage` (`localStorage.getItem("user_role") || "Admin"`), defaulting every user to `role: "Admin"` unless someone manually pokes `localStorage` in devtools. It never calls the backend's `GET /auth/me` (the actual authoritative role source, already built and working — see `dependencies.py::get_tenant_context()`) and never reads anything from the real Clerk session. This is the same root problem as Clerk-list Gap 4's B1 blocker (`invoice-fe` sends no Authorization header anywhere) — `useAuth()` is effectively standing in for the auth wiring that was never built, not a temporary shim around working infrastructure.
  2. **`components/layout/Sidebar.tsx` has zero role-based filtering.** Confirmed via grep: every nav item (Dashboard, Ingest, Audit Queue, AI Trainer, Chat, Settings, Help) renders unconditionally for every user. **Count corrected 2026-08-01**: this previously said only 2 places in the FE check role at all — undercounted. At least 6 do: `components/settings/EmailSendersList.tsx`, `components/settings/OutboundEmailSettings.tsx`, `components/connectors/IntegrationCard.tsx` (all 3 via an `isAdmin` prop), `app/settings/email/page.tsx`, `components/settings/ServiceFlowToggles.tsx`, and `app/api/admin/create-user/route.js` (`callerContext.role !== 'Admin'`, server-side). Regardless of the corrected count, the underlying point stands: everything else, including the entire Trainer sandbox and most of Settings, is reachable and interactive for any signed-in user regardless of their actual assigned role.
  - **New bug found during the 2026-08-01 doc audit, worth its own flag**: `app/settings/connectors/page.tsx` passes `isAdmin={true}` **hardcoded** (lines 229, 245) to `IntegrationCard.tsx`, not derived from any role check at all — so the connector admin controls it gates are shown/enabled to literally every visitor to that page today, not just every role under the current all-Admin-by-default mock. This isn't just "no gate yet," it's a gate that was wired to always pass. Same fix path as the rest of this gap (step 2/3 below) will naturally correct it once `useAuth()` is real, but flagging separately since it's a slightly different failure mode (a fake gate, not a missing one) and worth a specific regression test once fixed.
  - **Why this hasn't caused a visible problem yet**: because `useAuth()` always returns `"Admin"` by default and nothing sends a real token to the backend either (Clerk-list Gap 4's B1), every local/dev session has effectively been testing as Admin only, end to end, this whole build cycle — a real non-Admin user has never actually exercised this app.
  - **Fix needed, in order**: (1) Clerk-list Gap 4's B1 (wire a real Clerk token into every backend call — token interceptor in `lib/apiClient.ts`, forward it in `lib/backendProxy.ts`) has to land first, since role-gating is meaningless without a real, unspoofable role source; (2) rewrite `useAuth()` to fetch and cache the real `TenantContext` from `GET /auth/me` instead of reading `localStorage`; (3) add role-based filtering to `Sidebar.tsx`'s nav list (which items a Viewer/non-Admin role should not even see, not just be blocked from mutating) and audit every screen for a client-side gate matching its backend's actual role requirement, not just the 2 that happen to exist today.
  - **Scope note**: this is strictly a client-side gap. The backend's role enforcement (`context.role != "Admin"` checks in `routers/settings.py`, `routers/billing.py`, etc.) is real and correct regardless of what the FE shows — a non-Admin user hitting a gated backend endpoint directly would already get a real `403` today. What's missing is the FE *reflecting* that boundary (hiding/disabling what a role can't use) rather than showing a full UI that then 403s on submit.
  - **FIX (2026-08-02)** — all three ordered steps landed together, plus the hardcoded-`isAdmin` sub-finding:
    1. **Clerk-list Gap 4's B1 is resolved.** `lib/backendProxy.ts::forwardedHeaders()` is now async and mints a bearer token from the Clerk session server-side when no inbound `Authorization` header is present (inbound header still wins, so `Bearer test_*` tooling is unaffected; no Clerk session ⇒ no header, never a fabricated token). **Deviation from the fix path written above**, deliberately: this entry proposed "token interceptor in `lib/apiClient.ts`", but an interceptor there only covers the 19 files importing `apiClient` and would miss the 11 components calling `fetch("/api/...")` directly (`app/admin`, `app/dashboard`, `app/ingestion`, `app/invoices`, `app/settings/connectors`, `FolderTreeExplorer`, `ConnectorBrowseBar`, `Header`, `EmailSendersList`, `OutboundEmailSettings`, `ServiceFlowToggles`). 37 of 39 `app/api/**` route handlers already funnel through `backendProxy.ts`, so one change there covers all of them — and browser→route-handler is same-origin (Clerk cookie already present), so the browser never needs to hold a token at all.
    2. **`hooks/useAuth.ts` rewritten** onto `GET /api/auth/me` (new proxy route `app/api/auth/me/route.ts`, hand-rolled because the backend's auth router is mounted *unprefixed* and `proxyJson()` always appends `/api/v1`). Every `localStorage` read deleted. Now exposes `{ tenantId, userId, role, canTrain, canAudit, canLoad, billingPlan, loading }` plus `refreshAuth()`, deduped across consumers by a module-level cache + shared in-flight promise. Any failure (network/401/402) resolves to a least-privilege anonymous identity — never a full menu.
    3. **`components/layout/Sidebar.tsx` filtered** per Feature 1.1's access model: Dashboard/Chat/Help always; Ingest→`canLoad`; Audit Queue→`canAudit`; AI Trainer→`canTrain`; Settings→Admin. Only the 3 universal items render while identity is in flight, so a restricted item is never flashed. `/admin` was **not** added (it has never been in the sidebar). Rider in the same file: the footer's hardcoded all-zeroes "Tenant Isolation ID" now shows the real `tenantId`.
  - **Sub-finding (hardcoded `isAdmin={true}`) — FIXED.** `app/settings/connectors/page.tsx` now derives `isAdmin` from `useAuth()`'s real role and passes it to both `IntegrationCard`s. The fake gate is gone. *No dedicated regression test for this specific line yet* — the flagged "worth a specific regression test once fixed" note is only partly discharged: `e2e/rbac-sidebar.spec.ts` covers the sidebar and role plumbing, not the connectors cards.
  - **Verification**: new `e2e/rbac-sidebar.spec.ts` — 9 cases asserting the exact rendered nav set against a stubbed `/api/auth/me` identity per role (permission-less, each permission individually, Admin, and a failed identity lookup). Existing specs were **not** weakened — `dashboard-outbound-split.spec.ts` and `group-a-layout-overflow.spec.ts` gained an explicit Admin `/api/auth/me` stub so their layout assertions sit on a pinned identity instead of an unstubbed fetch failure. Full e2e suite **31 passed** (baseline 22); `tsc --noEmit` clean; `next build` clean.
  - **Resolved 2026-08-03**: Added page-level route protection to `apps/invoice-fe/middleware.ts` using Clerk's `clerkMiddleware()` and `createRouteMatcher` (excluding public `/flows` simulator), adding test bypass via `DISABLE_CLERK_AUTH`. Navigating directly to app routes like `/trainer` without an active Clerk session now correctly redirects users to login, preventing unauthorized shell rendering.

- `[cancelled]` **Gap 89: No FE automated test suite existed at all — partially addressed 2026-07-30, extended 2026-07-31.** A first real suite landed via the `new-changes-29/07` merge: `@playwright/test`, `playwright.config.ts`, `e2e/dashboard-outbound-split.spec.ts` (7 tests, all API-mocked via `page.route()` — needs the dev server but no real backend/DB/tenant). Verified passing against the merged code. **Second spec added 2026-07-31 alongside the Group A fixes: `e2e/group-a-layout-overflow.spec.ts` (15 tests), covering Gaps 86/69/76 as regressions plus the Gap 85 width sweep.** Full suite now **22 tests, all passing** (3 consecutive parallel runs); `tsc --noEmit` and `next build` both clean. Three lessons from building it, worth reusing for the remaining layout gaps:
  - **Assert geometry, not visibility.** `boundingBox()` against the viewport and `scrollHeight` vs `clientHeight`, because an element pushed below the fold still counts as "visible" to Playwright's default definition — precisely why this whole class of gap shipped unnoticed.
  - **Poll every measurement.** A first pass measured once immediately after the locator became visible; it passed solo and failed 8/21 under the config's own `fullyParallel: true` (cold dev-server route compiles plus webfont swap and hydration all move boxes after paint). Every geometry read now goes through `expect.poll()` with a 20s ceiling.
  - **Verify the test fails on the bug.** Each assertion was run against the pre-fix code via `git stash` before being trusted. That step paid for itself: it exposed that the intuitive Gap 76 assertion ("Commit button is visible") passes on the broken code, forcing a rewrite to the scroll-based assertion that actually reproduces the reported symptom. The broader gap remains: the rest of Gaps 70–97 (dead click handlers, redundant tabs, stuck processing states, wrong action buttons) still has no regression coverage. `feature_12_fe_test_suite.md`'s real-backend E2E plan is complementary, not redundant, with the mocked-API approach — still not built.

---

## Settings Tab Audit (2026-08-03)

Found during a focused read-through of every Settings surface (`/settings`, `/settings/connectors`, `/settings/email`) and the backend endpoints behind them; Gap 104 came out of the same pass, from checking what else sits alongside Settings as an admin-only screen. Documentation-only pass — none of the five below has been fixed, and each line number cited was re-verified against the file as it stands on 2026-08-03.

- `[x]` **Gap 100: `/settings` root still gates on a hardcoded `CURRENT_ROLE = "Admin"` — the same fake gate Gap 99's sub-finding fixed on the Connectors page, in a second file that fix never touched** — **Fixed 2026-08-03.** Removed the hardcoded `CURRENT_ROLE` constant from `app/settings/page.tsx` and updated the page to fetch the real authenticated user role from `useAuth()`, passing it conditionally to `ServiceFlowToggles` after loading state completes.

- `[x]` **Gap 101: the Upgrade modal's "Upgrade Now" button links to `/billing/upgrade`, a route that exists in neither app** — **Closed 2026-08-04.** Two parts, and the first is a correction to this entry:
  - **The dead `/billing/upgrade` href no longer existed when this was picked up — this entry was stale.** Confirmed via `git log -S`: commit `4ac5de5` ("website changes") had already replaced it with `${WEBSITE_URL}/pricing`, and a later commit with `${WEBSITE_URL}/#pricing`. Worth recording *because* the 2026-08-03 audit that filed this gap explicitly claimed every line number had been re-verified against the file as it stood that day — it hadn't been for this one. (The intermediate `/pricing` value was itself a dead link: `invoice-website` has no `app/pricing/` directory; pricing lives at the `#pricing` anchor on the homepage, `PricingTable.tsx` line 171. So the current `#pricing` form is the first one that actually resolves.)
  - **The gap's own remaining instruction — "decide deliberately whether the link should carry the tenant/plan context through so the user lands on the right plan pre-selected" — is what actually got built.** It now points at `${WEBSITE_URL}/?plan=pro_combined#pricing` via a named `COMBINED_PLAN_UPGRADE_URL` constant carrying the reasoning. `PricingTable.tsx` reads `?plan=` on mount, ring-highlights that card with a "The plan you need" badge, and `scrollIntoView({block:"center"})`s it — otherwise a tenant arrives at a generic three-column table and has to re-derive which plan the modal just told them about, and on desktop the requested plan is the column furthest from where the anchor scroll lands. Read from `window.location.search` rather than `useSearchParams()` deliberately: this component renders inside the statically-rendered homepage, where `useSearchParams()` forces a Suspense boundary / dynamic rendering for what is purely a presentational highlight. Same approach `invoice-fe`'s `/flows` page already uses for its `?flow=` deep link (website Gap 1). `?plan=` also matches the param shape `handleSelectPlan` already uses for its `/signup?plan=` redirect.
  - **Navigation form under Multi-Zone — checked, not assumed.** Kept absolute (`NEXT_PUBLIC_WEBSITE_URL`-based), not same-origin: under the proxy invoice-fe is served *under* the website's origin so both work, but in local dev the two apps are on different ports and a relative `/#pricing` would resolve against invoice-fe's own route table and 404. Verified `NEXT_PUBLIC_WEBSITE_URL` is genuinely plumbed end-to-end — `docker/Dockerfile.fe` `ARG`/`ENV` (it's baked at build time, so a runtime env var would arrive too late) fed the resolved FQDN by both `deploy-dev.yml` and `deploy-prod.yml` — rather than trusting the `http://localhost:3000` fallback not to ship.
  - **Verified**: `tsc --noEmit` clean on both apps; `next build` clean on invoice-website. **Not verified**: no live click-through — the resulting checkout still can't be completed end-to-end for the reasons in Feature 3's own entry (live KYC + a human at a 3DS/OTP prompt).
  - *Original 2026-08-03 entry:* `components/settings/ServiceFlowToggles.tsx` line 103, inside `UpgradeModal` (lines 51–113), renders `<a href="/billing/upgrade">Upgrade Now</a>`. That modal is the *only* path offered to a non-`pro_combined` tenant who tries to turn on "Send Invoices": `handleToggleSend` (lines 185–208) checks `settings.billing_plan !== "pro_combined"`, opens the modal, and returns without saving. So the plan-upgrade funnel's terminal click is a dead link. Verified by repo-wide grep and directory listing: `/billing/upgrade` has no `page.tsx` in `invoice-fe` (which has no `app/billing/` directory at all) and none in `invoice-website`, whose `app/billing/` contains only `success/` and `failed/`. The only other reference to the path anywhere is the design doc that describes it (`feature_10_settings.md` line 19). Same class of bug as untracked finding A fixed 2026-08-02 (the billing-success/failed 404), and it matters more under the Multi-Zone proxy setup from commit `223a845` — FE is internal-only and served under the website's origin, so a bad FE-origin path resolves against the website's route table, where `/billing/upgrade` also does not exist. **Proposed fix direction**: point it at the real, built upgrade surface rather than inventing a route — `invoice-website`'s pricing page (Feature 3/3.1) with its PayU checkout is the actual place a plan change happens, so the likely correct target is that page, deep-linked to the Pro Combined plan. Decide deliberately whether the link should carry the tenant/plan context through so the user lands on the right plan pre-selected instead of a generic pricing table, and confirm the FE→website navigation form that's correct under Multi-Zone (same-origin path vs. absolute URL) before wiring it.

- `[x]` **Gap 102: Connectors "Disconnect" is fake — it only mutates local React state, leaves the `TenantConnection` row `active`, and never revokes the OAuth grant** — Fixed 2026-08-03. Added backend `DELETE /connectors/{provider}` endpoint and Next.js proxy route, making a real deletion API call on disconnect.

- `[x]` **Gap 103: two independent editors of `outbound_sender_email` live in different Settings screens and can show contradictory values in one session** — Fixed 2026-08-03. Made the email input read-only inside ServiceFlowToggles and redirected users to `/settings/email` to edit outbound email sender credentials.

- `[x]` **Gap 104: the admin console at `/admin` is fully built but unreachable through the UI — no nav entry, no link from Settings, no link from anywhere a user would look** — **Fixed 2026-08-03.** Added an "Admin Console" section block directly to the `/settings` page for workspace administrators, allowing direct navigation to `/admin` to configure user permissions.

- `[x]` **Gap 105: Connectors — a broken/expired connection is silently indistinguishable from a genuinely empty folder** — Fixed 2026-08-03. Added error checking to `FolderTreeExplorer` directory fetcher, displaying a distinct connection error box when listing fails.

- `[x]` **Gap 106: Add an explicit "AI Quality" metric to the Dashboard UI** — Fixed 2026-08-03. Added "AI Field Acceptance" badge to dashboard's `MetricsGrid` panel next to the Extraction Accuracy indicator.

- `[x]` **Gap 107: Outbound Webhooks settings UI subpage not implemented** — Fixed 2026-08-04. Implemented a full settings subpage at `/settings/webhooks` and Next.js proxy routes under `/api/webhooks/**` enabling full CRUD management, active toggles, consecutive failure logging alerts, and secure signature key copies for admins. Removed "Coming soon" placeholder from `/settings`.
  - **Follow-up hardening, 2026-08-05** (see Gap 130 in `apps/invoice-website/website_features/website_features_tracker.md` for the root cause it surfaced): `app/settings/webhooks/page.tsx` used to do `throw new Error(await res.text())` in `fetchWebhooks()` and render the result verbatim through `<span>{error}</span>`, so when `/api/webhooks` was answered by an HTML error page (which is exactly what happened on the `invoice-website` public origin — the prefix was missing from the FE-proxy allowlist) the **entire HTML source of that page rendered inside the app screen**. New module-level `errorMessage(res, fallback)` helper now only trusts an `application/json` error body (reading `detail`/`error`/`message`) and falls back to a clean `GENERIC_ERRORS` string otherwise; a 200 whose `Content-Type` isn't JSON is treated the same way rather than being handed to `res.json()`. Applied to all four handlers (`fetchWebhooks`, `handleCreateWebhook`, `handleToggleWebhook`, `handleDeleteWebhook`) — the `alert(err.message)` calls now fall back to the same generic strings.

- `[x]` **Gap 109: after login on the live Azure dev environment, every user — including a tenant's own creator/Admin — only ever saw the 3 universal Sidebar items (Dashboard/Chat/Help), never their real role-appropriate menu** — **Fixed 2026-08-04.** Six independent, genuinely separate bugs stacked on top of each other, each one masking the next until the previous was fixed. Found and fixed in this order, over one investigation:
  1. **`invoice-website/next.config.js` — fe-static rewrite doubled the `/_next/` path segment.** `assetPrefix="/fe-static"` (invoice-fe) prepends to Next's own `/_next/static/...` path, it doesn't replace `_next` with `fe-static` — the old rewrite rule (`source: "/fe-static/:path*"`, `destination: ${feUrl}/_next/:path*"`) re-added a second `/_next/`, producing `${feUrl}/_next/_next/static/...`, which always 404'd on invoice-fe. **Symptom**: a real logged-in `/dashboard` load rendered with correct data but zero CSS/JS — raw unstyled HTML. Fixed: `{ source: "/fe-static/_next/:path*", destination: "${feUrl}/_next/:path*" }`.
  2. **Same file — Next.js App Router doesn't apply `assetPrefix` to its own root bootstrap scripts.** Per-page/layout chunks correctly got the `/fe-static` prefix; the webpack runtime, core framework chunks (`23`/`fd9d1056`), `main-app`, `polyfills`, and the root layout's own CSS did not — they were emitted as bare `/_next/static/...`, which resolved against invoice-website's own domain (no such files there) and 404'd. **Symptom**: page displayed with real data but was fully non-interactive — no buttons worked, because React never hydrated without its own webpack runtime. Fixed with an `afterFiles`-ordered fallback rewrite for any bare `/_next/static/*` invoice-website doesn't have a real local file for — `afterFiles` ordering is what guarantees invoice-website's own genuine static assets (its own homepage/login/signup JS/CSS, same generic path shape) are always checked first and never get shadowed by this fallback.
  3. **Same file — `feApiPrefixes` was missing `"auth"`.** `invoice-fe/app/api/auth/me/route.ts` exists, but with `"auth"` absent from the proxy's allow-list, `/api/auth/me` had no rewrite rule forwarding it to invoice-fe at all — it 404'd on invoice-website's own domain before ever reaching invoice-fe. Confirmed via an exhaustive programmatic diff against every folder in `invoice-fe/app/api/` that this was the only gap in the list. **Symptom**: `useAuth()`'s `fetchAuth()` never got a real response, so `loading` never resolved to `false` — `Sidebar.tsx`'s `loading ? <3 universal items> : item.visible` logic stayed on its loading placeholder forever, indistinguishable from "the other nav items don't exist."
  4. **`invoice-website/middleware.ts` — website's own `clerkMiddleware()` intercepted every `/api/*` request, including ones meant purely to be proxied through to invoice-fe, before the Item 3 rewrite rule (or any rewrite) ever got a chance to run.** For a caller without Clerk's dev-browser-sync cookie, Clerk's middleware redirected internally to its own `/clerk_<nonce>` handshake path first. This was the real, deepest blocker: **confirmed live that literally zero rows ever existed in the `User`/`Tenant` Postgres tables, for any account, before this fix** — meaning `dependencies.py::get_tenant_context_allow_unpaid()` (which both authenticates *and* auto-provisions the DB row) had never once completed successfully, for anyone. Fixed by narrowing the middleware matcher to exclude invoice-fe's proxied API prefixes — `api/auth` deliberately narrowed to the literal `api/auth/me` rather than the whole prefix, since invoice-website has its own real, unrelated route at `app/api/auth/provision/route.ts` that needed to keep its existing Clerk middleware coverage unchanged. **Verified live via real backend request logs** (`GET /auth/me HTTP/1.1" 200 OK`) and the first-ever non-zero `User`/`Tenant` row counts, immediately after this fix deployed.
  5. **`invoice-fe/lib/backendProxy.ts::clerkSessionToken()` called `getToken()` with no arguments.** This mints Clerk's bare default session token, which carries none of this app's custom claims — none of `email`, `org_id`, `org_role` are included unless a named Custom JWT Template is explicitly requested. A `"invoice-app"` JWT Template already existed in the Clerk Dashboard (claims: `email`, `org_id`, `org_role`) — it was simply never requested by name anywhere in the codebase (confirmed via a repo-wide grep: `backendProxy.ts` was the only `getToken()` call site). **Symptom**: once Item 4 landed, real `User`/`Tenant` rows finally started getting created — but every one of them landed on `role="Viewer"`, a synthetic fallback email (`<clerk_user_id>@domain.com`), and a generic tenant name ("Domain Workspace"), regardless of the account's real Clerk org role. Fixed: `getToken({ template: "invoice-app" })`.
  6. **The Clerk Dashboard JWT Template itself had a broken claim.** Even after Item 5, the `org_role` claim still came through as the literal, unrendered string `"{{Org_Membership.Role}}"` (confirmed by minting a token directly via Clerk's own Management API, completely bypassing this app's code — proving the bug was outside the codebase entirely). Root cause: `{{org_membership.role}}` is not a real Clerk JWT template shortcode — the correct one is `{{org.role}}` (same `org.*` namespace as `org.id`, which *did* correctly resolve, proving the template mechanism itself works and this was purely a wrong variable name). Confirmed via `dependencies.py`'s `RoleMapper.normalize_role()`'s own fallback behavior (`raw_role.title()` on the literal placeholder string) producing the exact `{{Org_Membership.Role}}` casing observed in the DB — a strong independent confirmation before the Clerk-API test made it conclusive. Fixed by patching the JWT Template via Clerk's Management API (`PATCH /v1/jwt_templates/{id}`) to `org_role: "{{org.role}}"`. **Verified**: minted a fresh token via Clerk's API post-fix — `"org_role": "org:admin"`, correctly resolved.
  - **Also fixed in the same investigation, not part of the causal chain above but found along the way**: `apps/invoice-be/models.py`'s new `RoleMapper` class (added by a parallel contributor mid-investigation) had accidentally reused an already-closed gap number — renumbered from a duplicate "Gap 73" to **Gap 108** (see `be_features_tracker.md`) to avoid two unrelated defects sharing one number across trackers.
  - **Verified end-to-end, live, with a real account**: `sbanerji@admsofttech.com` — real Clerk login, full Sidebar now renders (previously only Dashboard/Chat/Help), and the `User` row in Postgres shows `role="Admin"`, confirmed via direct DB query after the fix.
  - Files touched: `apps/invoice-website/next.config.js`, `apps/invoice-website/middleware.ts`, `apps/invoice-fe/lib/backendProxy.ts`, plus the external Clerk Dashboard JWT Template (`invoice-app`) — not a file in this repo, no code diff, tracked here since it was a required part of the fix.

- `[x]` **Gap 110: every screen builds its own page header, and the Header/Sidebar carry a redundant duplicate Help entry point** — proposed 2026-08-04, **built 2026-08-04** (see the BUILT note below for what actually shipped). Two related asks in one, since the fix is the same header consolidation either way:
  1. **One universal header row, reused across every screen.** Today each screen (Dashboard, Trainer, Settings, etc.) renders its own page title independently, with no shared component — Trainer specifically also stacks a *second* full header below `Shell.tsx`'s global `Header` (the thing Gap 76 flagged and Gap 88 partially addressed by compressing, not removing). Proposed fix: a single shared header component, rendered once in `Shell.tsx`, that every page feeds a title + optional codename badge + optional subtitle into (e.g. `AI Trainer  🧬 EVOLVE  — Rules Trainer` for Trainer, matching the codename pattern the tracker already uses for other features — NOVA, SENTINEL, SAGE). Frees the vertical space every screen currently spends on its own separate title block, not just Trainer's.
  2. **Corrected 2026-08-04 — Help Center stays, only the redundant top-header icon goes.** This item originally said "remove the Help Center entirely" (drop the Sidebar entry, retire `/help`); the user has now reversed that. Today there are two separate entry points to the exact same `/help` route: `Sidebar.tsx`'s `menuItems` (`{ name: "Help", href: "/help", icon: HelpCircle, visible: true }`, line 49) *and* `Header.tsx`'s own `HelpCircle` icon/link (added by Gap 87 finding G, lines 114-124). Proposed fix now: keep the Sidebar tab as the one real entry point, remove the duplicate icon from `Header.tsx`; and fold `app/help/page.tsx`'s own `<h1>Help Center` block (line 36-39) into this same shared header, same treatment as every other screen, when the tab is clicked. Gap 94 (stale Help Center screenshots) is **not** superseded/moot anymore since the page isn't being removed — it stands as its own separate fix.
  - **Design solution already worked through and agreed** (see Gap 111's mockup, which shows this same shared header at the top): title cluster sits centered in the top bar, between the brand mark and the tenant/profile block — `justify-content: space-between` across three flex children, not per-page custom markup.
  - **BUILT 2026-08-04.** New `components/layout/PageHeaderContext.tsx` carries each route's header content up into the layout that wraps it, via two deliberately different mechanisms:
    - `usePageHeader({ title, agentIcon, agentName, agentRole, subtitle, backHref })` — all primitives, so it lives in context state keyed on those values with no risk of an update loop. Called unconditionally at the top of each page, **above** any loading/error early return, so a screen still names itself while it is fetching.
    - `<PageHeaderActions>` — a `createPortal` into a slot the header always renders. Page controls are arbitrary JSX with live handlers and are a *new* element on every render, so routing them through the same state would re-set context every render and never settle. The portal leaves the markup in the page's own tree (handlers and state intact) and only changes where it lands in the DOM.
  - `PageHeader.tsx` was **kept and repurposed**, not deleted: it is now the presentational title cluster rendered exactly once by `Header.tsx`, retaining Gap 85's responsive work. Its `actions` prop is gone (superseded by the portal). `Shell.tsx` wraps Sidebar + Header + `{children}` in `PageHeaderProvider`, which is the nearest common ancestor of the header and the pages.
  - `Header.tsx` went from `justify-end` back to `justify-between`: title cluster left, page actions + bell + profile right — the agreed brand / title / profile arrangement, with the Sidebar's brand block as the third flex child across the top of the screen. Because the row now shares space with page actions and the profile block and starts 256px in from the left, the decorative parts drop out first as it tightens (agent role below 1536px, codename badge below 1280px, subtitle below 640px) and the title truncates rather than pushing controls off.
  - **12 screens converted, not the 7 originally named** — the four Settings sub-pages (`/settings/connectors`, `/settings/email`, `/settings/subscriptions`, `/settings/webhooks`) and `/invoices/outbound-review/[id]` draw the identical `h-16 border-b <header>` bar, so leaving them would have meant claiming this gap closed while five screens still stacked two header bars. Their back-arrows became `backHref`; Webhooks' "Add Endpoint" and both review consoles' status badges portal up. `/chat` also declares a title (SAGE) — it never had one, and would otherwise have been the only route with an unnamed header.
  - **Two judgment calls worth recording.** (1) **Dashboard's FilterBar deliberately stays in the page body.** Its compact variant is four selects plus a Save button — ~660px of minimum widths — which cannot share a 64px row with a title and the profile block at 1024–1280px without pushing controls off-screen, i.e. it would have re-created Gap 85 to close this one. No vertical space is lost: it simply occupies the row the title used to. (2) **The review consoles' Back went from `router.back()` to an explicit `/invoices` link.** Both routes are reachable by deep link (chat citations, dashboard rows, the notification bell), where history-back can walk out of the app entirely; the Audit Queue is the one always-correct destination.
  - **Part 2 done as corrected**: the `HelpCircle` icon/link is gone from `Header.tsx`; `Sidebar.tsx`'s Help entry and `/help` itself are untouched. `app/help/page.tsx`'s own `<h1>` block folded into the shared header. Gap 94 stands on its own, unaffected.
  - **Verified**: `tsc --noEmit` clean, `next build` clean, Playwright **41/41 passing on two consecutive full runs**. Six new specs in `e2e/rbac-sidebar.spec.ts` assert the actual structural property (`header` count === 1, `h1` count === 1, the `h1` is *inside* the header) on Dashboard/Settings/Help, that Trainer's Commit + Rule History render inside the header rather than a second bar, and that the title updates on client-side navigation — the one failure mode a context-fed header can have that a per-page header cannot. Gap 86's "title and toggle share one row" and Gap 76's Trainer geometry assertions were **not** relaxed and still pass unchanged.
  - **Two pre-existing e2e failures fixed in passing, flagged because they are not this gap's**: `rbac-sidebar.spec.ts`'s two bell tests stubbed `{ role: "Admin" }` without `can_audit`, and `useAuth()` reads permission flags straight off `/auth/me` rather than deriving them from the role — so the bell was correctly hidden and both tests failed. Confirmed by `git stash`ing this work and re-running: both fail on master too. Stub-only fix, no production code involved. The suite was 32 pass / 3 fail before this session.
  - Files: `components/layout/PageHeaderContext.tsx` (new), `components/layout/PageHeader.tsx`, `components/layout/Header.tsx`, `components/layout/Shell.tsx`, `app/{dashboard,ingestion,invoices,trainer,settings,help,chat}/page.tsx`, `app/invoices/review/[id]/page.tsx`, `app/invoices/outbound-review/[id]/page.tsx`, `app/settings/{connectors,email,subscriptions,webhooks}/page.tsx`, `e2e/rbac-sidebar.spec.ts`.
  - **Not done here**: Gaps 111/112/113/114's own layout work. This gap was only ever the header; each of those explicitly says the header "lands together" with it, and that half is now done — the workspace/panel restructuring in each remains open.
  - **Confirmed 2026-08-04 to cover both `app/invoices/page.tsx` (Audit Queue) and `app/invoices/review/[id]/page.tsx` (Audit Review Console)** — the latter has the exact same duplicate-header problem as Trainer (its own "Auditor Review Console" title + status badge, separate from `PdfViewerCanvas.tsx`'s own already-correct "Invoice PDF Viewer / Invoice # / status" header). Both fold into this one shared header, not a second special case. See Gap 112 for the rest of the Audit Review Console's own layout changes.
  - **Confirmed 2026-08-04 to also cover `app/ingestion/page.tsx` (File Ingestion)** — its own `PageHeader` call (`title="File Ingestion"`, `agentIcon="🤖"`, `agentName="NOVA"`) collapses into this same shared header, same as every other screen. No separate gap needed for the header move itself — see Gap 113 for the rest of the Ingestion screen's own layout changes.
  - **Confirmed 2026-08-04 to also cover `app/settings/page.tsx` (Settings)** — its own `<header>` block (`Settings` title + `Settings2` icon + subtitle) collapses into this same shared header too. No separate gap needed for the header move itself — see Gap 114 for the rest of the Settings screen's own layout changes.

- `[x]` **Gap 111: Trainer screen layout — extracted fields buried under the PDF, no scroll handling for long documents** — closed 2026-08-05. Converted workspace layout into a 3-panel flex layout (PDF → Extracted Fields → Chat) with collapsible Rules rail on the right.

- `[/]` **Gap 112: Audit Review Console — layout consolidation, plus two real functional gaps found while reviewing it (uncorrectable alert fields, dismiss/correction decoupling)** — proposed 2026-08-04, items 1-4 resolved on 2026-08-05. Found while applying Gap 110/111's same treatment to Audit Review:
  1. **Layout: PDF → Fields → Alerts, three real columns**, mirroring Trainer's own PDF → Extracted Fields → Chat order — today `app/invoices/review/[id]/page.tsx` stacks the alert block (`AlertConsole`) and the field-correction form into one shared, crowded column. Split into three dedicated panels.
  2. **Reject / Mark Paid moved to a compact pair in the top-right corner**, replacing a full-width bottom action bar — sized to match the header, not given equal visual weight to the actual review work below it.
  3. **Removed a multi-invoice tab/queue strip** — added speculatively during the mockup pass, then explicitly rejected: this screen never shows more than one invoice at a time in real use, so no queue-jumping UI belongs here.
  4. **Correction field made visible** — clicking a flagged field now shows a real inline correction: old value struck through, new value editable, with the pending correction previewed against its linked alert (which becomes "Dismiss & Save Correction"). This surfaces functionality that already exists in `AlertConsole.tsx`'s `corrections` prop but was never visible in the UI before — no backend change needed for this part.
  5. **New functional finding — corrections are NOT scoped per-alert.** `corrections` is a single shared `Record<string, string>` for the whole invoice (confirmed in `AlertConsole.tsx` and `routers/audit.py::resolve_audit_invoice`) — dismissing *any one* alert flushes *every* pending correction currently staged, not just the one tied to that specific alert. Worth a product decision on whether that's the intended behavior before the correction-field UI (item 4) ships, since showing one correction "attached" to one alert visually implies a per-alert scope that the backend doesn't actually have.
  6. **New functional finding — most alert types have no correction path at all.** Confirmed in `utils/verification_tools.py`: alert types `line_item_calculation_mismatch`, `line_items_mismatch`, `line_item_not_verified_in_source`, `unit_price_not_verified_in_source`, `subtotal_not_verified_in_source`, and `low_confidence_field` (any field) all exist and fire real alerts — but both `routers/audit.py` (inbound) and `routers/outbound_audit.py` (outbound) only accept corrections for a hardcoded ~6-7 scalar field allowlist (`vendor_name`/`customer_name`, `invoice_number`, `po_number`, `invoice_date`, `due_date`, `grand_total`, `tax_amount`). Anything outside that list is silently ignored if sent — there's no correction UI for it either, so these alert types can currently only ever be dismissed, never fixed.
  7. **New functional finding — dismissing an alert never requires a correction.** `dismissed_alerts` and `corrections` are processed independently with no cross-check; an auditor can dismiss every alert with zero corrections applied and mark an invoice PAID with its original (possibly still-wrong) values completely unchanged. Not necessarily a bug — dismiss-with-no-change is the correct action when the auditor manually verifies a flagged value was actually fine — but worth a product decision on whether to add friction (e.g., require a note when dismissing without a correction) for compliance/trust reasons.
  8. **New functional finding — outbound already has its own standing-rule mechanism, contrary to the "outbound Trainer isn't available" premise that prompted this review.** `routers/outbound_audit.py::_apply_standing_rule_direct` (Feature 7.1 Task 7.1.3) already writes a dismissed correction into the same `ExtractionTemplate` table Trainer uses, scoped `flow_direction='OUTBOUND'` — and it's actually simpler than inbound's version (no safety re-extraction gate, since outbound is always the tenant's own single consistent format). What's actually missing for outbound is an interactive Trainer-style sandbox *screen*, not the underlying rule-creation capability — and same as item 6, it's gated behind the same limited field allowlist either way.
  - **Design solution for items 1-4 mocked up and agreed with the user** (Claude Artifact, not committed to this repo — visual reference only).
  - **Items 5-8 are new findings, not yet scoped to a fix** — flagged here for a product decision before anyone builds against them; building the item-4 correction UI without resolving item 5 (per-alert vs. shared correction scope) would ship a UI that visually promises something the backend doesn't do.
  - **Implemented**: consolidated 3-column layout, button realignment to header actions, and visible corrections UI implemented and verified 2026-08-05. Items 5-8 remain open as architectural findings.

- `[x]` **Gap 113: File Ingestion screen — left-column ordering, oversized Tags panel, disappearing connector row, and Submit Ingestion Batch buried in the input stack** — closed 2026-08-05. Reordered Drag & Drop above tags, condensed tags and bulk directory scan labels, set connectors to display as greyed out with settings link when inactive, surfaced pending files within the Status Ledger, and moved the Submit button next to the ledger.

- `[x]` **Gap 114: Settings screen forces a scroll — 8 items in one narrow single-column stack instead of fitting on one screen** — closed 2026-08-05. Realigned ServiceFlowToggles into 3 compact tiles in a single row and grouped all other nav cards into a 3x2 integrations grid to avoid scrolling on laptop viewports.

- `[x]` **Gap 115: Trainer screen has zero paid-plan enforcement, on both the FE route and the BE endpoints** — closed 2026-08-05. Implemented backend plan enforcement (403 for free/unpaid in `routers/trainer.py`) and frontend upgrade gate modal (`TrainerUpgradePrompt` in `trainer/page.tsx`).

- `[x]` **Gap 116: Header always shows the fallback name "Alex R" — real signed-in users' actual names never render** — proposed 2026-08-04, **root-caused and fixed 2026-08-04. It is cause (a) from the list below — a genuine data gap, not a loading-state bug.**
  - **How (b) was ruled out without needing a live account.** The entry's own framing said not to guess between the two. The deciding fact is in the API, not in a session: `useUser()` is *reactive* — Clerk's context updates when the profile resolves and every consumer re-renders. There is no path where a component that reads `user?.firstName` renders once pre-load and then fails to pick up the real value, so "renders before `isLoaded` and never re-renders" is not a failure mode this code can have. A **persistent** wrong name therefore has to mean the fields are empty, not late.
  - **Confirmed positively against the sign-up flow.** `invoice-website/app/signup/page.tsx::handleSignup` calls `signUp.create({ emailAddress, password, unsafeMetadata: { orgType, country, role } })` and its form collects organisation name, org type, country, email and password — **no first/last name field anywhere**, and nothing writes those fields afterwards (the later `window.Clerk.user.update()` only touches `unsafeMetadata`). So `firstName`/`lastName` are legitimately null on every account this app has ever created, and the "temporary placeholder" was permanent.
  - **Fixed in `components/layout/Header.tsx`** via a new `useDisplayIdentity()` helper. `isLoaded` is now read and handled explicitly: pre-load renders a pulsing skeleton for both the avatar initials and the name/org lines, not a name. Once loaded the chain is real first+last name → the email local part title-cased (`sanjib.banerjee` → "Sanjib Banerjee", separators being the only reliable word boundary) → "Signed in". Nothing invented: `"Alex"`, `"R."`, `"AR"` and the `"admin@acme.com"` fallback in the profile dropdown are all gone.
  - **A second real bug found in the same three lines, fixed with it**: `userRole` read `user?.unsafeMetadata?.role`, and the sign-up flow writes the literal string `"admin,user"` there — so a real signed-in user's header read "Acme Corp. (admin,user)". The role now comes from `useAuth()`'s `GET /auth/me`, which is the authoritative source Gap 99 established and `Sidebar.tsx` already filters on. `orgName` still comes from `unsafeMetadata` (genuinely written at sign-up) but no longer falls back to the fabricated "Acme Corp." — the org line is simply omitted when unknown.
  - **Still open, and it is a sign-up-flow gap, not a Header one — flagged rather than guessed at**: until `invoice-website`'s sign-up form collects a name (or Clerk's Dashboard is configured to require one), every user will show the email-derived form. That is honest and user-specific rather than a fabricated person, so the misleading part of this gap is closed, but the underlying "we never ask for a name" gap is real and needs a product decision on where to ask — a field on the sign-up form, or a first-run profile prompt. Deliberately not built here: it is a change to a different app's registration flow, outside this gap's scope.
  - **Verified**: `tsc --noEmit` clean, `next build` clean, Playwright 41/41. **Not verified**: no live signed-in session was exercised (no Clerk-authenticated environment was running this session) — the reasoning above is from the sign-up code and Clerk's documented `useUser()` semantics, not from watching a real header render.
  - *Original 2026-08-04 report:* root-caused, needs a live-account check to close. Confirmed in `components/layout/Header.tsx` (uses the real `useUser()` hook from `@clerk/nextjs`, not a stray/wrong hook): `const firstName = user?.firstName || "Alex"`, `const lastName = user?.lastName || "R."`, `const initials = ... || "AR"` — these are only fallback values, meant to cover the brief pre-load moment before Clerk's `user` object resolves. If every signed-in user is seeing "Alex R" persistently (not just a flash before load), that means `user?.firstName`/`user?.lastName` are coming back empty for real accounts, which points at one of two root causes, not yet distinguished: (a) this app's Clerk sign-up flow doesn't actually collect/require first/last name, so real user profiles genuinely have nothing in those fields, or (b) a loading-state bug where the component renders before `useUser()`'s `isLoaded` flag is true and something prevents the later real-data render from taking effect.
  - **Not yet implemented**: no code changed — needs one live check first (inspect an actual signed-in user's Clerk profile fields, or check `isLoaded` handling in `Header.tsx`) to know which of the two causes it actually is before writing a fix; (a) would be a Clerk Dashboard/sign-up-form config fix, (b) would be a `Header.tsx` code fix — different fixes, don't guess which without checking.

- `[x]` **Gap 120: Settings "Change Subscription Plan" has no real in-app plan selector, and may not complete the upgrade flow at all** — closed 2026-08-05. Added a real plan picker (Pro / Pro Combined cards) on the Subscriptions page before the upgrade CTA so it pre-selects the correct plan on the PayU pricing page.

- `[x]` **Gap 122: AI Score Dashboard Widget & Rejection Reason Form UI (Frontend)** — closed 2026-08-05. Create a new dashboard visual section displaying the unified 3-parameter **AI Score** (Field Extraction, Alert Response, Alerts Missed).
  1. **AI Score Widget:** Replace the legacy single gauge on the Inbound and Outbound dashboards with a unified "AI Score" widget detailing the 3 rates (Field Extraction, Alert Response, and Escape Rate/Alerts Missed).
  2. **Rejection Modal Update:** In the Auditor Review Console, when an auditor clicks "Reject," prompt them with a modal to select a structured rejection reason (`AI Extraction Error`, `Business Logic Error`, `Illegible Document`, etc.). Send the chosen reason as `reject_reason` in the `PUT /api/v1/audit/resolve/{id}` request body so it is recorded in the audit log.

- `[x]` **Gap 123: Alert Severity UI Categorization** — closed 2026-08-05. Visualized alerts by severity color (error - red, warning - yellow, information - blue) in the Inbound and Outbound Alert Consoles, and updated backend accuracy metrics to only count error-level alerts in accuracy rates.

- `[x]` **Gap 127: Help Center has no article for Developer Webhooks** — **closed 2026-08-07.** Created `app/help/content/webhooks-guide.tsx` with event payloads, subscriber setup, and signature verification, registered into `HelpPage`.

- `[x]` **Gap 128: Help Center has no article for Inbound Email setup** — **closed 2026-08-07.** Created `app/help/content/inbound-email-guide.tsx` with alias forwarding, allowlist rules, and attachment processing steps, registered into `HelpPage`.

- `[x]` **Gap 129: Help Center has no article for Outbound Email setup** — **closed 2026-08-07.** Created `app/help/content/outbound-email-guide.tsx` covering target email addresses and outbound dispatch workflow, registered into `HelpPage`.

- `[x]` **Gap 132: Subscriptions page & Trainer upgrade links route out of FE to external website pages, and in-app PayU checkout fails** — **closed 2026-08-07.** Resolved two sub-issues: (1) Created missing Next.js API proxy route `app/api/billing/create-checkout-session/route.ts` in `invoice-fe` to forward requests to backend `/billing/create-checkout-session`, enabling PayU checkout form generation. (2) Replaced external website `WEBSITE_URL` redirects in `app/trainer/page.tsx` and `components/settings/ServiceFlowToggles.tsx` with in-app Next.js `Link` components to `/settings/subscriptions`.

- `[x]` **Gap 143: Subscription & Billing is hard to reach, and its usage-limit tracker doesn't work** — **closed 2026-08-12.** Both halves fixed as the audit's own fix direction specified, plus the backend endpoint it depended on.
  - **(1) Direct nav entry** — `components/layout/Sidebar.tsx`: new `Subscriptions` item → `/settings/subscriptions` (`CreditCard` icon), gated `role === "Admin"` exactly as `Settings` is; the page's plan-change action is Admin-only backend-side too (`routers/billing.py::create_checkout_session`). One click from anywhere, replacing the reported AI Trainer → View Plan → Subscriptions path. The Settings tile grid entry (`app/settings/page.tsx:77-84`) already existed and was left as-is — the audit's "no nav entry point" wording was about depth, not total absence. **Side-effect fixed in the same file**: `/settings` prefix-matches `/settings/subscriptions`, so the per-item `isActive` test would have highlighted both rows at once; `Sidebar.tsx` now resolves a single `activeHref` (longest matching href among visible items) and compares against it.
  - **(2) Header-read-as-body bug** — deleted outright rather than corrected. `app/settings/subscriptions/page.tsx` no longer calls `GET /api/invoices?limit=1` at all; the "read `X-Total-Count` like `Header.tsx:41-45` does" option was rejected because, per this entry's own point (3), a lifetime inbound-invoice count can never reconcile with a monthly decrement-only gate no matter how correctly it's read.
  - **(3) Hard-coded limit** — `planLimit = isProCombined ? 999999 : isPro ? 1000 : 25` (was `page.tsx:228`) is gone. There is no client-side plan number left on the usage bar; `used`, `limit`, `remaining` and the renewal date all come from the backend.
  - **New backend endpoint** — `GET /billing/usage` → `routers/billing.py::get_billing_usage()` + `BillingUsageResponse`, derived from `Tenant.free_invoices_remaining` / `settings.DEFAULT_FREE_INVOICES_LIMIT` (50) / `Tenant.free_quota_reset_at` — the exact fields `routers/invoices.py` gates on, so screen and gate cannot disagree. No Admin gate (reading your own allowance isn't an admin action; a non-Admin ingester is who hits the 402 it predicts) and `get_tenant_context_allow_unpaid` so a lapsed tenant can still load the page they came to fix their billing on. Proxied via new `app/api/billing/usage/route.ts`. See BE tracker Feature 11 and `feature_11_billing.md`.
  - **Rendering** — four states replace the single always-plausible one: loading; error ("Usage unavailable", stated as unknown rather than shown as `0` — the pre-fix behaviour is precisely why this stayed undetected); metered (`used / limit`, remaining + renewal date, bar amber at 80% / rose at 100%); not metered (no bar — a plan the backend reports as unmetered has no ceiling to draw one against). The not-metered branch deliberately asserts nothing about the Pro tier's advertised 1,000/month cap, and the `PLANS` card copy stating it (`page.tsx:23`) was **not** touched at the time — that was BE Gap 188, **since closed 2026-08-12** by correcting the copy (see the BE Gap 188 entry at the end of this file).
  - **Verified**: `npx tsc --noEmit` on invoice-fe — clean. `pytest tests/test_billing.py` — 28 passed (9 new, asserting against `DEFAULT_FREE_INVOICES_LIMIT` and the counter rather than any literal: config limit not 25, `used` tracking spend, `metered=False` + null numbers on `pro`/`pro_combined`/`unpaid`, non-Admin read allowed, out-of-range counter clamped). `tests/test_billing_free_quota.py` + `test_billing_lapse.py` — 49 passed, unchanged. **Not verified**: no live browser run of the screen; `e2e/rbac-sidebar.spec.ts` was updated for the new Admin-only nav item (`ADMIN_ONLY = ["Settings", "Subscriptions"]`, three assertions) but **not executed**.
  - **Live-verified (Checkpoint 4, functional-tester, 2026-08-12)**: real dev stack (Postgres + real backend + real invoice-fe, Playwright headless Chromium), not the unit/type checks above. Admin sidebar shows Subscriptions as its own item under Settings and navigates correctly with only that row highlighted; a Viewer-role identity sees neither Settings nor Subscriptions; the free-tier usage bar was cross-checked against a live DB row (used/limit/remaining all reconciled exactly, limit=50=DEFAULT_FREE_INVOICES_LIMIT); the paid-tier state renders "Not metered on this plan"; the error state renders "Usage unavailable" against a real (simulated) 500, not a silent zero; `e2e/rbac-sidebar.spec.ts` now passes 19/19 (one apparent failure during the first run was traced to this session's own backend process leaking into an unrelated, unstubbed bell-count test, not a Gap 143 regression -- confirmed clean on rerun with the backend stopped). Evidence: `docs/test_evidence/gap143_checkpoint4_retest/`.
  - *Original entry below, kept for the root-cause record.* **Tracker hygiene fix 2026-08-11: this entry previously existed twice with contradicting status** (closed 2026-08-07 here, reopened `[ ]` later in this file as "not yet root-caused") — found by an independent code audit (`AUDIT_FINDINGS_2026-08-10.md` A-03/A-05/A-06) that proved the 2026-08-07 "fix" cannot work and root-caused why. Merged into this one entry, reopened.
  - **(1) No direct nav entry point** — user-reported (GEN-01). Current path is AI-Trainer page → View Plan → Subscriptions & Billing, three steps for a page every paying tenant needs regularly.
  - **(2) The usage-limit tracker widget doesn't reflect real usage** — user-reported (GEN-02). The usage bar reads a response shape the backend never returns. The 2026-08-07 fix added `fetch("/api/invoices?limit=1")` then checked `data.total`/`data.invoices` — but `GET /invoices` returns a bare array (`response_model=list[Invoice]`, `routers/invoices.py:409`) with the real count in the `X-Total-Count` **header** (`:464`), not the body. Both branches silently fail and the count stays at its initial `0`. The correct pattern already exists in this codebase: `components/layout/Header.tsx:41-45`'s `readTotal()` reads the header correctly. **Same failure class as Gap 22** (FE/BE response-shape mismatch closed on a written description of the code, not a live check).
  - **(3) Even once the header is read correctly, two more defects remain**: the plan-limit numbers are hard-coded in the component (`planLimit = isProCombined ? 999999 : isPro ? 1000 : 25`, `page.tsx:228`) and disagree with the backend's real free-tier limit (`DEFAULT_FREE_INVOICES_LIMIT = 50` in `config.py:32`) — screen says 25, system gives 50. And the count itself is lifetime-all-invoices against a *monthly* allowance, inbound-only (`Invoice.flow_direction == "INBOUND"` hard-filtered at `routers/invoices.py:438-441`), so it will never reconcile with what's actually enforced and never drops after a monthly reset.
  - **Fix direction**: don't count rows from the FE at all — add `GET /billing/usage` returning `{used, limit, resets_at}` derived from `tenant.free_invoices_remaining`/`DEFAULT_FREE_INVOICES_LIMIT` (the fields the backend actually enforces against), and render that directly. Counting invoices will never agree with the gate; deriving from the same counter the gate reads always will. **Implemented as specified on 2026-08-12** — the response also carries `plan`, `metered` and `remaining`; see the closure notes at the top of this entry.

- `[x]` **Gap 134: Outbound ("Sending") processing-details view shows far less detail than inbound — no live processing log at all** — **closed 2026-08-07.** Updated `components/ingestion/SendInvoiceStatusTable.tsx` to embed `LogTerminal` with `batchId={invoiceId}` during outbound processing, bringing real-time scrolling stage log visibility (OCR → Classify → Extract → Verify) to outbound invoices matching inbound.

- `[x]` **Gap 145: Bulk Directory ingestion option reported not working** — **closed 2026-08-07.** Added a browser local folder picker button (`webkitdirectory`) to the **Bulk Directory Scan** card in `app/ingestion/page.tsx`, allowing browser clients to select local directories and queue all contained PDF invoices directly into the ingestion pipeline alongside the server directory path scanner.

- `[x]` **Gap 146: A PDF dropped on the ingestion screen gets silently deselected if the user navigates to another page before pressing Submit Ingestion** — **closed 2026-08-07.** Added `lib/ingestionDraft.ts` module draft file persistence to cache dropped/selected `File` objects in client memory across Next.js route transitions, preserving unsubmitted dropped files when navigating to and from the Ingestion screen. **Tracker hygiene fix 2026-08-11**: this exact symptom was re-reported and re-numbered as a second, open Gap 146 (ING-04) later in this file — same description, same screen. Treating as a duplicate of this closed fix rather than two gaps; if ING-04 predates awareness of this fix, no further action needed, otherwise it needs a live re-check of `lib/ingestionDraft.ts` against a fresh repro. **Distinct, still-open issue found by an independent code audit** (`NEW_ISSUES_2026-08-10.md` N-15): this fix only covers *unsubmitted* dropped files. A batch that has already been submitted and is actively processing loses all ledger/log state on navigation instead — `app/ingestion/page.tsx:123-125`'s `batchId`/`jobIds`/`trackedFiles` are plain `useState`, and `StatusTable`/`LogTerminal` unmount with the batch id on route change, so returning to Ingestion shows an empty ledger while the worker is still running. That is a new gap (see below), not covered by this one.

- `[x]` **Gap 135: Outbound Audit Review Console (`app/invoices/outbound-review/[id]/page.tsx`) never received Gap 112's treatment — still the old 2-column layout, action buttons buried at the page bottom instead of the header** — **closed 2026-08-07.** Consolidated `outbound-review/[id]/page.tsx` to match inbound's Gap 112 3-column layout (PDF | Fields | Discrepancy Warnings) and moved Approve & Send / Mark Paid action buttons into the shared `PageHeaderActions` header.

- `[x]` **Gap 153: Audit Queue's Action button menu renders hidden/clipped** — **Closed 2026-08-12**. Rewrote `RecentInvoicesTable.tsx`'s actions menu to dynamically open downward (top-full) instead of upward for the first two visible rows, preventing clipping against the sticky header.
- `[x]` **Gap 154: Auditor Review Console — zoom/rotation don't work in the expanded (lightbox) PDF view; rotation also leaks state into the inline view** — **Closed 2026-08-12**. Added isolated `modalZoom` and `modalRotation` state to the lightbox modal. The Zoom/Rotate controls inside the modal now drive this state separately, preventing the inline view behind it from rotating. Zoom and rotation transforms are applied to a wrapper `div` around the iframe so the transformed iframe borders stay scrollable.
- `[x]` **Gap 155: Auditor Review Console PDF requires downloading to view properly — wants an inline tap-to-open popup viewer instead** — **Closed 2026-08-12**. Re-implemented the fullscreen lightbox modal overlay (`isModalOpen`) with working isolated zoom/rotation (Gap 154), making the lightbox fully functional without requiring downloading.
- `[x]` **Gap 156: Extracted Fields panel in the Audit Review Console is not scrollable** — **Closed 2026-08-12**. Restored vertical scrolling inside Column 2 (Extracted Fields panel) of `review/[id]/page.tsx` by adding a responsive `max-h-[calc(100vh-180px)] xl:max-h-none` constraint to the fields scrollbox.

- `[x]` **Gap 136: Dashboard split-view (Receiving/Sending) metric cards overlap and clip — a viewport-relative-breakpoint bug, not a data problem — plus the 4 lower panels feel like a flat, cheap stack** — **closed 2026-08-07.** Updated `MetricsGrid.tsx` and `OutboundMetricsGrid.tsx` KPI rows with flexible `flex flex-wrap min-w-[200px]` responsive card sizing, preventing clipping on sub-1440px split views, and verified 4-tab panel consolidation in `app/dashboard/page.tsx`.

- `[x]` **Gap 141: Client/vendor and tag filter dropdowns show hardcoded mock data instead of the tenant's real values** — user-reported (DSH-02, DSH-03). **Tracker hygiene fix 2026-08-11: this entry existed twice** (closed 2026-08-07 here for the Dashboard specifically, then re-reported `[ ]` open later in this file as DSH-02/DSH-03, same symptom, same file). Merged into this one entry, reopened.
  - **2026-08-07 fix**: removed static mock fallback arrays (`DEFAULT_VENDORS`, `DEFAULT_TAGS`) from `app/dashboard/page.tsx`, so `FilterBar` dropdowns populate from real vendor names/tags.
  - **Fixed 2026-08-11 (Audit Queue side)**: `app/invoices/page.tsx` still had its own `DEFAULT_VENDORS`/`DEFAULT_TAGS` fallback at `uniqueVendors`/`uniqueTags` — removed, both now derive from `realVendors`/`realTags` only (empty state when a tenant has none, no fictional options to select). Also deleted the dead `DEFAULT_VENDORS`/`DEFAULT_TAGS` constants still declared (unused) in `app/dashboard/page.tsx`.
  - Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 138: `useAuth()`'s identity cache is never invalidated when a tenant's underlying data changes server-side** — **closed 2026-08-11.** Root cause: module-level cache + unused `refreshAuth()`. Fix: (a) debounced re-fetch on `window` focus / `visibilitychange` (2s) while any `useAuth` subscriber is mounted; (b) `clearAuth()` on Header sign-out; (c) `refreshAuth()` after PayU success on Subscriptions, after Admin saves own permissions, and once on Trainer when the upgrade gate is showing. Also exported `refreshAuthOnAuthError` for 402/403 call sites. Files: `hooks/useAuth.ts`, `Header.tsx`, `settings/subscriptions/page.tsx`, `trainer/page.tsx`, `admin/page.tsx`.

- `[x]` **Gap 199: Header “Needs Attention” bell count freezes across client navigations** — **closed 2026-08-11.** Root cause: `useNeedsAttentionCount` in `Header.tsx` fetched only when `enabled` (`canAudit`) changed; the app layout keeps `Header` mounted so route changes never re-ran the effect. Fix: depend on `usePathname()` and also re-fetch on debounced window focus / `visibilitychange` (500ms), still using `limit=1` + `X-Total-Count` for inbound `AUDIT_REQUIRED` and outbound `NEEDS_REVIEW`. File: `components/layout/Header.tsx`.

- `[x]` **Gap 139: Trainer's document-loading actions (New Vendor upload, Existing Vendor production-invoice load) show zero processing feedback — reads as broken/stuck even when working** — **closed 2026-08-12.** user-reported 2026-08-05 ("new vendor not uploading pdf, neither showing processing", "existing production should also show loading till pdf is shown"), root-caused precisely; not a data or upload-path bug, a missing-loading-state bug.
  1. **Confirmed the FE→proxy→backend path itself is not the problem.** `app/api/trainer/upload/route.ts` (New Vendor) and `app/api/trainer/sessions/global/route.ts` (Global-with-file) are structurally identical — both correctly forward the multipart `FormData` untouched. The backend endpoints (`upload_transient_file` and `start_global_session` in `routers/trainer.py`) are likewise nearly identical: save the file, run `_run_ocr_split` + `run_extraction_agent` synchronously, return the session. Nothing here is scope-specific or broken.
  2. **The real gap: none of Trainer's session-loading handlers track a loading state, and there's nowhere to show one if they did.** `app/trainer/page.tsx`'s `handleSelectVendor` (Existing Vendor), `handleUploadFile` (New Vendor / Global-with-file), and `handleClearFile` all just `await` their call directly and either set the new session or show an error toast — no `isLoading` state anywhere. `PdfViewerPanel.tsx` only has **two** render modes (a real PDF, or Global's chat-only empty state) — there is no third "loading" mode for it to show even if a loading flag existed. Compare this to chat: `handleSendMessage` does track `isSending`, and `QnAPanel.tsx` shows a real animated progress indicator ("Analyzing correction..." → "Re-extracting with updated rules..." → "Finalizing...") while it runs — this is the one Trainer action that already does this correctly.
  3. **Why this matters more than a cosmetic gap**: `_run_ocr_split` + `run_extraction_agent` run synchronously and can take real time (OCR + an LLM extraction call) — during that entire window today, the screen shows whatever was there *before* the click, unchanged, with no indication anything is happening. A slow-but-working request and a genuinely stuck one look identical to the user, which is exactly what was reported.
  - **Fix**: add an `isLoadingSession` (or similarly-named) state in `app/trainer/page.tsx`, set it around all three handlers' `await` calls, pass it down to `PdfViewerPanel` as a new prop, and give that component a real third loading mode — reusing the same visual language `QnAPanel`'s existing progress indicator already established (animated, named stages) rather than inventing a new one.
  - **Implemented as planned, 2026-08-12**, plus one deliberate addition: `app/trainer/page.tsx` now holds `isLoadingSession` + `loadingFileName`, set around the awaits in `handleSelectVendor`, `handleUploadFile` and `handleClearFile` **and in `handleScopeChange`** — switching *into* Existing Vendor auto-selects a vendor and runs the identical `from-production` load, so leaving it out would have left the same frozen window on the most common route into that scope. `handleSelectVendor`/`handleScopeChange` also clear `session` before awaiting, so the loading panel can't sit on top of the previous vendor's document. `PdfViewerPanel.tsx` gained `isLoadingSession`/`loadingFileName` props and a MODE 0 checked ahead of both existing modes: spinner badge, the filename being processed, and `QnAPanel`'s exact indicator (three `animate-bounce` dots, named stage, percentage, progress bar) driven by the same client-side elapsed-time estimate — 32s here vs. chat's 28s, since this call is OCR + extraction. Stage wording is derived from `scope` (upload → "Uploading document… / Running OCR and page split… / Extracting fields… / Finalizing…"; Existing Vendor → "Fetching production invoice…"; clear-file → "Clearing grounding document…"), so no extra prop was needed. `chatDisabledReason` gained a loading branch, since the cleared session would otherwise have told the user to pick a vendor they had just picked. **Out of scope, unchanged**: OCR/extraction is still synchronous on the backend — this is the loading-indicator fix only.
  - **Verified 2026-08-12**: `npx tsc --noEmit` clean, and `e2e/trainer-loading-state.spec.ts` (new, 2 tests, both passing) drives a real New Vendor upload and a real Existing Vendor selection against delayed route stubs, asserting the panel appears with its stage text/percentage/filename during the wait and is gone once the session lands.

- `[x]` **Gap 140: Email Settings screen (`app/settings/email/page.tsx`) doesn't fit one viewport — the Allowed Senders card, stacked full-width below the Inbound/Outbound row, pushes the page past the fold on a typical laptop screen** — **closed 2026-08-07.** Consolidated `app/settings/email/page.tsx` layout by merging `EmailSendersList` as a sub-section inside the Inbound card, rendering a clean 2-card side-by-side grid that fits completely within 1 viewport.

- `[x]` **Gap 144 (SECURITY): Tenant Isolation ID is visible in the UI to every user, including non-admins** — **closed 2026-08-07.** Gated Tenant Isolation ID display in `Sidebar.tsx` footer with `role === "Admin"`, hiding internal tenant IDs from regular users and viewers. **Re-verified 2026-08-12** (re-reported in a fresh gap batch, resolved as a tracker-hygiene duplicate, not a regression): `Sidebar.tsx:115-123` still gates the footer behind `role === "Admin"`, and `app/settings/security/page.tsx` never renders the raw ID at all — no code change needed, stays closed. Duplicate entry below merged into this one.

- `[x]` **Gap 147: Outbound Sender Email Address field only accepts a single email address** — **removed / superseded 2026-08-10.** Dual authorized email sets (`inbound`|`outbound` on `TenantEmailSender`) replaced the single `outbound_sender_email` Settings field; Email Setup lists many addresses per set. Open duplicate below deleted.

- `[x]` **Gap 152: Settings → Security page is linked but the route doesn't exist (404)** — user-reported (SET-01). **Tracker hygiene fix 2026-08-11**: this entry existed twice with directly contradicting claims (closed 2026-08-07 vs. reopened `[ ]` later, both cited above); reopened pending a live check rather than trusting either note. **Live-verified 2026-08-11** (Checkpoint 1, functional-tester): both the Settings nav link and direct navigation to `/settings/security` resolve cleanly — API key panel, tenant isolation status, and RBAC matrix all render, no 404. Closing as fixed. Evidence: `docs/test_evidence/gap152_settings_security/`. Note: this page's API key panel is still backed by mocked data — see Gap 184.

---

### Batch intake — Devashish's gap report, 2026-08-05
The 16 gaps below (141-156) come from a 32-item report submitted directly, triaged against the existing tracker for duplicates before filing. Items confirmed as exact duplicates (Simulator buttons → Gap 1 in `website_features_tracker.md`; Google/Salesforce connect failing → Gap 131 in `be_features_tracker.md`) were not re-filed. Items matching an existing gap's symptom were folded into that gap instead (see Gap 132, Gap 136 above). One item (admin able to log in through the regular user login) was not filed — a single shared login screen with role resolved post-auth is the app's intended design, not a defect. Most items below are report-level (not yet independently root-caused by me) unless a file/line is cited.

- `[x]` **Gap 142: Header's "My Profile" and "Account Settings" dropdown items are dead buttons — no route, nothing happens on click** — user-reported (GEN-04, GEN-05); re-confirmed in a later batch QA pass (2026-08-09) which also found the dropdown had no dismissal handling (no outside-click/route-change close). **Closed 2026-08-11 (merged from `Global-common-bugs-Fix`).** (1) **My Profile** opens Clerk's `openUserProfile()` modal (no in-app profile page). (2) **Account Settings** is an Admin-only `Link` to `/settings` (same gate as Sidebar). (3) Menu dismisses on outside `mousedown` and on `usePathname()` change. File: `components/layout/Header.tsx`. (Gap 141's duplicate entry that used to sit here was already consolidated into the single Gap 141 entry above — not re-added.)

- ~~Gap 144 (duplicate)~~ — user-reported (ING-02); merged 2026-08-12 into the primary Gap 144 entry above.

- ~~Gap 147 (open duplicate)~~ — **deleted 2026-08-10**; see superseded Gap 147 above (dual email sets).

- `[x]` **Gap 148: Help page images not loading properly** — **closed 2026-08-07.** Added stateful `onError` image fallback state rendering clean platform preview containers in `trainer-guide.tsx` and `auditor-guide.tsx`.

- `[x]` **Gap 149: Chat screen has no rename, delete, or search for existing chat threads** — **closed 2026-08-07.** Added real-time thread search input bar, inline thread title editing (`renameSession`), and per-thread deletion buttons to `ThreadSidebar` in `ChatWindow.tsx` and `useChatSession.ts`.

- `[x]` **Gap 150: Chat message timestamps are wrong for India time zone, and no date is shown** — **closed 2026-08-07.** Added `formatMessageTimestamp` helper in `MessageBubble.tsx` to force ISO/UTC string parsing, rendering both Date and Time in local time (IST).

- `[x]` **Gap 151: On logout, the user lands on a Clerk-hosted page (sometimes showing a "Log in with Google" option) instead of the app's own login screen** — user-reported (AUT-01, AUT-02) — sign-in itself still works and correctly leads into the product, but the intermediate screen isn't branded/owned by this app. Possible regression: Gap 66 (closed) fixed sign-out's backend call, and Gap 12 (closed, `website_features_tracker.md`) fixed the cross-app Clerk cookie handshake for the Multi-Zone proxy setup — neither gap's verification pass specifically confirmed the post-logout *landing page* in the current setup. **Not yet implemented, not yet root-caused**: needs a live logout repro to confirm whether this is a genuine regression in either prior fix or a gap neither one covered.
  - **Re-reported in a later batch QA pass (2026-08-09)**: source-checked `Header.tsx::handleSignOut` (line 148) again — it redirects to `` `${WEBSITE_URL}/login` ``, this app's own login page, not a Clerk-hosted URL by design. Two live things worth checking specifically, given the code itself looks correct: whether `NEXT_PUBLIC_WEBSITE_URL` is actually set correctly in the environment this was reported against (falls through to `http://localhost:3000` if empty), and whether the "Log in with Google" option this report mentions is actually a real SSO button rendered on *this app's own* `/login` page (which would make the report accurate but differently-explained) rather than evidence of a different, Clerk-hosted domain.
  - **Actual root cause found and fixed 2026-08-12**: neither prior theory was it. `Header.tsx:196` called `await signOut()` with **no destination**, then a manual `window.location.href = \`${WEBSITE_URL}/login\`` ran on the next line — Clerk's own post-sign-out navigation (falling back to its hosted Account Portal, since nothing told it otherwise) won the race against that manual redirect, landing on exactly the unbranded "Log in with Google" screen being reported. Neither `signOut()` nor `<ClerkProvider>` (`app/layout.tsx`) specified a destination anywhere in the repo — confirmed by grepping for `afterSignOutUrl|signOutUrl|redirectUrl`, zero hits. **Fix**: `Header.tsx:196` now calls `signOut({ redirectUrl: \`${WEBSITE_URL}/login\` })` directly (manual redirect kept only as a fallback if that call throws); `app/layout.tsx`'s `<ClerkProvider>` also gained `afterSignOutUrl` as a second layer, covering any sign-out path other than this one button (e.g. a Clerk-initiated session-expiry sign-out). Verified: `npx tsc --noEmit` clean.

---

### Batch intake — targeted code audit, Settings & Trainer screens, 2026-08-05
7 findings from a read-only audit specifically scoped to Settings and Trainer, cross-checked against the tracker to exclude anything already known (Gaps 137, 139, 140, 142, 143, 147, 152, 156).

- `[x]` **Gap 165: Connectors' "Folder Mappings" is decorative — it never actually changes where Ingestion pulls files from** — found via code audit, source-confirmed: `app/settings/connectors/page.tsx:17-21,182-197` and `components/settings/FolderTreeExplorer.tsx:104-109` — clicking "Map current folder" only writes a display name to `localStorage`, never to the backend. `ConnectorBrowseBar.tsx` (Ingestion's real browse entry point) never reads this value at all — every browse session opens at Root regardless of what was "mapped," and the mapping doesn't even sync across the same admin's other devices/browsers since it's local-only. **Closed 2026-08-06** (resolution: relabel honestly + make the saved folder actually do something, rather than half-build persistence). The backend has no connector auto-pull at all — `routers/connectors.py` exposes browse + explicit per-file import only — so a "real" mapping would have meant a new persisted model *and* a scheduled pull, i.e. a feature, not a fix; that tradeoff is recorded in `lib/connectorFolderShortcut.ts`'s header. What was built: the storage moved out of `app/settings/connectors/page.tsx` into `lib/connectorFolderShortcut.ts` (`readFolderShortcut`/`writeFolderShortcut`/`clearFolderShortcuts`, same keys, legacy plain-string values still readable), now storing `{id, name}` instead of a display string; `FolderTreeExplorer` tracks a real breadcrumb `path` (so the folder name is correct — the old code read it from `files`, which holds the *children*, and therefore always fell back to the raw folder id) and accepts `initialFolder`; `ConnectorBrowseBar` (Ingestion) both opens at the saved folder and writes one, which is the half that was entirely missing. UI copy in `IntegrationCard`/`FolderTreeExplorer` no longer claims automatic import/export — "Folder Mappings" → "Default Browse Folder", "Map current folder" → "Start here next time", with the per-browser caveat stated inline. Verified: `npx tsc --noEmit` clean; the unused `onUpdateFolder` prop on `IntegrationCard` (never referenced in its JSX) was dropped as part of the same change.

- `[x]` **Gap 166: Connector file import shows a success message even when the backend import actually failed** — found via code audit, source-confirmed: `components/settings/FolderTreeExplorer.tsx::handleImportSelected` (lines 112-134) never checks `res.ok` on the import `fetch` call — only a network-level throw is caught. **Closed 2026-08-06**. The file is `components/connectors/FolderTreeExplorer.tsx` (the tracker's `components/settings/...` path was wrong). `handleImportSelected` now checks `res.ok` per file, reads `detail`/`error` off the body for the message, and throws — matching `EmailSendersList.tsx`. The loop aborts on the first failure (a partial run reported as success is the same class of lie being fixed), and the error renders in the footer next to where the green banner would have been, via new `importError` state. Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 167: Admin Console tile has no role gate, and `/admin` itself misrepresents a non-Admin viewer as the org's Admin** — found via code audit, source-confirmed: `app/settings/page.tsx:44-53,132-157` renders the Admin Console tile unconditionally for every role — unlike every other tile on that page, which is gated behind `isAdmin`/`ServiceFlowToggles`. `app/admin/page.tsx::loadUsers()` (line 290) silently swallows a non-Admin's expected 403 with `if (!res.ok) return;`, and a hardcoded "Admin row" (lines 437-459) always labels the *current viewer* "Admin"/"Organisation Owner"/"All (Admin)" regardless of their real role. Adds a concrete new instance to Gap 99's "hardcoded isAdmin" family of findings, not previously filed for this specific page. **Closed 2026-08-06**. `app/settings/page.tsx`: `INTEGRATIONS` is now typed (`IntegrationTile`) with an `adminOnly` flag on the Admin Console tile, filtered against `role === "Admin"` from `useAuth()` — the same resolution `ServiceFlowToggles` and `app/settings/webhooks/page.tsx` use; while identity is loading `role` is `""`, so the tile stays hidden rather than flashing. `app/admin/page.tsx`: gained the route's own gate — an "Access Restricted" early return (styled in that page's inline-style vocabulary, mirroring the webhooks screen's version, and naming the viewer's real role) for a non-Admin or for `accessDenied`; `loadUsers()` distinguishes 401/403 (→ `accessDenied`) from other failures (→ visible `listError` instead of a silently blank table) and is not fired at all for a known non-Admin. The self row's "Organisation Owner"/"Admin"/"All (Admin)" literals now come from `useAuth()` (`role`, plus `canTrain`/`canAudit`/`canLoad` for the permissions cell), and the per-user Role cell renders `u.role` instead of a hardcoded "User". Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 168: Admin Console's "Remove" user button doesn't actually remove anyone** — found via code audit, source-confirmed: `app/admin/page.tsx::handleRemoveUser` (line 349) only calls `setUsers((prev) => prev.filter(...))` — pure client-side state, no DELETE call anywhere in `app/api/admin/**`. **Closed 2026-08-06** — full vertical slice, no endpoint existed anywhere. Backend: `routers/admin.py::remove_tenant_user` (`DELETE /api/v1/admin/users/{user_ref}`, `AdminUserRemoved` response), Admin-only via the router's `require_admin`, tenant-scoped 404, accepting the same UUID-or-Clerk-ID `user_ref` as the permissions endpoint. Two refusals, both deliberate: an Admin cannot remove themselves, and cannot remove another Admin here (Admin comes from the Clerk org role in the JWT, so deleting the row would be undone on their next request — demotion belongs in Clerk). A user referenced by `audit_logs.actor_user_id` is *detached* (`tenant_id` cleared, permissions revoked) rather than hard-deleted, so the audit trail keeps naming who acted and the FK holds; either way they disappear from the tenant-scoped `GET /admin/users`. FE: new `app/api/admin/users/[userRef]/route.ts` calls the backend **first** (so a non-Admin is rejected before anything touches Clerk), then deletes the Clerk account — the thing that actually revokes sign-in — using `CLERK_SECRET_KEY`, the same split `POST /api/admin/create-user` already uses; a failed/skipped Clerk delete returns 200 with `clerkDeleted: false` + a warning rather than a clean success. `handleRemoveUser` now confirms, awaits the call, drops the row only on success, and surfaces errors/partial success in the existing error banner. Verified: `npx tsc --noEmit` clean, `python -m py_compile routers/admin.py` clean. **Not verified**: no automated test covers the new endpoint (`pytest` is not installed in this workspace's env), and it has not been exercised against a live backend.

- `[x]` **Gap 169: Admin Console shows a dead-URL instruction for new users** — found via code audit, source-confirmed: `app/admin/page.tsx:526` tells the admin new users should log in at `localhost:3000/admin/login` — this route doesn't exist anywhere in the app (Gap 66, closed, already established there is no such route; login goes through `invoice-website`). **Closed 2026-08-06**. The banner now links `${NEXT_PUBLIC_WEBSITE_URL}/login` (the real `invoice-website` login route), using the same `WEBSITE_URL` const + `http://localhost:3000` fallback as `components/layout/Header.tsx` and `app/trainer/page.tsx` — so it is correct in deployed environments too, not just locally. The "→ select *User*" step, which no longer exists in the sign-in flow, was dropped. Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 170: Trainer's New Vendor flow never captures the backend's auto-extracted vendor name, causing "Rule History" to silently show the wrong (Global) template** — found via code audit, source-confirmed: `app/trainer/page.tsx::handleUploadFile` (~lines 292-301) never reads `session.vendorName`, which the backend already returns after a New Vendor upload (`routers/trainer.py` `_serialize_session`, line ~208, auto-extracted from OCR) — every downstream consumer keys off `selectedVendorName` instead, which only `handleSelectVendor` (the *Existing* Vendor flow) ever sets. Concretely: `CommitModal` falls back to a generic "New Vendor" label, `RuleHistoryDrawer.tsx:72` shows "Vendor: Active," and `handleOpenHistory` calls `getRuleHistory("new_vendor", "")` — an empty `vendor_name` that `routers/trainer.py`'s `GET /templates/history` (lines ~727-728) resolves, per its own docstring, to the tenant's **Global** template. **Closed 2026-08-06**. `handleUploadFile` now stores `newSess.vendorName` into `selectedVendorName` (scoped to `new_vendor` — a Global session may also carry a vendor name from its grounding PDF, and tenant-wide rules must not start keying off whichever sample was uploaded) and names the detected vendor in the toast, so `CommitModal`, `RuleHistoryDrawer` and `getRuleHistory` all see the real vendor. Two follow-on guards were needed and added: `handleScopeChange` now falls back to a known vendor when switching to Existing Vendor (the captured name is a brand-new vendor with no production invoices), and `handleOpenHistory` refuses a vendor-scoped history request with no vendor name instead of letting the backend resolve `""` to the Global template — which closes the wrong-timeline path for the pre-upload window too. Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 171: Trainer chat silently discards a typed correction if no session is active — input clears as if sent, message is dropped with zero feedback** — found via code audit, source-confirmed: `components/trainer/QnAPanel.tsx::handleSend` (lines 97-102) has no awareness of whether a session exists and always clears its input on submit; `page.tsx::handleSendMessage` (lines 318-319) guards with `if (!session || isSending) return;` and does nothing else — no toast, no re-populating the input, no visual indication the message wasn't sent. `session` is `null` in New Vendor scope before a PDF is uploaded, and in Existing Vendor scope when no vendor is selected (`page.tsx` lines 254-262). **Closed 2026-08-06** — both halves, since they reinforce each other. `page.tsx` derives a `chatDisabledReason` (null when usable; otherwise scope-specific: upload a PDF / select a vendor / session still starting) and passes it to `QnAPanel` as `disabledReason`. The panel disables the input, the send button and the suggestion chips while it is set, uses it as the placeholder and tooltip, and shows it inline above the input in the amber "not ready" treatment — so the text is never accepted, and therefore never lost. `handleSend` clears the input only after the message has actually been handed over. `handleSendMessage`'s early return is kept as a backstop for chip clicks and future callers, but now raises an error toast naming the same reason instead of returning silently. Verified: `npx tsc --noEmit` clean.

- `[x]` **Gap 175: PayU checkout replaces the Subscriptions tab — user cannot cancel/return without completing payment** — found 2026-08-09 during live retest after Gap 174. Gap 132's `SubscriptionsPage.handleUpgrade` POSTed PayU's classic hash form in the **same window**, so the hosted payment page fully replaced the app. PayU's page has no reliable merchant "cancel" that returns to Settings; browser Back after a POST is unreliable. An in-app side panel / iframe is not viable — PayU sets frame-busting headers.
  - **Fix applied (code) 2026-08-09**: open a named popup on the click gesture (avoids blockers after `await`), POST the checkout form with `target` set to that window, keep Subscriptions mounted with a hint to close the popup to cancel; listen for `postMessage` from the return pages. Website: new `components/billing/PayuPopupReturn.tsx` on `/billing/success` and `/billing/failed` — if opened as `window.opener`, notify origin and `window.close()`. Files: `apps/invoice-fe/app/settings/subscriptions/page.tsx`, `apps/invoice-website/components/billing/PayuPopupReturn.tsx`, `apps/invoice-website/app/billing/{success,failed}/page.tsx`. Cross-ref: `website_features_tracker.md` Gap 175.
  - **Closed 2026-08-09**: confirmed — popup checkout cancel/return path accepted; gap closed.

- `[x]` **Gap 177: Chat "Clear Chat" (delete thread) reported not working, despite the full call chain looking correct in source** — user-reported (batch QA list, 2026-08-09). Traced the entire path and found no defect in any single layer: `ChatWindow.tsx`'s "Clear Chat" button (line 487) → `onDeleteSession` prop → `useChatSession.ts::deleteSession()` (line 206, `await apiClient.delete('/chat/sessions/${id}')`, updates `sessions`/`activeSessionId`/`messages` state on success, sets an error banner on failure) → Next.js route `app/api/chat/sessions/[sessionId]/route.ts` (`DELETE` handler exists, proxies via `proxyJson`) → backend `DELETE /chat/sessions/{session_id}` (`routers/chat.py:85`, exists, 204 response). Every link in the chain is present and shaped correctly from static reading.
  - **Root cause found 2026-08-10** by an independent code audit (`NEW_ISSUES_2026-08-10.md` N-07), exactly the runtime failure this entry predicted: `lib/backendProxy.ts:112-131`'s `proxyJson()` always does `const data = await response.text()` (`""` for a 204) then constructs `new NextResponse(data, { status: response.status, ... })`. Per the Fetch spec, a null-body status (204/205/304) cannot be constructed with a non-null body, and `""` is non-null — confirmed against this repo's own installed Next.js: `NextResponse 204 THROWS: TypeError: Response constructor: Invalid response status code 204`. The route handler throws, Next returns 500 — **after the backend has already committed the delete**. `useChatSession.ts` only removes the item from local state on success, so the deleted thread stays on screen and only disappears on a page refresh, matching the report exactly.
  - **Same bug, same fix, three more reports it also explains**: the identical pattern hits `DELETE /webhooks/{id}` (`routers/webhooks.py:148` → `app/api/webhooks/[id]/route.ts:14`) — "webhook delete not working," "webhooks not disappearing after delete." One shared fix in `proxyJson()` closes all of them.
  - **Fix**: in `proxyJson()`, pass `null` as the body when the status is 204/205/304 or the response body is empty, instead of always passing `response.text()`.
  - **Separate, smaller defect found in the same investigation** (N-19): even after this proxy fix, the "Clear Chat" button itself calls the exact same `onDeleteSession` handler as the per-thread trash icon — it deletes the whole thread, there's no "clear messages, keep thread" capability anywhere in the FE or backend. Rename the button to "Delete Chat" (zero backend work, matches actual behavior) rather than building a real clear-without-delete path, since the per-thread delete already covers that need.
  - **Closed 2026-08-11** (Checkpoint 2). Two edits, both FE-only; no backend change was needed (`routers/chat.py:85` and `routers/webhooks.py:148` already return 204 correctly). (1) `lib/backendProxy.ts::proxyJson` — added a module-level `NULL_BODY_STATUSES = new Set([204, 205, 304])` and now computes `const body = NULL_BODY_STATUSES.has(response.status) || data === "" ? null : data` before `new NextResponse(body, { status, headers })`. Status, headers and the `X-Total-Count` passthrough are untouched, and an empty body on any other status is byte-identical over the wire, so nothing outside the null-body statuses changes behaviour. (2) `components/chat/ChatWindow.tsx` — the SAGE agent strip's button label changed "Clear Chat" → "Delete Chat" (N-19), with a comment recording why no clear-without-delete feature was built. `useChatSession.ts::deleteSession` was **not** modified: it was always correct, and now simply reaches its success branch.
  - **Verified**: `npx tsc --noEmit` clean. Runtime behaviour confirmed against this repo's own installed Next.js the same way the root cause was — a throwaway script importing the real `NextResponse` reproduced `OLD 204 THROWS: TypeError: Response constructor: Invalid response status code 204`, then confirmed the new expression constructs cleanly for 204/205/304 and still round-trips bodies for `200 {"a":1}` and `404 {"detail":"x"}`. **Not verified**: no live click-through of the chat/webhook delete UI, and no automated test covers `proxyJson` — the browser-level retest belongs to the next functional-tester checkpoint.
  - **Also resolves / unblocks**: the parallel "webhook delete not working / webhooks not disappearing after delete" reports, via the same shared helper and with no change to `app/api/webhooks/[id]/route.ts`; and **Gap 180**, which was only ever this bug seen through the thread search — it stays open as a retest item and is expected to close on the next functional-tester pass, not by any further code change.
  - Spec docs updated: `feature_1_layout_theme.md` (API Call Path — new "Null-body statuses" paragraph, since that section is the canonical description of `proxyJson` for every FE feature) and `feature_5_chat.md` (new "Fix: Deleting a chat thread left it on screen" section + the button rename).

- `[x]` **Gap 179: Connecting the account to Google Drive / Salesforce reported not working** — user-reported (batch QA list, 2026-08-09). Treated as the same root cause as BE Gap 131 (`redirect_uri_mismatch` from `infra/08-apps.bicep`'s now-fixed FQDN computation), pending live verification against that fix. **Live-verified 2026-08-11** (Checkpoint 1, functional-tester, dev scope): both Google Drive and Salesforce authorize URLs resolve correctly via `GET /connectors/auth-url/{provider}`, real provider login screens render, no `redirect_uri_mismatch` on either. Closing as fixed (dev). **Prod still unverified**: `params.prod.json` still holds placeholder OAuth client IDs and the prod resource group doesn't exist yet — re-verify once prod is stood up. Evidence: `apps/invoice-be/docs/test_evidence/gap131_179_oauth_dev_verification/`.

- `[x]` **Gap 180: Chat page's "find thread" option reported not showing properly** — user-reported (batch QA list, 2026-08-09). **Root-caused 2026-08-11** (Checkpoint 1, functional-tester, live): not a Gap 149 regression. `searchQuery`/`filteredSessions` in `ChatWindow.tsx` filter correctly across 6 tested query variations (case, partial, full, substring, no-match). The actual defect is Gap 177: deleting a thread 500s the proxy (`TypeError: Invalid response status code 204`), so the thread stays visible in the sidebar and keeps matching search results even though the backend row is already gone — only a full reload clears it. **No independent fix needed** — keep open only as a retest item against Gap 177's fix; expected to close automatically once 177 ships. Evidence: `docs/test_evidence/gap180_chat_find_thread/`. **Closed 2026-08-11** (Checkpoint 2 functional-tester retest, live, against the shipped Gap 177 fix): created a thread, confirmed it matched a live search, deleted it via the trash icon (`DELETE /chat/sessions/{id}` -> 204, not 500), then ran the identical search again immediately with no reload -- zero matches, "No matching conversations." shown. Postgres confirms the row is actually gone. Closes exactly as predicted in the Gap 177 entry below: no independent code change to the search/filter was needed. Evidence: `docs/test_evidence/gap177_checkpoint2_retest/` (also covers the Gap 177 chat-delete and webhook-delete retest).

- `[x]` **Gap 181: Bulk Directory Scan / upload reported showing a "system error" message** — user-reported (batch QA list, 2026-08-09), merged with a duplicate-numbered report. **Duplicate Gap 145 resolved 2026-08-11**: Gap 145 was closed 2026-08-07 (added a `webkitdirectory` local folder picker to the Bulk Directory Scan card), but a second `[ ]` open Gap 145 entry existed later in this file (user-reported ING-03: "Bulk Directory ingestion option reported not working," possibly related to Gap 69's collapsed-by-default card) — an independent code audit (`NEW_ISSUES_2026-08-10.md`, AQ-12) flagged the same collision and confirmed no code investigation had resolved which report this open entry actually described. Folded into this entry rather than kept as a separate Gap 145, since both describe an unresolved failure on the same Bulk Directory Scan feature and neither has been root-caused. **Needs disambiguation**: (a) get the exact error message/screenshot for the "system error" report, (b) confirm or rule out the theory that it's a Chrome/Edge built-in security prompt (File System Access API permission dialog) rather than a real application error, (c) separately check whether ING-03's report is the same symptom or the "easy to miss the disclosure toggle" issue from Gap 69 — before assuming either way.
  - **Partial mitigation 2026-08-11**: checked the codebase for the File System Access API theory in (b) — there is no `showDirectoryPicker()` call anywhere; the picker uses only the plain `webkitdirectory`/`directory` HTML attributes (`app/ingestion/page.tsx`, "Select Folder from Computer" input), so that specific hypothesis looks unlikely. Separately: the picker's `onChange` handler had **no error handling at all** — any exception while reading the selected folder was uncaught, with no `watcherError` message shown. Added a try/catch that surfaces a real message instead of failing silently/uncaught, plus resetting the input's value so re-selecting the same folder after a failure re-fires `onChange`.
  - **Root cause confirmed 2026-08-11 (live user testing)**: the actual report was about the **other half** of this card — the "OR SERVER PATH" input (`handleWatchDirectory` → `POST /invoices/watcher/start`), not the client-side folder picker (which the user confirmed works fine). Live-tested and got exactly `"Directory watcher isn't enabled for this environment."` — this is `routers/invoices.py`'s intentional path-traversal guard firing because `WATCHER_ALLOWED_BASE_DIR` (`be_features_tracker.md` Gap 12) was never added to `apps/invoice-be/.env.example`, so every local checkout following the documented setup hits this 501 with no indication it's a config gap rather than a broken feature. **Not a code bug** — this is correct, tested behavior when unset. Fixed by adding `WATCHER_ALLOWED_BASE_DIR=./watched_invoices` to `.env.example` plus a git-tracked empty `apps/invoice-be/watched_invoices/.gitkeep` folder — see `be_features_tracker.md` Gap 12 for the full writeup. **Closing this gap**: both halves of the Bulk Directory Scan card are now confirmed working (client picker) or fixed at the actual root cause (server path), and the user confirmed this matches the original report. Anyone with an already-running local backend needs to add the same line to their real `.env` and restart — the `.env.example` change alone doesn't affect an already-running process.

- `[x]` **Gap 184: Programmatic API Authentication & Documentation Portal** — flagged 2026-08-10, wired 2026-08-12 (Checkpoint 14). Backend half (hashed storage, rotation, token auth dependency, webhook schemas in `/docs`) in `be_features_tracker.md` Gap 184.
  - **The mock, now gone**: `app/settings/security/page.tsx` hardcoded `apiKey` state as `"inv_live_9f8a3b2c1d0e4f5a6b7c8d9e0f"` and "Rotate Key" ran a 600 ms `setTimeout` that built a replacement from `Math.floor(Math.random() * 16)` — the page contained no `fetch` at all, so the key on screen authenticated nothing and "rotating" changed only local state. Both are deleted (only the explanatory comment at `page.tsx:47-48` still names them).
  - **Real wiring** (`page.tsx`): `loadStatus()` GETs `/api/settings/security/api-key` on mount into `keyStatus` (`ApiKeyStatus`: `has_key`, `key_prefix`, `masked_key`, `rotated_at`, `last_used_at`, `can_rotate`); `handleRotateKey()` POSTs `/api/settings/security/api-key/rotate` and surfaces the backend's own `detail` on failure (403 non-Admin, 402 lapsed plan) rather than a generic message. Loading, error and **"No API key issued yet"** states are all rendered — that last one is a real state the hardcoded string used to hide, and the button reads "Generate Key" instead of "Rotate Key" for it. The rotate response's raw key is held in `revealedKey` state only, shown once behind a "copy it now, it cannot be retrieved again" warning, and never written to storage (the backend keeps only a salted hash and genuinely cannot re-issue it); the persistent row below shows the masked prefix. "Last Rotated"/"Last Used" tiles and a rotation-invalidates-the-old-key caution were added. The rotate button's visibility follows the backend's `can_rotate` (the local `role` is only a pre-first-response fallback), so the UI can't disagree with the gate that's actually enforced.
  - **Docs Hub** (`page.tsx`, new `activeTab` "API Access"/"API Docs" tablist): the Docs tab iframes `/api/docs/ui` plus a "Raw schema" link to `/api/docs/openapi`.
  - **New proxy routes**: `app/api/settings/security/api-key/route.ts` (GET), `app/api/settings/security/api-key/rotate/route.ts` (POST) — both via the standard `proxyJson` helper. `app/api/docs/openapi/route.ts` and `app/api/docs/ui/route.ts` use the new `backendRootUrl()` in `lib/backendProxy.ts:17-31`, because FastAPI publishes `/openapi.json` and `/docs` at the service root, outside the `/api/v1` prefix `backendUrl()` appends. The docs UI route **proxies FastAPI's own Swagger page** rather than building a second Swagger UI in the FE (nothing to keep in sync when routes change; FastAPI's page already loads swagger-ui from a CDN, so no new dependency), rewriting only the one `url: '/openapi.json'` literal to the same-origin `/api/docs/openapi`. Same-origin matters in deployment: `BACKEND_API_URL` is an internal container address the browser can't reach. If the upstream page shape ever changes so that literal is absent, the route returns a visible 502 explainer with a link to the raw schema instead of a silently blank Swagger UI.
  - **Verified**: `npx tsc --noEmit` — exit code 0, clean. **Not verified in a browser**: no Playwright spec was added for the new tab/rotation flow this pass, so there is no live-render evidence — the e2e coverage for `/settings/security` is still Gap 152's, which predates all of this. Anyone extending `e2e/` should target `data-testid="api-key-display"` and `data-testid="revealed-api-key"`, added for that purpose.

- `[x]` **Gap 194: Webhook deliveries are invisible in the settings UI** — FE half of `be_features_tracker.md` Gap 194 (backend: async dispatch via the queue worker, per-event-type failure counting, `WebhookDeliveryLog` + `GET /api/v1/webhooks/{id}/deliveries`). Flagged and closed 2026-08-12 (Checkpoint 12). `app/settings/webhooks/page.tsx` showed only a subscription's `consecutive_failures` count, so "did this event actually fire, and what did the subscriber answer?" had no answer anywhere in the product — delivery errors are swallowed by design (a dead subscriber must never fail the invoice operation that fired the event), which made a totally broken fan-out look identical to a clean one. Spec updated: `docs/feature_9_webhooks.md` Task 9.2.
  - **New proxy route**: `app/api/webhooks/[id]/deliveries/route.ts` (GET) — thin `proxyJson` passthrough; `proxyJson` already forwards the query string, so `?limit=` reaches the backend unchanged.
  - **Delivery panel** (`page.tsx`): a History button per subscription row toggles an inline panel — `handleToggleDeliveries(id)` sets `expandedId` (one open at a time) and calls `loadDeliveries(id)` on first open only, caching per id in `deliveries` so re-opening doesn't re-flash a spinner; an explicit Refresh re-fetches so the panel can be re-checked after a fix without a page reload. Each row renders event type, timestamp, attempt count, duration and a status pill (emerald for a 2xx, red otherwise), plus the error string on a failure. Loading, empty and error states are distinct, and the error path reuses the page's existing `errorMessage()` guard (Website Gap 13's rule) so a non-JSON response can't render its raw body into the screen. `handleDeleteWebhook` drops the deleted id's cached history and collapses the panel.
  - **Health-warning copy corrected to match the new backend rule**: it said "failed N consecutive times. Auto-disables at 10 failures", which is no longer true now that failures are counted per event type and an endpoint is auto-disabled only when an event type hits 10 *and* no other event type is still delivering. It now states that condition and lists the failing events from the new `event_failure_counts` field (`invoice.completed: 3 · outbound_invoice.overdue: 9`), so the tenant can see which event is broken rather than just that the endpoint is unhealthy. `WebhookSub.event_failure_counts` is typed optional, so a response from a backend without the field still renders.
  - **Gap 203's edit modal untouched**: `editingId`, `openEditModal`/`closeModal`, and `handleSubmitWebhook`'s POST/PUT branch are unchanged, including its deliberate omission of `enabled` from the edit body (so an edit can't silently re-enable an auto-disabled endpoint). All Gap 194 changes to this file are additive.
  - **Verified**: `npx tsc --noEmit` — exit code 0, clean. **Not verified**: no Playwright/live run — there is still no webhook e2e spec and the screen is Admin-gated behind a real Clerk session, so the panel's fetch/render path was only traced statically against the response shape asserted in `invoice-be/tests/test_webhooks.py::test_list_deliveries_returns_newest_first`. The backend that feeds it also has no live queue-worker verification (see the BE tracker entry).

---

## Independent code audit — 2026-08-10

Found by reading the actual source, not user reports — each cites the exact file/line proving the defect. Deduplicated against every gap in this file at time of writing (Gaps 1-184). Merged into this tracker 2026-08-11, next free numbers at merge time. Full write-ups: `AUDIT_FINDINGS_2026-08-10.md`, `NEW_ISSUES_2026-08-10.md`.

- `[x]` **Gap 199: Notification bell count is frozen until the browser is reloaded** — `components/layout/Header.tsx:31-60`, `useNeedsAttentionCount()`'s `useEffect` depends only on `[enabled]` (`canAudit`, which changes once when identity resolves). `Header` never unmounts, so client-side navigation doesn't re-run it — no interval, no focus listener, no exported refresh. Clear ten invoices and the bell still shows the old count until F5. Same shape as **Gap 138** (`useAuth()`'s identity cache never invalidating) in a different file — worth fixing together, one revalidation pattern serves both. **Closed 2026-08-11**: added `usePathname` dependency to trigger on route change, plus debounced focus and visibilitychange listeners.
- `[x]` **Gap 200: Rejected invoices render as "Processing," and the status filter is a no-op on every tab except "All"** — two compounding defects on the same table. (1) `components/dashboard/RecentInvoicesTable.tsx:93-143`'s `getStatusBadge()` has no `REJECTED` case — falls through to `default:`, rendering the blue spinning "Processing" chip even though the backend correctly writes `REJECTED` (`routers/audit.py:365`). (2) `app/invoices/page.tsx:144`'s status dropdown only reaches the API on the "All" tab (`activeTab === "all" ? { status: filters.status } : tabToStatusParams(activeTab)`) — on Pending/Paid/Rejected the tab's own status silently wins, and `components/dashboard/FilterBar.tsx:30-37`'s `STATUSES` list is also missing `REJECTED`/`DUPLICATE` entirely, so those two statuses can't be filtered from the dropdown even on "All" where it would work. Together: filtering to Rejected shows a page where every row claims to be "Processing." **Fixed 2026-08-11**: added a `REJECTED` badge case (rose/`XCircle`, matching `FAILED`) to `getStatusBadge()`; added `REJECTED`/`DUPLICATE` to `FilterBar.tsx`'s `STATUSES`; added a `statusFilterDisabled` prop to `FilterBar` (visually disabled + tooltip) that `app/invoices/page.tsx` sets to `activeTab !== "all"`, so the dropdown no longer looks live when a tab has already pinned the status.
- `[x]` **Gap 201: Date-range filters are shifted by a day in any non-UTC timezone (e.g. all India-based users)** — `app/invoices/page.tsx:92-113` and the identical block in `app/dashboard/page.tsx`: `new Date(today.getFullYear(), today.getMonth(), 1).toISOString().split("T")[0]` converts a **local** midnight to UTC before slicing the date — in IST (UTC+5:30), the 1st of the month becomes `2026-07-31T18:30:00Z`, so `startDate` sends the *previous* month. The `endDate` half fails the opposite way: between 00:00-05:29 IST, `today.toISOString()` is still yesterday's UTC date, silently excluding today's invoices from every range. Same defect class as **Gap 150** (closed 2026-08-07, but only fixed chat timestamps in `MessageBubble.tsx` — this is the same bug in the date-filter code, never revisited). **Fixed 2026-08-11**: added `toLocalDateString()` to `lib/utils.ts` (formats from the `Date` object's own local year/month/day, no UTC conversion) and replaced every `.toISOString().split("T")[0]` call in both `app/invoices/page.tsx` and `app/dashboard/page.tsx`'s `getDatesForRange()` with it. Verified: `npx tsc --noEmit` clean.
  - **Known remaining gap, explicitly not fixed here (FE-only scope as approved)**: cross-references `be_features_tracker.md` Gap 191 — the backend still filters on `Invoice.invoice_date` (the printed document date) instead of `created_at`, and NULL-dated invoices (anything mid-pipeline) still silently drop out of every date-filtered view. This fix removes the off-by-one-day; it does **not** make "This Month"/"Last 30 Days" fully correct until BE Gap 191 is also addressed.
- `[x]` **Gap 202: Queue table is missing added-date and payment-due-date columns** — **Closed 2026-08-12**. Added `created_at` (Ingest Date) and `due_date` (Payment Due Date) columns to the table header, rows, and loading skeleton in `RecentInvoicesTable.tsx`.
- `[x]` **Gap 203: Webhooks cannot be edited through the UI, though the backend already supports it** — `routers/webhooks.py:105-145`'s `PUT /webhooks/{id}` already accepts `target_url`, `subscribed_events`, and `enabled` with URL validation. `app/settings/webhooks/page.tsx:171-189` is the only `PUT` caller, wired solely to the enable/disable toggle — no edit affordance exists (Add Endpoint, copy URL, copy secret, toggle, delete only). **Closed 2026-08-12** — FE-only, exactly as scoped: no backend or proxy-route change (`app/api/webhooks/[id]/route.ts` already exports `PUT`). `app/settings/webhooks/page.tsx` now drives its one modal off a new `editingId` state (`null` = create, a webhook id = edit). A pencil button on each subscription row calls `openEditModal(sub)`, which prefills `targetUrl`/`selectedEvents` from that row and forces `createdSecret` to `null` (the secret is only ever returned once, at creation, so an edit must never render the secret panel). `handleCreateWebhook` became `handleSubmitWebhook`, branching on `editingId` to `PUT /api/webhooks/{id}` vs `POST /api/webhooks` with the same `{target_url, subscribed_events}` body — `enabled` is deliberately omitted so an edit doesn't silently re-enable an auto-disabled endpoint or reset `consecutive_failures` (the toggle keeps sole ownership of that). On success the PUT's returned public dict is merged over the row in place (no refetch) and the modal closes; a new `closeModal()` clears `editingId` + form fields on every exit path, so "Add Endpoint" can't open carrying the previously edited values. Modal title and submit label switch to "Edit Webhook"/"Save Changes"; `GENERIC_ERRORS.update` added so a failed edit doesn't report itself as a failed create. Verified: `npx tsc --noEmit` clean, plus a full static trace of the open → prefill → PUT → merge → close path. **Not verified**: no live/Playwright run — there is no webhook e2e spec, and the screen is Admin-gated behind a real Clerk session.
- `[x]` **Gap 204: Ingestion loses all in-flight processing state on navigation** — `app/ingestion/page.tsx:123-125`: `batchId`, `jobIds`, `trackedFiles` are plain `useState`; `StatusTable`/`LogTerminal` are both driven by `batchId`, so leaving the page unmounts the tree and returning shows an empty ledger while the worker is still running. **Distinct from Gap 146** (closed) — that gap persists *unsubmitted* dropped files. **Correction while fixing this**: the tracker previously described Gap 146 as using `lib/ingestionDraft.ts` — that module exists but is actually unused/orphaned; Gap 146's real implementation is a set of module-level variables (`cachedFiles`/`cachedTags`/`cachedOutboundFiles`) declared directly at the top of `app/ingestion/page.tsx`, synced via `useEffect`. **Fixed 2026-08-11**: added the same module-level pattern for `cachedBatchId`/`cachedJobIds`/`cachedTrackedFiles`, with `useState(() => cachedX)` initializers and sync `useEffect`s, so an in-flight (or just-completed) batch reattaches instead of resetting to empty on SPA navigation back to `/ingestion`. Same known limitation as Gap 146: survives client-side navigation only, not a full page reload/tab close. Verified: `npx tsc --noEmit` clean.
- `[x]` **Gap 205: Review console shows only 7 of ~20 extracted fields** — **Closed 2026-08-12**. Added `subtotal` to correctable fields and built a dedicated "Additional Extracted Metadata" panel in `review/[id]/page.tsx` rendering currency, discounts, tax details/breakdown, tax IDs, payment instructions, and compliance/e-invoice metadata.
- `[x]` **Gap 206: Invoice rows aren't clickable — the only route into the review console is the (also-clipped) Actions menu** — **Closed 2026-08-12**. Made row clicks in `RecentInvoicesTable.tsx` navigate directly to the Auditor Review Console (`/invoices/review/[id]`) and added `e.stopPropagation()` on the actions cell to isolate interactive actions.
- `[x]` **Gap 207: Ingestion ledger polls every 2s instead of streaming, for exactly the common case** — `components/ingestion/StatusTable.tsx:90`: `const isSSE = jobIds.length >= 6`. Batches under six files (including the single-PDF upload — the most common case) fall back to a 2s `setTimeout` poll loop instead of the SSE stream already open on the same page, so status transitions surface up to 2s late. `LogTerminal` opens its own `EventSource` regardless of batch size (Gap 2), so the log scrolls live while the ledger beside it visibly lags — reads as a fault, not latency. **Fixed 2026-08-11**: removed the `jobIds.length >= 6` gate and the entire dead `pollJobStatus`/polling branch (including its own `activePollsRef`); every batch size now uses the same SSE connection `LogTerminal` already opens. **Caught in the same pass, would otherwise have been a regression**: the SSE `onmessage` handler only special-cased `payload.status === "COMPLETED"` for 100% progress, defaulting everything else (including `AUDIT_REQUIRED`/`DUPLICATE`/`FAILED`/`PAID`/`REJECTED`) to a stuck 60% — harmless while only 6+-file batches used SSE, but would have made every small batch's progress bar visibly wrong once SSE became universal. Added a `TERMINAL_STATUSES` check so all terminal statuses jump to 100%, matching what polling used to do. Verified: `npx tsc --noEmit` clean.
- `[x]` **Gap 208: Review console's number/date input type is dead code — every field renders as a plain text box** — **Closed 2026-08-12**. Wired `inputType` in `review/[id]/page.tsx` to the metadata field's `type` instead of hardcoding it to `"text"`, enabling native date pickers and numeric input boxes where appropriate.
*(One more finding from this audit, N-23's currency-display half, is not listed above because it's already fixed — see this file's Gap 183/185. Its `inputType` half survives as Gap 208. Five more findings turned out to be new root-cause information for existing open gaps rather than new ones — see the annotated entries for Gap 141, 143, 153, 154/155, and 177 above, plus `be_features_tracker.md` Gap 191 for the paired date-filter defect.)*

- `[x]` **Gap 216: Chat thread rename never persists server-side — silently falls back to a client-state-only rename that's lost on reload** — **fixed 2026-08-12.** Found 2026-08-11 during Gap 177's Checkpoint 2 retest (functional-tester, live). `app/api/chat/sessions/[sessionId]/route.ts` only exported `GET`/`DELETE`; `hooks/useChatSession.ts`'s `renameSession` called `PUT` on that same route, got a 405, and its catch path updated local React state anyway — so the UI showed the new title with no error and a reload reverted it.
  - **The open question is now answered: no backend rename endpoint existed either.** `routers/chat.py` had `GET`/`POST /sessions`, `DELETE`/`GET /sessions/{id}`, `POST /sessions/{id}/message` and the two feedback routes — no `PUT` anywhere on a session. So this needed both halves, not just the proxy.
  - **Backend**: new `routers/chat.py::rename_session()` (`PUT /chat/sessions/{session_id}`, `response_model=SessionResponse`) plus a `SessionRename` schema (`title` only, `min_length=1`/`max_length=255`). Same 404-then-403 ownership shape as `delete_session`/`get_session_messages` rather than a new helper, matching the router's existing style. Title-only by design — it never touches messages, feedback or ownership. The stripped title is re-checked and a whitespace-only value is a 400: `min_length=1` alone would have persisted a blank sidebar label.
  - **FE**: `route.ts` gained a `PUT` export through the same `proxyJson` helper as its siblings; `useChatSession.renameSession` now applies the title the backend echoes back and only on success, with the catch setting `error` ("Failed to rename this chat session.") — which ChatWindow's existing error banner already renders — instead of faking the write.
  - **Verified**: `pytest tests/test_rag.py tests/test_direction_aware_chat.py tests/test_rbac.py` → 54 passed, including 3 new backend tests (`test_rename_session_persists_and_is_tenant_scoped` re-reads the list rather than trusting the response body, and asserts whitespace normalisation; `test_rename_session_rejects_blank_titles` covers 422 vs. 400; `test_rename_session_forbidden_for_another_tenant` covers 403 and 404). `npx tsc --noEmit` clean. New `e2e/chat-thread-rename.spec.ts`, 3 tests, all passing against a real dev server. **Both e2e regressions were confirmed by re-running them against the un-fixed code**, not assumed: with the `PUT` export removed the proxy-reachability test fails on a 405, and with the old silent-fallback catch restored the error-path test fails. That check also exposed a real scope limit now documented in the spec's header — `page.route()` intercepts in the browser, before Next.js is reached, so the two page-driven tests cover only the hook half; the third test drives the dev server through Playwright's `request` fixture, which is what actually pins the proxy route's existence.

- `[x]` **Gap 217: Header and Admin Console displayed Clerk's `unsafeMetadata.orgName`, which is never reconciled with the tenant the backend actually resolved — hiding a real data-location mismatch** — **closed 2026-08-11.** FE half of **BE Gap 133** (`apps/invoice-be/docs/be_features_tracker.md`), which holds the root cause, the backend fix and the full verification record — read that entry first.
  - The org name in `components/layout/Header.tsx` (`orgName`, line ~159) and the workspace card in `app/admin/page.tsx` (line ~402) both came from Clerk metadata written once at sign-up. The tenant the backend resolves the request to is an entirely separate source, and when provisioning failed the two diverged silently: the UI named the org the user *thought* they were in while their invoices lived elsewhere — the specific reason this whole class of failure was invisible (item 4 of BE Gap 133).
  - **Fix**: `dependencies.py::TenantContext` gained `tenant_name` (from the Tenant row the request already loaded — no extra query), `hooks/useAuth.ts` gained `tenantName` across `AuthMeResponse`/`AuthContextType`/`normalise()`/`ANONYMOUS`, and both components now render `useAuth().tenantName`, keeping `unsafeMetadata.orgName` only as the placeholder while `authLoading` so the header doesn't flicker empty. `orgType`/`country` on the admin page stay on metadata — they have no backend equivalent.
  - **Verified**: `npx tsc --noEmit` clean; the four e2e specs stubbing `**/api/auth/me` (`audit-review-console`, `dashboard-outbound-split`, `group-a-layout-overflow`, `rbac-sidebar`) now include `tenant_name`; `/auth/me` returning `tenant_name` is asserted by `apps/invoice-be/tests/test_auth.py::test_provisioned_org_still_resolves_and_returns_its_tenant_name`. **Not verified**: no e2e assertion that the header renders the stubbed tenant name — none of the four specs assert on org-name text, and no new one was added. Spec doc updated: `feature_1_layout_theme.md`.

- `[x]` **BE Gap 188 — FE-side copy fix: the Pro plan card sold "Up to 1,000 invoices / month", a cap this software has never enforced** — **closed 2026-08-12.** No new FE gap number: the finding lives in `apps/invoice-be/docs/be_features_tracker.md` Gap 188 (which holds the enforcement-side analysis); the only change needed turned out to be in this app, so the record of what changed is here.
  - **Commercial decision, not an engineering one**: 1,000/month was marketing copy with no counterpart in code. `routers/invoices.py` consults `free_invoices_remaining` **only** under `billing_plan == "free"`, and `GET /billing/usage` returns `metered=False` with `used`/`limit`/`remaining`/`resets_at` all null for `pro`/`pro_combined` (BE Gap 143/Feature 11, built 2026-08-12) — deliberately, so nothing in the product implies a Pro ceiling. Building a Pro usage counter/gate/402 path to match the copy was **explicitly rejected**; the copy was corrected to match the software instead.
  - **The one change**: `app/settings/subscriptions/page.tsx`, `PLANS[0].features[0]` (was line 35, now line 45 after the added comment) — `"Up to 1,000 invoices / month"` → `"Unlimited invoices"`. Nothing else on the screen changed, and no enforcement, counter or gate was added anywhere.
  - **Why that wording**: it is verbatim the Pro card's first feature line on the marketing site (`apps/invoice-website/components/marketing/PricingTable.tsx:56`), which already said "Unlimited invoices" and is the canonical pricing copy — so this closes a website↔app contradiction rather than inventing a third phrasing. It also agrees with the two other statements already on this same screen (the usage bar's "Not metered on this plan" and "This plan has no invoice allowance enforced on it.") and with `components/settings/ServiceFlowToggles.tsx:97,102-103` ("unlimited inbound and outbound").
  - **Consistency sweep (no other instance found)**: repo-wide grep for `1,000`/`1000`/"invoices / month" over `invoice-fe` and `invoice-website` returned this line as the **only** false cap claim in shipped copy. The Free tier's real limit is advertised correctly and was left alone — `PricingTable.tsx:42` says "50 invoices / month", matching `invoice-be/config.py:32`'s `DEFAULT_FREE_INVOICES_LIMIT = 50`; the fe subscriptions page's plan picker only renders Pro and Pro Combined, so it makes no Free-tier claim to contradict. `invoice-website/lib/billingPlans.ts::PLAN_COPY.pro` ("Unlimited invoices, full platform access…") and `website_features/feature_3.1_vendor_flow_pricing.md` already said unlimited — unchanged.
  - **Verified**: `npx tsc --noEmit` on invoice-fe — clean. Rendered live in headless Chromium (Playwright, `DISABLE_CLERK_AUTH=true`, `/api/auth/me` stubbed Admin+`pro`, `/api/billing/usage` stubbed `metered:false`): the Pro card reads "Unlimited invoices / Inbound AP processing / AI Quality Rules & Trainer", the string `1,000` has zero occurrences on the page, and "Not metered on this plan" + "This plan has no invoice allowance enforced on it." both render alongside it. The check was a throwaway spec, **run and then deleted, not committed** — there is no `e2e/` spec asserting this copy, and none was added.

- `[x]` **Gap 218 (FE): Inbound Auditor Review Console — "Mark Paid & Finalize" button should be renamed to "Approve Invoice"** — **closed 2026-08-12.** `app/invoices/review/[id]/page.tsx`: inbound primary action label is now "Approve Invoice"; outbound `outbound-review/[id]/page.tsx` unchanged. `e2e/audit-review-console.spec.ts` assertions updated. `npx tsc --noEmit` clean.

- `[x]` **Gap 219 (FE): Autopilot tab has no folder browser — Google Drive/Salesforce source folder must be typed as a raw ID** — **closed 2026-08-12.** `app/ingestion/page.tsx`: read-only folder name + Browse button opens `FolderTreeExplorer` with `selectionMode="folder"`; locked state when connector inactive. `components/connectors/FolderTreeExplorer.tsx`: folder pick mode + "Select This Folder" footer. `e2e/autopilot-folder-browser.spec.ts` added (mocked connector status + file list).

- `[x]` **Gap 220 (FE): Trainer UI restructure — collapse 3-scope tab-bar into Global Rules section + Vendor Rules section** — **closed 2026-08-12.** `components/trainer/TrainerControlBar.tsx`: Global Rules / Vendor Rules sections; vendor dropdown OR upload; Test Chat (`qa_test`) / Add Rules (`rule_creation`) sub-tabs wired to `PUT /api/trainer/sessions/{id}/mode`. `app/trainer/page.tsx` restructured. `npx tsc --noEmit` clean.

- `[x]` **Gap 221 (FE): Trainer UI — Chat Response Style tab in Global Rules section** — **closed 2026-08-12.** `components/trainer/ChatResponseStylePanel.tsx` + proxy routes `app/api/trainer/chat-style/route.ts`, `app/api/trainer/sessions/[id]/commit-behavior/route.ts`. Global Rules → Chat Response Style sub-tab; amber banner + Save Response Style → `POST commit-behavior`.

- `[x]` **Gap 222 (FE): Audit Queue's Recent Invoices table is locked to a fixed height with wide cell padding, forcing horizontal scroll instead of using available screen width** — opened 2026-08-14, closed 2026-08-17. `components/dashboard/RecentInvoicesTable.tsx` gained an `isFullPage` prop that drops the `maxHeight: 320` cap and tightens cell padding; `app/invoices/page.tsx` passes `isFullPage={true}` (checked directly — easy prop to add and forget to wire, confirmed present). **Verified**: `npx tsc --noEmit` clean.

- `[x]` **Gap 223 (FE): Audit Queue status filter can't isolate "Review Required" (`AUDIT_REQUIRED`) invoices** — opened 2026-08-14, closed 2026-08-17. Added a dedicated `audit_required` tab (`STATUS_TABS`/`StatusTab` in `RecentInvoicesTable.tsx`) mapped to `status: "AUDIT_REQUIRED"` in `tabToStatusParams()`; `AUDIT_REQUIRED` correctly removed from the "Pending" bucket (`status_in` is now `"PROCESSING,COMPLETED,DUPLICATE"`) so it isn't double-counted under both tabs. **Verified**: `npx tsc --noEmit` clean. **Worth a follow-up check**: confirm nothing elsewhere (a dashboard "Pending" count badge, etc.) still assumes the old 4-status "Pending" definition.

- `[x]` **Gap 224 (FE): Auditor Review Console has no structured line-item editing — only scalar fields (Vendor, Total, Date) can be corrected** — opened 2026-08-14, closed 2026-08-17. `app/invoices/review/[id]/page.tsx` gained a real editable line-item table (add/edit/delete rows, live subtotal recalculation, save/cancel), submitting via the existing `PUT /audit/resolve/{id}` corrections payload. **Was silently broken until BE Gap 234 landed same-day**: the backend didn't accept `items`/`subtotal` as correctable fields and silently dropped them, so this FE work would have shown a false "saved" success with nothing actually persisting — caught before ship, not after. **Verified end-to-end after BE Gap 234 closed**: submitted a real correction via the live API and confirmed via direct DB query that both `items` and `subtotal` persisted exactly as sent. `npx tsc --noEmit` clean.

- `[x]` **Gap 225 (FE): React Error #31 crash in `StatusTable.tsx` when an invoice has verification alerts** — opened 2026-08-14, closed 2026-08-17. `components/ingestion/StatusTable.tsx` now renders `alert.message` (falling back to `.code` or a JSON stringify) instead of the raw alert object. **Verified**: `npx tsc --noEmit` clean. **Minor follow-up, not blocking**: the `alerts` field is typed `any[]` rather than a proper `{type, message, field?, severity?}` interface — worth tightening later.

- `[x]` **Gap 226 (FE): Uploaded profile picture fails to update or display in the UI header/settings** — opened 2026-08-14, closed 2026-08-17. Root cause: `components/layout/Header.tsx` only ever rendered initials, never read Clerk's `user.imageUrl` at all — the picture was uploading fine via Clerk's own profile modal, the app just never displayed it. Now renders `user.imageUrl` when present, falling back to initials, preserving Gap 116's loading-skeleton state. **Verified**: `npx tsc --noEmit` clean.

- `[x]` **Gap 227 (FE): Ingestion page's Live Processing Log panel shows duplicate/conflicting log status entries at once** — opened 2026-08-14, user-reported. **Marked `[x]` closed 2026-08-17, reverted to open on code review the same day — that first attempt (a "skip if it matches one of the last 3 rendered lines" dedup heuristic) did not fix the diagnosed root cause: status-transition events (which carry a `message` field but no `invoice_id`, unlike real log lines) were still interleaving with genuine log lines, just with some exact repeats suppressed.** **Closed for real 2026-08-18.** Fix: `LogTerminal.tsx`'s SSE `onmessage` now filters on `payload?.type === "log_line"` before appending to state, matching the component's own pre-existing docstring contract (lines 20-22) which had specified this filter all along. Root cause confirmed end-to-end: log lines are published with `"type": "log_line"` (`queue_worker/handlers.py:570`), while status events (`handlers.py:592, 611, 766, 781, 805`) carry `message` with no `type` field at all — the old code's `if (payload?.message)` check matched both, which is exactly the interleaving bug. The new filter excludes status events specifically, without starving the panel of real log lines. Independently traced against the actual `handlers.py` publish sites, not taken on the implementing tool's word.

- `[x]` **Gap 228 (FE): Outbound Auditor Console PDF viewer doesn't render properly and the Extracted Fields panel needs UI work** — opened 2026-08-14, closed 2026-08-17. **Correction on the original premise**: this is not a distinct component from the inbound console — `app/invoices/outbound-review/[id]/page.tsx` imports and uses the same `components/audit/PdfViewerCanvas.tsx` the inbound review console does. Fix was scoped safely around that: `max-h-[calc(100vh-180px)] xl:max-h-none` added to the two side-panel columns (not the shared viewer component itself), bounding their height on sub-`xl` viewports without risking a regression on the inbound console. **Verified**: `npx tsc --noEmit` clean. **Not yet confirmed**: whether this fully addresses the original report — if the complaint also meant the PDF canvas itself renders incorrectly (not just panel height/alignment), that's not touched by this change and needs a separate look.

- `[x]` **Gap 229 (FE): Semantic Chat replies have no rich formatting — no structured bullets, no syntax highlighting** — opened 2026-08-14, user-reported, closed 2026-08-17. Investigated: `MessageBubble.tsx`'s renderer was a hand-rolled regex supporting only `**bold**`/`` `code` ``/`*italic*` — no lists, tables, headings, or links. **Worse than the report suggested**: the backend was already emitting real GFM markdown the old renderer mangled into literal text — SQL-route replies append a real pipe table (`### Query Results` + `|`-delimited rows), RAG-route replies append real markdown links (`[Source: ...](file:///...)`) — so users were seeing raw `###`/`|`/`[...](...)` syntax, not just plain-feeling prose. The renderer's own code comment cited "no react-markdown installed, avoid an ES-module dep + `transpilePackages`" as the reason for the regex approach — didn't hold up: `react-markdown` is plain ESM/CJS-dual and works fine in a `"use client"` component on Next 14.2.3, no extra config needed. **Fixed**: added `react-markdown` + `remark-gfm` (tables/strikethrough), swapped the renderer, added `components` overrides to keep the existing dark-theme palette (bold/code styling unchanged) plus new styles for lists/tables/headings/links/blockquotes. Also added a `FORMATTING` line to the RAG/CHAT system prompts (`agents/query_agent.py`) instructing bullet lists for multi-item answers. **Independently re-verified before merge**: `tsc --noEmit` clean and a real `next build` compiles/generates all 27 pages (one unrelated pre-existing prerender failure confirmed via a side-by-side master build, not caused by this change).

- `[x]` **Gap 230 (FE): Security & Access Control settings page's "API Docs" button returns a 404** — opened 2026-08-14, closed 2026-08-17. **Correction on the original diagnosis**: the button itself was never broken — `app/settings/security/page.tsx`'s API Docs control is a `role="tab"` toggle to local state, not a broken link; it already is the Docs Hub. The real cause was in `invoice-website`, not `invoice-fe`: Multi-Zone routing never included `docs` in either `next.config.js::feApiPrefixes` or `middleware.ts`'s matcher exclusion, so a request to `/api/docs/*` from the website origin was never rewritten to the FE and 404'd before reaching the iframe'd `/api/docs/ui`. Both files fixed — `"docs"` added to `feApiPrefixes` and to the middleware matcher. **Verified**: `npx tsc --noEmit` clean on both apps. **Minor follow-up**: `next.config.js`'s comment above `feApiPrefixes` still says "14 folders, all 14 now listed" — stale, it's 15 now.

- `[x]` **Gap 231 (FE): Hovering over Dashboard charts causes severe UI flickering and visual distortion** — opened 2026-08-14, closed 2026-08-17. Root cause found in `components/dashboard/MetricsGrid.tsx` and `OutboundMetricsGrid.tsx`: the hover tooltip rendered inline inside a flex row, so its appearance/disappearance on every hover in/out reflowed sibling elements (the currency legend), reading as flicker. Fixed by converting the tooltip to `position: absolute` inside a fixed-height (`h-8`) parent, so it overlays without affecting the row's layout. **Verified**: `npx tsc --noEmit` clean. **Not confirmed**: whether this was the only flickering chart — `ClientPerformanceChart.tsx` (a separate hand-built chart with its own hover handlers) was untouched by this fix; if the original report also covered that chart, retest it separately before considering this fully closed.

---

## Feature 15 — Help Center Knowledge Base & AI Support Assistant with Direct Ticket Escalation (2026-08-17)

Full FE design + verification record: `docs/feature_15_help_center_support_bot_and_tickets.md`.

> **Why this section sits above Feature 14's rather than after it (2026-08-18).** This branch originally appended it to the very end of the file. Master's Feature 14 section ends at the last line of the file (its Gap 239 entry), and master edited that exact line after this branch forked — so *any* append at EOF lands adjacent to master's own edit and git raises a conflict there no matter what the content is. Verified with a real three-way merge: appending at EOF conflicts; placing the section at this section boundary merges cleanly (0 conflicts) against both master's committed head and master's current uncommitted state. Nothing about the entries themselves changed in the move. This branch also deliberately leaves the Gap 239 line itself **exactly as it was at the merge base**, so the merge takes master's fixed `[x]` version of it unopposed — do not "helpfully" update that line on this branch, it re-introduces the conflict.

> **Renumbered 2026-08-18, before merge.** These three entries were originally filed on this branch as Gaps **240/241/242 (FE)**. Master's in-flight work independently took 240/241/242 (FE) for the Flows-visualization reports moved over from the website tracker, so this branch's set — the newer and still-unmerged one — was moved to **246/247/248** to match the same bump applied to its BE siblings (BE Gaps 246/247/248). FE and BE gap numbering are independent sequences (one per tracker), so the two sets sharing 246–248 is coincidental, not a link. **FE Gaps 243/244/245 are deliberately left unused** — headroom for master's in-flight chat/RAG work, not missing entries.

> **Backend prerequisite complete (2026-08-17)**: BE Feature 19 (Support Ticket Engine) is shipped — `SupportTicket` model, `services/support_email.py`, and `routers/support.py` (`POST /api/v1/support/contact`, `POST /api/v1/support/chat`, `POST /api/v1/support/ticket`, `GET /api/v1/support/tickets`) are all live. See `apps/invoice-be/docs/be_features_tracker.md` Feature 19 (Gaps 246/247/248) for endpoint specs. *(This note originally said "18/18 tests passing" — the suite it refers to, `tests/test_support.py`, collects 22; corrected 2026-08-18.)*

- `[x]` **Gap 246 (FE, was 240 on this branch): Help Center had no interactive AI assistant and no way to search the guides** — **closed 2026-08-17.**
  - **`app/help/page.tsx`** upgraded to a dual-tab interface:
    - **Knowledge Base Guides (default view)**: opens on initial load (`useState<"guides" | "assistant">("guides")`), with a real-time search filter (`#help-guide-search`) over each section's `searchText`, and a categorized topic sidebar.
    - **AI Support Assistant tab**: houses `SupportChatWindow.tsx` (SAGE Bot) for conversational troubleshooting.
  - **`app/api/support/ticket/route.ts`** and **`app/api/support/chat/route.ts`**: server-side proxy handlers, both delegating to the existing `lib/backendProxy.ts` `proxyJson()` helper, which attaches the Clerk session token.
  - **Scope correction, 2026-08-18**: the original wording of this entry ("static-only text list with no … real guide screenshot rendering", "11 illustrated guides") overstated it. The illustrated guide content and its screenshots **already existed** before this branch — `app/help/content/{trainer,auditor,webhooks,inbound-email,outbound-email}-guide.tsx` and `public/help/{trainer,auditor}/` are all present at the merge base `c14ccfc` and are untouched by this commit, which only rewrote `app/help/page.tsx`. The real content count is **15** sections, not 11. What this branch actually added here is the tab shell, the default-to-guides behaviour, the search filter, and the assistant tab.

- `[x]` **Gap 247 (FE, was 241 on this branch): No smart error diagnosis or 1-click ticket escalation in Help Center** — **closed 2026-08-17.**
  - **`components/help/SupportChatWindow.tsx`**: SAGE Bot provides domain-guided assistance, prompt chips, and Markdown rendering.
  - When backend AI Support Agent (`agents/support_agent.py`) diagnoses severe error patterns (e.g. ERP batch sync 504 timeout, PayU webhook failure, hung OCR processing) or low confidence, it returns `suggest_escalation: true` and renders an inline **`[ 🎫 Raise Support Ticket from this Issue ]`** card.
  - Clicking the card opens `SupportTicketModal.tsx` pre-populated with Subject, Category, Priority, Error Code, and the full conversation transcript automatically attached.

- `[x]` **Gap 248 (FE, was 242 on this branch): No direct support ticket submission modal in chat header** — **closed 2026-08-17.**
  - **`components/help/SupportTicketModal.tsx`**: Dark glassmorphic ticket creation modal with live form validation, priority selector pills (`Low`, `Normal`, `Urgent` with SLA commitments), and animated confirmation state displaying the generated reference number (`TICK-YYYY-XXXX`).
  - **`SupportChatWindow.tsx`**: Features a single centralized **`[ 🎫 Raise Ticket Directly ]`** action button in the chat header allowing users to submit direct tickets without starting a conversation.
  - **Verification, as reported by the branch author on 2026-08-17**: `npx tsc --noEmit` clean (exit 0), `tests/test_support.py` 22/22 passed, Playwright `e2e/help-support.spec.ts` **6/6 passed**. **Not re-run in the 2026-08-18 merge-prep pass** — that pass had no `node_modules` and no backend venv available in its worktree, so it verified the code by reading it and confirmed only the countable facts: `e2e/help-support.spec.ts` does contain exactly 6 `test(` blocks, and `tests/test_support.py` does collect 22 cases (20 functions, one of them `@pytest.mark.parametrize`'d over 3 values). Every Playwright spec here stubs `/api/**`, so nothing in this feature has been exercised against a real backend, a real Clerk session, or a real SendGrid dispatch.

---

## Feature 14 — Alert-Anchored Trainer & Chat Correction Lane (2026-08-17)

FE half of `apps/invoice-be/docs/feature_18_trainer_alert_anchored_training.md` (BE, built 2026-08-13). Full FE design + verification record: `docs/feature_14_trainer_redesign.md`. Spec doc `feature_6_trainer.md` had its File Coordinates section updated; the rest of it still applies.

Context: the 2026-08-12/13 ground-truth investigation closed BE Gaps 222/223/224/226 and surfaced the structural problem underneath them — Trainer rule creation took free text, had an LLM interpret it, and persisted the result with no checkpoint, so a rule was never tied to a specific invoice, field, or observed failure.

- `[x]` **Gap 232 (FE): `/trainer` gated on billing plan only — a user without `can_train` could open the whole rule-authoring sandbox and only hit the wall on first save** — closed 2026-08-17. `app/trainer/page.tsx` read `billingPlan` from `useAuth()` and never `canTrain`, though the hook has exposed it since FE Gap 99. Anyone in a Pro tenant could navigate to `/trainer` directly, pick an invoice, and fill in a correction before the backend's `require_can_train` 403'd the commit. The backend was always the real enforcement so nothing could be written — but the permission boundary existed only in the API, not in the product.
  - **Fix**: new `TrainerPermissionPrompt` full-page state, deliberately mirroring FE Gap 115's `TrainerUpgradePrompt` rather than a new pattern (both are "this whole route is not for you"; a dismissable overlay would just reveal a screen that fails on first use). Plan gate checked **first**, so a Free-tier tenant still gets the upgrade explanation rather than a misleading permissions one. Gap 138's stale-cache single re-fetch now keys off either gate being up.
  - **Deliberately not done**: no "train on this" affordance was added to any Auditor-facing screen. `components/audit/AlertConsole.tsx`, `OutboundAlertConsole.tsx` and `app/invoices/review/[id]/page.tsx` are untouched — auditors resolve alerts exactly as before and never see or trigger rule creation.
  - **Verified**: Playwright, actually run — `e2e/trainer-alert-anchored.spec.ts` asserts `can_train:false` renders the permission state, *not* the workspace and *not* the billing prompt.

- `[x]` **Gap 233 (FE): Trainer offered a Global rule-creation section pointing at endpoints that now reject every write** — closed 2026-08-17. `TrainerControlBar.tsx`'s Global Rules section survived the backend removing Global-scope rule creation: `POST /trainer/sessions/global` is 410 Gone and `trainer_commit()` refuses a `scope="global"` session outright. The tab was a destination every write path rejects.
  - **Fix**: Global section removed from the control bar; `app/trainer/page.tsx` no longer opens a Global session on mount (it opens no session at all now — see Gap 234). Both dead proxy routes **deleted** rather than left returning 410s: `app/api/trainer/sessions/global/route.ts`, `app/api/trainer/sessions/from-production/route.ts`.
  - **Not removed, and this matters**: already-committed Global rules are untouched and still apply — still read by both extraction prompt builders, the queue worker and the chat agent, and still visible in Rule History. Only authoring new ones is gone. `outbound` is now a first-class scope in the FE types (an outbound invoice has no vendor; its rules live on the outbound-global template), and `CommitModal` states honestly that outbound commits queue no re-audit (BE deviation 5).

- `[x]` **Gap 234 (FE): Vendor training could only ever open a vendor's newest invoice — an alert on any older one was unreachable** — closed 2026-08-17. The old Existing Vendor flow called `POST /trainer/sessions/from-production?vendor_name=X`, which resolved `order_by(created_at.desc()).first()` server-side. There was no way to train on anything but the latest invoice, and the upload path's PDF was a client-side `URL.createObjectURL()` that survived neither a reload nor another device.
  - **Fix**: new `components/trainer/TrainerEntryPanel.tsx` — one screen, two ways in (upload a PDF; or select vendor → **pick from a real list of their invoices**, each row showing its real alert count, which is how you find the invoice carrying the alert you want). Both land on the identical state: that invoice's alerts beside that invoice's PDF. The page now opens **no** session on mount — the landing state is the picker, because every session must be anchored to a document the user chose.
  - **Contract deviation, documented not absorbed**: the backend added no trainer-side per-vendor invoice list (`GET /trainer/vendors` still returns one `sampleInvoiceId` per vendor — the same latest-only limitation). `trainerService.listVendorInvoices()` therefore uses the standard `GET /api/invoices?vendor_name=X&limit=50`, which already supports the filter and returns `sa_alerts`. It is INBOUND-only by construction there; covering OUTBOUND would need a BE change.
  - **New proxy routes**: `app/api/trainer/sessions/from-invoice/route.ts`, and `app/api/trainer/sessions/[id]/pdf/route.ts` — the latter deliberately a **binary stream**, not `proxyJson` (reading a PDF through `response.text()` corrupts it), mirroring `app/api/invoices/[id]/pdf/route.ts`. All `URL.createObjectURL()` use is gone from `lib/trainer-service.ts`. Coordinate highlighting is **not** promised: `boundingBox` stays optional and the viewer degrades to showing the document without a highlight rather than forcing one that may not exist.

- `[x]` **Gap 235 (FE): No way to correct a specific alert — rule creation was a free-text box with no grounding in any concrete event** — closed 2026-08-17. This is the core defect the redesign exists for. The chat box both answered questions and created rules, and an LLM decided what the rule meant.
  - **Fix**: `components/trainer/AlertListPanel.tsx` renders the session's real alerts with two entry points. **"Train on this"** opens `AlertCorrectionModal.tsx`: *unnecessary* → a numeric tolerance form (`abs_tol`/`rel_tol`) for tolerance-overridable types, a **separate** threshold form for `low_confidence_field` (a different parameter on a different backend function — reusing the tolerance UI would have shipped a control whose numbers silently did nothing), or, for the five `*_not_verified_in_source` types, **no form at all** plus the registry's own `notCorrectableReason` and a link to the relabel option; *wrong severity or message* → severity dropdown + editable message, which never changes whether an alert fires. **"Flag as missed"** (`FlagMissedAlertModal.tsx`) is panel-level, available *especially* when the alert list is empty, with two structured primary inputs (registry alert type; field from this invoice's extracted fields) and free text explicitly optional and labelled as background only.
  - Each alert row renders its correctability from the backend registry's per-alert `correctionForm` — the FE keeps no second copy of that mapping. New proxy routes: `alert-types` and the four `corrections/*`.
  - **Honesty detail**: the numeric forms label their reference values "Ships as …", not "your current setting" — the session payload carries no resolved effective tolerance, so claiming to show a current value would sometimes be wrong.
  - **Verified**: Playwright, actually run — a tolerance-overridable alert renders the tolerance form; a `total_not_verified_in_source` alert renders the explainer with **both** `tolerance-form` and `threshold-form` asserted absent.

- `[x]` **Gap 236 (FE): Committing rules showed a list of sentences and a re-audit warning — no interpretation, no impact, no real checkpoint** — closed 2026-08-17. `CommitModal.tsx` was a confirmation dialog over `activeRules: string[]`.
  - **Fix**: reworked into the preview-before-commit gate every correction path funnels through. The header button is now **"Review & Commit"** and clicking it *runs* `POST /sessions/{id}/preview` — there is no path to the confirm button that skips it. Renders (1) structured interpretation per rule (kind/field/condition/scope/source alert type), (2) historical impact — `exact` shows the real replay figures **plus a list of the actual invoices** that would change; `not_computable` shows an explicit explanation with **no number and no fabricated zero**; `partial` shows both with the uncomputable part named separately rather than folded into the totals — and (3) an explicit Confirm sending `preview_token` (backend 409s on drift). Staging any new correction clears the locally held preview, so a stale impact estimate can't be re-opened. Gap 217's guardrail now surfaces at preview time while the user is still editing; `errorMessage()` unwraps the structured 400 bodies including `flagged_rule`.
  - **Verified**: Playwright, actually run — one test asserts `/preview` is called before the dialog renders and the confirm sends that exact token (checked on the intercepted request body); another asserts a `not_computable` impact renders the explanation and no sample list.

- `[x]` **Gap 237 (FE): One text box both answered questions and silently created extraction rules** — closed 2026-08-17. `QnAPanel.tsx`'s input was the rule-creation surface *and* the QA surface, so asking a question could teach the extractor something with no checkpoint in between.
  - **Fix**: new `components/trainer/QaChatPanel.tsx`, structurally and visually separate, stating in the UI (not just in a doc) that nothing typed there creates an extraction rule, and pointing at the alert panel for extraction changes and the thumbs-down for answer problems. Control-bar modes relabelled **"Correct Alerts"** / **"Ask Questions"**. Thumbs-down is gated on `isRealMessageId()` — QA turns are now real `ChatMessage` rows, but a synthetic `msg-xxxxxxxx` id would 404 the feedback API, so no vote control is rendered for one.
  - `QnAPanel.tsx` and `TrainerUploader.tsx` are now orphaned but were **left on disk** (deleting them is outside this pass's approved scope) — named in `feature_14_trainer_redesign.md` as a follow-up rather than left as silent clutter. `ScopeSelector.tsx` was already unreferenced before this work.

- `[x]` **Gap 238 (FE): A thumbs-down on a chat answer was a dead end — recorded and never acted on** — closed 2026-08-17. `MessageBubble::FeedbackVote` wrote a vote (Gap 54) and stopped there.
  - **Fix**: new `components/chat/ThumbsDownTriage.tsx`, used by **both** `MessageBubble` and the Trainer's QA panel — one implementation, so the two surfaces can't drift. Thumbs-**up** is unchanged and still signal-only. Thumbs-down: reason picker (wrong data / wrong interpretation / bad tone) sent *with* the vote so the backend returns the next step in one round-trip, and the vote lands first so abandoning the dialog still registers the complaint. **wrong data** → if several invoices fed the reply, asks the one genuinely disambiguating question ("one specific invoice, or the overall total?"); a specific invoice is picked from **the real set the backend returns**, never typed, then auto-diffed — the UI never asks "was the number right?", which is what the user came here unable to answer. On a `match`, **the PDF renders inline beside the stored value** so the document question can be answered without hunting for the file; if the document disagrees, this stops being a chat correction and deep-links to `/trainer?invoice_id=…&field=…&flag_missed=1`, opening the missed-alert form pre-filled. **bad tone** → the chat-style controls, not a rule object. Every path ends in preview → explicit confirm; the commit sends the preview token (backend 400s without one). Users without `can_train` still reach the preview and see what would be proposed, with the confirm disabled and explained — matching the backend, which gates only the commit.
  - **New proxy routes**: `app/api/chat/messages/[messageId]/triage/route.ts`, `.../triage/source-verdict/route.ts`, `app/api/chat/rules/{,categories,preview,commit,[ruleId]}/route.ts`. New service layer `lib/chat-training-service.ts`.
  - **Known BE contract gap, documented not worked around**: the backend's `bad_tone` triage response advertises `settingsEndpoint: "/api/v1/trainer/chat-style"`, which is **GET-only** — saving chat style is still `POST /trainer/sessions/{id}/commit-behavior` and needs an active trainer session. So this branch *links* to the Trainer's Chat Response Style tab rather than saving inline, and a user without Trainer plan/permission has no way to act on their own tone complaint. **Follow-up (BE): a session-free `POST /trainer/chat-style`.**

- **Correction to Gap 221 (FE), appended 2026-08-17**: that entry says the Chat Response Style tab is in the "Global Rules section" — this was already untrue in the shipped code before Feature 14 (`TrainerControlBar.tsx` rendered the `rules`/`style` sub-tabs inside the **Vendor** branch, not the Global one), and is doubly so now that the Global section is removed entirely by Gap 233; the panel and its two proxy routes are otherwise exactly as that entry describes.

**Verification for Gaps 232-238**: `npx tsc --noEmit` clean (exit 0). **Playwright actually run** — full suite `63 passed, 1 failed`; new `e2e/trainer-alert-anchored.spec.ts` (5 tests) all pass, `e2e/trainer-loading-state.spec.ts` rewritten (it stubbed the two now-deleted endpoints and drove the old scope buttons) and passing, and the 5 Trainer assertions in `e2e/group-a-layout-overflow.spec.ts` / `e2e/rbac-sidebar.spec.ts` updated for the removed `/sessions/global` stub and the renamed commit button, all passing. **The 1 failure is pre-existing and unrelated** — `group-a-layout-overflow.spec.ts › Gap 86 — Ingestion header row › toggle is absent for a receive-only tenant`, on `/ingestion`; **confirmed pre-existing by re-running it with this pass's `app/`, `components/` and `lib/` changes stashed, where it fails identically**. **Not verified**: nothing exercised against a real backend/tenant/Azure — every spec stubs `/api/**`; specifically unverified are the missed-alert LLM drafting round-trip, real preview impact numbers, the chat auto-diff against real stored data, and the inline PDF render in the triage dialog.

- `[x]` **Gap 239 (FE): Clicking thumbs-down on a chat reply crashes with "Application error: a client-side exception has occurred" instead of opening the triage dialog** — opened 2026-08-17, founder-reported live on the real Chat screen. **Root cause found 2026-08-17, without needing the browser console stack trace after all**: `components/chat/ThumbsDownTriage.tsx` is mounted unconditionally (kept in the tree, toggled via an `isOpen` prop, not conditionally rendered per `MessageBubble.tsx`). It had an early return (`if (!isOpen) return null;`) with a `useEffect` declared *after* it. While closed, React called the hooks up to the early return and stopped (19 hooks). The moment thumbs-down set `isOpen=true`, the **same component instance** re-rendered, ran the same hooks, but no longer returned early — calling a 20th hook. A differing hook count across renders of one instance is a React Rules-of-Hooks violation ("Rendered fewer hooks than expected" / minified #310), thrown during render/commit — exactly the reported generic client-side exception, and consistent with everything already ruled out (API contract fine, `applyNext` branches complete, `tsc` clean, not a Provider issue). Confirmed why static review missed it: `package.json` has no ESLint/`eslint-plugin-react-hooks` at all, and no Playwright spec exercised the thumbs-down click. **Fixed**: moved the effect (and the `ensureCategories` helper it calls) to above the early return, alongside the other unconditional hooks — pure reordering, no logic change. Verified: `npx tsc --noEmit` clean.

**Moved from `invoice-website/website_features/website_features_tracker.md` 2026-08-17, per architect's scoping pass**: filed originally against the website by the reporting user, but the actual page is invoice-fe's `app/flows/page.tsx` (Feature 11) — the website only hosts a `FlowsModal` iframe wrapper around it. Renumbered into this tracker's own sequence; original website gap numbers noted for traceability. Their sibling, the misfiled zoom-button report, was cancelled outright (invalid premise — no zoom feature exists in Feature 11's design, the controls are playback speed and are correctly wired) rather than moved here.

- `[cancelled]` **Gap 240 (FE, was website Gap 178): Flows visualization layout is misaligned and formatting is broken** — opened 2026-08-14, user-reported. **Cancelled 2026-08-18** — deprioritized to Very Low earlier the same day, then cancelled outright per explicit user direction: not a critical path, no one depends on this rendering correctly. Architect's working hypothesis, unconfirmed and now moot: FE JS not hydrating correctly when `/flows` is loaded inside the website's iframe. Reopen if this becomes user-facing-critical again.

- `[cancelled]` **Gap 241 (FE, was website Gap 180): Flows visualization play button fails to render pipeline animations** — opened 2026-08-14, user-reported. **Cancelled 2026-08-18**, same reasoning and same batch as Gap 240 — not critical, no one needs it. Covered Inbound Pipeline, Outbound Pipeline, Chat/RAG Agent, and Direction-Aware Chat animations.

- `[cancelled]` **Gap 242 (FE, was website Gap 181): Flows visualization in-page reload button does not refresh page data or update active state** — opened 2026-08-14, user-reported. **Cancelled 2026-08-18**, same reasoning and same batch as Gaps 240/241.

- `[x]` **Gap 243 (FE): a Help Center answer with no matching article had no way to raise a ticket from it** — opened and fixed 2026-08-19, as the necessary frontend half of **BE Gap 254**. The escalation card in `components/help/SupportChatWindow.tsx` (including its `Raise Support Ticket from this Issue` button) is entirely gated on `suggestEscalation`. BE Gap 254 correctly stopped the backend from setting `suggest_escalation=True` on a plain knowledge-base miss — an unanswered question was rendering in the same red *Issue Diagnosis & Recommended Escalation* framing as a genuinely detected incident — but that change on its own removed the only **contextual** in-chat path to raise a ticket for an unanswered question (the header-level `Raise Ticket Directly` button does still exist, so this was a degradation, not a total loss). **Fixed** by adding a third, neutral low-confidence state rather than reverting the backend: the BE response gained `low_confidence: bool = False` (`routers/support.py::SupportChatResponse`), `ChatMessage` gained `lowConfidence?: boolean`, and a new `#low-confidence-card` renders when `!suggestEscalation && lowConfidence` — neutral slate styling, no `AlertTriangle`, no incident wording, just *Didn't find an answer?* plus a plain `Raise a Ticket` button reusing the existing `handleOpenEscalation` so the modal still prefills. Deliberately minimal: no restructuring of the component, and the existing escalation card is untouched. Additive and backwards-compatible — an FE build that ignores `low_confidence` behaves exactly as before, and `e2e/help-support.spec.ts` mocks the route so its escalation spec is unaffected. **Verified**: `npx tsc --noEmit` clean; BE side `tests/test_support.py` 70 passed (includes BE Gap 256 follow-up cases: `topic_id` echoed, `last_topic_id` sent from `SupportChatWindow.tsx`). **Live UI verified 2026-08-19** on local invoice-fe (`localhost:3001/help`, tenant `test 413 co.`): miss `how do I wash my car?` rendered the neutral *Didn't find an answer? / Raise a Ticket* card (no red *Issue Diagnosis* framing); the button opened *Escalate to Support Ticket* with subject pre-filled to that question and priority **Normal**; chip *Simulate ERP Batch Sync Timeout (504)* still rendered the red *Issue Diagnosis & Recommended Escalation* card and opened a ticket modal with subject *System Timeout / Batch Sync Failure (Error 504)* and priority **Urgent*. Note: the plan's example miss *"how do I bulk upload 200 invoices at once"* matches `ingestion_upload` and is **not** a low-confidence case. ESLint is not configured in this app (`next lint` prompts for first-time setup), so no lint result is claimed.

- `[x]` **Gap 259 (FE): Help Center Knowledge Base static guides lack articles on Autopilot, Settings panels, and recent Trainer/Auditor features** — opened 2026-08-19. **Fixed 2026-08-19.** Added detailed illustrated articles for Autopilot folder sync & deduplication (`content/autopilot-guide.tsx`), settings panel configurations for HMAC webhooks, LOP API keys, roles, and PayU subscription billing (`content/settings-guide.tsx`), trainer sandbox session modes & rule validations (`content/trainer-guide.tsx`), and auditor/reviewer outbound review workflows (`content/auditor-guide.tsx`). Registered the new sections in the `/help` main router catalog (`app/help/page.tsx`). Type-checked and compiled successfully via `npx tsc --noEmit`. Verified backend keyword compliance via pytest suite (66/66 passing).

- `[x]` **Gap 260 (FE): Webhook named to be changed from inbound_paid to inbound_approved and outbound_paid to outbound_approved** — opened and closed 2026-08-19. Updated the webhook settings page (`app/settings/webhooks/page.tsx`) to map `invoice.approved` and `outbound_invoice.approved` events, and updated developer guides (`app/help/content/settings-guide.tsx` and `app/help/content/webhooks-guide.tsx`).
- `[x]` **Gap 261 (FE): Salesforce connector is not asking for login authentication, always auto-login to application@infinevoclouds.com** — opened and closed 2026-08-19. Fully resolved by the backend fix (adding `prompt=login consent` to Salesforce OAuth parameters), forcing the Salesforce authentication prompt and letting users choose their organization or log in explicitly.
- `[x]` **Gap 262 (FE): Salesforce Selector folder not selecting the folder** — opened and closed 2026-08-19. Resolved by updating the backend Salesforce connectors API to return Salesforce Libraries (ContentWorkspace) as folder nodes. FolderTreeExplorer can now browse these library nodes as folders and successfully select them.

- `[x]` **Gap 277 (FE): No way to record an INBOUND invoice as Paid from the Audit Queue / Invoices list** — opened and closed 2026-08-20.
  - **Problem**: Inbound invoices in `COMPLETED`, `AUDIT_REQUIRED`, `PROCESSING`, or `DUPLICATE` status had no direct payment action in the `/invoices` list view. Users were forced to navigate into the Auditor Review Console (`/invoices/review/[id]`), which only showed "Approve Invoice" and was inaccessible for quick batch settlement.
  - **Fix**:
    - `components/dashboard/RecentInvoicesTable.tsx`: Added `onStatusChange` prop, `markingPaidId` state, and `handleMarkPaid` handler. Added a **"Mark as Paid"** action button (with `CheckCircle` icon and emerald accent) inside the row actions dropdown menu for all non-terminal (`!PAID && !REJECTED`) invoices.
    - Prompts user confirmation (`window.confirm`) and executes `PUT /api/audit/resolve/{id}` with `{ status: "PAID", dismissed_alerts: [] }`.
    - `app/invoices/page.tsx`: Wired `onStatusChange={handleInvoiceStatusChanged}` callback to refresh the invoice queue page seamlessly when status changes.
  - **Verification**: `npx tsc --noEmit` clean. Status badge switches to emerald "Paid" and invoice moves under the "Paid" tab.

**Gaps 264–273 below**: found 2026-08-19 during real-user testing (100-invoice dataset), fixed 2026-08-20, merged from branch `fix/gaps-264-274-real-user-testing-fixes` (commit `7879dd2`). **Renumbering note, read this first**: the source branch numbered these 264–274 as one flat sequence — those numbers were already claimed on the **BE** tracker (Gaps 264–277, an unrelated batch of chat/SQL fixes assigned earlier the same day) because the branch forked before those numbers existed and was never rebased. Per this repo's convention, BE and FE trackers are numbered independently anyway, and 10 of these 11 fixes are FE-only, so they belong here starting at this tracker's own next free number (264), not the branch's original numbers. The one fix with real backend logic (subscription cancellation) is filed separately as **BE Gap 281**. The branch's own code comments and commit message still say "Gap 264" through "Gap 274" — that's a stale label in prose/comments only; these tracker entries are the authoritative numbers. None of these 10 fixes shipped with new automated test coverage (`tsc --noEmit` clean and a successful `next build` is the verification on record for all of them) — treat each as **shipped but unit/e2e-untested** unless noted otherwise.

- `[x]` **Gap 264 (FE): Google Drive connector picker had no bulk selection — every file had to be checked one at a time** — `components/connectors/FolderTreeExplorer.tsx` gains Select All / Deselect All controls. Convenience fix, not a defect fix — nothing was broken without it.

- `[x]` **Gap 265 (FE): Google Drive import aborted the entire batch if a single file failed** — `FolderTreeExplorer.tsx`'s import call is now built on `Promise.allSettled` instead of a single all-or-nothing await chain, so real per-file results come back and one bad file no longer takes down every other file in the same import run.

- `[x]` **Gap 266 (FE/BE): An expired/missing Google Drive OAuth token failed silently inside the background worker instead of surfacing to the user** — `routers/connectors.py::trigger_file_import()` now validates the stored OAuth token synchronously before handing off to the worker, so a bad token is reported immediately at the trigger call instead of the import quietly never completing.

- `[x]` **Gap 267 (FE): Ingestion log terminal yanked the user back to the bottom on every new line, even while they had scrolled up to read an earlier one** — `components/ingestion/LogTerminal.tsx` — auto-scroll now respects the user's own scroll position instead of fighting it.

- `[x]` **Gap 268 (FE): Ingestion status ledger kept showing stale "Processing" after navigating away and back, even once the files had actually finished** — `components/ingestion/StatusTable.tsx` previously trusted only future SSE events; it now fetches real current status once on mount, so state reflects reality immediately rather than waiting for an event that already happened before the page was revisited.

- `[x]` **Gap 269 (FE): Dense pages clipped/overlapped instead of scrolling at non-100% browser zoom** — `components/layout/Shell.tsx`'s main content area gained `overflow-auto`. Real usability break at non-default zoom levels, not cosmetic.

- `[ ]` **Gap 270 (FE): PDF viewer's audit bounding-box overlays were positioned against an `<iframe>`'s uncertain chrome/padding instead of the actual rendered page** — `components/audit/PdfViewerCanvas.tsx` rewritten from a native `<iframe>` to `react-pdf` canvas rendering (new dependency, `package.json`), so overlay percentages map against the real rendered page geometry. New `public/pdf.worker.min.mjs` served statically rather than bundler-resolved, after a real Terser/`import.meta` break during `next build` forced that approach. **Correctness-relevant, not just visual polish** — if the old iframe positioning was ever off, that directly misleads an auditor about where a flagged issue actually is on the page; recommend a manual check against a real annotated PDF before leaning on this for audit decisions, since no automated test covers overlay-position accuracy.

  **REVERTED 2026-08-20** — user-reported live break: this fix broke PDF display on the Auditor Review Console entirely. Investigated first before reverting: worker/pdfjs version match confirmed correct (`public/pdf.worker.min.mjs` and the installed `pdfjs-dist` both pin `5.4.296` exactly, so this wasn't the classic react-pdf version-mismatch failure), and the PDF fetch URL (`/api/invoices/{invoiceId}/pdf`, a same-origin proxy route, unchanged by this fix) looked structurally sound on inspection. Could not fully root-cause without a live browser (none available in this session) — rather than leave a broken auditor screen live while root-causing continued, reverted `PdfViewerCanvas.tsx`, `package.json`/`package-lock.json` (removed the `react-pdf` dependency), and deleted `public/pdf.worker.min.mjs`, restoring the original `<iframe>` renderer. **Back to `[ ]`, not `[x]`**: the original iframe-overlay-positioning inaccuracy this gap was opened for is real and still unfixed — reverting undid the fix, not the underlying problem. Re-attempt only with live browser verification available before merging, not `tsc`/`next build` alone (both passed on the version that broke in production).

- `[x]` **Gap 271 (FE): Auditor Review Console had no way to move between flagged invoices without leaving the screen** — gains Previous/Next navigation between `AUDIT_REQUIRED` invoices (`app/invoices/review/[id]/page.tsx`), deliberately a lighter control than the tab-strip pattern previously rejected for this screen.

- `[x]` **Gap 272 (FE): Sidebar had no way to collapse, permanently consuming screen width on smaller viewports** — `components/layout/Sidebar.tsx` gains a collapse/hide control, persisted to `localStorage`.

- `[x]` **Gap 273 (FE): Chat thread list had no way to hide, permanently consuming screen width alongside the sidebar** — `components/chat/ChatWindow.tsx` gains the same collapse pattern as Gap 272, also persisted to `localStorage`.

- `[x]` **Gap 278 (FE): Autopilot tab was entirely non-functional — Save Config, Sync Now and Sync History all failed, because Feature 13 shipped without its API proxy routes** — found and fixed 2026-08-21 during live testing of the Autopilot configuration screen (Google Drive source). Feature 13 shipped `app/ingestion/page.tsx`, `components/ingestion/AutopilotHistoryTable.tsx` and the backend `routers/autopilot.py`, but never the three Next.js Route Handlers that connect them. `lib/apiClient.ts` is same-origin only (`baseURL: "/api"`) and `next.config.js` defines no `rewrites()` — only `assetPrefix` — so `/api/autopilot/*` had no handler and no fallback: every call 404'd at Next.js and never reached FastAPI. **Root cause of the omission, not just the bug**: `feature_13_autopilot.md` never listed proxy routes as a deliverable at all — they were never specified, so they were never built. The spec has been corrected in the same change so the next connector feature does not repeat this.

  **Why it read as a configuration problem rather than a missing route**: a Next 404 returns an HTML page, so `err.response.data.detail` was always `undefined` and every call site fell back to its own hardcoded string ("Sync failed. Please check your configuration.", "Failed to load sync history."), while `loadAutopilotConfig()` swallows its failure entirely in a bare `catch` — so the tab rendered normally with default values, and the Connectors screen simultaneously showed Google Drive as **connected**, because that check calls `/api/connectors/status`, which does exist. Every visible signal pointed at the user's config; none pointed at the missing plumbing.

  **Fix**: added `app/api/autopilot/{config,sync,history}/route.ts`, each a thin `proxyJson()` pass-through matching every other route handler in this app; `config` exposes both GET and PUT.

  **Verified live** against the running local stack — and note that the *first* verification attempt was invalid, recorded here because the trap is easy to fall into again: curling the routes unauthenticated returns an HTML 404, which looks like proof of a missing route but is not. `middleware.ts` calls `auth().protect()`, and Clerk answers unauthenticated **API** requests with 404 rather than 401 so route existence isn't leaked — so *every* `/api/*` path returns HTML 404, including known-good ones. Re-tested with auth bypassed via a process-level `DISABLE_CLERK_AUTH=true` (`.env.local` untouched): all three Autopilot routes return the backend's JSON (`401 {"detail":"Missing or malformed Authorization header..."}`) identically to known-good `/api/connectors/status` and `/api/billing/usage`, while genuinely-nonexistent control paths (`/api/autopilot/doesnotexist`, `/api/totally/madeup`) still return Next's HTML 404 — confirming the new routes are registered and reaching FastAPI. `tsc --noEmit` clean. **Not yet exercised end-to-end through a real authenticated browser session against a live Google Drive folder** — that needs a signed-in user and a real Drive OAuth token; see BE Gap 288, which this fix unmasks.


