# invoice-website Test Coverage Map

Live record of what's actually automated vs. manually verified, and when. Maintained by `functional-tester` per `.claude/CONVENTIONS.md`. Not the same as a `feature_N_*.md`'s Verification Plan (stable design intent) — this is the running log of real test execution.

**Automated suite:** `e2e/` (smoke, billing, email relay, proxy mode). Proxy pass (`npm run test:e2e:proxy`) covers Multi-Zone `ENABLE_FE_PROXY` behaviour including Website Gap 184.

| Gap / Feature | Test type | Automated or manual | Last verified | Evidence |
|---|---|---|---|---|
| Website Gap 184 — Multi-Zone proxy rewrites (`/api/billing/usage`, `/api/support/*`) | E2E (proxy mode) | Automated — `e2e/billing-proxy-mode.spec.ts` (Gap 184 block) + `e2e/fe-proxy-stub.mjs` | 2026-08-19 | `npm run test:e2e:proxy` → **6/6 passed**. Stub upstream on `FE_INTERNAL_URL` :3399; website on :3201 with `ENABLE_FE_PROXY=true`. Both paths return `200` + `x-fe-stub: gap-184` — not website 404 / Clerk handshake |
| Feature 3/3.1 pricing cards + signed-out redirect | Manual (interactive Playwright MCP session) | Manual | 2026-07-31 | No committed artifact — see `feature_3_pricing_payu.md` Verification Plan for what was checked |
| Website Gaps 345/346/347/348 (Feature 7 -- HeroModeTabs, SENTINEL discrepancy sample, SageChatPreview, WorkflowRecipeSelector) | E2E | Automated -- `e2e/plug-and-play-homepage.spec.ts` (new, 11 tests) | 2026-08-30 | 11/11 passed in an isolated run. Covers: HeroModeTabs default-app-tab + tab switch swaps the mounted panel (inactive panel has 0 DOM nodes, confirming unmount-not-hide) + plug-primitive `/signup?intent=...` links; Gap 346's flagged `FRT-1048` sample -- clean default shows no warning card, selecting the flagged chip renders the discrepancy card/`. Held for Review` stage label/`Risk Score 61.2%` after the 4-step ~1.8s animation settles, switching back to a clean sample removes it again; SageChatPreview -- no answer pane before a click, a chip click reveals its answer/SQL/citations, a second chip swaps rather than appends; WorkflowRecipeSelector -- default selection produces the expected summary sentence, per-click updates, `aria-checked` exclusivity within a step; SandboxKeyCta default (flag-unset) state renders only the Gap 348 `Start Free Trial` link with zero `/api/` requests on load. |
| Website Gap 350 (SandboxKeyCta -- `NEXT_PUBLIC_SANDBOX_KEYS_ENABLED=true` state) | E2E (second Playwright config + stub backend) | Automated -- `playwright.sandbox.config.ts` + `e2e/sandbox-backend-stub.mjs` + `e2e/sandbox-key-cta-enabled.spec.ts` (new, 3 tests) | 2026-08-30 | 3/3 passed in an isolated run (`npx playwright test --config=playwright.sandbox.config.ts`). Mirrors the `playwright.proxy.config.ts` precedent: a second `next dev` on :3202 with the flag baked in, `BACKEND_API_URL` pointed at a tiny stub server playing BE Gap 340's `POST /api/v1/sandbox/keys` response shape. Covers: no fetch before the click, issuing reveals the key once with the real limits/expiry text from the (stubbed) response, `Start Free Trial` survives as a secondary link; the key is written to `localStorage` under `invoiceeq.sandbox_key.v1`; a reload restores the key from storage with **no** second issuance call. Not covered here (out of scope for a stub): the real backend's rate limiting, the claim step through a real Clerk signup (feature doc's own Verification Plan §6 Task 7.5(d) already covers the claim contract against a real backend/Postgres via a different technique), and any real Google/SendGrid-style external call (there are none in this feature). |
| *(remaining gaps — populate as functional-tester runs scenarios)* | | | | |
