# Feature 7.1: Service Flow — Outbound Auditor (pre-send validation + standing rules) — **SENTINEL Agent**

**SENTINEL** (Invoice Risk Detection) powers this flow. Extends [feature_7_audit.md](feature_7_audit.md). **Built 2026-07-29** — see Tasks below.

Reviews `NEEDS_REVIEW` outbound invoices (produced by [feature_2.1_vendor_flow_ingestion.md](feature_2.1_vendor_flow_ingestion.md)'s verify step) before they can be marked `SENT`, surfaces `SENT` invoices past their `due_date` for follow-up, and — new, folded in during design review — lets a correction be taught back as a standing rule for all future outbound invoices.

### File Coordinates (planned)
* New router: `apps/invoice-be/routers/outbound_audit.py` — `PUT /outbound-audit/resolve/{id}`.
* Model: `apps/invoice-be/models.py::ExtractionTemplate` — one new additive column.
* Existing, imported-not-edited: `apps/invoice-be/models.py::ExtractionTemplateVersion` (history/rollback), same table Trainer already uses.
* Existing, unmodified: `apps/invoice-be/routers/trainer.py`, `apps/invoice-be/routers/audit.py` — neither is touched or extended; this is a deliberately separate, smaller mechanism (see design note below).

### Functionality

**Alert types** — produced by `outbound_extraction_agent.py::verify_node` ([feature_2.1](feature_2.1_vendor_flow_ingestion.md)), same `{"type", "message"}` shape as inbound, stored in the same `Invoice.sa_alerts` column: `missing_required_field`, math/faithfulness mismatches (via the imported `verification_tools.py` checks), `duplicate_invoice_number` (scoped per `customer_name` rather than per `vendor_name`).

**Overdue detection (v1 choice):** computed at read-time — `status == "SENT" and due_date < today()` surfaces the invoice as needing follow-up in the outbound Auditor list. Not persisted via a new scheduled job; a `main_worker.py`-driven periodic sweep can be added later if a truly persisted `OVERDUE` status turns out to be needed for reporting.

**Resolve endpoint:** `PUT /outbound-audit/resolve/{id}` in a new file, not importing from `routers/audit.py` — that file's resolve logic isn't factored into reusable pieces, and extracting a shared helper would itself be an edit to shipped code. The corrections-dict → update `Invoice` → `AuditLog` diff logic is duplicated in the new file; accepted price for zero-touch, consistent with the rest of Service Flow.

**Deliberately not included:** the Gap 27-style "detect a recurring pattern → suggest a rule → deep-link to Trainer" flow. That exists inbound because Trainer's Existing/New Vendor scopes can act on the suggestion; outbound has no such scope (see below for what it gets instead).

### Outbound Global Rules — a standing correction, not a Trainer session

Design conclusion from review: full 3-scope Trainer (Global/Existing Vendor/New Vendor) doesn't fit outbound — there's no vendor variability, since every outbound invoice is the tenant's own single, consistent template. But that same fixed template can still have a systematic misread (e.g., extraction always reads the wrong address block as `customer_name`), and because it's the *same* format every time, a standing fix is worth more here than inbound's per-vendor rules are individually.

Rather than reuse Trainer's sandbox UI (which would mean editing `trainer.py`/`ScopeSelector.tsx` to add a 4th scope) or build a parallel sandbox, this is collapsed to its simplest form:

- `ExtractionTemplate` gets one additive column: `flow_direction: str` (default `"INBOUND"`). An outbound rule is a row with `flow_direction="OUTBOUND"`, `vendor_name=NULL` — Global-only, no vendor field needed.
- `outbound_extraction_agent.py::extract_node` queries this tenant's `OUTBOUND` Global template by import (same pattern as inbound's `_get_template_rules()`), injecting it into the extraction prompt. Zero edits to Trainer's existing query logic — this is a new query, in a new file.
- **No sandbox, no chat-based refinement, no re-audit fan-out.** When resolving a `NEEDS_REVIEW` outbound invoice, the correction UI shows one checkbox: *"Apply this as a standing rule for all future outbound invoices?"* Checking it writes directly to `ExtractionTemplate` (new row or version bump via the existing, imported `ExtractionTemplateVersion` table) — no "try before commit" step, because there's no vendor variability to de-risk against; it's always the same one document format.
- History/rollback reuses `ExtractionTemplateVersion` (imported, not edited).

### Explicitly out of scope
- Vendor-style (per-customer) rule scoping — rejected; there's no format variability per customer to justify it.
- Persisted `OVERDUE` status / scheduled sweep job — deferred, v1 computes it at read-time only.

### Tasks
- [x] **Task 7.1.1:** Add `flow_direction` column to `ExtractionTemplate` — done 2026-07-29, bundled into Feature 2.1's migration (`c4d5e6f7a8b9`) since that feature's `extract_node` wiring needed the outbound Global template lookup immediately. Not purely additive: the existing tenant-only partial unique index (one Global row per tenant) had to be reworked to `(tenant_id, flow_direction)` so an `OUTBOUND` Global row can coexist with the tenant's existing `INBOUND` one — every current row defaults to `INBOUND`, so this is a no-op for existing data.
- [x] **Task 7.1.2:** Built `routers/outbound_audit.py::resolve_outbound_alert()` — `PUT /outbound-audit/resolve/{id}`, corrections + `AuditLog` diff (action `RESOLVE_OUTBOUND_INVOICE`), no pattern-detection/suggestion logic. Correctable field set matches `OutboundInvoiceExtractionSchema` (`customer_name`/`invoice_number`/`invoice_date`/`due_date`/`grand_total`/`tax_amount`), not inbound's. Does not import from `routers/audit.py` — duplicated logic, per the doc's own zero-touch rationale.
- [x] **Task 7.1.3:** Added the "apply as standing rule" path via `_apply_standing_rule_direct()` — **no safety gate**, unlike inbound's Gap 62/Task 7.5 mechanism (deliberate: every outbound invoice is the tenant's own single, consistent format, so there's nothing to re-verify against). Upserts the tenant's `OUTBOUND` Global `ExtractionTemplate` row (vendor_name always `NULL`) and writes an `ExtractionTemplateVersion` entry, versioning on repeat corrections rather than creating duplicate rows.
- [x] **Task 7.1.4:** Built `routers/outbound_dashboard.py::list_outbound_invoices()` (`GET /outbound-dashboard/invoices`) with the overdue computation inline — `status=overdue` is a virtual query filter (`SENT` + `due_date < today()`), and every returned row carries a computed `is_overdue` flag. This is also Feature 8.1's Task 8.1.4 list-endpoint half (see that doc) — built here since Task 7.1.4 needed it immediately for the Auditor's own queue view; `get_outbound_dashboard_metrics()` (the aggregate-metrics half of Task 8.1.4) is still Dev 2's remaining work.
- [x] **Task 7.1.5:** Already done as part of Feature 2.1's build (2026-07-29) — `queue_worker/outbound_handlers.py::_get_outbound_global_rules()` queries the tenant's `OUTBOUND` Global `ExtractionTemplate` and passes it as `rules` into `run_outbound_extraction_agent()`, which threads it through to `extract_node`. The query lives in the handler, not inside `extract_node` itself — matching the inbound pattern exactly (`extract_node` is a pure function taking `rules` from state; `queue_worker/handlers.py` does the DB lookup there too).

### Gaps
- `[~]` **BE Gap 541: the outbound resolve takes a row lock** — built 2026-09-15. `resolve_outbound_alert()` now loads the invoice `with_for_update()`, the same change as the inbound resolve, so two corrections to one outbound invoice from different backend replicas run one after the other and the second corrects the value the first saved instead of silently overwriting it. Test: `tests/test_audit.py::test_resolve_locks_the_invoice_row` (asserts `FOR UPDATE` as Postgres compiles it — SQLite ignores locks). `[~]` until the manual Postgres wait check runs. Full record: `be_features_tracker.md` Gap 541.
- `[x]` **BE Gaps 530, 533, 534: outbound corrections are read, or refused** — built 2026-09-15. `resolve_outbound_alert()` reads corrections through the same `utils/correction_values.py` functions as the inbound resolve: amounts via `parse_money` (common formats read, `nan`/`inf` refused, negatives kept), dates via `parse_date` (never guesses day/month order), line items validated against `OutboundInvoiceLineItem`. A field outside this router's `_CORRECTABLE_FIELDS` or a value that cannot be read returns 422 `invalid_corrections` and saves nothing, instead of `200` with the correction silently skipped. Tests (outbound cases): `test_unreadable_money_correction_is_refused_and_nothing_saved`, `test_malformed_line_items_are_refused_and_nothing_saved`, `test_common_money_and_date_formats_are_saved`, `test_ambiguous_date_or_uncorrectable_field_is_refused` in `tests/test_audit.py`. Full records: `be_features_tracker.md` Gaps 530, 533, 534.
- `[x]` **BE Gap 551: Confirm Send and Mark Paid are recorded in the audit trail** — built 2026-09-15. `routers/outbound_invoices.py::confirm_send_outbound_invoice()` and `::mark_outbound_invoice_paid()` now write a `CONFIRM_SEND_OUTBOUND_INVOICE` / `MARK_PAID_OUTBOUND_INVOICE` `AuditLog` row — actor, role, previous and target status, the staff emails notified, and BE Gap 552's `auth_method` / `api_key_prefix` — in the same transaction as the status change, so the row exists exactly when the transition does; a refused transition writes nothing. These actions are not counted by the outbound AI scores (which read `RESOLVE_OUTBOUND_INVOICE` / `RESOLVE_INVOICE`, BE Gap 565). Tests: `test_confirm_send_and_mark_paid_each_write_a_trail_row`, `test_refused_outbound_transition_writes_no_trail_row` (`tests/test_outbound_ingestion.py`). Full record: `be_features_tracker.md` Gap 551.
- `[x]` **BE Gap 531: every outbound field with a column can be corrected** — built 2026-09-15 (founder ruling: all of them). `resolve_outbound_alert()`'s `_CORRECTABLE_FIELDS` now covers everything `OutboundInvoiceExtractionSchema` stores since BE Gap 467: adds `vendor_name`, `po_number`, `notes`, `currency`, `discount_amount`, `discount_percent`, `taxes`, `tax_ids`, `payment_instructions`, `references`, `addresses`, `compliance_metadata` (only `round_off` stays out — no column). Same readers and rules as inbound (`utils/correction_values.py`); list entries are checked against the outbound schema's own entry models. The outbound review page gains the new header fields and an "Additional Details" panel of list editors. A standing rule written from these corrections goes through the unchanged `_apply_standing_rule_direct()`. Tests (outbound cases): `test_every_previously_uncorrectable_field_is_saved`, `test_every_extracted_field_with_a_column_is_correctable`, `test_unreadable_new_field_correction_is_refused`, `test_clearing_a_list_field_stores_an_empty_list` in `tests/test_audit.py`. Full record: `be_features_tracker.md` Gap 531.
- `[x]` **BE Gaps 537, 538, 535: outbound alert dismissal and the arithmetic re-check** — built 2026-09-15. `resolve_outbound_alert()` uses the same `utils/alert_dismissal.py::dismiss_alerts` as inbound (one alert per dismissal, by id or type + field + message; unmatched entries returned as `unmatched_dismissals`, everything else saved) and the same `utils/correction_recheck.py` re-check after a money correction, with the tenant's OUTBOUND Global template supplying tolerance overrides. The response adds `remaining_alerts`, `unmatched_dismissals` and `raised_alerts`; the trail adds `dismissed_alerts`, `unmatched_dismissals` and `raised_alerts`, which `routers/outbound_dashboard.py`'s AI alert score now reads. Tests: the outbound cases of the Gap 537/538/535 tests in `tests/test_audit.py`. Full records: `be_features_tracker.md` Gaps 535, 537, 538.
- `[x]` **BE Gap 532: outbound required fields cannot be emptied** — built 2026-09-15. `resolve_outbound_alert()` refuses a blank correction on `customer_name`, `invoice_number`, `invoice_date` or `grand_total` with 422 and saves nothing (the outbound cases of `test_a_required_field_cannot_be_emptied_by_a_correction` / `test_an_optional_field_can_still_be_cleared`). Full record: `be_features_tracker.md` Gap 532.
- `[x]` **BE Gap 559: index for the outbound invoice list** — built 2026-09-15. Task 7.1.4's `GET /outbound-dashboard/invoices` (`tenant_id` + `flow_direction = OUTBOUND`, newest first) is served by `ix_invoice_tenant_flow_created_at` from migration `c553d559e0a1_audit_flow_indexes.py`. Full record: `be_features_tracker.md` Gap 559.

### Verification Plan
* **Automated Tests**: `uv run pytest tests/test_outbound_audit.py` — 9 tests: correction-only resolve (no rule written), tenant isolation 404, standing-rule direct write (no safety gate) with correct field-mapping text, versioning across two separate corrections (same Global row, `version` bumps, `ExtractionTemplateVersion` count matches), a dedicated cross-contamination check confirming an existing `INBOUND` Global template is untouched by an `OUTBOUND` commit, list pagination + `customer_name` filter, list correctly excludes `INBOUND` rows, `status_in` bundling multiple statuses, and the `overdue` virtual filter (confirmed both as a filter and as the `is_overdue` flag on unfiltered rows). Full BE suite run alongside — see `be_features_tracker.md` for the pass count.
* **Manual Verification** (not yet done — no live BE/DB in this pass):
  - Correct a field on a `NEEDS_REVIEW` outbound invoice without checking the standing-rule box; confirm only that invoice's data changes, no `ExtractionTemplate` row is created.
  - Repeat with the box checked; confirm a new `OUTBOUND` Global template row (or version bump) exists, and that the *next* uploaded outbound invoice's extraction reflects the rule without any further correction.
  - Confirm inbound Trainer's Global/Existing Vendor/New Vendor scopes and `ScopeSelector.tsx` are completely unaffected — no new scope appears, no existing endpoint changes behavior.
  - Confirm a `SENT` invoice with a past `due_date` appears in the outbound Auditor's follow-up list, and a `SENT` invoice with a future `due_date` does not.
