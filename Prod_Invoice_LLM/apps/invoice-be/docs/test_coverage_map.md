# invoice-be Test Coverage Map

Live record of what's actually automated vs. manually verified, and when. Maintained by `functional-tester` per `.claude/CONVENTIONS.md`. Not the same as a `feature_N_*.md`'s Verification Plan (stable design intent) — this is the running log of real test execution.

Automated suite lives at `apps/invoice-be/tests/` (pytest, see `docs/feature_13_test_benchmark_suite.md`).

| Gap / Feature | Test type | Automated or manual | Last verified | Evidence |
|---|---|---|---|---|
| Gap 131 (Google Drive / Salesforce `redirect_uri_mismatch`) | Manual, real backend, real dev Google/Salesforce OAuth client IDs, Playwright headless nav to real authorize URLs | Manual | 2026-08-12 | `test_evidence/gap131_179_oauth_dev_verification/` (2 screenshots + README + log) — CONFIRMED-FIXED in dev: `GET /connectors/auth-url/{provider}` returns the correct `.env`-configured `redirect_uri`, both Google and Salesforce accept it (real sign-in screens render, no `redirect_uri_mismatch`). Full token exchange not completable by an automated agent (same limitation as FE's Gap 96 evidence). Prod out of scope (placeholder OAuth creds, no prod RG yet) |
| BE Gaps 217–218, 221 (Trainer structured rejection, session mode, commit-behavior) | Unit/API | Automated — `tests/test_trainer.py` | 2026-08-12 | 60 passed in gap-resolution run; new: `test_commit_rejects_instruction_like_rule_end_to_end`, `test_set_session_mode_qa_test`, `test_commit_behavior_persists_chat_style` |
| BE Gap 219 (Chat conciseness + style injection) | Unit | Automated — code path in `agents/query_agent.py` | 2026-08-12 | `_CONCISENESS_INSTRUCTION` + `_get_chat_style_block()`; full `test_rag.py` suite not re-run (torch access violation on Windows in vector test) |
| BE Gap 220 (Autopilot notify emails) | Unit | Automated — `tests/test_autopilot.py::test_T19_autopilot_sends_notify_email_after_import` | 2026-08-12 | Mocks `send_email`; asserts review deep link in body when `send_approval_links=True`. Live SendGrid blocked on Gap 125 |
| BE Gap 228 (Feature 18 — alert-anchored Trainer, structured rules, preview gate) | Unit/API | Automated — `tests/test_trainer.py` (63, was 35) | 2026-08-13 | Registry contents + the 5 tolerance-excluded types (parametrized); all 4 correction shapes; exact vs `not_computable` preview impact; guardrail at preview time; stale-preview-token 409; 410s on the two removed entry points; `test_from_invoice_does_not_rerun_ocr` (asserts `_run_ocr` never called); specific-invoice-not-latest selection; `test_committed_global_rules_are_still_read_after_creation_is_removed` |
| BE Gap 228 (dual-format rule reads — **highest-risk regression**) | Unit | Automated — `tests/test_rule_schema.py` (18) | 2026-08-13 | Legacy string and structured object render identically; a legacy-only template still reaches every real read site (Chat injection, worker two-stage resolution, both extraction prompt builders); malformed stored rules never raise; `for_prompt` keeps numeric rules out of extraction prompts; audit-correction text byte-identical to pre-Feature-18 |
| BE Gap 228 (tolerance/threshold parameterization + `verify_node` wiring) | Unit | Automated — `tests/test_verification_overrides.py` (9) | 2026-08-13 | Omitting `tolerances` byte-identical to pre-Feature-18; per-alert-type (not global) widening; `verify_node` actually reads `state["rules"]` — inbound and outbound; severity/message override preserves `original_message`; legacy prose never parsed as config |
| BE Gaps 230–232 (chat style relocation, result-set snapshot, thumbs-down triage) | Unit/API | Automated — `tests/test_chat_training.py` (29) | 2026-08-13 | Snapshot harvest both paths + companion-query refusals; triage routing per reason; auto-diff match/mismatch and the numeric-vs-string coercion; extraction redirect on `pdf_agrees=false`; chat-rule preview/commit/409 and commit-without-preview 400; chat rules never touch `ExtractionTemplate`; the two prompt blocks stay separate; chat-style new table + legacy fallback + default |
| BE Gap 232 (Trainer QA turns as real `ChatMessage` rows) | Unit/API | Automated — `tests/test_trainer.py` | 2026-08-13 | `test_qa_test_turn_persists_real_chat_messages` (real rows + a thumbs-down attaches); `test_qa_test_passes_a_real_uuid_so_chat_history_actually_loads` — includes a direct repro of the latent bug (`get_chat_history("trainer-qa-<uuid>")` returns `""`, silently, always) |
| Feature 18 migration `f18a0c4b7d21` | Schema migration | **Manual, real Postgres** (throwaway DB `f18_migtest` on `invoice-postgres-local`) | 2026-08-13 | Full chain upgrade → head; seeded a real `rules["chat_style"]` row; re-upgrade confirmed it copied into `tenant_chat_settings` **with the source key still present** (non-destructive); downgrade dropped both tables and left the source data intact. Not runnable on SQLite — pre-existing migration `71d18e2c3349` uses a non-batch `ALTER`, unrelated to this work |
| Feature 18/14 — full BE regression suite, re-run independently | Unit/API | Automated — `pytest tests/ -p no:randomly` | 2026-08-17 | **552 passed, 0 failed, 5 deselected in 197.60s**, real Postgres/Redis — exact match to the developer-claimed baseline (up from 470). `test_evidence/feature18_trainer_redesign_2026-08-17/01_pytest_full_suite.log` |
| Feature 18 migration `f18a0c4b7d21` — re-verified independently | Schema migration | **Manual, real Postgres**, fresh throwaway DB `f18_migtest_ft` (different DB than the developer's `f18_migtest`) | 2026-08-17 | Full chain upgrade → head; seeded a real `chat_style` row; upgrade copied it into `tenant_chat_settings` with the source key intact; downgrade dropped both new tables/columns and left the source data untouched. Confirms the developer's claim independently. `test_evidence/feature18_trainer_redesign_2026-08-17/README.md` §2 |
| Feature 18/14 — live/manual verification, real backend + real tenant + real Azure OCR/LLM | End-to-end | **DONE** (2026-08-17, this pass) | 2026-08-17 | Permission boundary (real BE 403s + real FE gate, both roles), dual-format regression against 3 real pre-existing legacy templates (byte-identical render), committed Global-scope rule confirmed still read live, exact preview-impact math hand-verified against a real invoice, chat-lane DB isolation confirmed by direct query, wrong-data auto-diff both outcomes + both source-verdict branches against a real chat answer, all 5 source-text types 400 live, QA-mode multi-turn memory confirmed with a real recalled value. **Found BE Gap 235** (missed-alert LLM drafting fails against the real deployed model). `test_evidence/feature18_trainer_redesign_2026-08-17/README.md` |
| BE Gap 235 (missed-alert LLM drafting fails at `max_tokens=512` against the real deployed reasoning model) | Live API + direct LLM call | Manual, real Azure OpenAI (`gpt-5-mini`) | 2026-08-17 | Reproduced 3x (two live 502s via the real endpoint, one direct call at `max_tokens=1024` still failing, success only at 4096). `test_evidence/feature18_trainer_redesign_2026-08-17/02_gap235_llm_token_budget_failure_server_log.log` |
| BE Gap 237 step 1 (live SQL-drop repro against a seeded multi-invoice tenant) | Live API, real backend + real Postgres + real LLM | Manual | 2026-08-17 | Core defect reproduces live (2 of 7 runs: a same-session narrowing follow-up silently dropped a real invoice). Tracker hypothesis (item-only match specifically dropped) is **disconfirmed** — both reproductions dropped the `vendor_name` branch instead, `items` survived intact both times; 4 of 7 runs instead skipped SQL generation entirely for the follow-up (a distinct, undocumented failure mode); the already-shipped step-3 hedge never fired in either reproduction (its trigger condition compares against the prior turn's *total* row count, not a sub-count mentioned in its prose).  `test_evidence/gap237_sql_repro_2026-08-17/README.md` (raw generated SQL for both turns, all 7 runs) |
| BE Gap 242 test-data reseed (Blue Ridge Logistics items/subtotal, corrupted by an earlier Gap 234 verification run) | Data prep, direct DB correction | Manual | 2026-08-17 | Restored realistic freight/logistics line items (4 items summing to $2225.00) and corrected `subtotal` to match (reverse-solved from the invoice's untouched `tax_amount`/`grand_total`, which reconciles exactly); `grand_total`/`tax_amount`/`tags` left untouched. Unblocks honest verification of the upcoming BE Gap 241/242 fix against this vendor.  `test_evidence/gap242_blue_ridge_reseed_2026-08-17/README.md` (before/after JSON) |
| BE Gap 6d (jsonb-cast SQL fix live verification) | Live API, real backend x2 + real Postgres + real LLM | Manual | 2026-08-18 | Before/after: 3/8 -> 8/8 correct across the repro harness; pre-fix instance aborted 13/16 chat messages on `lower(jsonb) does not exist`, fixed instance 0/32. `test_evidence/gap237_jsonb_cast_2026-08-18/README.md` |
| Feature 19 / Website Feature 5 (Contact Us public inquiry, `POST /api/v1/support/contact`) — **unmerged branch `feature/contact-us-and-support-tickets` @ `fc48ef0`** | Unit/API | Automated — `tests/test_support.py::TestContactEndpoint` (7 test functions + 3-way `missing_field` parametrize = 10 collected cases) | 2026-08-18 (coverage documented this pass; not independently re-run) | Valid payload -> 201, `success=True`, `ticket_number` returned; number matches `INQ-YYYY-XXXX`; row persisted in `SupportTicket` with `source=WEBSITE_CONTACT`, correct `user_email`/`category`; 422 on missing `name`, missing `email`, missing `message` (parametrized), malformed email, message >5000 chars; unrecognised `category`/`urgency` silently coerced to `GENERAL`/`NORMAL` rather than rejected. Not covered: unauthenticated-route-is-actually-public assertion (no negative auth-required test), rate limiting/spam protection on this public endpoint (see BE Gap 249), XSS/HTML-injection in `message`/`name` fields (see BE Gap 250) |
| Feature 19 (Authenticated direct support ticket, `POST /api/v1/support/ticket`) — same branch | Unit/API | Automated — `tests/test_support.py::TestAppTicketEndpoint` (4 cases) | 2026-08-18 (documented) | Valid payload -> 201, `success=True`, `ticket_number` matches `TICK-YYYY-XXXX`, `source=DIRECT_TICKET`; `chat_transcript` list persisted verbatim in the DB column; `source=HELP_CHATBOT` accepted and stored correctly. Not covered: unauthenticated request rejected (401/403), tenant isolation (ticket created under tenant A not visible/writable by tenant B), missing `subject`/`description` validation errors |
| Feature 19 (List own tickets, `GET /api/v1/support/tickets`) — same branch | Unit/API | Automated — `tests/test_support.py::TestListTicketsEndpoint` (2 cases) | 2026-08-18 (documented) | Empty tenant returns `tickets: []`; after creating 2 tickets, list contains them with `ticket_number`/`subject`/`status` fields present. Not covered: cross-tenant isolation (tickets from a different tenant excluded), pagination/filtering, sort order |
| Feature 19 (Support ticket email dispatch) — same branch | Unit/API | Automated — `tests/test_support.py::TestEmailDispatch` (2 cases) | 2026-08-18 (documented) | `dispatch_support_ticket_email` called exactly once per successful ticket creation; a `RuntimeError` raised from email dispatch (simulated SendGrid outage) is caught by the router and does NOT prevent the 201 response — ticket row still persisted, response reports `email_dispatched=False`. Not covered: actual SendGrid integration/delivery (mocked in every test, by design) |
| Feature 19 / Website Feature 5 (AI Support Assistant chat, `POST /api/v1/support/chat`) — same branch | Unit/API | Automated — `tests/test_support.py::TestSupportChatEndpoint` (4 cases; class docstring says "Tests 20-24" but only 4 test functions/cases exist, i.e. tests 20-23 -- confirmed by reading the file, not taken on faith) | 2026-08-18 (documented) | Documentation-style query ("train a new vendor format") answered with `suggest_escalation=False`, `escalation_context=None`; error-keyword query ("504 gateway timeout") returns `suggest_escalation=True` with `escalation_context.category=TECHNICAL_SUPPORT`, `priority=URGENT`; explicit human-help request ("talk to an agent") also triggers escalation; blank/whitespace-only message -> 422. Not covered: unauthenticated access, malformed/oversized message, chat history/multi-turn context across calls |
| BE Gap 250 (HTML escaping in support email templates) | Unit/API | Automated — `tests/test_support.py::TestSecurityHardening` (tests 25–28) | 2026-08-18 | HTML-injection in `name`, `description`, `company` → all strictly escaped with `html.escape` in both staff alert and user receipt email HTML; `&lt;`/`&gt;` verified |
| BE Gap 250 (Input length validation) | Unit/API | Automated — `tests/test_support.py::TestSecurityHardening` (tests 29–30) | 2026-08-18 | `name` > 255 → 422; `company` > 255 → 422 |
| BE Gap 249 (Rate limiting & honeypot on public contact form) | Unit/API | Automated — `tests/test_support.py::TestSecurityHardening` (test 31) | 2026-08-18 | 6th rapid contact submission returns `429 Too Many Requests` with `Retry-After: 300` header |
| BE Gap 249 pass 2 — rate-limit key cannot be spoofed | Unit/API | Automated — `tests/test_support.py::TestClientIpResolution` (13 cases) + `::TestRateLimitBypassResistance` (3 cases) | 2026-08-18 | `_parse_ip` accepts real v4/v6 (incl. `host:port` and `[v6]:port`) and rejects junk (`not-an-ip`, `999.999.999.999`, `1.2.3.4.5`, HTML, 5000-char string, empty/None) so junk cannot become a rate-limit key; `_get_client_ip` takes the **rightmost** valid `X-Forwarded-For` entry, skips junk right-to-left, prefers `X-Client-IP` over XFF, ignores `X-Real-IP`, falls back to socket peer then `"unknown"`; `X-Azure-ClientIP` ignored when `FRONT_DOOR_ID` is unset or `X-Azure-FDID` mismatches, honoured only on exact match. End-to-end: 6 submissions with a rotating spoofed *leftmost* XFF but a constant rightmost → 429 (distinct emails, so only the IP dimension can trigger it); same via forged `X-Azure-*`; email dimension still limits across genuinely distinct IPs. **Confirmed to fail against the pre-fix code** (201 instead of 429), not merely to pass against the new |
| BE Gap 249 pass 2 — limiter state is bounded and survives Redis loss | Unit/API | Automated — `tests/test_support.py::TestRateLimiterStateBounds` (5 cases) | 2026-08-18 | In-process fallback enforces the 5/window limit; prunes out-of-window keys on every check; caps total tracked keys at `_MEMORY_MAX_TRACKED_KEYS` (monkeypatched to 50, driven with 400 distinct key pairs); a rejected check does not create a permanent key (the old `defaultdict` did); a mid-flight Redis failure degrades to the in-process window returning 201 rather than 500, and still returns 429 at the threshold — i.e. does not fail open. Redis path itself exercised implicitly (local Redis reachable during the run; keys carry a 300s TTL, verified out-of-band). Not covered: genuine multi-replica sharing (needs >1 process) |
| BE Gap 249 pass 2 — contact subject cannot overflow its column | Unit/API | Automated — `tests/test_support.py::TestSubjectLengthOverflow` (3 cases) | 2026-08-18 | A 255-char `name` (legal per its own validator) with the longest category prefix, and the tight 256-char boundary case, both yield a stored `subject` ≤ 255 — the pre-fix code produced 296 and 256 and would have raised Postgres `StringDataRightTruncation` → uncaught 500. A normal-length subject is verified *not* truncated. Assertions are on the constructed/stored string's length, deliberately: the suite runs on in-memory SQLite which does not enforce `max_length`, so a status-code-only assertion would pass with the bug present. **Confirmed to fail against the pre-fix code.** Not covered: execution against a real Postgres |
| BE Gap 251 (Ticket number keyspace & graceful 503 exhaustion) | Unit/API | Automated — `tests/test_support.py::TestSecurityHardening` (tests 32–33) | 2026-08-18 | Ticket number suffix matches 8-hex uppercase format (`INQ-YYYY-XXXXXXXX`); forced collision retry exhaustion returns `503 Service Unavailable` instead of 500 |
| BE Gap 133 (Orphan tenant prevention in provision_tenant) | Unit/API | Automated — `tests/test_auth.py` (3 tests) | 2026-08-18 | Existing user with `tenant_id` attempting second provision → 409 Conflict, 0 new tenants; email collision → 409 Conflict, 0 new tenants; user with `tenant_id=None` successfully linked |
| BE Gap 133 sub-item (1) — Postgres concurrency / advisory-lock TOCTOU | Real-engine execution | Automated — `tests/test_auth.py::test_provision_concurrent_same_org_id_creates_one_tenant_on_postgres` | 2026-08-19 | Two threaded `provision_tenant()` calls for the same `clerk_org_id` against Docker Postgres (`localhost:5433`) → exactly **one** tenant row, same `tenant_id` returned, `{is_new: True, False}` split. Complements the mocked lock-statement test `test_provision_concurrency_locking`. Adoption-path TOCTOU under concurrent domain adoption not covered |
| BE Gap 133 sub-item (2) + original defect — live local signup | Manual live | Clerk `great-baboon-11` + local website/FE/BE + Docker Postgres | 2026-08-19 | New org signup `POST /auth/provision` **200**; JWT `[jwt-diag]` `org_id` + `email_present=True`; `users.email=madusudhanmishra@olipii.com` bound to tenant `test 413 co.` with matching `clerk_org_id`; FE `/dashboard` loaded. Gap **closed**. Not covered: Azure deploy; independent 3c security re-review; JWT `iat` leeway 401s on later `/auth/me` |
| BE Gap 253 (Chat SQL rule 6d — line-item extraction, dialect-conditioned) — **SQLite half** | Unit + real-engine execution | Automated — `tests/test_chat_sql_quality.py` (12 cases: 38 passed / 6 skipped for the file) | 2026-08-19 | The behavioural cases **execute** the taught SQL, pulled verbatim out of `_LINE_ITEM_RULE_SQLITE` by `_taught_sql()` rather than re-typed, against the fixture's real SQLite engine. Gap 253's own reported data seeded (Cloud Storage 765.36 + Training & Onboarding 29,302.94 inside a 35,480.59 grand total) plus a NULL-`items` row and a not-JSON-at-all row: returns **29,302.94 only**, Cloud Storage absent, grand total never surfaced, and no `malformed JSON` abort from the junk rows (the Fix-2 `CASE` guard under test — hand-confirmed that an unguarded `json_each` aborts the whole query on SQLite 3.50.4, and that a `LEFT JOIN ... ON <guard>` does **not** prevent it). Multi-currency aggregate grouped, not summed (`{"USD": 29502.94, "INR": 50.0}`). Gap 231 id-snapshot non-empty for a 6d query (`test_line_item_query_still_yields_a_result_set_snapshot`), with the whitelist proven to be by join *shape* (`test_harvester_still_refuses_a_join_to_a_real_table`). Prompt-level: dialect conditioning (no Postgres-only token reaches a SQLite bind, and an unreadable bind falls back to the Postgres rule), rule-7 currency selection, rule-9 FROM exception, and the summary format carrying no hardcoded `$`. **Not covered: anything live** — mocked LLM throughout, per this file's standing caveat |
| BE Gap 253 — **Postgres half** | Real-engine execution | Automated — `tests/test_chat_sql_quality.py::test_taught_line_item_sql_runs_on_postgres` (2 parametrized cases) + `test_recommended_cast_form_runs_on_postgres` (4 cases) | 2026-08-19 | **Passed** against Docker Postgres on `localhost:5433` (`invoice-postgres-local` via `docker-compose.yml`, `DATABASE_URL=postgresql://postgres:localpassword123@localhost:5433/invoice_db`). Rule 6d's Postgres form (`jsonb_array_elements` / `LATERAL` / `::numeric`) now has executed evidence on the production engine — **BE Gap 255 closed** |
| BE Gap 255 (rule 6d Postgres execution) | Real-engine execution | Automated — same as Postgres half row above | 2026-08-19 | Closed: `uv run pytest tests/test_chat_sql_quality.py -k postgres -v` → **8 passed, 0 skipped**. Both `test_taught_line_item_sql_runs_on_postgres` parametrized cases execute the taught SQL verbatim and return without syntax/runtime errors on Postgres |
| BE Gap 254 (Support assistant matcher, topic set, low-confidence path) | Unit/API | Automated — `tests/test_support.py::TestSupportChatEndpoint` (**15 passed** in class; **70 passed** for the file) | 2026-08-19 | Word-boundary matching kills the `"id"`-in-"confidence" / `"pro"`-in-"processing" collisions; one probe per added topic (`ingestion_upload`, `invoice_statuses`, `dashboard_analytics`, `user_management`, `security_retention`); `test_generic_keyword_does_not_beat_a_more_specific_topic` verifies the gap report's exact "Is my data encrypted at rest?" wording now returns the security article, not the CSV-export guide; `low_confidence` is True only on a real miss and never on an answered or escalated turn. **Not covered:** any live Help Center click-through — the FE neutral card is `tsc --noEmit`-clean but has not been seen rendered |
| BE Gap 256 (Support assistant anaphoric follow-up via `last_topic_id`) | Unit/API | Automated — `tests/test_support.py::TestSupportChatEndpoint` (4 new cases) | 2026-08-19 | `SupportChatResponse.topic_id` echoes the matched topic; `SupportChatRequest.last_topic_id` carries it forward from `SupportChatWindow.tsx`; `evaluate_support_query()` resolves phrases like "how do I do that?" against the prior topic via `_FOLLOW_UP_PHRASES` / `_is_anaphoric_follow_up()`. `history` accepted but unread — only `last_topic_id` resolves follow-ups. Cases: `test_matched_topic_echoes_topic_id`, `test_follow_up_resolves_via_last_topic_id`, `test_follow_up_without_last_topic_id_still_misses`, `test_invalid_last_topic_id_is_ignored`, `test_history_is_accepted_but_does_not_resolve_follow_ups`. **Not covered:** live Help Center follow-up click-through |
| BE Gap 258 (dev post-deploy health gate) | CI/CD workflow | Code review — `.github/workflows/deploy-dev.yml` + `_deploy-service.yml` | 2026-08-19 | Per-service: poll latest revision `healthState`/`runningState` for up to 4 min after `az containerapp update`, fail on `ActivationFailed`/`Failed`/timeout. Aggregate: new `verify-deployment` job curls website ingress + `GET /health/readiness` + asserts all four dev Container Apps traffic revision is `Healthy`/`Running`. **Not yet exercised on a live Azure deploy** — closes on code; next `deploy-dev.yml` run is the live proof |
| BE Gap 257 (DLQ poison alert cannot fire — bogus QueueName dimension) | IaC compile | Automated — `az bicep build --file infra/09-monitoring.bicep` | 2026-08-19 | Metric alert replaced with `scheduledQueryRules` KQL `ContainerAppConsoleLogs_CL \| where Log_s has "POISON MESSAGE ISOLATED"`. Build compiles (pre-existing BCP334 only). **Not covered:** Azure Stage 9 deploy; live poison-message alert fire |
| BE Gap 254 / Fix 7 — error triggers not shadowed by KB keywords | Unit/API, standing screen | Automated — `tests/test_support.py::test_error_triggers_are_not_shadowed_by_kb_keywords` | 2026-08-19 | 10 phrasings across all 3 `ERROR_TRIGGERS` must each reach the error path with the right `error_code`. Catches the **pre-existing, live** bug this pass fixed as a bonus: `billing`'s `"payu"`/`"checkout"` keywords made `ERR_PAYU_BILLING_FAILURE` unreachable, because KB topics are matched before `ERROR_TRIGGERS` and return early — "payu error on checkout", "my payment failed at checkout" and "checkout crash" all returned the pricing article. **Confirmed failing against the pre-fix keyword lists** (reproduced by direct matcher probe before the change), not merely passing after. The pre-existing `test_error_keyword_triggers_escalation` ("504 gateway timeout during batch sync") still passes — every new Gap 254 keyword was screened against all trigger phrasings before the topic list was finalised |
| Full BE suite after Gaps 253/254 corrections | Unit/API | Automated — `.venv/Scripts/python.exe -m pytest tests/ -q` | 2026-08-19 | **691 passed, 2 failed, 6 skipped, 5 deselected in 297s.** Both failures are `tests/test_connectors.py::test_salesforce_pkce_flow` / `::test_oauth_callback_salesforce_verification_failure`, raising `redis.exceptions.ConnectionError` against `localhost:6379` — no local Redis in this environment. **Confirmed identical on a stashed clean tree**, so environmental and pre-existing, not caused by this pass |
| BE Gap 334 (Salesforce connector removal — Postgres checkpoint) | Unit/API, real-engine execution | Automated — `tests/test_connectors.py::test_connectors_status_google_drive_only_on_postgres` + `tests/test_autopilot.py::test_run_sync_google_drive_translation_and_unsupported_source_type_on_postgres` (both new, added this pass, mirroring the repo's existing `*_on_postgres` pattern) | 2026-08-28 | Neither target file had any Postgres-pointed test before this pass (both hardcode `sqlite:///:memory:` with an autouse dependency override, confirmed by reading, not guessed). `pytest tests/test_connectors.py tests/test_autopilot.py -v` against Docker Postgres (`invoice-postgres-local`, `localhost:5433`) → **38 passed** (36 original SQLite-backed + 2 new). Postgres row cleanup independently verified via `docker exec ... psql` — zero orphaned rows. Live unstubbed backend process (real Postgres, real Google OAuth creds, no dependency override) confirms `/connectors/status` returns only `google_drive`, `/connectors/auth-url/google_drive` generates a genuine `accounts.google.com` URL, `/connectors/auth-url/salesforce` now 400s. `test_evidence/gap334_salesforce_removal_postgres_checkpoint_2026-08-28/README.md` |
| Full BE suite re-run after Gap 334 Salesforce removal | Unit/API | Automated — `pytest tests/ -p no:randomly -q --ignore=tests/us/run_chat_live_test.py --ignore=tests/realworld_tenant/run_chat_live_test.py` | 2026-08-28 | **1498 passed, 9 failed, 1 skipped, 5 deselected in 450.58s.** The `--ignore`s work around a pre-existing pytest module-name collision between two untracked scratch scripts, unrelated to this session. All 9 failures independently root-caused as pre-existing/unrelated: 8x `test_ops_recommendation.py` `KeyError: 'tileSettings'` (already-uncommitted Gap 325/326 workbook-JSON restructuring, present in `git status` before this session started) + 1x `test_rag.py` `post_chat_message() missing background_tasks` (routers/chat.py last changed by the pre-session Gap 280 commits, not in this session's diff). `test_evidence/gap334_salesforce_removal_postgres_checkpoint_2026-08-28/README.md` |
| Feature 25 (Plug & Play Workflows, Gaps 335-352) -- combined 17-file targeted run | Unit/API, real Postgres | Automated -- `.venv/Scripts/python.exe -m pytest tests/test_api_keys.py tests/test_rbac.py tests/test_audit.py tests/test_outbound_ingestion.py tests/test_outbound_audit.py tests/test_workflow_config.py tests/test_workflow_email_summary.py tests/test_workflow_drive_archive.py tests/test_auth.py tests/test_billing_free_quota.py tests/test_autopilot.py tests/test_connectors.py tests/test_sandbox_keys.py tests/test_widget_token.py tests/test_chat_queue.py tests/test_settings.py tests/test_staff_notify.py -q -p no:randomly` (the task-requested `test_outbound_invoices.py` does not exist in this repo -- substituted `test_outbound_ingestion.py`/`test_outbound_audit.py`/`test_staff_notify.py`, the real files that cover the outbound confirm-send/mark-paid gate) | 2026-08-30 | **454 passed, 1 failed** in 31.30s. The 1 failure (`test_sandbox_keys.py::test_concurrent_chat_charges_cannot_exceed_the_allowance_on_postgres`) is a **real, reproducible ~20% flake** in Gap 352's own concurrency-fix test -- re-run 5x isolated, failed 1/5. Root cause read from `services/sandbox.py::charge_sandbox_chat_message()`: the `used` value returned comes from `db_session.refresh(sandbox)` called *after* `db_session.commit()`, i.e. after the `SELECT ... FOR UPDATE` lock is already released, so two racing writers can each read back the *other's* newer counter value and report a duplicate `used` position. The actual security bound (total turns granted, persisted counter) is unaffected and correct in every run observed -- only the per-caller `used` sequence number in the response is unreliable under concurrency. Not fixed (pre-existing app code, not this pass's spec). `test_evidence/feature25_final_gate_2026-08-30/gap352_used_value_race_repro.log` |
| Full BE suite (`pytest tests/`) after Feature 25 (Gaps 335-352) | Unit/API, real Postgres | Automated -- `pytest tests/ --ignore=tests/us --ignore=tests/realworld_tenant -q -p no:randomly` (same pre-existing module-basename-collision workaround this map already used on 2026-08-28, still present) | 2026-08-30 | **1734 passed, 10 failed, 1 skipped, 5 deselected** in 63.65s. All 10 root-caused via `git stash` on the whole uncommitted `apps/invoice-be` diff (verified clean, restored byte-for-byte after): **9 are pre-existing/unrelated** -- 8x `test_ops_recommendation.py` band-threshold failures + 1x `test_rag.py::test_process_crash_during_agent_leaves_no_orphan_user_message` (`background_tasks` TypeError), both independently confirmed identical on clean HEAD and matching this same map's 2026-08-28 entry for the same two failure classes. **1 is a GENUINE regression from this feature**: `test_rag.py::test_session_lifecycle_and_tenant_isolation` passes on clean HEAD in isolation, fails on current code in isolation (`assert 200 == 403`). Root cause: BE Gap 335 rewired chat-session routes from `Depends(get_tenant_context)` to `Depends(get_tenant_or_api_key_context)`; this test's `app.dependency_overrides[get_tenant_context] = ...` (used to simulate a foreign tenant) no longer targets the route's real dependency, so the override is silently inert and the isolation branch is never exercised. **Confirmed NOT a live security hole** -- `routers/chat.py:314`'s `if chat_session.tenant_id != tenant_context.tenant_id: raise 403` is present and untouched; only this test's ability to simulate cross-tenant access broke. `test_rag.py` was not in Gap 335's own modified-file list or any of its narrow verification runs, so this is exactly what the full-suite checkpoint convention exists to catch. **Not fixed** -- reported to senior-dev; the fix is a one-line override-target change in a pre-existing test file, not this pass's own spec. `test_evidence/feature25_final_gate_2026-08-30/02_full_suite_run_summary.log` |
| *(remaining gaps — populate as functional-tester runs scenarios)* | | | | |
| Gaps 364/365/366 (chat queue limiter, live-progress SSE, chat attachments) — Phase 3 real-Postgres verification pass, RE-RUN after Docker engine fixed | Real Postgres + real Redis (per hard rule 2) | **RUN — T1/T3 VERIFIED, T2 PARTIAL, T4 flag stays False, T5 56/56 (real infra up)** | 2026-09-01 | `test_evidence/gap364_365_366_chat_attachments_phase3_2026-09-01/README.md`. T1 (Gap 364, real Postgres+Redis): 4th-job real-429 + Retry-After:5 + zero orphan ChatMessage row, real slot-leak rollback, real chat_inflight returns to 0 on completion/failure — all VERIFIED. T2 (Gap 365): real Redis per-session lock VERIFIED (serialises same-session, parallel cross-session); real SSE transcript over the actual `/chat/jobs/{id}/stream` endpoint captured only 5 distinct steps with 0 repair-attempt events (flip criterion 1 FAILS on real timing — a likely poll-vs-pubsub race in `stream_chat_job()`, flagged back not fixed); Gap 237 route-override check did not reproduce (probable test-input mistake, not re-run in the time box, scored FAIL not assumed PASS). T3 (Gap 366): full upload→Tier1/Tier2 match→confirm→compare flow VERIFIED against real Postgres + real Azurite blob storage, including D2 (no Invoice row), D3 (no quota movement), currency-mismatch hard stop, and tenant isolation on all 3 endpoints (all real HTTP calls, 2 real tenants). T4: flip criteria 2 and 4 PASS, criterion 1 FAILS, criterion 3 inconclusive, criterion 5 not attempted — **`ENABLE_ASYNC_CHAT_QUEUE` stays `False`**, all 5 required and this run did not clear that bar. T5: `tests/test_chat_queue.py tests/test_chat_progress.py tests/test_chat_attachments.py` → 56 passed with real Postgres/Redis containers up (only `test_job_isolation_on_postgres` actually executes against Postgres by its own fixture design; the other 55 remain SQLite/mocked-Redis regardless of containers running). Also discovered and documented: `ALLOW_MOCK_AUTH` `test_<uuid>` tokens are not tenant-isolatable across multiple calls in one process against a persistent Postgres (test-harness footgun, not a Gap 364/365/366 defect) — worked around via `app.dependency_overrides` for this pass's multi-tenant HTTP checks. |
| Feature 27 Task F (generic-extraction doc-type fixture sourcing, section 7 of feature_27_generic_extraction.md) -- 1-hour time-boxed slice, IN PROGRESS | Fixture creation (no consuming test code -- classifier module services/document_type_classifier.py does not exist yet, G2 not started) | **IN PROGRESS -- NOT COMPLETE.** 5 of the roughly 30-40 target files produced this pass | 2026-09-02 | `tests/fixtures/doc_types/MANIFEST.md` (per-file doc_type/family/region/real-or-synthetic/provenance/expected-evidence-phrase) plus `tests/fixtures/doc_types/{delivery_note,proforma_invoice}/ground_truth_line_items.md`. Covers 2 of 10 section 7 doc types: DELIVERY_NOTE (India "Delivery Challan" + Germany "Lieferschein", both required per section 7, both with NO prices printed -- the quantity-rubric case) and PROFORMA_INVOICE (India + EU + US, all 3 regions per section 7 table -- this type had zero fixtures anywhere in the repo before this pass). All 5 are synthetic, declared as such, generated via `tests/fixtures/doc_types/_generate_fixtures.py` (reportlab, standalone -- does not reuse `tests/e2e/pdf_builder.py`, which hardcodes a literal INVOICE title). Rendered text sanity-checked via PyMuPDF extraction on 2 of the 5 files; not run through the actual OCR/classifier pipeline since G2 does not exist yet. **7 of 10 doc types have ZERO fixtures and are explicitly marked NOT YET SOURCED in MANIFEST.md**: QUOTATION, PURCHASE_ORDER, CONTRACT (incl. the required no-grand-total case), GRN, CREDIT_NOTE, DEBIT_NOTE, OTHER (bill-of-lading + e-way-bill pair for the E5 routing proof, T-C-4). Classifier confidence per fixture is NOT YET RECORDED (N2 requirement) -- deferred until G2 ships, recording it now would be a fabricated number. Per section 7: "Nothing in section 9's verification plan may be marked passing before F is complete for the cells that verification touches" -- **T-C-1 through T-C-4, T-R-1, T-R-7 and the rest of section 9 must NOT be marked passing on this manifest alone.** Tasklist: `.claude/tasklists/functional-tester-feature27-taskF-doctype-fixtures.md`. |
| Feature 27 Task F, Dispatch B (generic-extraction doc-type fixture coverage, section 7 of feature_27_generic_extraction.md) -- brings coverage from 2/10 to 10/10 doc types | Fixture creation + real classifier measurement | **DONE for this dispatch's scope (10/10 doc types, 1 defensible sample each; NOT the full 30-40 file regional matrix -- that is the taxonomy wave's job later)** | 2026-09-02 | 11 new fixtures added to the prior pass's 5 (16 total): PURCHASE_ORDER (India, US), CONTRACT (India "Rate Contract" + Germany "Rahmenvertrag", **both with no grand total** -- the required T-R-2 case, satisfied twice), QUOTATION (India), GRN (India, synthetic per E4's explicit allowance), OTHER (India bill of lading + India e-way bill quoting a real tax-invoice number in its body -- the hard T-C-4 case), CREDIT_NOTE (India), DEBIT_NOTE (India), DELIVERY_NOTE (added US "Packing Slip", completing the 3-region proof). Generated via the same standalone `tests/fixtures/doc_types/_generate_fixtures.py` (reportlab; does not reuse `tests/e2e/pdf_builder.py`'s hardcoded INVOICE title). **Ran the real, shipped `classify_doc_type()` from `services/document_type_classifier.py` (G2/Gap 369, confirmed landed -- corrects the prior pass's stale "does not exist yet" claim) over all 16 fixtures**, via PyMuPDF text extraction feeding the same `content` string `queue_worker/handlers.py` would produce. **16/16 classified correctly.** 13/16 resolved via the deterministic title-band pass (confidence 1.0, no LLM call); 3/16 (the German-titled CONTRACT and both OTHER fixtures) required the real LLM fallback against the live deployed Azure OpenAI `gpt-5-mini` (not `MockInvoiceLLM` — real `.env` credentials present), scoring 0.90/0.92/0.95 — all well above the `0.6` threshold with zero real data points observed in the 0.6-0.90 range. **N2's confidence-distribution ask is now satisfied with real (not fabricated) numbers**; functional-tester's recommendation (not a change made) is to raise `DOC_TYPE_CONFIDENCE_THRESHOLD` from 0.6 to roughly 0.75-0.8, on the reasoning that every real confident answer clustered at 0.90+ so a higher floor would not have demoted any observed fixture, while giving more margin against a genuinely low-confidence guess this small fixture set did not happen to produce — flagged explicitly as directional, not a final calibration, since it rests on only 3 real LLM-path data points. Full per-fixture confidence table, per-file ground truth (expected doc_type/family/evidence phrase/line items), and the full reasoning are in `tests/fixtures/doc_types/MANIFEST.md` (rewritten this pass) plus new/extended `ground_truth_line_items.md` files under `tests/fixtures/doc_types/{purchase_order,contract,quotation,grn,other,credit_note,debit_note,delivery_note}/`. **Still not covered by this dispatch, deliberately out of scope**: the full regional matrix (EU/US QUOTATION, EU/US CREDIT_NOTE/DEBIT_NOTE, India E-Invoice IRN+QR / Bill of Supply sub-cases, additional CONTRACT/PURCHASE_ORDER regions), erroneous/seeded-defect variants for any new type, and running these fixtures through the full extraction+verification pipeline (task V, still blocked on the rest of F27's ledger per `active-work.md`). Section 7's own gate still applies: **T-C-1 through T-C-4 and T-R-1/T-R-2/T-R-7 must not be marked passing on fixture existence alone** — that requires task V against real Postgres. Tasklist: `.claude/tasklists/functional-tester-f27-dispatchB-fixture-coverage.md`. |
