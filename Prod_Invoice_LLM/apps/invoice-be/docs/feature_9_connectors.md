# Feature 9: Third-Party Connectors & Ingestion

Integrate secure API credentials exchanges and import pipelines to retrieve files from Google Drive.

> ### ⚠️ Salesforce — removed 2026-08-28, see Gap 334 (BE) / Gap 322 (FE)
>
> **Google Drive is now the only connector.** Everything below that refers to Salesforce is
> historical and struck through — kept, not deleted, because it is the institutional record of
> why the plumbing (PKCE, `instance_url`, the per-org API base) existed at all. Do not treat any
> struck-through text as current design.
>
> **Why it was removed — two independent causes, either one sufficient:**
>
> 1. **Cross-org OAuth is structurally blocked.** The OAuth app is a Salesforce **External Client
>    App** with **Distribution State = Local**, which by design only permits authorization from the
>    org that created it. Confirmed live, not inferred — a Log Analytics query against
>    `ca-invoice-be-dev` returned Salesforce's own error:
>    `OAUTH_AUTHORIZATION_BLOCKED — Cross-org OAuth flows are not supported for this external
>    client app`. A multi-tenant connector needs precisely what this blocks, and no setting
>    changes it. The fallback — a classic Connected App, which is what Task 9.5 / Gap 98 below
>    were originally built against — is also unavailable: Salesforce stopped allowing new classic
>    Connected Apps as of **Spring '26** without a support exception.
> 2. **Wrong data model for invoices.** This connector browsed Salesforce **Libraries**
>    (`ContentWorkspace`) — see Gap 262. Real-world invoices in Salesforce orgs are attached to
>    **Account/Opportunity** records, not filed in Libraries; Salesforce is a CRM, not a document
>    or billing system for most orgs. Even a working connection would have shown a near-empty
>    folder tree. This is the more decisive cause: fixing the OAuth block would not have produced
>    a feature worth shipping.
>
> **What was actually deleted from the code:** the 4 Salesforce functions and
> `SALESFORCE_API_VERSION` in `utils/connector_files.py`; `SALESFORCE_TOKEN_URL`,
> `generate_pkce_pair()`, and the Salesforce credential/refresh arms in `utils/connector_oauth.py`;
> the Salesforce branches in `routers/connectors.py` (all 5 handlers), `queue_worker/handlers.py`,
> `services/autopilot_sync.py`, and `routers/autopilot.py`; the 3 `SALESFORCE_*` settings in
> `config.py`; the Salesforce keyword routing and two "Salesforce is supported" claims in the live
> `agents/support_agent.py`.
>
> **What was deliberately KEPT, and why:** `TenantConnection.instance_url` remains as a dead
> nullable column and migration **`a7b8c9d0e1f2` was not edited or deleted** — it sits mid-chain,
> so removing either would break `alembic upgrade` for any database that already ran it.
> Everything under `infra/` (the `SALESFORCE-CLIENT-SECRET` Key Vault secret, container env
> wiring, params files) is out of scope and untouched: an unused env var is harmless, a deleted
> Key Vault secret is not trivially reversible. Existing `TenantConnection` /
> `TenantAutopilotConfig` rows are data, not code, and were not touched.

### File Coordinates
* Router: [apps/invoice-be/routers/connectors.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/routers/connectors.py) → `get_connectors_status()`, `get_auth_url()`, `oauth_callback()`, `list_connector_files()`, `trigger_file_import()`
* Encryption Helper: [apps/invoice-be/utils/encryption.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/encryption.py) → `encrypt_token()` / `decrypt_token()`
* OAuth Plumbing: [apps/invoice-be/utils/connector_oauth.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/connector_oauth.py) → `has_real_credentials()`, `get_valid_access_token()` (transparent refresh-token renewal), ~~`generate_pkce_pair()` (Salesforce PKCE)~~ *(removed, Gap 334)*
* Real File APIs: [apps/invoice-be/utils/connector_files.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/connector_files.py) → `list_google_drive_files()`, `download_google_drive_file()`, ~~`list_salesforce_files()`, `download_salesforce_file()`, `list_salesforce_libraries()`, `verify_salesforce_instance()`~~ *(removed, Gap 334)*
* Queue Worker: [apps/invoice-be/queue_worker/handlers.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/queue_worker/handlers.py) → `handle_import_connector_file()`
* Autopilot Sync History (Gap 427): [apps/invoice-be/routers/autopilot.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/routers/autopilot.py) → `get_autopilot_history()`, `get_run_files()`, `get_legacy_run_files()`, `_derive_run_status()`, `_run_aggregate_columns()`, `_status_count()`, `_row_to_run()`, `_to_file_entry()`; models `AutopilotRunEntry`, `AutopilotLogEntry`, `AutopilotHistoryResponse`, `AutopilotRunFilesResponse`
* Autopilot Sync Engine: [apps/invoice-be/services/autopilot_sync.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/services/autopilot_sync.py) → `run_sync(tenant_id, db_session, trigger='scheduled')`, `run_sync_for_all_due_tenants()`, `_write_log(..., batch_id, trigger, source_file_name)`
* Autopilot Ledger Model: [apps/invoice-be/models.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/models.py) → `TenantAutopilotLog` (`batch_id`, `trigger`, `source_file_name`, index `idx_autopilot_log_tenant_batch`)

### Functionality
`get_connectors_status()` reads the tenant's `TenantConnection` rows and reports `Active`/`Inactive`/`Not Configured` per provider. **The OAuth app itself is one shared, company-owned app per provider (`GOOGLE_CLIENT_ID`/`SECRET`, ~~`SALESFORCE_CLIENT_ID`/`SECRET`~~ env vars) — not per-tenant.** Each end-user connects *their own* Drive account through it; the resulting per-tenant token is what `TenantConnection` stores.

`get_auth_url()` / `oauth_callback()` (`_has_real_credentials()` gate, added 2026-07-30): once a provider's real Client ID is configured (Google is, as of this date, using the dev's personal GCP OAuth app standing in for the company app pending a dedicated one), the redirect URL is real and `oauth_callback()` makes a real `POST` to that provider's token endpoint (`GOOGLE_TOKEN_URL`~~/`SALESFORCE_TOKEN_URL`~~) to exchange the code for a real access/refresh token. Google requests are sent with `access_type=offline&prompt=consent` so a `refresh_token` is always returned (needed for the "connect once" experience — `TenantConnection.encrypted_refresh_token` already existed but nothing wrote a real value into it before this). A provider with no real Client ID configured yet ~~(Salesforce, as of this date — still consumer-key placeholders)~~ transparently falls back to the previous simulated exchange so local/dev testing keeps working without a registered app.

**Real file listing/download (Gap 98, 2026-07-30):** `list_connector_files()` and `handle_import_connector_file()` now call the real Google Drive API (`utils/connector_files.py`) whenever an active connection exists for a provider with `has_real_credentials()` true — real `files.list` (folder-scoped query, PDFs + folders only) for browsing, real `alt=media` download for import. `utils/connector_oauth.py::get_valid_access_token()` transparently refreshes an expired access token via the stored `refresh_token` before either call, and persists the renewed token/expiry — this is what makes "connect once" actually mean "never ask again," not just a schema field with nothing reading/writing it. Verified against the live Drive API with a deliberately invalid token (real `401` back, confirming the request shape and that the Client ID/Secret are genuinely registered with Google) since a full real-token test requires an interactive browser consent step. Falls back to the previous mock file list/stub bytes when no active real connection exists, or for a provider without real credentials.

~~**Salesforce plumbing, live-tested 2026-07-30:** `list_salesforce_files()`/`download_salesforce_file()` in `utils/connector_files.py` mirror the Drive functions against Salesforce's documented REST API (SOQL query over `ContentVersion` for listing, `ContentVersion/{id}/VersionData` for download). The one real structural difference from Google: Salesforce's API base is per-org (`instance_url`), returned in every token response rather than fixed like Drive's — added as a new nullable `TenantConnection.instance_url` column (migration `a7b8c9d0e1f2`), captured in `oauth_callback()` on initial connect and kept current on every refresh via `get_valid_access_token()`. Everything is wired through the same `has_real_credentials()` gate as Google. Real Consumer Key/Secret from the user's Salesforce Developer Edition org now configured — Connect flow verified reaching Salesforce's real login screen.~~ **— REMOVED 2026-08-28 (Gap 334).** The `instance_url` column and migration `a7b8c9d0e1f2` described here are deliberately retained; everything else in this paragraph is gone.

~~**PKCE (RFC 7636), added 2026-07-30 after a real failed Connect attempt:** this Salesforce org's Connected App requires PKCE on the authorization flow (`invalid_request: missing required code challenge` was the real error hit). `utils/connector_oauth.py::generate_pkce_pair()` generates an S256 code_verifier/code_challenge; `get_auth_url()`'s Salesforce branch stashes the verifier in Redis keyed by a one-time `state` (10-min TTL) and attaches `code_challenge`/`code_challenge_method`/`state` to the authorize URL; `oauth_callback()` retrieves and deletes the verifier by `state` and includes it in the token exchange. Google's flow doesn't need this and is unaffected. Covered by `tests/test_connectors.py::test_salesforce_pkce_flow`.~~ **— REMOVED 2026-08-28 (Gap 334).** PKCE existed solely for Salesforce; `generate_pkce_pair()`, the Redis `oauth_pkce:{state}` stash, the `state` query param, and `test_salesforce_pkce_flow` are all deleted. Google's flow never required PKCE and is unaffected.

**OAuth popup instead of full-page redirect, added 2026-07-30:** the Settings page's Connect button originally did a full `window.location.href` navigation to reach a real login screen, which took the user out of the app entirely. A true `<iframe>` embed of Google's ~~/Salesforce's~~ login page is not possible (they block being framed via `X-Frame-Options`/CSP to prevent clickjacking) — so `handleConnect` now opens the consent screen in a `window.open()` popup instead, auto-closing itself on success (`window.opener` + `?connected=` detection) while the main window polls for the popup closing and refreshes its own status. See `apps/invoice-fe/docs/fe_features_tracker.md` Gap 98 for the FE-side detail.

**Autopilot sync history is a list of RUNS, not a list of files (Gap 427, 2026-09-04).** Feature 13's `services/autopilot_sync.py::run_sync()` had always minted a per-run `batch_id` and stamped it on the `Invoice` rows it created — but never on its own `TenantAutopilotLog` rows. `GET /autopilot/history` therefore had no notion of a run at all and could only page over individual files, which is what made the FE Sync History table an unreadable wall of raw Google Drive file IDs with no batch summary. Three changes make a run a first-class thing:

1. **`TenantAutopilotLog` gained `batch_id`, `trigger` and `source_file_name`** (all nullable; migration `a1b2c3d4e5f7`, plus index `idx_autopilot_log_tenant_batch` on `(tenant_id, batch_id)` — neither existing index serves a GROUP BY read path). Nullable *permanently*, not just for the migration window: the run a pre-existing row belonged to was never recorded anywhere and Drive file names are not recoverable from a `fileId` alone, so there is nothing to back-fill from.
2. **`run_sync()` takes a `trigger` kwarg** (`'scheduled'` default; `routers/autopilot.py::trigger_sync()` passes `'manual'`, `run_sync_for_all_due_tenants()` passes `'scheduled'` explicitly) and threads `batch_id`/`trigger`/`file_name` into **every** `_write_log()` call — SUCCESS, SKIPPED_DUPLICATE and both FAILED paths. A run that finds nothing new writes **one `NO_NEW_FILES` marker row** with an empty `source_file_id`, so an empty run is still visible instead of leaving no trace. `NO_NEW_FILES` is deliberately not `SUCCESS`: both dedup layers and the incremental `since_dt` lookup filter on `status == 'SUCCESS'`, and a marker row carrying an empty file id/hash must not participate in any of them.
3. **`GET /autopilot/history` returns runs** — a single `GROUP BY batch_id` (tenant-scoped, `min`/`max(ingested_at)` for `started_at`/`finished_at`, `SUM(CASE ...)` per status), ordered by `started_at` desc. This is a **breaking response-shape change**; the FE is rewritten against it in parallel (FE Gap 428).

```
GET /autopilot/history?page=&page_size=  -> {
  items: [{ batch_id: str|null, trigger: str|null, source_type: str,
            started_at: iso, finished_at: iso,
            files_seen: int, imported: int, skipped: int, failed: int,
            status: "SUCCESS"|"PARTIAL"|"FAILED"|"NO_NEW_FILES" }],
  total: int, page: int, page_size: int }
GET /autopilot/history/{batch_id}/files -> { items: [{ id, source_file_id,
    source_file_name, content_hash, ingested_at, status, error_detail }] }
GET /autopilot/history/legacy/files      -> same shape, for the batch_id: null bucket
```

`_derive_run_status()` collapses a run's file statuses: all succeeded → `SUCCESS`; any failure alongside any success **or skip** → `PARTIAL`; failures and nothing else → `FAILED`; the marker row alone → `NO_NEW_FILES`; every file a duplicate → `SUCCESS` with `imported: 0` (dedup working is the system doing its job, not an error). `files_seen` excludes the marker row, so an empty run reports `files_seen: 0` rather than claiming to have processed a file it never saw.

Run totals are **derived** from the file rows rather than stored in a summary row — a summary row would be a second copy of facts that can drift out of agreement with the rows it summarizes.

Legacy rows (`batch_id IS NULL`) collapse into **one** synthetic item with `batch_id: null`/`trigger: null`, always ordered last and occupying exactly one slot in the paged ordering — one "before run tracking" bucket rather than thousands of one-file pseudo-runs. Its files come from `GET /autopilot/history/legacy/files`, declared **before** the `/{batch_id}/files` route because FastAPI matches in declaration order and `legacy` is not a UUID. `get_run_files()` filters on `tenant_id` as well as `batch_id` and returns **404** for both an unknown id and another tenant's id — deliberately indistinguishable, so the endpoint cannot be used to probe which run ids exist.

* **Bi-directional Support**:
  * **Inbound (AP)**: `list_connector_files()` and `trigger_file_import()` pull supplier invoice PDFs into the inbound extraction pipeline.
  * **Outbound (AR)**: Connectors can be pointed to outbound directories to import pre-made outbound invoices or auto-export verified outbound PDFs to Google Drive ~~/Salesforce~~ once finalized.

### Tasks
- `[x]` **Task 9.1: Code Connection Status Check Route** — `GET /api/v1/connectors/status` implemented in `routers/connectors.py`.
- `[x]` **Task 9.2: Implement Secured OAuth Credentials Flow (Google Drive ~~& Salesforce~~)** — `GET /connectors/auth-url/{provider}` and `GET /connectors/callback/{provider}` with AES-256 Fernet token encryption. **Updated 2026-08-28 (Gap 334)**: the Salesforce half of this task is removed. Both endpoints now validate against a single-provider list (`["google_drive"]`); the `state` PKCE query param on the callback is gone. The Google Drive half is unchanged and still live.
- `[x]` ~~Task 9.3: Build Ingestion MCP Server & Directory Explorer APIs~~ — **Removed (Gap 35, 2026-07-22)**: the MCP server was never wired into any agent and duplicated `routers/connectors.py`'s own logic; decided not to build it out. Directory listing already lives directly on the router (`list_connector_files()`), which is where new directory-listing work belongs going forward.
- `[x]` **Task 9.4: Implement Background Import Tasks** — `handle_import_connector_file()` added to `queue_worker/handlers.py` (2026-07-28). Supports `direction=inbound` (downloads, uploads to `{tenant_id}/inbound/` blob prefix, enqueues `process_invoice`) and `direction=outbound` (uploads to `{tenant_id}/outbound/` blob prefix, no extraction). Graceful no-op if `AZURE_STORAGE_CONNECTION_STRING` missing (local dev). `direction` kwarg forwarded through router → queue message → worker dispatch → handler.
- `[x]` **Task 9.5: Real OAuth token exchange (2026-07-30)** — `get_auth_url()`/`oauth_callback()` now perform a real Google ~~/Salesforce~~ token exchange once that provider's Client ID is configured (`_has_real_credentials()`); `access_type=offline&prompt=consent` on the Google auth URL so `refresh_token` is reliably populated. Falls back to the previous mock exchange per-provider when no real Client ID is set. Company OAuth app credentials wired Key Vault → `GOOGLE-CLIENT-SECRET`/~~`SALESFORCE-CLIENT-SECRET`~~ secrets → `invoice-be` container env (`infra/05-secrets.bicep`, `infra/modules/compute/invoice-be.bicep`, `infra/08-apps.bicep`). **Redirect URIs and post-OAuth `FRONTEND_URL` bounce both use the public website FQDN** (Multi-Zone: FE is internal-only; website proxies `/api/connectors/callback/[provider]` and `/settings/*`). See Gap 131 for the still-open long-term APIM/path-external-BE direction.
  - **Gap 363 (2026-09-01): "public website FQDN" above was silently resolving to the *internal* CAE domain, not the real custom domain.** `infra/08-apps.bicep`'s `publicOrigin` var falls back to the internal `ca-invoice-website-dev.<CAE-default-domain>` FQDN whenever `customDomainName` is unset — and `infra/params.dev.json` never set it, even though Azure Front Door (deployed manually, outside this bicep — see the infra report for the naming-mismatch finding this surfaced) has served the real custom domain live for a while. Effect: `GOOGLE_REDIRECT_URI`, `FRONTEND_URL` (this task's post-OAuth bounce target), and `ALLOWED_ORIGINS` on `ca-invoice-be-dev` all pointed at the internal FQDN — a browser bounced there after Google consent lands on a domain Clerk's session cookie isn't valid for (the internal `azurecontainerapps.io` FQDN is a different origin from the custom domain the user is actually signed in on), which is what surfaced as "session loss" after Drive connect. Fixed: `customDomainName: "invoicellm.admsofttech.com"` added to `params.dev.json`; the three env vars corrected live on `ca-invoice-be-dev`. **Google Cloud Console still has only the old internal-FQDN redirect URI registered as of this fix — the founder is adding the new one and will remove the old one only after confirming the new flow end-to-end, so Drive connect stays broken until that manual step happens.** Full detail: infra chat report, 2026-09-01.
- `[x]` **Gap 98: real file listing/download — Google Drive and Salesforce both live-tested (2026-07-30).** `list_connector_files()`/`handle_import_connector_file()` now call the real Drive/Salesforce APIs instead of returning a hardcoded list / stub PDF; `get_valid_access_token()` added for transparent refresh-token renewal for both providers. Salesforce needed one schema addition (`TenantConnection.instance_url`, migration `a7b8c9d0e1f2`) since its API base is per-org, unlike Google's fixed one, plus PKCE support (see above). Both stay gated behind `has_real_credentials()`, so nothing changes in behavior until real credentials exist for a given provider. FE side (Settings → Connectors Connect button, Ingestion browse bar) tracked in `apps/invoice-fe/docs/fe_features_tracker.md` Gap 98.
  - **Updated 2026-08-28 (Gap 334)**: the Salesforce half of this Gap is removed — `list_salesforce_files()`/`download_salesforce_file()` and the PKCE support are deleted. `TenantConnection.instance_url` and migration `a7b8c9d0e1f2` are deliberately retained (dead but harmless; the migration is mid-chain). The Google Drive half, and `get_valid_access_token()`, are unchanged and still live.
- `[x]` **Gap 427: Autopilot sync history exposed as runs (2026-09-04)** — `TenantAutopilotLog` gained `batch_id`/`trigger`/`source_file_name` (migration `a1b2c3d4e5f7`, index `idx_autopilot_log_tenant_batch`); `run_sync()` stamps all three on every log row and writes a `NO_NEW_FILES` marker for an empty run; `GET /autopilot/history` now returns one item per run with derived totals, with per-file detail behind `GET /autopilot/history/{batch_id}/files` and `GET /autopilot/history/legacy/files`. Breaking response-shape change, paired with **FE Gap 428**. See the Functionality section above for the full contract. Verified: `pytest tests/test_autopilot.py` → **33 passed** against real Postgres.

### Verification Plan
* **Automated Tests**: `uv run pytest tests/test_connectors.py`. ~~15 tests, including real listing/download/refresh-token/PKCE paths~~ — **updated 2026-08-28 (Gap 334)**: still **15 tests, all passing**, but the composition changed. Removed: the `real_salesforce_credentials` fixture, `test_salesforce_pkce_flow`, and `test_oauth_callback_salesforce_verification_failure`. Re-pointed from `provider="salesforce"` to `"google_drive"` rather than deleted, because they test *generic* connector behaviour and deleting them would have silently dropped real coverage: `test_oauth_callback`, `test_list_files`, `test_handle_import_connector_file_outbound_no_azure`. `test_connectors_status_*` now assert `"salesforce" not in data` (the status map key is gone). All still mock `httpx`/Redis — no live network calls in CI. `tests/test_autopilot.py` (21 passing) also re-run, since `services/autopilot_sync.py` changed.
* **Autopilot sync-run history (Gap 427, 2026-09-04)**: `DATABASE_URL=postgresql://…:5433/invoice_db uv run pytest tests/test_autopilot.py -q` → **33 passed in 14.69s** (was 21). Nine new tests: `test_T20a_history_groups_rows_into_runs_newest_first`, `test_T20b_run_status_derivation` (all five status rules, one run each), `test_T20c_no_new_files_run_is_visible_with_zero_files`, `test_T20d_legacy_rows_collapse_into_one_bucket_ordered_last`, `test_T20d2_legacy_bucket_lands_on_the_last_page_only`, `test_T20e_run_files_endpoint_and_tenant_isolation` (cross-tenant batch_id → 404, and the foreign run absent from the runs list), `test_T20f_legacy_files_endpoint_is_tenant_scoped`, `test_T20g_run_sync_stamps_run_identity_and_logs_empty_runs` (the write side), plus `test_gap_427_run_grouping_on_postgres` — a real-engine checkpoint, because this is the first `GROUP BY` + `SUM(CASE …)` + `COUNT(DISTINCT …)` in this router and SQLite is markedly more forgiving than Postgres about grouping shape. `test_T08_get_history_paginated` was rewritten to assert `total == 3` for three runs of two files each; under the old contract it would have been 6.
* **Manual/Live Verification (done 2026-07-30)**: Google — smoke-tested `list_google_drive_files()`/`download_google_drive_file()`/token-refresh directly against the real endpoints with a deliberately invalid token (got real `401`/`invalid_grant` back). Salesforce — real Consumer Key/Secret from the user's Developer Edition org, Connect flow reaching the real Salesforce login screen, PKCE error hit and fixed live. Full real-token connect-and-browse pass (actually completing login and pulling a real file) still pending the user's own manual click-through.
* **Salesforce live verification — outcome, 2026-08-28**: the pending real-token connect-and-browse pass referenced above was attempted and **failed permanently, not transiently**. Salesforce returned `OAUTH_AUTHORIZATION_BLOCKED — Cross-org OAuth flows are not supported for this external client app` (confirmed via Log Analytics against `ca-invoice-be-dev`). See the removal notice at the top of this document. No further Salesforce verification is possible or planned.

Note: SendGrid (inbound/outbound transactional email) setup steps live in `apps/invoice-be/docs/feature_14_email_ingestion.md`, not here — it's not an OAuth connector a tenant clicks "Connect" on, so it doesn't belong in this doc's scope.

