# Feature 9: Third-Party Connectors & Ingestion

Integrate secure API credentials exchanges and import pipelines to retrieve files from Google Drive or Salesforce.

### File Coordinates
* Router: [apps/invoice-be/routers/connectors.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/routers/connectors.py) → `get_connectors_status()`, `get_auth_url()`, `oauth_callback()`, `list_connector_files()`, `trigger_file_import()`
* Encryption Helper: [apps/invoice-be/utils/encryption.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/encryption.py) → `encrypt_token()` / `decrypt_token()`
* OAuth Plumbing: [apps/invoice-be/utils/connector_oauth.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/connector_oauth.py) → `has_real_credentials()`, `get_valid_access_token()` (transparent refresh-token renewal)
* Real File APIs: [apps/invoice-be/utils/connector_files.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/utils/connector_files.py) → `list_google_drive_files()`, `download_google_drive_file()`, `list_salesforce_files()`, `download_salesforce_file()`
* Queue Worker: [apps/invoice-be/queue_worker/handlers.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/queue_worker/handlers.py) → `handle_import_connector_file()`

### Functionality
`get_connectors_status()` reads the tenant's `TenantConnection` rows and reports `Active`/`Inactive`/`Not Configured` per provider. **The OAuth app itself is one shared, company-owned app per provider (`GOOGLE_CLIENT_ID`/`SECRET`, `SALESFORCE_CLIENT_ID`/`SECRET` env vars) — not per-tenant.** Each end-user connects *their own* Drive/Salesforce account through it; the resulting per-tenant token is what `TenantConnection` stores.

`get_auth_url()` / `oauth_callback()` (`_has_real_credentials()` gate, added 2026-07-30): once a provider's real Client ID is configured (Google is, as of this date, using the dev's personal GCP OAuth app standing in for the company app pending a dedicated one), the redirect URL is real and `oauth_callback()` makes a real `POST` to that provider's token endpoint (`GOOGLE_TOKEN_URL`/`SALESFORCE_TOKEN_URL`) to exchange the code for a real access/refresh token. Google requests are sent with `access_type=offline&prompt=consent` so a `refresh_token` is always returned (needed for the "connect once" experience — `TenantConnection.encrypted_refresh_token` already existed but nothing wrote a real value into it before this). A provider with no real Client ID configured yet (Salesforce, as of this date — still consumer-key placeholders) transparently falls back to the previous simulated exchange so local/dev testing keeps working without a registered app.

**Real file listing/download (Gap 98, 2026-07-30):** `list_connector_files()` and `handle_import_connector_file()` now call the real Google Drive API (`utils/connector_files.py`) whenever an active connection exists for a provider with `has_real_credentials()` true — real `files.list` (folder-scoped query, PDFs + folders only) for browsing, real `alt=media` download for import. `utils/connector_oauth.py::get_valid_access_token()` transparently refreshes an expired access token via the stored `refresh_token` before either call, and persists the renewed token/expiry — this is what makes "connect once" actually mean "never ask again," not just a schema field with nothing reading/writing it. Verified against the live Drive API with a deliberately invalid token (real `401` back, confirming the request shape and that the Client ID/Secret are genuinely registered with Google) since a full real-token test requires an interactive browser consent step. Falls back to the previous mock file list/stub bytes when no active real connection exists, or for a provider without real credentials.

**Salesforce plumbing (added 2026-07-30, not yet live-tested):** `list_salesforce_files()`/`download_salesforce_file()` in `utils/connector_files.py` mirror the Drive functions against Salesforce's documented REST API (SOQL query over `ContentVersion` for listing, `ContentVersion/{id}/VersionData` for download). The one real structural difference from Google: Salesforce's API base is per-org (`instance_url`), returned in every token response rather than fixed like Drive's — added as a new nullable `TenantConnection.instance_url` column (migration `a7b8c9d0e1f2`), captured in `oauth_callback()` on initial connect and kept current on every refresh via `get_valid_access_token()`. Everything is wired through the same `has_real_credentials()` gate as Google, so it stays completely inert (mock path, unchanged behavior) until a real Salesforce Connected App is configured. Reachability-checked against the real `login.salesforce.com` token endpoint with placeholder credentials (got Salesforce's real `invalid_client_id` error back, confirming the URL/request shape is correct) — **not yet tested against a real Salesforce org/Connected App**; that requires the user to create one and supply real `SALESFORCE_CLIENT_ID`/`SECRET`/`REDIRECT_URI`.

* **Bi-directional Support**:
  * **Inbound (AP)**: `list_connector_files()` and `trigger_file_import()` pull supplier invoice PDFs into the inbound extraction pipeline.
  * **Outbound (AR)**: Connectors can be pointed to outbound directories to import pre-made outbound invoices or auto-export verified outbound PDFs to Google Drive/Salesforce once finalized.

### Tasks
- `[x]` **Task 9.1: Code Connection Status Check Route** — `GET /api/v1/connectors/status` implemented in `routers/connectors.py`.
- `[x]` **Task 9.2: Implement Secured OAuth Credentials Flow (Google Drive & Salesforce)** — `GET /connectors/auth-url/{provider}` and `GET /connectors/callback/{provider}` with AES-256 Fernet token encryption.
- `[x]` ~~Task 9.3: Build Ingestion MCP Server & Directory Explorer APIs~~ — **Removed (Gap 35, 2026-07-22)**: the MCP server was never wired into any agent and duplicated `routers/connectors.py`'s own logic; decided not to build it out. Directory listing already lives directly on the router (`list_connector_files()`), which is where new directory-listing work belongs going forward.
- `[x]` **Task 9.4: Implement Background Import Tasks** — `handle_import_connector_file()` added to `queue_worker/handlers.py` (2026-07-28). Supports `direction=inbound` (downloads, uploads to `{tenant_id}/inbound/` blob prefix, enqueues `process_invoice`) and `direction=outbound` (uploads to `{tenant_id}/outbound/` blob prefix, no extraction). Graceful no-op if `AZURE_STORAGE_CONNECTION_STRING` missing (local dev). `direction` kwarg forwarded through router → queue message → worker dispatch → handler.
- `[x]` **Task 9.5: Real OAuth token exchange (2026-07-30)** — `get_auth_url()`/`oauth_callback()` now perform a real Google/Salesforce token exchange once that provider's Client ID is configured (`_has_real_credentials()`); `access_type=offline&prompt=consent` on the Google auth URL so `refresh_token` is reliably populated. Falls back to the previous mock exchange per-provider when no real Client ID is set. Company OAuth app credentials wired Key Vault → `GOOGLE-CLIENT-SECRET`/`SALESFORCE-CLIENT-SECRET` secrets → `invoice-be` container env (`infra/05-secrets.bicep`, `infra/modules/compute/invoice-be.bicep`, `infra/08-apps.bicep`); redirect URIs computed from the FE's public FQDN since the backend's ingress is internal-only and the FE proxies the callback route through.
- `[x]` **Gap 98: real file listing/download — Google Drive fixed and live-smoke-tested; Salesforce coded, pending real Connected App (2026-07-30).** `list_connector_files()`/`handle_import_connector_file()` now call the real Drive/Salesforce APIs instead of returning a hardcoded list / stub PDF; `get_valid_access_token()` added for transparent refresh-token renewal for both providers. Salesforce needed one schema addition (`TenantConnection.instance_url`, migration `a7b8c9d0e1f2`) since its API base is per-org, unlike Google's fixed one. Both stay gated behind `has_real_credentials()`, so nothing changes in behavior until real credentials exist for a given provider. **User will create a real Salesforce Connected App and manually test this end-to-end.** FE side of this gap (no Settings→Connectors page exists yet to actually reach this) tracked separately in `apps/invoice-fe/docs/fe_features_tracker.md` Gap 96/98.

### Verification Plan
* **Automated Tests**: Run `uv run pytest tests/test_connectors.py` (14 tests, including 3 for the real Drive listing/download/refresh paths, all mocking `httpx` — no live network calls in CI) and `uv run pytest tests/` for full-suite regression (151 tests).
* **Manual/Live Verification (done 2026-07-30)**: Ran `list_google_drive_files()`/`download_google_drive_file()`/the token-refresh POST directly against the real Google endpoints with a deliberately invalid token — got Google's real `401`/`invalid_grant` responses back, confirming the real company Client ID/Secret are valid and the request shapes are correct. A full real-token pass (actually connecting a real Google account through the browser consent screen) still needs the FE Connectors page to exist first (Gap 96/98).

