# Feature 7: Audit Resolution & Finalization — **SENTINEL Agent**

**SENTINEL** is the Invoice Risk Detection agent. Enable human auditor overrides, update database transaction states, and save template learning rules.

### File Coordinates
* Router: [apps/invoice-be/routers/audit.py](file:///c:/Users/S%20Banerjee/Desktop/Invoice_LLM/Prod_Invoice_LLM/apps/invoice-be/routers/audit.py) → `PUT /audit/resolve/{invoice_id}` → `resolve_audit_invoice()`

### Functionality
`resolve_audit_invoice()` fetches the tenant-scoped `Invoice` row by id (no status filter — this works on an invoice in any status, not just `AUDIT_REQUIRED`; see Gap 53 below), then: (1) filters `invoice.sa_alerts` down to whatever wasn't named in `payload.dismissed_alerts` (matching by string, `id`, `type`, or `message` — alerts can be stored as either plain strings or dicts, so both shapes are handled); (2) if `payload.status` is present, validates it's `PAID`/`REJECTED` and writes it — `status` is optional, so a call can dismiss alerts and/or save corrections without finalizing the invoice at all; (3) if `payload.corrections` is present, persists each corrected field onto the `Invoice` row via `_apply_corrections()`, producing a before/after diff (Task 7.3). It inserts one `AuditLog` row (`action="RESOLVE_INVOICE"`, `details` = target status, dismissed/remaining alerts, and the correction diff) capturing `actor_user_id`/`actor_role` from the JWT context, and commits everything in one transaction. If a correction was made, `_detect_correction_pattern()` checks whether that field has recurred often enough (≥3 times for one vendor, or ≥3 distinct vendors) to suggest a Trainer rule (Task 7.4), returned as `suggested_rule` on the response.

### Tasks
- [x] **Task 7.1: Code Audit Resolution Endpoint** — `PUT /api/v1/audit/resolve/{invoice_id}`, dismisses warnings, updates status to `PAID`/`REJECTED`. Fields are no longer strictly read-only — superseded/extended by Task 7.3.
- [x] **Task 7.2: Implement Audit Logging** — every resolve call writes one `AuditLog` row with actor/action/timestamp/details.
- [x] **Task 7.3: Accept field corrections on resolve** — `corrections: dict[str, Any]` payload (field name → corrected value), persisted onto the `Invoice` row with a before/after diff logged in `audit_logs.details` — the raw training signal for what the AI got wrong and what a human said instead. FE: `docs/feature_4_auditor.md` Task 4.6.
- [x] **Task 7.4: Detect correction patterns and suggest a trainer rule** — after a correction, checks whether the same field recurred ≥3 times (per-vendor or across vendors) and returns `suggested_rule: {scope, field, vendor_name, sample_correction} | null` so the FE can prompt "Want to save this as a rule?" (`feature_4_auditor.md` Task 4.7). Threshold and lookback window (`_RULE_SUGGESTION_THRESHOLD`, `_RULE_SUGGESTION_LOOKBACK_DAYS` in `routers/audit.py`) are hardcoded per-tenant-uniform constants today, not per-tenant configurable — flagged as a known simplification, not revisited since no false-positive-suggestion complaints have surfaced.
- [x] **Task 7.5 (Gap 62): Let `resolve_audit_invoice()` commit a standing rule directly — done 2026-07-29.**
- [x] **Task 7.6 / Gap 125:** Optional `notify_emails[]` on resolve when status is PAID/REJECTED — must be ⊆ inbound authorized set; SendGrid staff notify only (never customers).
  - `AuditResolutionPayload` gained `apply_as_standing_rule: bool = False`. When true and `corrections` are present, `_apply_standing_rule()` builds one candidate-rule sentence per corrected field, merges them with the vendor's existing `ExtractionTemplate` constraints (if any), and re-runs `run_extraction_agent()` against a fresh OCR pass of the same file with the merged rules applied.
  - **Safety gate**: only commits the rule if the re-extraction's result actually reproduces every corrected field's new value; otherwise returns `{"applied": False, "reason": ...}` with no DB write. Vendor-scoped only (no vendor name on the invoice → skipped, not attempted).
  - On pass: writes/versions the vendor's `ExtractionTemplate` row plus an `ExtractionTemplateVersion` history entry, in the same transaction as the invoice correction itself (correction always persists regardless of the rule's outcome).
  - Duplicated small helpers (`_run_ocr_split`, vendor template fetch, `changed_by` resolution) rather than importing from `routers/trainer.py` — same zero-touch convention used throughout this codebase's parallel audit/trainer mechanisms.
  - FE piece: `feature_4_auditor.md` Task 4.10.
  - Verified: 5 new tests in `tests/test_audit.py` (rule applied when safety check passes, rejected when it fails but correction still persists, skipped with no vendor name, never attempted when the checkbox is unset — confirms zero LLM call in that case). Full BE suite green (86 passed).

### Gaps
- `[x]` **BE Gap 536: the inbound resolve refuses OUTBOUND invoices** — built 2026-09-15. `resolve_audit_invoice()` now loads the invoice only when `flow_direction == "INBOUND"`, so an outbound invoice gets 404 — the mirror of `routers/outbound_audit.py`, which already 404s inbound ones. Before this, an OUTBOUND invoice could be set PAID/REJECTED/REVIEW_LATER here, skipping SENT and firing inbound `invoice.approved`/`invoice.rejected` webhooks. Outbound invoices are decided only through `PUT /outbound-audit/resolve/{id}`, `confirm-send` and `mark-paid`. Test: `tests/test_audit.py::test_resolve_refuses_outbound_invoice`. Fixing it exposed BE Gap 565 (outbound dashboard AI scores read the inbound trail action). Full record: `be_features_tracker.md` Gap 536.
- `[x]` **BE Gap 539: approve/reject only after extraction has finished** — built 2026-09-15. `_FINALIZABLE_FROM_STATUSES` lists where a PAID/REJECTED decision may start: `COMPLETED`, `AUDIT_REQUIRED`, `REVIEW_LATER`, `NEEDS_RESUBMISSION` (plus `PAID`/`REJECTED`, which Gaps 529 and 554 decide). Anything else — `UPLOADED`, `PROCESSING`, `PROCESSING_OCR`, `EXTRACTING_DATA`, `FAILED`, `EXTRACT_FAILED`, `DUPLICATE`, or an unknown value — gets 400 before anything is saved or sent, so no `invoice.approved` webhook can carry an unextracted invoice's null vendor and total. Deferral and correction-only calls are unchanged. Tests: `test_resolve_refuses_to_finalize_unfinished_invoice`, `test_resolve_still_finalizes_reviewable_invoice`.
- `[x]` **BE Gap 529: a final decision never flips directly** — built 2026-09-15 (founder ruling: not even an Admin). PAID → REJECTED and REJECTED → PAID get 400 "reopen it first (Admin-only)"; the supported path is the Gap 193 reopen (`REOPEN_INVOICE` in the trail), then a fresh decision. The review page already hides Approve/Reject on a final invoice, so this closes the API-key path and a stale second tab. Tests: `test_final_decision_cannot_be_flipped_directly` (Admin and non-Admin), `test_final_decision_changes_through_admin_reopen`.
- `[x]` **BE Gap 554: repeating a decision re-sends nothing** — built 2026-09-15. The insights-cache refresh, `invoice.approved`/`invoice.rejected` webhook, email summary, Drive archive and staff notification now fire only when the status actually changes. A pure repeat (same status, no correction, no alert removed) returns 200 with `already_resolved: true` and writes no trail row; the same final status sent with a real correction saves and logs the correction without re-sending the approval. Every response now carries `already_resolved` (additive field). Tests: `test_repeated_decision_is_a_no_op`, `test_repeated_decision_with_a_correction_saves_it_without_resending`.
- `[~]` **BE Gap 541: the resolve lookup takes a row lock** — built 2026-09-15. Both `resolve_audit_invoice()` and `routers/outbound_audit.py::resolve_outbound_alert()` load the invoice `with_for_update()`, so two decisions on one invoice from different backend replicas run one after the other; the second then meets Gaps 529/554 instead of overwriting the first. (Within a single process requests were already sequential: the handler does its database work synchronously with no await before commit.) The session is opened with `with Session(engine)`, so an error, early return or commit always releases the lock. Test: `test_resolve_locks_the_invoice_row` (asserts `FOR UPDATE` as Postgres compiles it — SQLite ignores locks). `[~]` until the manual Postgres wait check runs.
- `[x]` **BE Gap 533: a money correction that is not a finite number is refused** — built 2026-09-15. `nan`, `inf`, `-inf` and `1e400` used to crash the resolve with HTTP 500 at the JSONB `AuditLog` insert. They now get 422 with `detail.invalid_corrections: [{field, reason}]`, and nothing in the request is saved. Negative amounts stay valid — credit and debit notes are negative on purpose. Tests: `test_unreadable_money_correction_is_refused_and_nothing_saved`, `test_negative_money_correction_is_still_accepted` (inbound and outbound).
- `[x]` **BE Gap 534: corrected line items must be valid line items** — built 2026-09-15. `utils/correction_values.py::parse_line_items` validates every entry against `InvoiceLineItem` (`description` and `amount` required, unknown keys refused, non-finite numbers refused) and keeps exactly the keys sent; plain text, a single object or a malformed entry gets 422 naming the line. It used to wrap anything into `[raw_value]` and store it. Tests: `test_malformed_line_items_are_refused_and_nothing_saved`, `test_valid_line_items_including_a_credit_line_are_saved`, `tests/test_correction_values.py`.
- `[x]` **BE Gap 530: corrections are read, or refused — never silently dropped** — built 2026-09-15. Amounts go through `parse_money` ("$1,250.60", "1,250.50", "₹12,50,000", "INR 1,250.50", "(500.00)" → −500; "1.250,50" and other ambiguous groupings refused), dates through `parse_date` (ISO, "15/01/2026", "01/15/2026", "15 Jan 2026"; "01/02/2026" refused with "use YYYY-MM-DD" — never guessed). A field outside `_CORRECTABLE_FIELDS`, or a value that cannot be read, returns 422 `invalid_corrections` instead of `success: true` with nothing saved; the review page only ever sends allowed fields, so a normal save is unaffected. Unreadable values are no longer logged raw (the log names the field and error type only). Tests: `test_common_money_and_date_formats_are_saved`, `test_ambiguous_date_or_uncorrectable_field_is_refused`, `tests/test_correction_values.py`.
- `[x]` **BE Gap 552: trail rows record how the actor signed in, and which API key** — built 2026-09-15. `TenantContext` gained `api_key_prefix` (set on the API-key path from `Tenant.api_key_prefix`, the non-secret indexed prefix; `None` for a browser session) and `trail_identity()`. Every resolve trail row — inbound, outbound, and the Gap 551 Confirm Send / Mark Paid rows — now carries `auth_method` (`clerk` or `api_key`) and, for a key, `api_key_prefix`, so an action by an old key can be told apart from one by its rotated replacement. `GET /auth/me` returns the new field (additive). Tests: `test_trail_row_records_a_browser_session_without_a_key_prefix` (`tests/test_audit.py`), `test_api_key_actions_record_which_key_in_the_trail` (`tests/test_rbac.py`).
- `[x]` **BE Gap 560: rule-suggestion pattern detection reads a bounded window** — built 2026-09-15. Task 7.4's `_detect_correction_pattern()` reads at most the newest `_RULE_SUGGESTION_SCAN_LIMIT` (500) corrected resolves inside the 90-day lookback, only their `details` and vendor name, instead of every `RESOLVE_INVOICE` row in the window, on each corrected resolve. The threshold (3) and lookback are unchanged, so normal-volume tenants see the same suggestions; `sample_correction` now quotes the newest matching correction. Tests: `test_pattern_detection_reads_only_the_newest_corrections`, `test_pattern_detection_still_suggests_from_recent_repeats_and_quotes_the_newest`. Full record: `be_features_tracker.md` Gap 560.
- `[x]` **BE Gap 531: every extracted field with a column can be corrected** — built 2026-09-15 (founder ruling: all of them). `_CORRECTABLE_FIELDS` adds `currency`, `discount_amount`, `discount_percent`, `tags` and the eight list fields (`taxes`, `discounts`, `deductions`, `tax_ids`, `payment_instructions`, `references`, `addresses`, `compliance_metadata`); only `round_off` stays out (no `Invoice` column). Currency must be a 3-letter code, a percentage 0–100, tags a list or comma-separated text, and every list entry must match the entry model `InvoiceExtractionSchema` uses for that field (read from the schema via `list_entry_model`, because `extraction_agent.py` defines `DeductionItem` twice). An empty correction on a list field stores `[]`. The review page edits the new header fields inline and each list through `components/audit/DetailListEditor.tsx`. Tests: `test_every_previously_uncorrectable_field_is_saved`, `test_every_extracted_field_with_a_column_is_correctable`, `test_unreadable_new_field_correction_is_refused`, `test_clearing_a_list_field_stores_an_empty_list`. Full record: `be_features_tracker.md` Gap 531.
- `[x]` **BE Gaps 537, 538: a dismissal removes exactly one alert, and unmatched dismissals are reported** — built 2026-09-15. `utils/alert_dismissal.py::dismiss_alerts` (shared with the outbound resolve): each entry removes at most one alert — by `id` when the alert has one, else by type + field + message (founder ruling); an older string entry tries id, plain text, message, then type. Entries that match nothing are returned as `unmatched_dismissals` and everything else still saves (founder ruling: warn, not 400). The trail records `dismissed_alerts` (the exact alerts removed) and `unmatched_dismissals`; the alert-precision rollup and the dashboards' AI alert scores read `dismissed_alerts`. Ids are not stamped at emission (extraction-owned code). Tests: `tests/test_alert_dismissal.py`, `test_dismissing_one_alert_keeps_alerts_that_share_its_message`, `test_a_message_string_dismissal_removes_one_alert_per_entry`, `test_dismissals_that_match_nothing_are_reported_and_the_rest_is_saved`. Full records: `be_features_tracker.md` Gaps 537, 538.
- `[x]` **BE Gap 535: a correction that breaks the arithmetic raises an alert** — built 2026-09-15 (founder ruling: alert, never block). `utils/correction_recheck.py` re-runs `verify_line_items_math` / `verify_totals_math` with the tenant's tolerance overrides on the money fields before and after a correction; a check that passed before and fails after adds its alert (`raised_by: "correction"`). "Passed before" keeps invoices with a printed Round Off line (no `round_off` column) from being re-flagged. The response returns `remaining_alerts` and `raised_alerts`, and the review page shows the server's alert list after a save. Tests: `tests/test_correction_recheck.py`, `test_a_correction_that_breaks_the_totals_raises_an_alert_without_blocking` and two siblings in `tests/test_audit.py`. Full record: `be_features_tracker.md` Gap 535.
- `[x]` **BE Gap 550: deleting an invoice keeps its audit trail** — built 2026-09-15 (founder ruling: keep `invoice_id` as the historical id). `services/invoice_deletion.py::delete_invoice_rows()` no longer deletes the invoice's `AuditLog` rows; the `DELETE_INVOICE` summary row is still added, and the invoice itself is still hard-deleted. The correction history keeps counting in `extraction_quality_rollup`. Tests: `tests/test_invoice_deletion_trail.py`; `tests/test_invoice_soft_delete.py` (Postgres) updated. Full record: `be_features_tracker.md` Gap 550.
- `[x]` **BE Gap 532: a required field cannot be emptied by a correction** — built 2026-09-15 (founder ruling: refuse). A blank value (`None`, `""`, whitespace) on `vendor_name`, `invoice_number`, `invoice_date` or `grand_total` gets 422 `invalid_corrections` ("this field is required and cannot be left empty") and nothing is saved; a blank on a required field that is already empty is ignored, and optional fields still clear. Tests: `test_a_required_field_cannot_be_emptied_by_a_correction`, `test_an_optional_field_can_still_be_cleared`. Full record: `be_features_tracker.md` Gap 532.
- `[x]` **Change history for Admins (built with BE Gap 532, founder request 2026-09-15)** — `routers/audit_history.py::get_invoice_change_history()`, `GET /audit-history/{invoice_id}`: Admin session only (403 otherwise; no API keys), tenant-scoped, newest first. Reads the invoice's existing `AuditLog` rows — resolve, reopen, outbound resolve, Confirm Send, Mark Paid — and returns who (email, name, role, auth method, API key prefix), when (UTC) and what (status change, reject reason, field old → new, dismissed and raised alerts, notified staff). Saved changes only: a refused correction writes no row (founder ruling). Shown in the review consoles' "Change history (Admin)" panel. Tests: the four `test_*change_history*` / `test_admin_sees_who_changed_what_newest_first` tests in `tests/test_audit.py`. Full record: `be_features_tracker.md` Gap 532.
- `[x]` **BE Gap 566: new alerts carry an `id`** — built 2026-09-15. Alerts get an id where they are stored (`utils/alert_ids.py::with_alert_ids`), including alerts raised by a correction (Gap 535), so a dismissal of one of two otherwise identical alerts removes exactly that one by id (`utils/alert_dismissal.py` matches id first). Alerts stored before this keep being matched by type + field + message. Full record: `be_features_tracker.md` Gap 566.
- `[x]` **BE Gaps 553, 559: indexes for the audit trail and the Audit Queue** — built 2026-09-15 (founder ruling: index-only migration now). Migration `c553d559e0a1_audit_flow_indexes.py` builds `ix_audit_logs_tenant_action_timestamp` (pattern detection, extraction-quality rollup, dashboard AI scores) and `ix_invoice_tenant_flow_created_at` (the newest-first invoice queues) with `CREATE INDEX CONCURRENTLY`, both also declared in `models.py`. It shares parent `a1b2c3f30004` with the extraction branch's `a1b2c3f30005` — whichever merges second must repoint. Tests: `tests/test_audit_flow_indexes.py`. Full records: `be_features_tracker.md` Gaps 553, 559.
- `[x]` **Gap 407: invoice status state machine gains 'Review Later' and 'Needs Resubmission' — two non-terminal deferral states** — opened and built 2026-09-02 from an external defect review ("General — Invoice 'Review Later' & 'Needs Resubmission' States"). `resolve_audit_invoice()`'s status validation now accepts `REVIEW_LATER` and `NEEDS_RESUBMISSION` alongside the existing `PAID`/`REJECTED`/`AUDIT_REQUIRED`. Both are reachable by any user who can already resolve invoices (no Admin gate, unlike Gap 193's reopen — neither undoes a prior finalization), but neither is settable directly on an already-terminal (PAID/REJECTED) invoice — that must go through the Admin-only reopen first, so a deferral action can never silently un-finalize an invoice. Confirmed the finalization-only side-effect blocks (webhook dispatch, staff notify, Drive archive, email summary — all gated on `target_status in ("PAID", "REJECTED")` specifically) do not fire for either new status. **Inbound only in this pass** — outbound invoices (`routers/outbound_invoices.py`) have their own separate status machine (`NEEDS_REVIEW`/`VERIFIED`/`SENT`/`PAID`) with dedicated endpoints (`confirm-send`, `mark-paid`) and were not touched; extending outbound is a separate follow-up, not bundled in here. **No database migration** — `Invoice.status` is a plain string column with no enum/CHECK constraint, so adding new values is a pure application-logic change. **FE minimum-viable surfacing** (row actions + status badges) is Gap 407's FE half — see `fe_features_tracker.md`. **No new filter tab on `/invoices`** for either status in this pass — flagged as a reasonable fast-follow, not included here. **Verified**: `tests/test_audit.py` — 21 passed (5 new: both statuses succeed from `AUDIT_REQUIRED`, both rejected from a terminal invoice with the reopen-first message, both confirmed to skip the PAID/REJECTED-only side effects via `assert_not_called()`, and the 400 error message lists all five valid values). Full write-up: `be_features_tracker.md` Gap 407.
- `[x]` **Gap 53: "Report an issue" only reachable from `AUDIT_REQUIRED` invoices** — Fixed Jul 27, 2026. Turned out `resolve_audit_invoice()` already worked on any invoice regardless of status (no gate was ever added) — `status` and `dismissed_alerts` are both optional, so a correction-only payload (`{"corrections": {...}}`) on a `COMPLETED` invoice with zero alerts already persisted the field, logged it, and left status untouched. No backend change was needed; what was actually missing was the FE entry point (`fe_features_tracker.md` FE Gap 26) — the review page had no button to save a correction without also forcing `PAID`/`REJECTED`, and the review page itself was unreachable from anywhere in the app due to a separate, larger bug found in the same pass (every nav link pointed at a `/audit` route that never existed). Added `test_resolve_correction_only_on_completed_invoice` to `tests/test_audit.py`; also fixed a stale assertion in `test_resolve_invoice_paid` left over from before Task 7.3/7.4 added fields to the response. Verified live: a real `PUT /audit/resolve/{id}` correction-only call against a real `COMPLETED` invoice in the running dev DB, confirmed the value persisted and status stayed `COMPLETED`, then reverted it.

### Recent Fixes
* **P0 Bug - AlertConsole 400 Error**: Fixed Jul 25, 2026. The `status` field was previously required on every resolve call, causing `AlertConsole.tsx`'s "Dismiss" button (which sent `status: currentStatus`, e.g., `"AUDIT_REQUIRED"`) to always fail with a 400 error since the backend only accepted `PAID`/`REJECTED`. Made `status` optional on the backend so omitting it correctly dismisses/corrects without finalizing the invoice.

### Verification Plan
* **Automated Tests**: Run `uv run pytest tests/test_audit.py` testing resolution overrides and status updates.
* **Manual Verification**: Correct a mock invoice in the Auditor UI tab, click Approve, and verify that the alerts are removed and status changes in the database.
