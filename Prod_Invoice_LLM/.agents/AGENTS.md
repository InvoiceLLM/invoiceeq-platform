# Development Rules

- Work on exactly one feature tracker task at a time. Do not multi-task across features.
- Keep all discussions, queries, and file updates strictly limited to the current active feature context.
- Implement code changes one file at a time. Present the changes for that file and wait for explicit user approval before moving to the next file or running any terminal commands.
