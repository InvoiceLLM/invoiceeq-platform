# Infra Change Reports

Filed by `infra-devops` per `.claude/CONVENTIONS.md`: one file per change, `<date>-<topic>.md`, containing the rationale and summary of what changed (the actual bicep/ps1 diff lives in `infra/` itself, visible via git — not duplicated here).
