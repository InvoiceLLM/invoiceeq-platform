# Load Test Reports

Filed by `load-tester` per `.claude/CONVENTIONS.md`: one file per run, `<date>-<topic>.md`, containing scope (target, method, thresholds — stated before the run) and output (real numbers, pass/fail against those pre-stated thresholds) together.
