# Security Review Reports

Filed by `security-tester` per `.claude/CONVENTIONS.md`: one file per review, `<date>-<topic>.md`, containing scope (which of the 5 review areas, why) and findings (file:line, concrete failure scenario, confirmed/plausible) together.
