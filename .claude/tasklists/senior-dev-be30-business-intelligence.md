# Feature 30: Business Intelligence — chat-attachment intelligence bubble — build
Spec: Prod_Invoice_LLM/apps/invoice-be/docs/feature_30_business_intelligence.md (§1–§7 as amended by §8; §8 wins)
FE counterpart (not built here): Prod_Invoice_LLM/apps/invoice-fe/docs/feature_21_business_intelligence.md
Approval: founder 2026-09-08 09:40 — "comit and start development" (after the ten §8.1 rulings)
Started: 2026-09-08 09:45
Hard stop: **2026-09-08 19:00** (extended by the founder 2026-09-08: "feature 30 BE full completed"; the 13:45 stop was the first pass)
Definition of done: every task below checked or marked not-done with a reason; every Verification Plan item run on real Postgres (`localhost:5433/invoice_db`) and cited; spec body + tracker updated; a Gap entry per defect found; changes uncommitted at the end.
Preflight (2026-09-08): not frozen in active-work.md. Overlaps Feature 26 Part 2 in-flight items (H7 async wiring, `ENABLE_GENERIC_DOC_CHAT` default False — do not flip). Feature 30 stays behind its own `ENABLE_ATTACHMENT_INSIGHTS=False`; nothing here changes F26 behaviour when off.
Status: **COMPLETE 2026-09-08 16:50** (well inside the 19:00 hard stop) - every BE task is done except 30.9 (India cards blocked on unreachable primary sources - founder call), 30.14 (not done by its own precondition) and 30.16 (deferred by spec). Was: **RESUMED 2026-09-08 12:10** - pass 1 landed phase 0 (bar 30.0b/30.0c) + the sync/async bubble; pass 2 is finishing every remaining BE task by 19:00. Gap 492 (bubble is information only - no invoice ever written from a finding) is in the tree and binding on everything below.

## Order of work (phase 0 first, then the sync bubble, then async, then the knowledge tasks)

### Phase 0 — prerequisites (§8.4)
- [x] 30.0g `services/insight_thresholds.py` — `THRESHOLDS` constants + `threshold(name, tenant_id)` with `tenant_setting` override
- [x] 30.0e `insight` table + migration; `services/insights.py::open_insight()`, `transition_insight()`, `rank_open_insights()`, `notify_insight_update()`; `POST /insights/{id}/transition`; `GET /insights?status=OPEN`; SSE `insight_update` event
- [x] 30.0f `vendor` + `vendor_alias` tables + migration; `services/vendor_master.py::resolve_vendor()`, `confirm_alias()`; entity resolver reads it
- [x] 30.0a `ChatAttachment.retained` (+ `.region`, `.statement_date`, `.insights_version`, `.insights` in one migration); TTL sweep skips retained rows while any insight is OPEN
- [x] 30.0b `services/doc_linking.py::link_attachment()`, `LinkResult`, `vendor_link_policy()`; `POST /attachments/{id}/links/confirm`
- [ ] ~~30.0c~~ (PARTIAL - `bank_statement_line` table + migration landed; extractor output and `statement_date` population not built (hard stop)) `bank_statement_line` table + migration; extractor output for bank statements; `statement_date`
- [x] 30.0d `services/region.py::detect_region()` → `ChatAttachment.region`

### Sync bubble
- [x] 30.1 `ENABLE_ATTACHMENT_INSIGHTS`; `INSIGHT_DOC_TYPES` (eight §8.1 R2 types); REFERENCE discriminator extended; `attachment_insights.py` skeleton (`InsightCard`, `CARDS_BY_DOC_TYPE`, `build_insight_block(stage)`, `verdict_template()`, `narrate_insight_block()`, `rank_findings()`, `post_insight_turn()`); hooks in both extraction paths; sync cards: what-this-is, agreed-vs-billed, terms-check, net-position, delivery-vs-order (reuse Feature 29 code)
- [x] 30.2 Two-stage runtime: `enqueue_insight_job()` / `handle_insight_job()` (task type `insight`); sync block + template verdict posted inline; async update in place + `insights_version` + SSE push; answer-contract gate on narration; `MessageResponse.insights` additive. No pin.
- [x] 30.17 Bubble actions: note; Discuss seed; dismiss (hold / dispute / paid removed — Gap 492, information only)
- [x] 30.12 Confidence & gaps ("checks not run + why")

### Async cards
- [x] 30.3 Semantic views migration (`v_vendor_spend`, `v_overdue`, `v_tax_summary`, `v_3way_match`, each with `tenant_id`)
- [x] 30.4 `METRICS` + `query_metric()` with the parameterised tenant guard
- [ ] ~~30.5~~ (not done - hard stop (depends on 30.0c's extractor output)) `services/bank_matching.py` (R5 tolerances) + bank-statement sync card + cash-cover async card
- [x] 30.6 Cash-impact card
- [x] 30.18 Threshold-gated async cards: open PO value, cash-out timing, over-invoicing history, quote drift, partial-delivery balance, repeat short delivery, contract deviations

### Knowledge, compliance, flywheel, eval
- [x] 30.7 Knowledge layer + glossary-aware narration
- [x] 30.8 Certified examples + suggested-questions card
- [ ] ~~30.9~~ (not done - hard stop; no primary source was fetched, so no rule card was invented (hard rule 8)) India rule cards from fetched CBIC/GSTN primary sources (session drafts, founder spot-checks)
- [x] 30.10 EU + US cards
- [ ] ~~30.11~~ (not done - hard stop (depends on 30.9/30.10)) Compliance card: deterministic `verify` per card and doc type
- [x] 30.13 Flywheel: `chat_correction`, insight-feedback endpoint, `record_correction()`, `promote_to_example()`; workbook panels "Chat quality trend" + "Open insights"
- [x] 30.14 CLOSED BY FOUNDER RULING 2026-09-08 ("Close as not needed") — no reranker built; revisit only if a future eval shows retrieval ranking failures. Reranker behind `ENABLE_RERANK` — only if Feature 29 recall instrumentation shows ranking is the limit; otherwise mark not-done with that reason
- [x] 30.15 `insight_golden.json` (20 attachments) + `run_insight_eval.py`; no live LLM run without the founder's word
- [-] 30.16 Planner — deferred by spec; not in this build

## Log
- 2026-09-08 09:45 tasklist created; senior-dev dispatched in background.
- 2026-09-08 11:05 30.0g done — `services/insight_thresholds.py` (THRESHOLDS, threshold(), set_threshold(), all_thresholds()) + `tenant_insight_setting` table in migration `a1b2c3f30001`; `pytest tests/test_insight_thresholds.py -q` -> **6 passed in 7.81s** (real Postgres 5433).
- 2026-09-08 11:40 30.0e done - migration `a1b2c3f30001` (insight table, unique (attachment_id, finding_key)); `services/insights.py` (open_insight/transition_insight/list_insights/rank_open_insights/is_effectively_open/notify_insight_update/_sync_retained); `routers/chat.py` GET /chat/insights, POST /chat/insights/{id}/transition, GET /chat/insights/{id}/discuss. `pytest tests/test_insight_lifecycle.py -q` -> **22 passed in 24.44s** (real Postgres 5433).
- 2026-09-08 11:40 BE **Gap 491** filed: ruling R4's "hold"/"dispute" are not real `Invoice.status` values; mapped to REVIEW_LATER/AUDIT_REQUIRED pending a founder call.
- 2026-09-08 12:00 30.0f done - `services/vendor_master.py` (normalise_vendor_name/resolve_vendor/get_or_create_vendor/propose_alias/confirm_alias/vendor_invoice_names); `agents/entity_resolver.py::_vendor_master_candidates()` reads it and `ResolutionResult.vendor_names` widened to every confirmed spelling; `POST /chat/vendors/aliases/confirm`. `pytest tests/test_vendor_master.py -q` -> **18 passed in 17.22s**; regression `pytest tests/test_entity_resolver.py -q` -> **17 passed in 17.91s** (real Postgres 5433).
- 2026-09-08 12:15 30.0a done - `ChatAttachment.retained/.region/.statement_date/.insights_version/.insights` in migration `a1b2c3f30001`; `scripts/sweep_chat_attachments.py::expired_attachments()` now excludes retained rows; retention follows OPEN **or SNOOZED** findings (wider than the spec's literal wording, recorded in the test). `pytest tests/test_insight_retention.py tests/test_insight_lifecycle.py -q` -> **26 passed in 10.81s**.
- 2026-09-08 12:16 ORDER DEVIATION (recorded, not silent): 30.0b (doc linking) and 30.0c (bank ledger) are deferred behind 30.0d + 30.1 + 30.2 so the sync bubble itself lands before the hard stop. The sync cards use Feature 26's existing `candidate_invoice_ids` / `match_attachment()` for linking, which is what 30.0b was going to refine.
- 2026-09-08 12:25 30.0d done - `services/region.py::detect_region()` + `set_attachment_region()` (GSTIN -> IN, EU/EEA VAT prefix allow-list -> EU, EIN or state+ZIP -> US, else None; None never defaults to US). `pytest tests/test_region_detection.py -q` -> **13 passed in 7.23s**.
- 2026-09-08 13:12 30.1 done - `config.ENABLE_ATTACHMENT_INSIGHTS=False`; `services/attachment_insights.py` (INSIGHT_DOC_TYPES, DOC_TYPE_LABELS, is_insight_doc_type, InsightCard, finding(), CARDS_BY_DOC_TYPE, BUBBLE_ACTIONS, card_what_this_is / card_agreed_vs_billed / card_terms_check / card_net_position / card_delivery_vs_order / card_confidence_gaps, build_insight_block, rank_findings, verdict_template, narrate_insight_block, post_insight_turn, open_insights_from_block, run_sync_insights); hook `services/attachment_extraction.py::insight_attachment()` + new `STAGE_INSIGHTS` on both extraction paths. **REFERENCE discriminator NOT extended** - the Feature 27 taxonomy is frozen in `active-work.md`, so R2's "bank statement" uses the existing `STATEMENT_OF_ACCOUNT`; recorded in the module docstring and the spec.
- 2026-09-08 13:12 30.2 done - `ChatQueueService.enqueue_insight_job()` (task type `insight`), `queue_worker/handlers.py::handle_insight_job()`, dispatch in `main_worker._process_redis_chat_tasks()`; async block replaces the sync bubble on the SAME message, `insights_version` 1->2, `insight_update` pushed via `notify_insight_update()`; `MessageResponse.insights` + `ATTACHMENT_CONTRACT_KEYS += "insights"` (additive, `exclude_none` keeps old turns identical). Answer-contract gate reused from Feature 29 - a narration figure not in the facts JSON is rejected and the template verdict stands, no retry.
- 2026-09-08 13:12 30.12 done (card_confidence_gaps: skipped/blocked cards with reasons, low-confidence fields, the tenant's threshold values) and 30.17 done (five actions as `BUBBLE_ACTIONS` + the transition/discuss endpoints from 30.0e; hold/dispute/paid write `Invoice.status` under BE Gap 491's mapping).
- 2026-09-08 13:12 `pytest tests/test_attachment_insights.py -q` -> **26 passed in 24.28s** (real Postgres 5433, `LLM_PROVIDER` never used - narration driven by a stub `invoke()`).
- 2026-09-08 13:30 Final combined run: `pytest tests/test_insight_thresholds.py tests/test_insight_lifecycle.py tests/test_vendor_master.py tests/test_insight_retention.py tests/test_region_detection.py tests/test_attachment_insights.py -q` -> **89 passed in 23.76s** (real Postgres 5433).
- 2026-09-08 13:30 Neighbour regression: `pytest tests/test_chat_progress.py tests/test_chat_doc_content_branch.py tests/test_h16_answer_contract.py tests/test_answer_contract.py tests/test_full_records.py tests/test_entity_resolver.py -q` -> **1 failed, 136 passed** then **137 passed** after updating `test_v27_the_persist_side_and_the_wire_side_cannot_drift` for the new `insights` contract key (test expectation, not a product change).
- 2026-09-08 13:35 Spec §9 build note appended; tracker Feature 30 row moved to `[~]` with the done/not-done split; BE Gap 491 filed. Nothing committed - all changes are in the working tree.
- 2026-09-08 13:35 HARD STOP reached. Remaining tasks marked not-done above with reasons.
- 2026-09-08 12:10 PASS 2 START. Hard stop 19:00. Order: 30.0b, 30.0c, 30.3, 30.4, 30.5, 30.6, 30.18, 30.7, 30.8, 30.11, 30.9, 30.10, 30.13, 30.15, 30.14. Gap 492 verified in the tree first (BUBBLE_ACTIONS = note/discuss/dismiss, ALLOWED_OUTCOMES = note/dismissed/snoozed, no `invoice_id` on the transition payload, `_INSIGHT_INVOICE_STATUS` gone) - nothing in pass 2 acts on an invoice.
- 2026-09-08 12:25 30.0b done - `services/doc_linking.py` (`LinkResult`, `link_attachment()`, `vendor_link_policy()`, `confirm_link()`): Feature 26's `find_candidate_invoices()` is called, not reimplemented; the vendor master decides who the supplier is; a candidate belonging to a different confirmed vendor is rejected; tier 1 links without asking, tier 2 needs the first confirmation per vendor and auto-confirms after. `GET /chat/attachments/{id}/links` + `POST /chat/attachments/{id}/links/confirm` (Feature 26's `confirm-matches` untouched). Gap 492 asserted: confirming a link never writes `invoice`. `pytest tests/test_doc_linking.py -q` -> **11 passed in 10.09s**.
- 2026-09-08 12:50 30.0c done - REFERENCE schema widened by six additive optional fields + new `BankStatementLineItem` + `REFERENCE_DOC_FAMILY_DIRECTIVE` (BE **Gap 493**, filed and fixed in the same change); `services/bank_ledger.py` (`parse_statement_lines`, `detect_statement_date`, `land_statement_lines` (replace, carrying user-confirmed matches forward), `statement_lines_for`, `closing_balance`, `is_bank_charge`); landed from `insight_attachment()` before the cards run; `ChatAttachment.statement_date` set from the printed date, never from today. `pytest tests/test_bank_ledger.py -q` -> **15 passed in 7.12s**; `pytest tests/test_generic_extraction.py -q` -> **380 passed, 2 skipped** (pin test updated to assert A2's real rule instead of a field count).
- 2026-09-08 13:05 30.3 done - migration `a1b2c3f30002` creates `v_vendor_spend`, `v_overdue`, `v_tax_summary`, `v_3way_match`, each exposing `tenant_id` as a COLUMN (never a baked-in filter) and each excluding `deleted_at IS NOT NULL` and `status = 'DUPLICATE'`; OVERDUE is derived from `due_date`/`paid_at` at read time because no invoice ever stores that status. `alembic upgrade head` applied; smoke counts 91/7/63/55 on the dev database.
- 2026-09-08 13:05 30.4 done - `services/semantic_views.py`: `Metric`, `METRICS` (vendor_spend, overdue, tax_summary, three_way_match), `query_metric()` (raises `MetricError` without a tenant, unknown metric or undeclared filter - never an empty list, which is a legitimate answer), `_validate_registry()` at import (a metric without `:tenant_id` fails process start), `metric_definitions()`, `preferred_metric_for()` (keyword map, not a model call), `semantic_views_enabled()`. Every filter value is a bind parameter; nothing concatenates caller input into SQL. `pytest tests/test_semantic_views.py -q` -> **21 passed in 8.38s**, including view-vs-base-table equality and cross-tenant isolation with the same vendor name in two tenants.
- 2026-09-08 13:25 30.5 done - `services/bank_matching.py`: `match_statement_lines()` (R5's three CONJUNCTIVE rules - amount within `bank_amount_tolerance`, date within `bank_date_tolerance_days` of the DUE date, vendor name present in the narration - all read through `threshold()` so the per-tenant override works end to end), four verdicts (MATCHED / AMBIGUOUS / POSSIBLE_DUPLICATE / UNMATCHED), `apply_matches()` (writes `bank_statement_line` ONLY - Gap 492), `cash_cover()` (7/30/60-day buckets anchored on the STATEMENT date, never today). Cards `card_bank_reconcile` (sync) and `card_cash_cover` (async, skips itself with a stated reason at the sync stage and when there is no closing balance/date) registered for STATEMENT_OF_ACCOUNT. `pytest tests/test_bank_matching.py -q` -> **14 passed in 6.96s**, including the 6-line fixture (4 matched, 1 paid-twice, 1 unmatched), the wrong-vendor rejection, the double-payment case and the tenant override.
- 2026-09-08 13:50 30.6 done - `card_cash_impact` over `query_metric("overdue")` + `query_metric("vendor_spend")`, scoped by the vendor master's spellings (`_vendor_spellings()`); refuses to add two currencies and says so instead.
- 2026-09-08 13:50 30.18 done - seven async cards, each gated by its R9 threshold read through `threshold()` and each reporting the number it used: `card_open_po_value`, `card_cash_out_timing`, `card_over_invoicing_history` (>=3 pairs), `card_quote_drift` (>=2 invoices), `card_partial_delivery_balance`, `card_repeat_short_delivery` (>=3 challans), `card_contract_deviations` (>=2 invoices). All live in `CARDS_BY_DOC_TYPE` (one list per type, not a second registry) and each skips ITSELF at the sync stage so the sync bubble stays fast. `pytest tests/test_insight_async_cards.py -q` -> **24 passed in 9.70s**, including a tenant override moving a threshold and the 30.12 gaps card listing every skip.
- 2026-09-08 13:55 BE **Gap 494** filed and fixed in the same change: cards reading semantic views put `UUID`/`date`/`Decimal` into the block and the bubble failed at COMMIT; `jsonable()` now converts once in `build_insight_block()`. Found by this feature's own regression run, not by inspection. A stale-row test flake (select by `finding_key` without the tenant) was fixed alongside.
- 2026-09-08 14:20 30.7 done - `services/knowledge.py`: `KnowledgeDoc`, `index_knowledge()` (REFUSES a rule with no `source_url` - hard rule 8 enforced at the boundary, not by convention), `lookup_glossary()` (exact term match, never a nearest neighbour - a glossary is a lookup table), `glossary_for_narration()`, `lookup_rule()` (region + doc-type filtered, tenant collection then `knowledge_global`, every hit carrying its citation), `seed_glossary()`, `delete_knowledge()`. One collection per tenant (`knowledge_{tenant}`), structural isolation like Gap 55 and F26 E-2. `narrate_insight_block()` now fetches the tenant glossary and passes it with a note that it is vocabulary, not figures. Behind `ENABLE_KNOWLEDGE_LAYER` (default False) - off, every lookup returns nothing. `pytest tests/test_knowledge_layer.py -q` -> **11 passed in 40.39s**.
- 2026-09-08 14:45 30.8 done - migration `a1b2c3f30003` (`certified_sql_example` with a nullable `tenant_id` = global per F29 decision 5, plus `chat_correction` for 30.13); `services/certified_examples.py` (`certify_example`, `retrieve_examples` - CERTIFIED rows only, tenant's own outranking global on a tie, `suggested_questions`, `STARTER_QUESTIONS` per doc type, `seed_from_golden`); `card_suggested_questions` added to all ten document types and deliberately opens no `insight` row (a question is not a finding). Similarity is stopword-free Jaccard over content words, not `SequenceMatcher` over raw strings - the first version matched "who signed the contract?" to "how much did we spend on packaging last year?" on shared function words, caught by its own test. `pytest tests/test_certified_examples.py -q` -> **12 passed in 6.19s**.
- 2026-09-08 15:05 30.11 done - `services/rule_cards.py`: `RuleCard`, `parse_rule_card()` (hand-rolled front matter; a card missing any REQUIRED_FIELD is skipped, never half-loaded), `load_rule_cards()` (filters `status != verified` out of the display path), `CHECKS` (8 deterministic checks), `verify_checks_for()` (a card naming a missing check reports `not_applicable`, NEVER a pass; a raising check is contained), `run_compliance_checks()`. `card_compliance` added to all ten doc types: no region -> skipped with a stated reason, failures carry the citing rule id and `source_url`, and a compliance finding has NO impact amount (inventing one would be the fabrication this feature prevents).
- 2026-09-08 15:05 30.10 done - **5 cards written from sources actually fetched on 2026-09-08**: EU x3 from the European Commission VAT invoicing page (credit/debit-note reference, supplier VAT id, "reverse charge" wording) and US x2 from IRS Publication 583 (supporting documents - deliberately `verify: null` guidance, because there IS no federal content rule for a PO; document number). Each quotes its source and records `fetched_at`.
- 2026-09-08 15:05 30.9 **NOT DONE as specified - India cards are `status: unverified` skeletons with NO rule text** (hard rule 8). Every CBIC/GSTN primary source was attempted and none could be fetched from this environment: `cbic-gst.gov.in` act/rules pages are JS-driven (7 direct paths -> 404 while its homepage and unrelated PDFs load), `taxinformation.cbic.gov.in` -> SSL CERTIFICATE_VERIFY_FAILED, `gst.gov.in` -> WAF "Request Rejected", `indiacode.nic.in` / `gstcouncil.gov.in` -> no reachable CGST Act PDF. The three skeletons name their source and their `verify` check and are filtered out of every display path; the attempt log is in `knowledge/rule_cards/README.md`. Needs a founder call (fetch by hand, or accept a secondary source).
- 2026-09-08 15:05 `pytest tests/test_rule_cards.py -q` -> **51 passed in 6.89s**, including a per-file property test that a `verified` card HAS text and an `unverified` card has NONE.
- 2026-09-08 15:35 30.13 done - `chat_correction` table (migration `a1b2c3f30003`); `services/chat_corrections.py` (`record_correction` - PENDING always, `promote_to_example` - idempotent, tenant-scoped, refuses a promotion with no SQL, `reject_correction`, `correction_stats`); endpoints `POST /chat/messages/{id}/insight-feedback`, `POST /chat/corrections/{id}/promote` (behind `require_can_train` - promoting shapes every later answer), `GET /chat/corrections/stats`. Nothing is learned automatically: a correction is PENDING until a human promotes it, and promotion produces a CERTIFIED example, so the loop closes on the same gate it started from. Telemetry `insight_bubble` + `insight_feedback` (counts and outcomes ONLY - no finding text, no party name, since these land in a shared workspace) and two workbook panels ("Open insights", "Chat quality trend") in `ai_control_tower_workbook.json`. `pytest tests/test_chat_corrections.py -q` -> **16 passed in 6.84s**, including a test that every field each panel parses out of `Properties` is a field the emitter actually writes (the KQL itself cannot be executed without a Log Analytics workspace - stated, not claimed).
- 2026-09-08 16:05 30.15 done (harness only, NO live run) - `benchmarks/insight_golden.json`: 20 synthetic attachments in the spec's exact mix (4 PO, 3 quotation, 3 contract, 3 credit note, 2 debit note, 2 statement, 2 remittance, 1 delivery note) across IN/EU/US, with `expected_figures` (exact match), `expected_cards`, `expected_skip_reasons`, `expected_findings` and `must_not_contain` (the two Gap 475 regression numbers, 144800.0 and 475.2, are pinned as forbidden). `scripts/run_insight_eval.py` grades figures deterministically and narration ONLY under an opt-in `--narrate` that prints a warning; every case runs under a throwaway tenant and is cleaned up. **Deterministic run against real Postgres, no LLM: 20/20 cases passed, 21/21 figures exact (100%, target >=90%), 0 fabricated figures (target 0)** - and the database was left with 0 rows in `insight`, `bank_statement_line` and no `insight-eval/%` invoices. `pytest tests/test_insight_eval.py -q` -> **14 passed in 8.03s**.
- 2026-09-08 16:05 30.14 **NOT DONE, by its own precondition.** The spec gates the reranker on "only if Feature 29 recall instrumentation shows ranking is the limit". That instrumentation does not exist: there is no recall measurement in the repo attributing misses to ranking rather than retrieval, `ENABLE_RERANK` is documented in `config.py` as additionally gated on a Cohere spend go/no-go and on CP2, and CP2 (the 100-turn calibration) has not been run. Building it now would be a paid dependency added on a hunch, which the flag's own docstring forbids. No code was written.

## Pass 2 close-out (2026-09-08 16:50)
- **Feature 30 suite: `pytest` over all 16 F30 test files -> 278 passed in 26.54s** (real Postgres `localhost:5433/invoice_db`, no live model anywhere).
- **Full backend suite: `pytest tests/ -q --ignore=tests/us --ignore=tests/realworld_tenant` -> 6 failed, 3738 passed, 13 skipped in 168.72s.** One failure was mine and is fixed (`test_the_notes_column_is_a_migrated_column_on_a_single_head` pinned the Alembic head by NAME; now asserts one head + ancestry, which is what its docstring says it protects). The other five are environment-dependent, untouched by this change and import no F30 module -> **Gap 495** filed with evidence and options, not silently fixed.
- **Three migrations, one head:** `a1b2c3f30001` -> `a1b2c3f30002` -> `a1b2c3f30003`; `alembic heads` -> `a1b2c3f30003 (head)`.
- **Gaps filed this pass:** 493 (REFERENCE schema could not carry the fields the cards read - `card_terms_check` was structurally dead), 494 (semantic-view `UUID`/`date`/`Decimal` broke the bubble at COMMIT), 495 (five pre-existing environment-dependent suite failures + the uncollectable duplicate `run_chat_live_test.py`).
- **Nothing committed.** Everything is in the working tree.
- **Needs a founder call:** 30.9's India rule cards (every CBIC/GSTN source unreachable - fetch by hand or accept a secondary source) and Gap 495's five tests.
- 2026-09-08 17:20 Founder rulings on open items: India cards — founder will paste CBIC/GSTN wording (30.9 stays open until then); Gap 495 — five tests DELETED (AST removal, 117 passed across the four files); 30.14 — closed as not needed; commit + push authorised; FE Feature 21 build dispatched.
