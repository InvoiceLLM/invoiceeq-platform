# FE Feature 20: Invoice Builder — Clone & Edit Screen — build
Spec: Prod_Invoice_LLM/apps/invoice-fe/docs/feature_20_invoice_builder.md
Started: 2026-09-04 18:05
Hard stop: 2026-09-04 22:05
Definition of done: every task below checked; every Verification Plan item run and cited;
spec body and tracker updated; changes uncommitted.
Status: code-complete, partially verified — ends uncommitted. 16/16 + 10/10 Playwright, tsc exit 0. Live end-to-end still owed (blocked on BE Feature 17). Nothing committed, pushed, merged or stashed.

- [x] 20.1 Proxy routes (`build-defaults`, `build/preview`, `build`) and `types/invoice.ts` shapes. — done 2026-09-04: `app/api/outbound-invoices/[id]/build-defaults/route.ts` + `build/route.ts` via `proxyJson` (statuses pass through), `build/preview/route.ts` streams the body like the invoice-PDF route so `application/pdf` survives; `types/invoice.ts` new (`BuildItem`/`BuildRequest`/`BuildDefaults`/`BuildResponse`).
- [x] 20.2 `lib/invoiceBuilderMath.ts` + unit test against the same cases as BE 17.1. — done 2026-09-04: `computeTotals`/`roundHalfUp`/`toNumber`; `e2e/invoice-builder-math.spec.ts` **10/10 passed (16.1s)**, incl. `[(3,19.99),(1,0.005)]` → `[59.97, 0.01]`, subtotal `59.98`.
- [x] 20.3 `BuilderForm`, `LineItemGrid` (add/remove, layout pill), `OutboundBuilderPage`. — done 2026-09-04 (found already written by the killed run; re-read in full against File Coordinates and verified, not rewritten): `components/builder/BuilderForm.tsx` (190 ln, revert-to-source strip, 409 shown on the number field), `components/builder/LineItemGrid.tsx` (215 ln, add/remove + `predictRenderMode()` layout pill + totals), `app/invoices/outbound-builder/page.tsx` (279 ln). `tsc --noEmit` exit 0.
- [x] 20.4 `BuilderPreview` with the 422 / 409 handling. — done 2026-09-04 (also pre-existing; verified complete): `components/builder/BuilderPreview.tsx` branches on content-type first, 409 → `onDuplicateNumber`, 422 → `onUnlocatedFields`, object URL revoked on swap/unmount. The `PdfViewerCanvas` `srcUrl` prop edit the killed run died inside **was in fact complete** (optional `invoiceId`, `srcUrl ?? /api/invoices/{id}/pdf`) — `tsc --noEmit` exit 0 confirms no half-written prop.
- [x] 20.5 Entry points on the outbound review page and `OutboundInvoicesTable`, plus the "cloned from" link. — done 2026-09-04: `canCloneSource()`/`CLONE_ELIGIBLE_STATUSES` added to `types/invoice.ts` so both entry points share one D4 rule; "New invoice from this" header action on `app/invoices/outbound-review/[id]/page.tsx`; `FilePlus2` row action + `GitBranch` "Cloned from" link (on `source_invoice_id`) in `components/dashboard/OutboundInvoicesTable.tsx`. `node node_modules/typescript/bin/tsc --noEmit` exit 0.
- [x] 20.6 `e2e/outbound-builder.spec.ts`. — done 2026-09-04: 16 tests (prefill, totals, layout pill both directions, PDF preview into a blob URL, stale-preview marker, 409 on preview and on create, 422 + revert, create body assertions incl. "no totals sent", build-defaults 409/missing-source, table Clone eligibility + "Cloned from" link, review-header eligibility across VERIFIED/SENT/PAID/NEEDS_REVIEW). **`npx playwright test e2e/outbound-builder.spec.ts` → 16 passed (1.4m)**; `e2e/invoice-builder-math.spec.ts` re-run → **10 passed (13.0s)**. All `/api/**` stubbed — no live BE run (BE Feature 17 not landed).
- [x] 20.7 Doc + tracker close-out — done 2026-09-04: spec `feature_20_invoice_builder.md` gained a "Built (2026-09-04)" section, two File-Coordinates rows for files touched but not originally listed (`PdfViewerCanvas`, `canCloneSource` in `types/invoice.ts`), all 6 task boxes ticked, and a Verification Plan rewritten with a Result column carrying the real run output. `fe_features_tracker.md` Feature 20 row → **`[~]`, not `[x]`** — the `done` gate's item 2 fails: the live end-to-end row was not run because BE Feature 17 has not landed. Two gaps filed: **FE Gap 456** (closed — `.gitignore build/` swallowed the `build/` proxy routes) and **FE Gap 457** (open — the post-create `/ingestion?tab=sending&builtInvoice=...` redirect is inert; fix proposed but `app/ingestion/page.tsx` is owned by the concurrent Feature 19 build).
