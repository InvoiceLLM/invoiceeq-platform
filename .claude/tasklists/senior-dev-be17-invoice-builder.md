# BE Feature 17: Invoice Builder — Clone & Edit — build
Spec: Prod_Invoice_LLM/apps/invoice-be/docs/feature_17_invoice_builder.md (Solution section, 2026-09-04)
Started: 2026-09-04 18:05
Hard stop: 2026-09-04 22:05
Definition of done: every task below checked; every Verification Plan item run and cited;
spec body and tracker updated; changes uncommitted.
Status: COMPLETE (code + Postgres verification) 2026-09-04

- [x] 17.1 `services/invoice_builder.py`: `BuildRequest`/`BuildItem`/`Totals`, `compute_totals`, `next_invoice_number`, `default_build_from_source`, `plan_substitutions`. Pure; unit-tested. — done 2026-09-04: module written; compute_totals([(3,19.99),(1,0.005)]) → [59.97, 0.01] subtotal 59.98, next_invoice_number INV-0099→INV-0100 / 2026/07→2026/08 / ACME→None verified in-process. Formal unit tests land in 17.9.
- [x] 17.2 `services/pdf_substitute.py`: `format_like`, `locate_field`, `substitute`. Unit-tested against fixture PDFs. — done 2026-09-04: token-anchored location (substring matching for numbers rewrote the wrong cell), inset redaction rect (full-height rect deleted the next line). Round-trips us_style/eu_style/date_twice: EU keeps 1.500,00 and dd.mm.yyyy, date_twice replaces the header date only.
- [x] 17.2b `services/pdf_render.py`: `harvest_branding`, `render_invoice`; `plan_render_mode` in `invoice_builder.py` (D3 hybrid). — done 2026-09-04: raster fixture yields a 240x80 logo, vector fixture yields none without raising; a 40-row EU request renders 2 pages with the totals block on the last and every figure in `1.234,56` style. Header harvest drops the source's own invoice metadata (it was reprinting the old invoice number).
- [x] 17.3 Migration + `models.py` columns `source_invoice_id`, `builder_intent`. — done 2026-09-04: `a1b2c3d4e5f8_add_invoice_builder_columns.py` on head `f6a1b2c3d4e5` (single head confirmed via ScriptDirectory.get_heads()); `alembic upgrade head` → `downgrade -1` → `upgrade head` all clean on real Postgres; columns land as `builder_intent jsonb NULL` / `source_invoice_id uuid NULL` with index `ix_invoice_tenant_source_invoice_id`.
- [x] 17.4 Factor `_store_and_enqueue_outbound()` out of `upload_outbound_invoice()`; `tests/test_outbound_ingestion.py` must pass unchanged before new endpoints are added. — done 2026-09-04: helper extracted with Feature 28's normalisation left in place above it; `pytest tests/test_outbound_ingestion.py -q` on Postgres → 14 passed (file untouched by me, `git diff` shows only Feature 28's edits), and `tests/test_invoice_upload_formats.py -q` → 22 passed.
- [x] 17.5 `GET /outbound-invoices/{id}/build-defaults`, `POST /outbound-invoices/build/preview`, `POST /outbound-invoices/build`; source-eligibility rules; 422 unlocated-fields contract; `_assert_invoice_number_unused()` (D5); render-mode dispatch. — done 2026-09-04: all three routes live (`/openapi.json` confirms the paths); 422 body is `{"unlocated_fields": [...]}` at the top level via JSONResponse, not nested under `detail`. Postgres tests cover 404/409/200, preview-persists-nothing, build creates the row + charges one unit, D5 409 before any render.
- [x] 17.6 `utils/verification_tools.py::verify_builder_readback` + hook in `handle_process_outbound_invoice()`. — done 2026-09-04: pure comparator (text/date whitespace-normalised, money and line amounts at 0.01) plus the worker hook that appends `builder_render_mismatch` and forces NEEDS_REVIEW; inert when `builder_intent` is NULL. Test evidence in 17.9.
- [x] 17.7 `list_outbound_invoices()` gains `source_invoice_id`. — done 2026-09-04: one extra key on the hand-built row dict, no filter change. Test evidence in 17.9.
- [x] 17.8 Fixtures under `tests/fixtures/invoice_builder/`: US `1,250.00`, EU `1.250,00`, date-printed-twice, raster-logo source, vector-text-only source; each with stored `coordinates` JSON. — done 2026-09-04: `tests/fixtures/invoice_builder/` holds 5 PDFs + 5 JSON sidecars (source field values + 7 DI polygons each, 0-100 page percentages as Gap 330 stores them), regenerated by `_generate.py`.
- [x] 17.9 `tests/test_invoice_builder.py` — done 2026-09-04: 53 tests, own tenant + auth-dependency overrides (incl. `get_tenant_context`, which the outbound list depends on). `pytest tests/test_invoice_builder.py -q` on Postgres → **53 passed in 11.22s**.
- [x] 17.10 Doc + tracker close-out — done 2026-09-04: spec gained a "Built (2026-09-04)" section (4 named deviations + the FE-contract table) and both Verification Plan tables now carry a Result column with the real run lines. Tracker row set to **`[~]`, not `[x]`** — the two "Manual, Azure path" rows were not run (no dev-stack session), per the `done` skill's rule against rounding up. BE Gap 459 filed and closed (reportlab was a dev-only dependency but is imported at runtime by the re-render path).

**Final status (2026-09-04):** all 10 tasks done, changes uncommitted.
Postgres runs: `tests/test_invoice_builder.py` **53 passed in 11.22s**;
`test_invoice_builder + test_outbound_ingestion + test_outbound_extraction + test_outbound_dashboard`
**95 passed in 13.14s**; full suite **43 failed, 3080 passed, 3 skipped, 5 deselected in 223.44s**
— same 43 failures as the Feature 28 baseline, in the same six untouched files, with
passes up by exactly the 53 tests this build added. (The full run needs
`--ignore=tests/us/run_chat_live_test.py`: that file shares a basename with
`tests/realworld_tenant/run_chat_live_test.py` and collection aborts on the clash.
Pre-existing, both files tracked, neither touched here.)
Open: the spec's two "Manual, Azure path" rows — not run, not claimed.
