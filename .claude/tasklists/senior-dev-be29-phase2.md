# Feature 29: LLM Optimisation — phase 2 tasklist (remaining work + open items)
Spec: Prod_Invoice_LLM/apps/invoice-be/docs/feature_29_llm_optimisation.md
Parent tasklist: senior-dev-be29-llm-optimisation.md (runs 1–2, Tracks A–C; closed 2026-09-07 00:43)
Created: 2026-09-07
Started: 2026-09-07 10:25
Hard stop: 4 h per phase (founder gave none; tasklist default). Phase 0 stop 2026-09-07 14:25; Phase 1 stop 4 h after Phase 0 closes.
Outage: build agent lost ~12:00-16:10 to an API session limit (HTTP 429, reset 15:00) mid-P0.2. Phase 0 stop shifted to 2026-09-07 17:00; Phase 1 + CP1 stop 4 h after Phase 0 closes. No work was lost; P0.2's 69 failures were classified, the write-up was in progress.
Approval (founder, 2026-09-07, this conversation): "approved to start the tasklist and send email update after each phase is closed at sbanerji@admsofttech.com."
Email rule: one email per closed phase to sbanerji@admsofttech.com via `scratchpad/phase_mail.py` (sender invoice@notify.invoicellm.admsofttech.com, key from Key Vault in-process). No interval emails.
Assumptions taken at start (founder did not rule the five calls below): P0.3 (Gap 474) stays untouched until an explicit go; Terra dev deployment is created at CP1 only (pay-per-token, reversible) and deleted if the 2-pt rule says so; P2.7 second rater still open; Cohere decided after CP2.
Definition of done (founder, 2026-09-07 18:16 — "once luna and terra is compared and finalised we will code accordingly and mark feature completed, i dont want your nonsense testing and baselining"): **Luna vs Terra on 5 long-doc cases → set the long_doc role + length switch, delete the losing deployment → P2.1 entity resolver (unit tests only) → 29.20 rollout plan → close.**
Status: **CLOSED 2026-09-07** — 29.10 decided (Luna kept, Terra deleted, long_doc role removed, Gap 489), P2.1 done (Gap 490), P4.1 done (spec §10.1), P4.2 done (tracker row [x]). All changes uncommitted.

Method (agreed in chat 2026-09-07): deterministic tests per task (mocked LLM, real Postgres `localhost:5433/invoice_db`);
live LLM evals only at the three checkpoints below. Every capability behind its own flag, default off, so one live run
attributes a regression to a capability without re-running. Live answers are recorded once and re-judged offline.

Legend — **Code**: S small (<1 file-day) / M medium / L large. **Live**: which live eval the item needs, if any.
**Founder**: decision the founder must make before or during the item.

---

## Phase 0 — close the open items (no new capability)

| id | item | what is done | code | deterministic tests | live eval | infra | depends on | founder | status |
|---|---|---|---|---|---|---|---|---|---|
| P0.1 | 29.5 / 29.9 live number | Finish the in-flight 36-case golden run on the fixed judge (`runs/f29-golden-20260907/`); κ + pass % into tracker Gap 479 "Re-score results", spec §2.3, tasklist 29.5/29.9 → `[x]` if the answer-contract abstain cases hold | – | – | golden 36 (running) | – | – | commit run artefacts (needs the word) | `[x]` kappa 0.852 (gate PASSES), pass 72.2%; 29.5 ticked, 29.9 stays [~] (Gap 480 + Gap 474)|
| P0.2 | Gap 477 stale test baseline | Re-run the full backend suite once on real Postgres with the live Azure env; classify the 43 failures: real regression / live-endpoint test needing a mock / stale expectation; file the list in the Gap, fix only the regressions | S | full suite | – | – | endpoint quiet window (throttling killed the last attempt) | which stale tests to delete vs re-baseline | `[x]` 69 failed -> 5 failed / 3450 passed, 177s (was 268s); 24 mocked, 38 re-baselined, 5 env DEFERRED (token budget); 5 own regressions found+fixed. Gap 477 [~]|
| P0.3 | Gap 474 provenance to browser | Widen `routers/chat.py::MessageResponse` with `provenance` / `abstain` / `amount_owed` / `contract_terms` keys (additive, optional); FE renders provenance line under the answer (FE gap to file) | S BE + S FE | contract test on `MessageResponse`; FE component test | – | – | – | **go / no-go**: widen the response model (active-work.md lists it as an open contradiction) | `[x]` provenance + abstention added (only the 2 keys the agent really emits); 7 passed incl. a real-Postgres JSON round trip; BE Gap 474 closed, FE Gap 470 filed|
| P0.4 | Registry env wiring | Add `AZURE_OPENAI_CHAT_SUMMARY_DEPLOYMENT_NAME` and `AZURE_OPENAI_LONG_DOC_DEPLOYMENT_NAME` params to `invoice-be.bicep`, `queue-worker.bicep`, `scheduled-job.bicep`, `params.dev.json` (chat_summary = `gpt-5-mini`; long_doc empty until 29.10) so the two roles are explicit instead of fallback-derived | S | `test_model_registry` role resolution from env | – | 3 bicep modules + params | – | – | `[x]` bicep+params wired, az bicep build exit 0 x4, 49 passed (16 new); Gap 481. NOT deployed|
| P0.5 | Merge probe into golden | Convert the 16-turn `attach_chat_eval.py` probe into golden cases with `required` / `forbidden` facts so one live run covers attachments; probe script kept for ad-hoc use | S | facts-file integrity tests (existing) | – | – | – | – | `[x]` 16 cases + facts + Gap 483 loader; CASES now 57; live smoke passed (attach_a1 correct). Gaps 483/486|
| P0.6 | Capability flags | Add `ENABLE_ENTITY_RESOLVER`, `ENABLE_SEMANTIC_VIEWS`, `ENABLE_CERTIFIED_EXAMPLES`, `ENABLE_KNOWLEDGE_LAYER`, `ENABLE_RERANK` to `config.py` (default False) and bicep env; every golden turn record lists active flags + route taken | S | settings default tests; turn-record schema test | – | 3 bicep modules + params | – | – | `[x]` 5 flags default False + bicep/params + turn record route/capability_flags; 36 passed; Gap 482|

## Phase 1 — honest baseline (Track A/C leftovers) → **Live checkpoint 1**

| id | item | what is done | code | deterministic tests | live eval | infra | depends on | founder | status |
|---|---|---|---|---|---|---|---|---|---|
| P1.1 | 29.4a `--fresh-tenants` | `run_agent_eval.py` creates a tenant per run and seeds it, so cache and prior answers cannot leak between runs | S | Postgres: tenant created, seeded rows counted, torn down | – | – | – | – | `[x]` fresh_tenant_map + --fresh-tenants; 13 passed on real Postgres. Scope stated honestly: cache leak is already stubbed 3x, this isolates the persist path. Gap 484|
| P1.2 | 29.4b SQL eval denominator | SQL eval reports n = all cases; timeout / error = wrong; `sql_pass_pct` over the fixed n | S | unit: timeout counted as fail | – | – | – | – | `[x]` denominator = case set, timeout=wrong, n/sql_pass_pct reported; ALSO found the deterministic grader was dead code and the judge was grading it. Gap 484|
| P1.3 | 29.4c extraction report | Report lists every ground-truth field with per-field accuracy; composite stays the headline | S | unit on report writer | – | – | – | – | `[x]` field_accuracy_by_field + worst-first table in md and console; composite unchanged as headline; 124 passed. Gap 485|
| P1.4 | 29.10a long-doc fixtures | Author 5 fixtures: multi-page contract, 3-page statement, 2 long POs, long delivery note; ground truth + facts | authoring | fixture parse test | – | – | – | spot-check ground truth | `[x]` 5 multi-page fixtures (4/3/3/3/3 pages), fact buried off page 1, ground truth DERIVED not transcribed; 30 passed. FOUNDER SPOT-CHECK OWED. Gap 486|
| P1.5 | 29.10b `tests/golden_long_doc.json` | 5 golden cases on the attachment branches | S | facts integrity | – | – | P1.4 | – | `[x]` tests/golden_long_doc.json GENERATED from the fixture module, 5 cases with required/forbidden; execution still blocked on Gap 483 attachment seeding. Gap 486|
| **CP1** | **Live checkpoint 1 — REMOVED by founder 2026-09-07 18:12 ("stop luna 57 case run", "remove it from tasklist")** | Luna 57-case run killed at case ~52. Standing numbers: 36-case interim 75.0% pass, SQL 66.7% (n=39); Terra long-doc 3/3 on gradable cases (`runs/f29-terra-longdoc-20260907/`). No further live Luna run in Feature 29. | – | – | none | – | – | – | `[-]` removed |

## Phase 2 — deterministic grounding (Track D, part 1) → **Live checkpoint 2**

| id | item | what is done | code | deterministic tests | live eval | infra | depends on | founder | status |
|---|---|---|---|---|---|---|---|---|---|
| P2.1 | 29.11 entity resolver | `agents/entity_resolver.py::resolve_entities(question, tenant, attachments)` → invoice / vendor / attachment ids from number, name, ordinal, "this", "the attached"; 0 or >1 match → clarify card listing candidates; keyword lists in `query_agent.py` demoted to fallback behind the flag | M | new `tests/test_entity_resolver.py` on Postgres: exact, fuzzy vendor, ordinal, ambiguous → clarify, none → clarify | – | – | P0.6 | – | `[x]` |
| P2.2 | 29.14a semantic views | Add-only Alembic migration: `v_vendor_spend`, `v_overdue`, `v_tax_summary`, `v_3way_match`; each with `tenant_id` column | M | migration up on Postgres; view row counts vs base tables | – | migration runs at deploy | – | – | `[→30]` moved to Feature 30 as 30.3 (was 29.14a)|
| P2.3 | 29.14b `METRICS` + `query_metric()` | Metric registry (name → view, columns, filters); parameterised `WHERE tenant_id = :t` enforced by the AST guard; schema link prefers a metric over raw tables | M | unit: guard rejects a query without tenant param; metric SQL round-trips | – | – | P2.2 | – | `[→30]` moved to Feature 30 as 30.4 (was 29.14b)|
| P2.4 | 29.15a certified examples table | Migration: `certified_sql_example(id, tenant_id nullable, question, sql, certified_by, certified_at)`; `certify_example()` | S | Postgres CRUD | – | migration | – | – | `[→30]` moved to Feature 30 as 30.8 (was 29.15)|
| P2.5 | 29.15b retrieval | Chroma index of certified rows; `retrieve_examples(question, k=5)` certified-only; replaces the static examples tail in the SQL prompt behind the flag; seeded from every golden case whose SQL is verified | M | unit: uncertified row never retrieved; seeding count | – | – | P2.4 | – | `[→30]` moved to Feature 30 as 30.8 (was 29.15)|
| P2.6 | 29.16 knowledge layer | `knowledge_{tenant}` Chroma collection, `KnowledgeDoc`, `index_knowledge()`, `lookup_glossary()`, `lookup_rule()`; glossary seeded (total / grand total / net / tax / due) | M | unit: glossary lookup resolves "total" → grand_total; tenant isolation | – | – | – | – | `[→30]` moved to Feature 30 as 30.7 (was 29.16)|
| P2.7 | REMOVED by founder 2026-09-07 18:16 ("i dont want your nonsense testing and baselining") | 100-turn golden set dropped | – | – | none | – | – | – | `[-]` removed 2026-09-07 (founder: "i dont want your nonsense testing and baselining")|
| **CP2** | **REMOVED by founder 2026-09-07 18:16** | no further live run in Feature 29 | – | – | none | – | – | – | `[-]` removed 2026-09-07 (founder: "i dont want your nonsense testing and baselining")|

## Phase 3 — generic layer and rules (Track D, part 2) → **Live checkpoint 3**

| id | item | what is done | code | deterministic tests | live eval | infra | depends on | founder | status |
|---|---|---|---|---|---|---|---|---|---|
| P3.1 | 29.13a capability registry | `CAPABILITIES` dict: name → input schema, deterministic handler, evidence type; every existing branch (sql, rag, compare, reconcile, amount_owed, contract_terms, line_arithmetic, clarify) registered | M | unit: every capability has a handler and a schema; registry is closed | – | – | P2.1 | – | `[→30]` moved to Feature 30 as 30.16, deferred (was 29.13a)|
| P3.2 | 29.13b `plan_turn()` | One fast-model call → typed plan validated against the registry; invalid plan → fallback to today's router; `ENABLE_CHAT_PLANNER` default off; attachment branches first, then all routes | L | unit with mocked planner output: valid plan dispatches, invalid plan falls back, unknown capability rejected | – | flag in bicep + params | P3.1, CP2 κ ≥ 0.6 | flag flip to on in dev | `[→30]` moved to Feature 30 as 30.16, deferred (was 29.13b)|
| P3.3 | 29.17a India rule cards | Cards from fetched CBIC/GSTN primary sources only (GST rates, RCM, IRN, ITC, TDS 194Q, e-way bill): `source_url`, `effective_from`, `owner = founder`, `review_at`; indexed via P2.6 | authoring + S | unit: every card has all four fields and a reachable `source_url` | – | – | P2.6 | spot-check each card | `[→30]` moved to Feature 30 as 30.9 (was 29.17a)|
| P3.4 | 29.17b EU + US cards | Same for EU VAT / reverse charge / OSS and US sales tax nexus basics | authoring | same | – | – | P3.3 | spot-check | `[→30]` moved to Feature 30 as 30.10 (was 29.17b)|
| P3.5 | 29.17c compliance `verify` | Deterministic checks a rule card can trigger (e.g. RCM invoice must carry the RCM flag; IRN present for B2B above threshold); results narrated, never computed by the model | M | unit per check on Postgres fixtures | – | – | P3.3 | – | `[→30]` moved to Feature 30 as 30.11 (was 29.17c)|
| P3.6 | 29.18 reranker | Cohere rerank-v4 between bge-m3 and the LLM behind `ENABLE_RERANK`; **only if** CP2 recall@k shows ranking, not retrieval, is the limit | S | unit with mocked rerank; flag off path unchanged | – | Cohere key in Key Vault `kv-invoicellm-dev`; env var; cost line in workbook | CP2 recall numbers | go / no-go on the Cohere spend | `[→30]` moved to Feature 30 as 30.14 (was 29.18)|
| P3.7 | 29.19a flywheel table | Migration `chat_correction(id, tenant_id, message_id, corrected_answer, corrected_sql, promoted_example_id)`; `record_correction()` endpoint on thumbs-down; `promote_to_example()` → P2.4 | M BE + S FE | endpoint tests on Postgres; promotion creates a certified row | – | migration | P2.4 | – | `[→30]` moved to Feature 30 as 30.13 (was 29.19)|
| P3.8 | 29.19b workbook panel | "Chat quality trend" (pass %, abstain %, bucket counts by week) from `agent_eval_summary` in `ai_control_tower_workbook.json` | S | KQL query dry-run | – | redeploy `workbook-ai-control-tower-only.bicep` | – | – | `[→30]` moved to Feature 30 as 30.13 (was 29.19)|
| **CP3** | **Live checkpoint 3 — final** | 100-turn golden with all flags on, planner on for attachments first then all routes; compliance cases graded; number recorded for 29.20 | – | – | **100 golden, planner on** | – | P3.* | accept the final number | `[→30]` moved to Feature 30 as 30.16, deferred — Feature 29 now closes at CP2|

## Phase 4 — rollout

| id | item | what is done | code | deterministic tests | live eval | infra | depends on | founder | status |
|---|---|---|---|---|---|---|---|---|---|
| P4.1 | 29.20 rollout plan | Written plan dev → soak (7 days, nightly eval ≥ CP3 number − 2 pts) → prod; `params.prod.json` diff listed, not applied; prod OpenAI deployments listed (Luna, gpt-5-mini, Terra if kept) | doc | – | – | none applied | CP3 | approve the plan | `[x]` |
| P4.2 | Tracker + spec close | Feature 29 row `[x]`; every Gap 470–479 ticked with evidence; spec §2.3 numbers table complete | doc | – | – | – | P4.1 | – | `[x]` |

---

## Live evals — total for the phase

| checkpoint | turns | models | why it cannot be skipped |
|---|---|---|---|
| **CP1** | **Live checkpoint 1 — REMOVED by founder 2026-09-07 18:12 ("stop luna 57 case run", "remove it from tasklist")** | Luna 57-case run killed at case ~52. Standing numbers: 36-case interim 75.0% pass, SQL 66.7% (n=39); Terra long-doc 3/3 on gradable cases (`runs/f29-terra-longdoc-20260907/`). No further live Luna run in Feature 29. | – | – | none | – | – | – | `[-]` removed |
| **CP2** | **REMOVED by founder 2026-09-07 18:16** | no further live run in Feature 29 | – | – | none | – | – | – | `[-]` removed |
| CP3 | 100 golden + 16 compliance + SQL eval | Luna, gpt-5-mini judge | Final number for rollout |

Everything else is `pytest` per task with `LLM_PROVIDER=mock` on real Postgres. No live call inside a task.

## Founder calls needed before starting

1. Approve this phase plan and give a hard stop per phase (default 4 h per phase run).
2. Gap 474: widen `MessageResponse` (P0.3) — yes / no.
3. Name the second rater for P2.7.
4. Terra: create the dev deployment for CP1 (cost for the run only), decide keep/delete after.
5. Cohere: go / no-go on P3.6 spend, decided after CP2.

## Log
- 2026-09-07 18:18 P2.7 and CP2 REMOVED, definition of done rewritten per founder 18:16.
- 2026-09-07 18:13 CP1 REMOVED by founder order; Luna 57-case run killed (pids 9596/28100). Terra 5-case pass run by main session: 3/3 correct on the gradable cases, 2 harness failures (statement seeding, delivery-note routing).
- 2026-09-07 — tasklist created from the phase-2 discussion; nothing started.
- 2026-09-07 10:35 P0.1 DONE. Live golden run finished (36 turns, 02:47-10:26). kappa 0.151 -> 0.852, agreement 94.4%, gate >=0.6 PASSES, 0 false passes, 2 false fails. Pass 22.2% -> 72.2%, accuracy 0.757, faithfulness 0.891, errors 0. Gap 478 proved fixed live (evidence_source=app 31/36, FULL INVOICE RECORD in graded context 22/36 vs 0/36). Tracker Gap 479 CLOSED [x]; Gap 480 filed (abstain vs faithfulness-0 floor, investigation only). Spec 2.3 row added. Parent 29.5 -> [x], 29.9 stays [~].
- 2026-09-07 10:44 P0.4 DONE. Both role env vars threaded through invoice-be/queue-worker/scheduled-job bicep + 08-apps (all 6 module sites) + params.dev (chat_summary=gpt-5-mini, long_doc empty) + params.prod (both empty). az bicep build exit 0 on all 4 touched files. tests/test_model_role_env_wiring.py new (16 tests) -> 49 passed in 7.11s with test_model_registry. Gap 481 filed [x]. Not deployed - push is the founder's.
- 2026-09-07 10:57 P0.6 DONE. ENABLE_ENTITY_RESOLVER/SEMANTIC_VIEWS/CERTIFIED_EXAMPLES/KNOWLEDGE_LAYER/RERANK on Settings (all default False), 5 bool params + env on all 3 compute modules, threaded from 08-apps to backendApp/queueWorker/benchmarkEvalJob, both params files pin false. run_agent_eval turn record gains route + capability_flags. az bicep build exit 0 x4. tests/test_capability_flags.py new -> 36 passed in 8.54s. Regression: 1 failed 148 passed, the failure being the standing 21-vs-20 golden-case baseline, untouched. Gap 482 [x].
- 2026-09-07 11:45 P0.5 PARTIAL. ATTACHMENT_CASES (16, attach_a1..attach_b7) + GoldenCase.attachment_keys/attachment_intent + 16 facts (37->53 keys). Kept OFF default CASES (still 36) so CP1's baseline is not corrupted by 12 turns that would run with no attachment. attach_chat_eval.py unchanged. tests/test_attachment_golden_cases.py new -> 46 passed in 7.25s incl. a turn-for-turn drift guard against the probe SESSIONS. Widened test_facts_file_names_only_real_cases to CASES|ATTACHMENT_CASES (intent unchanged, reason in the test body). Regression 1 failed 194 passed = standing 21-vs-20 baseline only. OPEN: harness cannot seed attachment documents so the 12 attachment turns are not executable; same missing piece as P1.5; founder call.
- 2026-09-07 11:47 P0.3 NOT STARTED by instruction: Gap 474 is not approved. routers/chat.py::MessageResponse deliberately untouched; 29.9 therefore stays [~] in the parent tasklist.
- 2026-09-07 16:18 P0.2 DONE. Full suite on real Postgres (localhost:5433/invoice_db) with the live Azure env: 69 failed, 3319 passed, 13 skipped, 5 deselected in 267.93s. FIRST FINDING: plain 'pytest tests' aborts in collection - tests/us/run_chat_live_test.py and tests/realworld_tenant/run_chat_live_test.py share a basename and both match pytest's *_test.py glob though neither is a unit test; zero tests ran until they were --ignore'd, which is how a 43-failure baseline survived unmeasured. Classification: 26 live-endpoint-needs-mock (Gap 471 shape: chat_sql_quality 18, chat_doc_content_branch 6, chat_progress 2), 38 stale-expectation (generic_extraction 27 asserting the flag is False after 1228edf pinned it True, ops_recommendation 8 KeyError tileSettings, query_tools 1 + a3_streaming 1 from 29.5, agent_eval 1 standing 21-vs-20), 5 environment/fixture (connectors 1, autopilot 2, workflow drive/email 2). REGRESSIONS: 0 - nothing traces to phase-2 work; every changed file re-run green. No fix applied; 5 founder calls listed in Gap 477.
- 2026-09-07 16:16 PHASE 0 EMAIL SENT to sbanerji@admsofttech.com via phase_mail.py -> **HTTP 202**. Subject "[InvoiceLLM] Feature 29 phase P0 closed". Body: the 22.2%->72.2% / kappa 0.151->0.852 result, P0.5 partial (harness cannot seed attachment documents), P0.3 not approved, the 69-failure classification with 0 regressions, and the founder calls due.
- 2026-09-07 16:35 P1.1 DONE. fresh_tenant_map() + --fresh-tenants: one uuid4 per logical tenant, seeds under them, re-points cases via dataclasses.replace, prints the mapping; flag off = byte-identical default path. Recorded honestly that the answer-cache leak the task names is ALREADY closed 3x (in-memory SQLite per run, get_cached_answer stubbed, Chroma stubbed) and that this isolates the persist path and future un-stubbed runs instead.
- 2026-09-07 16:36 P1.2 DONE. Found a second, larger defect: expected_invoice_numbers was never written to a turn record, so digest_eval's deterministic 'set(expected) <= set(fetched)' branch had NEVER executed and every published SQL exec-correct % (spec 2.1 row c: 32.3/40.7/44.4/28.0/44.4) came from the judge's accuracy_score >= 0.8 - a model grading a metric hard rule 3 requires to be deterministic, and that model was the kappa-0.151 judge. Fixed both: ground truth now recorded per turn with () vs None kept distinct; denominator is the case set with errored turns graded as wrong; payload gains n, sql_pass_pct, errored, no_statement, graded_by. 13 passed on real Postgres; regression 1 failed 230 passed (standing 21-vs-20 only).
- 2026-09-07 16:44 P1.3 DONE. field_accuracy_by_field() + normalise_field_name() collapsing items[i]. to items[]. so a field has a real sample size; summary, markdown report and console all list every ground-truth field worst-first; composite untouched and still the headline. tests/test_extraction_field_report.py new (9) -> 124 passed, 1 skipped with test_extraction_benchmark. Gap 485 [x].
- 2026-09-07 17:05 P1.4 DONE. benchmarks/long_doc_fixtures.py: 4-page MSA (decoy 2.0% in cl.12.1 before the real 1.5% in cl.27.4), 3-page 70-line statement (unmatched line 62), two 3-page POs (60-line with outlier at 47; 55-line with the delivery term as the last footer line), 3-page 70-line delivery note (shortfalls at 11/29/63). Real paginated PDFs 3.9-6.8 KB vs the probe's ~2 KB. Ground truth derived from the same data that renders the PDF. 30 passed.
- 2026-09-07 17:07 P1.5 DONE. tests/golden_long_doc.json generated (not hand-written) from benchmarks/long_doc_fixtures.py: 5 cases, prose required/forbidden facts, _generated_from + _founder_spot_check recorded in the file. Transcription guard test asserts every headline figure equals the fixture module's own. NOTE: these run on the attachment branches, so execution is blocked on the same missing harness seeding as P0.5 (Gap 483).
- 2026-09-07 17:05 RE-SCOPE (founder, ~16:50): Feature 29 split. Tasks 29.14-29.19 move verbatim to new BE **Feature 30 "Business Intelligence"** (temporary insight block posted into chat when a NON-invoice financial document -- PO, quotation, contract, credit/debit note, statement, remittance, delivery note -- is attached in the chat area). Rows P2.2-P2.6 and P3.3-P3.8 marked `[->30]` with their 30.x ids. **Kept in Feature 29:** P2.1 (29.11 entity resolver), P2.7 (100-turn set), CP2, P3.1-P3.2 (29.13 planner), P4.x -- 29.11 and 29.13 stay because they are what make attachment chat generic. CP2's live eval is now "100 golden + SQL eval, flags: entity resolver only"; CP3's is "100 golden, planner on". Tracker gained a `[ ]` Feature 30 row under Feature 29; spec 29 gained an appended "Scope note - split to Feature 30" (nothing rewritten). The four flags for the moved tasks stay in config.py and bicep, default False -- they are Feature 30's switches now. `feature_30_business_intelligence.md` and the FE spec were NOT edited.
- 2026-09-07 17:12 Corrected the 30.x ids on the moved rows against Feature 30 section 5's own "was" column, which is authoritative: 29.15 -> **30.8** (both P2.4 and P2.5), 29.16 -> **30.7**, 29.17a/b/c -> **30.9/30.10/30.11**, 29.18 -> **30.14**, 29.19 -> **30.13** (both P3.7 and P3.8). My first pass had read the founder's id list positionally and mis-assigned six rows; the set of ids was right, the pairing was not.
- 2026-09-07 17:24 P0.3 DONE (founder: "Yes, widen it now"). `MessageResponse` gained `provenance` and `abstention`, both Optional/None, both added to `ATTACHMENT_CONTRACT_KEYS` so persist and serialise cannot drift. **Only those two** — checked against the agent, not the brief: `amount_owed`/`contract_terms` are nested in the comparison payloads and already reached the browser, and `line_arithmetic`/`abstain_reason` are not top-level result keys at all. 7 passed incl. a real-Postgres JSON round trip of both nested shapes; 81 passed on the chat regression set. BE Gap 474 `[x]`; **FE Gap 470** filed for rendering (no FE code written). **29.9 still stays `[~]`** — Gap 480 (a correct abstention scores faithfulness 0.0 and fails) is the other half of that task's measurement and is still open.
- 2026-09-07 17:26 RE-SCOPE 2 (founder 17:12): (1) decision 4 is now **one rater, the founder, 100 turns, κ ≥ 0.6** — spec §11 amended by appended note, P2.7 rewritten, CP2's founder column updated. (2) **29.13 leaves Feature 29** — P3.1, P3.2 and CP3 marked `[→30]` as Feature 30 task 30.16 (deferred). 29.11 stays. Definition-of-done line added to this header.
- 2026-09-07 18:20 P0.5 UPGRADED to [x]. Gap 483 loader built (founder: 'Fixture loader in the harness'): build_attachment_fixtures() + seed_case_attachments() seed a case's documents through upload_pdf_to_blob_storage + services.attachment_extraction.extract_attachment - the SAME function the queue worker calls, not a fabricated extracted_json. Live smoke on attach_a1: EXTRACTED / PURCHASE_ORDER / PO-US-7001, then 1 llm call, 2162ms, answer carried all three required facts. Two bugs caught by smoking it before trusting it: tenant_id str vs UUID column, and (quieter) the loader's chat session differing from run_turn's, which made the agent answer 'I can't find that attachment' in 9ms with 0 LLM calls - would have scored all 17 attachment cases as model failures. 7 passed (loader, real Postgres) + 92 passed (cases/fixtures/judge). CASES now 57.
- 2026-09-07 17:55 Gap 477 WORKED: 69 -> 5 failures, 3319 -> 3450 passed, 268s -> 177s (live Azure calls gone from the unit suite). 24 live-endpoint mocked via Gap 471's build_llm pattern; 2 more were misclassified by me and were stale (Gap 470 reconcile default, Gap 473 contract-terms search). 38 stale re-baselined with a reason each, none deleted - incl. a new generic_flag_off fixture for the 26 tests that had been inheriting a default 1228edf reversed. Found and fixed 5 regressions from THIS phase's own work, incl. a false pass in my own loader test. The 5 environment failures are DEFERRED on token budget and stay [ ]: root cause established for the 2 workflow ones (Postgres rows vs a SQLite-wired TestClient - hard rule 2's exact shape), partially fixed, mock-auth path still 404s. All 5 were red before this phase and are no worse.
- 2026-09-07 18:18 RE-SCOPE 3 (founder 18:16). **P2.7 and CP2 removed** `[-]`, quote: "once luna and terra is compared and finalised we will code accordingly and mark feature completed, i dont want your nonsense testing and baselining." No further live run belongs to Feature 29. Definition of done rewritten to: Luna vs Terra on the 5 long-doc cases -> set the long_doc role + length switch and delete the losing deployment -> P2.1 entity resolver (unit tests only) -> 29.20 rollout plan -> close. **Gap 488** filed for the four long-doc harness defects: no facts merged for LONG_DOC_CASES, empty judge context on attachment turns, the statement case's 69 invoices never seeded, and the delivery-note question routed to `compare` instead of `read` -- the last two mean 2 of the 5 cases cannot pass for ANY model, so the Terra 3/3 and any Luna-vs-Terra delta are over **n = 3**.
- 2026-09-07 18:20 PHASE 1 EMAIL **NOT SENT** - founder 18:16: "no need to send email". Summary drafted at scratchpad/p1_summary.txt and left unsent. Phase 0 email was sent earlier (HTTP 202).
- 2026-09-07 29.10 DECIDED. Luna 3/3 vs Terra 3/3 on the gradable long-doc cases (2 harness-blocked, Gap 488); 2-pt rule keeps Luna. Founder: "delete terra", then "lets delete AZURE_OPENAI_LONG_DOC_DEPLOYMENT_NAME". Terra deployment deleted (account: gpt-5-mini, gpt-4o, gpt-5.6-luna); long_doc role + setting + get_long_doc_llm + bicep param/env + params keys removed; Gap 489. test_model_registry + test_model_role_env_wiring + test_capability_flags: 79 passed.
- 2026-09-07 P2.1 DONE (founder: "delete terra and complete feature 29 development"). agents/entity_resolver.py + wiring behind ENABLE_ENTITY_RESOLVER; Gap 490; tests/test_entity_resolver.py 17 passed on Postgres; neighbours 213 passed. One bug found and fixed in my own wiring before tests passed (get_settings not module-level in query_agent).
- 2026-09-07 P4.1 DONE: rollout plan written as spec §10.1; nothing applied; params.prod.json untouched.
- 2026-09-07 P4.2 DONE: tracker Feature 29 row [x]; spec §3/§5/§8/§9/§10/§11 updated. Not done in P4.2's original wording: "every Gap 470–479 ticked" — Gaps 477 and 478 remain open and are listed as open on the row, not ticked.
- Final status: CLOSED. Nothing committed; nothing pushed; no email sent.
