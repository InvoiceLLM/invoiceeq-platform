# Feature 29: LLM Optimisation — build
Spec: Prod_Invoice_LLM/apps/invoice-be/docs/feature_29_llm_optimisation.md
Started: 2026-09-06 20:26
Hard stop: run 1 2026-09-06 22:26 (2 h, founder). **Run 2 started 2026-09-06 22:30, hard stop 2026-09-07 02:30** (4 h default — founder gave none; scope "finishing track a-c").
Definition of done: every task below checked; every Verification Plan item run and cited;
spec body and tracker updated; changes uncommitted.
Status: run 2 complete (2026-09-07 00:43) — see "Run 2 final status" at the bottom.

Approval (founder, 2026-09-06, this conversation): "Approved to implement the whole feature solution to Azure env".
All nine §11 decisions ruled the same day; see the spec.

Supersedes `senior-dev-luna-migration-sql-chat-loop.md` (Step 5 items 5.5/5.6 = 29.7/29.8; Step 1 leftovers = 29.1;
Step 2 = 29.4; Step 4 = 29.5). That tasklist is closed into this one.

Execution order inside the 2-hour window (spec §10): 29.7 → 29.8 → 29.1 → 29.2 → 29.3 → 29.4 → 29.5 → 29.6 → 29.9 → 29.12
→ 29.10 → 29.11 → 29.13 → 29.14 → 29.15 → 29.16 → 29.17 → 29.18 → 29.19 → 29.20. Whatever is unticked at 22:26 stays
unticked with a note; nothing is ticked in a batch. Full-suite checkpoints after 29.8, 29.12, 29.10, 29.17.

Preflight notes (2026-09-06 20:26): nothing frozen is touched. `active-work.md` open contradiction "chat answer-contract
plumbing" (`routers/chat.py::MessageResponse` drops attachment keys) affects 29.9's provenance — file a Gap, do not
silently widen the response model.

## Track A — close the migration
- [x] 29.1 Registry: 5.6-family context → 1.05M/922k; `recall_note` on Luna; `long_doc` role + `get_long_doc_llm()` (falls back to fast). **Delete `gpt-5.6-sol` and `gpt-6-astra` now; keep `gpt-5.6-terra` until 29.10 decides the long_doc role** (decision 9). Update `model-deployment.bicep` comment. Set luna/luna/gpt-5-mini env on the six `caj-*-dev` jobs.
  - DONE run 2, 22:50. Registry: 5.6 context 1,050,000 + `max_input_tokens` 922,000, `recall_note` on Luna/Terra, `long_doc` role -> fast fallback, `get_long_doc_llm()`, Sol/Astra rows kept and marked DELETED. `az` found the desired state ALREADY in place: Sol and Astra are gone from `openai-invoicellm-dev` (only gpt-5-mini/gpt-4o/gpt-5.6-luna/gpt-5.6-terra remain) and all six `caj-*-dev` jobs already carry luna/luna/gpt-5-mini + api-version 2024-10-21, so **no `az` mutation was run** — every command and result is in the tracker bullet. `model-deployment.bicep` role comment corrected (it wrongly claimed Terra was deleted); `az bicep build` clean. `pytest tests/test_model_registry.py -q` -> **33 passed in 6.40s** (+5); `pytest tests/test_extraction.py tests/test_outbound_extraction.py -q` -> **22 passed in 6.78s**.
- [x] 29.2 Judge calibration: `tests/golden_calibration.json` with **30 hand-graded turns now, 100 with two raters before 29.13's flag flips** (decision 4); `run_agent_eval.py --calibration-set` reports Cohen's κ; **κ < 0.6 blocks every later accuracy claim in this feature.**
  - DONE run 2. `tests/golden_calibration.json` (36 turns from the 2026-09-06 golden run) + `--calibration-set` computing Cohen's kappa with no model call. **kappa = 0.151, n = 36, agreement 0.444 vs 0.346 expected by chance. THE GATE (>= 0.6) FAILS.** 2x2: both_pass 8, both_fail 8, judge_pass_human_fail 0, human_pass_judge_fail **20** -- the judge never passes what a reader would fail and fails 20 of 28 answers a reader marks correct. **Every accuracy claim in Feature 29 is now blocked by decision 4, including 29.5's 22.2%.** Verdicts are PROVISIONAL (`grader: "expected-text (founder to confirm)"`, `provisional: true`, `rater_count: 1`, and the file states that the rater is the same session that wrote the code under test); 6 entries marked FOUNDER TO CONFIRM. `pytest tests/test_eval_calibration.py -q` -> **17 passed in 6.78s**. The 29.8 probe numbers are deterministic regex grading and are unaffected.
- [x] 29.3 Failure taxonomy: `run_agent_eval.py --taxonomy` tags every failed golden turn with one of {no_route, no_evidence, wrong_evidence, no_computation, narration, judge}; baseline counts recorded.
  - DONE run 2. `--taxonomy` on `run_agent_eval.py`, deterministic, counts asserted to sum to the failure count, and the counts ride in every saved payload. **Baseline (28 failures of 36 on the 2026-09-06 run): wrong_evidence 9, no_computation 9, narration 4, judge 4, no_route 2, no_evidence 0.** Read `no_evidence = 0` with Gap 478 -- the harness's context always carries the SQL table, so the bucket cannot fire there. `pytest tests/test_eval_calibration.py -q` -> **17 passed**.
- [ ] 29.4 Harness fixes from tasklist Step 2: `--fresh-tenants`; SQL eval fixed denominator (timeouts = wrong, n reported); extraction report lists all ground-truth fields with the composite as headline. Luna baseline re-run on the fixed harness → `runs/luna-baseline-<date>/`.
  - **NOT STARTED in run 2 either.** Deliberate: its deliverable is a Luna baseline re-run on a fixed harness, and on this evening a single 36-case golden run took ~95 minutes against the throttled dev endpoint -- it could not have been finished AND verified before the hard stop. Its scope also GREW tonight: **Gap 478** (the harness grades `recorder.context()` and never sees the full-record / computed-figures / line-arithmetic blocks, so faithfulness is systematically understated and 29.5 is unmeasurable) and the second half of that gap (`CANDIDATE_LLM_PATCH_TARGETS` patches only `get_llm`, so a `--provider/--model` run does not override the narration call while `AZURE_OPENAI_FAST_DEPLOYMENT_NAME` is set) both belong here. Both are filed with proposed fixes and need a founder go before any code.

## Track B — evidence and contract (chat without attachment)
- [x] 29.5 `fetch_full_records()` + `full_record_block()`; wired into the SQL route after ids are known and into the general route when the resolver binds an invoice; **cap = 25** (decision 1); **summary call on gpt-5-mini for this route, Luna stays on the attachment branches** (decision 2). Golden re-run; target: `no_evidence` bucket → 0.
  - **TICKED 2026-09-07 (phase-2 P0.1): both blockers cleared and the task is MEASURED.** On the fixed judge, live (`runs/f29-golden-20260907/`, 36 turns): **pass 72.2% (was 22.2%), accuracy 0.757, faithfulness 0.891, errors 0, $0.0025/turn**, and **kappa 0.852** against the reference verdicts (gate >= 0.6 PASSES), so decision 4 no longer blocks the claim. **Gap 478 is proved fixed on live data**: `evidence_source = "app"` on 31/36 turns and `FULL INVOICE RECORD(S)` in the graded context on 22/36, against 0/36 on the same build on 2026-09-06 -- the harness can now see the block this task attaches. Taxonomy moved with it: `wrong_evidence` 9 -> 4, `no_computation` 9 -> 4, `narration` 4 -> 0, `judge` 4 -> 0. Original run-2 note follows.
  - **(run 2) CODE COMPLETE AND UNIT-VERIFIED, MEASUREMENT BLOCKED -> `[~]`.** `services/full_records.py` (`fetch_full_records`/`full_record_block`/`full_record_block_for`, cap 25, chunk-invoice bound 5, over-cap disclosure), `chat_summary` role + `get_chat_summary_llm()` (decision 2), `invoice_ids_named_in()`, wired on SQL + RAG + CHAT. `pytest tests/test_full_records.py -q` -> **12 passed in 8.79s on REAL Postgres**; `tests/test_chat_sql_quality.py` at its exact HEAD baseline (18 failed / 130 passed, all Gap 477). Golden re-run recorded: **pass 22.2%, acc 0.611, faith 0.751, p50 9.7s, $0.00248/turn** vs 22.2/27.8 as-is and 52.8 full-record. NOT ticked, because the run cannot see the block at all (**Gap 478**: the harness grades `recorder.context()`, so 0 of 36 turns show `FULL INVOICE RECORD(S)` on a build that attaches it three ways) and because **29.2's kappa = 0.151 blocks every accuracy claim**. Next action: founder decision on Gap 478's one-line fix, then re-run. **The WIRING is proved independently of the harness**: two tests in `tests/test_full_records.py` drive a real SQL-route turn on live Postgres and assert `FULL INVOICE RECORD(S)` + the line-arithmetic figures are in the prompt the route built, with provenance on the result -> `14 passed in 7.02s`. That run also caught a defect in this session's own code (`_chat_summary_llm()` reached `utils.llm.build_llm` and so bypassed both the harness's model override and any test patch of this module -- Gap 471's shape reintroduced); fixed to construct through this module's `build_llm`, like `_fast_llm()`.
- [x] 29.6 `check_line_arithmetic()` in `document_comparison.py`; wired into `_computed_figures_block_for()`; the three line-check golden cases pass with the figure computed.
  - DONE run 2. `check_line_arithmetic()` + `render_line_arithmetic()` in `services/document_comparison.py`, reading the invoice's own `items` (not the results table, which is why the golden line-check cases never got arithmetic); wired into `_computed_figures_block_for(db_result, record_set)` through a shared `_computed_block_wrapper()`, and it fires even with no results table at all. `pytest tests/test_compare_documents.py -q` -> **65 passed in 6.30s** (+12). The three golden line-check cases' *figures* are now computed in Python; whether they PASS the golden set cannot be asserted while 29.2's kappa gate fails and Gap 478 stands.
- [~] 29.9 `_answer_contract_gate()` + `_abstain_payload()` on every route; provenance on every claim. Abstain wording per decision 3: name what is missing, state what *is* on file, offer the nearest next step.
  - **STAYS `[~]` after phase-2 P0.1.** The abstain cases were re-measured on the live fixed-judge run and **5 of 6 hold**: `cross_currency_total_refused`, `india_no_due_date_refusal`, `zero_result_vendor` (accuracy 1.0 / faithfulness 1.0) and `zero_result_with_useful_redirect` (1.0 / 0.67) pass, and `zero_result_typo_vendor` fails with the reference agreeing. **One fails: `unsupported_field_asks_for_alternative`** -- accuracy 1.00 (1/1 required facts met) but faithfulness 0.0, because an abstention that says "this field does not exist" makes a *schema* claim and schema is not in the judge's evidence. That is **Gap 480** (investigation only, options filed, founder picks). The other half of the `[~]`, **Gap 474** (provenance does not reach the browser), is untouched: phase-2 P0.3 was explicitly NOT approved, so `routers/chat.py::MessageResponse` was not widened. Original run-2 note follows.
  - **(run 2) SERVER SIDE DONE AND VERIFIED; BROWSER-VISIBLE PROVENANCE STILL BLOCKED BY GAP 474 -> `[~]`.** `_answer_contract_gate()`, `_gate_evidence_numbers()`, `_gate_regeneration_directive()`, `_abstain_payload()` (decision 3's three clauses), `_abstain_on_file_from()`, `ENABLE_ANSWER_CONTRACT_GATE` (default on), `ChatTurn.answer_gate` telemetry, wired on the SQL route (gate -> one regeneration naming the figure -> abstain), and `result["provenance"]` from the FullRecordSet. `pytest tests/test_answer_contract.py -q` -> **18 passed in 5.91s**; `pytest tests/test_telemetry.py -q` -> **64 passed**. `routers/chat.py::MessageResponse` untouched as instructed, so provenance does not reach the browser. Next action: founder go on Gap 474.
- [x] 29.12 Answer cache keyed on `(tenant, plan-or-normalised-question, attachment ids)`; the content branch's documented cache bypass becomes unnecessary and is removed.
  - DONE run 2. `_attachment_dimension()` folds the sorted attachment ids into the key (no-attachment keys unchanged byte-for-byte, so nothing in Redis is orphaned); the documented bypass is removed and the attachment gate now reads/writes the cache, guarded by `_attachment_answer_is_cacheable()` (a confirm/clarify card is a question and is never replayed). Feature 26's B1 invariant amended, not deleted. `pytest tests/test_c2_cache_correctness.py -q` -> **16 passed in 9.56s**; `pytest tests/test_chat_doc_content_branch.py -q` -> **6 failed, 63 passed**, identical to its HEAD baseline.

## Track C — attachments
- [x] 29.7 Two attachments + invoice question: per-document comparison against each one's own invoice, merged; doc-to-doc only on explicit request. Probe turn B5 passes; A4 leaves the pair branch.
- [x] 29.8 `scripts/attach_chat_eval.py` versioned; 16-turn probe re-run on Luna / gpt-5-mini / Terra with cache flushed and fresh sessions; results recorded under Gaps 470, 472, 473 and this feature. **Gaps 470/472/473 tick here.**
- [ ] 29.10 `tests/golden_long_doc.json` (5 cases: multi-page contract, 3-page statement, 2 long POs, long delivery note); Terra vs Luna on the attachment branches; role decided by the 2-pt rule; env + bicep for `AZURE_OPENAI_LONG_DOC_DEPLOYMENT_NAME`.
  - **NOT STARTED in run 2 either — no `tests/golden_long_doc.json`, no Terra/Luna leg, no bicep or env change.** The seam is in place from 29.1 (`AZURE_OPENAI_LONG_DOC_DEPLOYMENT_NAME`, `long_doc` role, `get_long_doc_llm()`, all falling back to fast so nothing changes until a decision is made), so what remains is the measurement only. It was not started because it could not have been FINISHED and verified before the hard stop: two model legs over 5 multi-part long-document cases, on an evening where one 36-case golden run took ~95 minutes and a two-file pytest run took 70 minutes against the same throttled dev endpoint, and run 1's Terra leg had already stalled for 55 minutes. Starting it would have burned dev tokens for an unfinishable result. **A second, harder blocker was found while scoping it, and it changes the next action.** The eight probe PDFs in `runs/f29-probe-20260906/attach_docs/` are **single-page documents of ~2 KB each** (`ls -la` recorded in the log) -- they are not the 'multi-page contract, 3-page statement, 2 long POs, long delivery note' the task specifies, and a long-context recall gate built on 2 KB documents would measure nothing: Luna and Terra would both score whatever the agent logic scores, which is what the 29.8 probe already measured. **The long fixtures do not exist and have to be generated first** (the `benchmarks/_generate_fixture_pdfs.py` route), then uploaded, then run on two legs. **Third blocker, and the principled one:** spec section 9 says 29.10 is 'judged by the calibrated judge' -- and 29.2 has just measured that judge at kappa 0.151 against a 0.6 gate, so deciding a permanent model role on it tonight would be exactly the accuracy claim decision 4 forbids. Next action, in order: (1) founder go on Gap 478 + the judge, (2) generate the five long fixtures, (3) write `tests/golden_long_doc.json` from the text of those fixtures, (4) run Luna then Terra with a per-turn timeout and per-turn logging, killing a leg that stalls >10 min on one turn and recording the stall as evidence.

## Track D — grounding and the generic layer
- [ ] 29.11 `resolve_entities()`; automatic clarify on 0/>1; keyword lists demoted to fallback behind `ENABLE_CHAT_PLANNER=false` (still on) — measured by clarify-card rate on the probe → 0 for resolvable questions.
  - NOT STARTED at the hard stop. Nothing written.
- [ ] 29.13 `plan_turn()` + `CAPABILITIES` registry, flag-gated, **attachment branches first** (decision 8), gated by the 16-turn probe; then all routes once the golden set shows no regression and the 100-turn calibration (decision 4) is in; keyword lists deleted one release after the flag defaults on.
  - NOT STARTED at the hard stop. Nothing written. Gated on 29.2's 100-turn calibration in any case.
- [ ] 29.14 Semantic layer: 4 views + `METRICS` + `query_metric()` with a parameterised `WHERE tenant_id = :t` enforced by the AST guard, no RLS (decision 6); schema link prefers a metric; `alembic upgrade head` once. Target on the golden SQL set: +15 pts exec-correct vs 29.4's baseline.
  - NOT STARTED at the hard stop. No migration written, no Alembic revision created.
- [ ] 29.15 Certified examples: table + Chroma index + `retrieve_examples()` (certified only) replacing the C4 static examples tail; seeded from every golden case whose SQL is verified; `certify_example()`. **Global rows only at first; `tenant_id` nullable for the flywheel** (decision 5).
  - NOT STARTED at the hard stop. No migration written.
- [ ] 29.16 Knowledge layer: `knowledge_{tenant}` collection, `KnowledgeDoc`, `index_knowledge()`, `lookup_glossary()`, `lookup_rule()`; glossary seeded (total / grand total / net / tax / due).
  - NOT STARTED at the hard stop. Nothing written.
- [ ] 29.17 Regional rule cards — **India first** — one paragraph each with `source_url`, `effective_from`, `owner = founder`, `review_at`; the session drafts each card from a primary source it has fetched and cites (CBIC/GSTN), indexes it, and the founder spot-checks a sample (decision 7); then EU (EN 16931, mandates), then US. Playbooks (20–40) alongside. Compliance `verify` capability: deterministic field checks against a rule card's checklist.
  - NOT STARTED at the hard stop. **No rule card was authored, and none should be back-filled from memory** — decision 7 requires a primary source fetched and cited per card. No WebFetch against cbic.gov.in / gst.gov.in was made in this window.
- [ ] 29.18 Reranker (Cohere rerank-v4 on Foundry) between bge-m3 and the LLM — **only after** RAG recall@k is instrumented in 29.5 and shows ranking, not retrieval, as the limit. Chat tier pinned to minimal reasoning effort.
  - NOT STARTED at the hard stop. Correctly blocked anyway: it needs 29.5's recall instrumentation first.
- [ ] 29.19 Flywheel: `chat_correction` table, `record_correction()` on thumbs-down, `promote_to_example()`; control-tower workbook panel "chat quality trend" (pass %, abstain %, bucket counts by week) fed from `agent_eval_summary`.
  - NOT STARTED at the hard stop. Nothing written.

## Track E — rollout
- [ ] 29.20 Rollout plan dev → soak → prod written (not applied); prod params untouched.
  - NOT STARTED at the hard stop. Nothing written.

## Run 2 — Tracks A–C (founder 2026-09-06 22:28: "push the code and doc as per methodology to github master and then start the run for finishing track a-c")
Commits a9565de + b6605b1 pushed to origin/master at 22:29; tree clean at start. Order: 29.5 → 29.1 → 29.2 → 29.3 → 29.4 → 29.6 → 29.9 (server-side gate + abstain only; browser provenance stays blocked on Gap 474) → 29.12 → 29.10. Track D/E untouched this run. Full-suite checkpoint after 29.12 and at the end.

## Log
- 20:26 tasklist created; senior-dev dispatched in background; status mailer started (every 10 min → sbanerji@admsofttech.com).
- 20:31 29.7 start — read spec §5/§6/§11, CONVENTIONS, Gaps 470-473, `_run_attached_document_turn()` routing (line 4718) and `_run_attachment_pair_branch()` (line 5301). Plan: extract the attachment-vs-invoice computation into a reusable helper, add `_wants_doc_to_doc()` + `_run_attachment_multi_invoice_branch()`, reroute the two-attachment case.
- 20:40 29.7 DONE and ticked. `_wants_doc_to_doc()` + `_compare_attachment_to_invoices()` + `_run_attachment_multi_invoice_branch()` in `agents/query_agent.py`; routing rerouted; clarify-card bypass for two-document turns. Evidence: `pytest tests/test_chat_attachments.py -q` -> `47 passed in 26.63s` (42 -> 47); six attachment files together -> `144 passed in 37.73s` (139 -> 144). Tracker bullet 29.7 written; spec Build note added under section 6 (three deviations: any-confirmed not both; comparison verb required; clarify-card bypass).
- 20:41 29.8 start - formalise the 16-turn probe as `scripts/attach_chat_eval.py`.
- 20:45 29.8 - found the previous session's ad-hoc probe + its 8 PDFs in a scratchpad; wrote `scripts/attach_chat_eval.py` (versioned: argparse, `--flush-cache` via redis, UTC-stamped fresh sessions, deployments-in-force recorded in the JSON).
- 20:53 restarted backend + worker: the running uvicorn was started 18:41, BEFORE 29.7's code, so it would have measured the old branch. Killed by command line, restarted from `.venv`.
- 20:56 Luna probe run 1: **14/16** (was 8/16). BUT reading the answers, A4 PASSED on a WRONG net (475.20; the answer is 432.00) and B5 still ran the doc-to-doc diff. Two real defects, neither visible in the pass count.
- 21:07 filed and fixed **Gap 475** (credit note printing its own total negative was double-negated: sign taken from the doc type AND from the printed sign) and **Gap 476** (29.7's `_DOC_TO_DOC_PATTERN` matched the bare phrase "both documents", which is B5's opening words). 4 tests added; `pytest tests/test_compare_documents.py tests/test_chat_attachments.py -q` -> `102 passed in 17.96s`.
- 21:16 Luna probe run 2, after the fixes: **16/16** (single-doc 9/9, two-doc 3/3, no-attachment 4/4, p95 8.3s). A4 = 432.0 and equals the PO total; B5 = per-document, Redwood tax under-charged 123.75 vs 90.00 with the contract span quoted.
- 21:33 gpt-5-mini: **15/16** (was 10/16). Sole failure B2 is a grading artefact - the model says the quantities match and then mentions an extra freight line, tripping the `forbidden` pattern.
- 21:44 Terra leg started; stalled with an empty log for ~55 min and was killed at 22:05. Not run. Moved to 29.10, which is where the long_doc role is decided anyway.
- 22:06 `.env` restored from backup to luna/luna; backend + worker restarted on it and healthy. The machine is as it was found.
- 22:07 tracker written: Gaps 470/472/473 ticked with the probe evidence; Gaps 474 (filed, NOT fixed - needs founder go), 475 and 476 filed with symptom/root cause/fix/evidence; Feature 29 row carries the 29.7 and 29.8 bullets.
- 22:12 STOPPED AT HARD STOP. 2 of 20 tasks ticked (29.7, 29.8), both actually verified against real Postgres and real dev Azure. 18 unticked, each with a one-line state note above. **Nothing committed** - every change is uncommitted in the working tree. Full-suite checkpoint after 29.8 was NOT run (no time); the six attachment-related files were run instead and are green at `144 passed`, plus `102 passed` on the two files the Gap 475/476 fixes touch.

## Final status (hand-back 2026-09-06 22:14)
Stopped at the founder's 2-hour hard stop. 2/20 ticked and verified (29.7, 29.8); Gaps 470/472/473 closed on the probe re-run; Gaps 475/476 filed and fixed; Gap 474 filed, awaiting founder go. 18 tasks not started, each with a state note above. No Azure change made. Nothing committed; HEAD unchanged. Next session resumes at 29.5 (full records) then 29.1.
- 22:30 run 2 started (Tracks A–C); senior-dev dispatched; mailer restarted, hard stop 02:30.
- 22:22 29.5 start — reading `agents/query_agent.py` SQL/general routes, `chroma_client.py`, `config.py`, `utils/llm.py` to place `services/full_records.py`.
- 22:38 29.5 — `services/full_records.py` written (fetch_full_records/full_record_block/full_record_block_for), cap 25 + chunk-invoice bound 5 in `config.py`, `chat_summary` role in the registry + `get_chat_summary_llm()`, `_full_record_block_for()` delegated, `invoice_ids_named_in()` added, and the block wired on ALL THREE routes (SQL, RAG, CHAT) with the summary model switched to gpt-5-mini whenever a full-record block is present. `pytest tests/test_full_records.py -q` -> **12 passed in 8.79s** on real Postgres.
- 22:38 29.5 — found while verifying: `tests/test_chat_sql_quality.py` has **18 pre-existing failures on HEAD** (`git show HEAD:...query_agent.py` swapped in and restored -> `18 failed, 130 passed`), all the Gap 471 live-Azure shape. They are NOT in the standing 43-failure baseline, so the baseline is stale. Filing as a new Gap. My change adds exactly 3 more, all in the two bound tests that assert Gap 310's old cap-of-3 behaviour that decision 1 replaced — updating those tests to the ruled behaviour.
- 22:51 29.1 DONE and ticked (details in the item note + tracker bullet). Gap 477 filed (18 pre-existing `test_chat_sql_quality.py` failures, Gap 471 shape, baseline stale) — investigation only per the discuss-before-fixing rule.
- 22:51 29.5 code complete; the 36-case golden re-run is running against dev Azure (`docs/extraction_benchmark/runs/f29-golden-20260906/`). 29.5 stays unticked until its numbers are recorded. Starting 29.2 (judge calibration) while it runs.
- 22:53 29.5 golden run RESTARTED. The first attempt was launched with `--out <directory>`; `run_agent_eval.py` treats `--out` as a FILE path and would have crashed on `open()` after every paid turn had already run. Caught by reading the script's write path rather than by waiting for the crash; the run was killed at ~10 min and relaunched with `--out .../agent_eval_output.json` and `--no-mirror`. Cost of the mistake: ~10 minutes of dev tokens, no data.
- 23:08 29.6 code + tests DONE: `check_line_arithmetic()` + `render_line_arithmetic()` in `services/document_comparison.py`, wired into `_computed_figures_block_for(db_result, record_set)` via a shared `_computed_block_wrapper()`; the SQL route now fetches the record set ONCE (`_full_record_set_for()`, same dependency span) and feeds both the full-record block and the line check. `pytest tests/test_compare_documents.py -q` -> **65 passed in 6.30s** (53 -> 65, +12). `pytest tests/test_chat_attachments.py tests/test_dependency_spans.py tests/test_full_records.py -q` -> **74 passed**.
- 23:08 29.9 code + tests DONE (server side): `_answer_contract_gate()`, `_gate_evidence_numbers()`, `_gate_regeneration_directive()`, `_abstain_payload()`, `_abstain_on_file_from()`, flag `ENABLE_ANSWER_CONTRACT_GATE` (default on), `ChatTurn.answer_gate` telemetry field, wired on the SQL route (one regeneration naming the figure, then abstain), and `result["provenance"]` populated from the FullRecordSet. `pytest tests/test_answer_contract.py -q` -> **18 passed in 5.91s**; `pytest tests/test_telemetry.py -q` -> **64 passed in 45.26s**. Browser-visible provenance stays blocked on Gap 474 as instructed — `MessageResponse` untouched.
- 00:41 29.2 + 29.3 DONE and ticked from the completed golden run. **kappa = 0.151 (n=36), gate >= 0.6 FAILS** — the judge fails 20 of 28 answers a reader marks correct and passes none it would fail. Taxonomy baseline: wrong_evidence 9, no_computation 9, narration 4, judge 4, no_route 2, no_evidence 0.
- 00:41 Gap 478 filed: `run_agent_eval.py` grades `recorder.context()` and throws away the turn's own `judge_evidence.context`, so the full-record / computed-figures / line-arithmetic blocks are invisible to the judge and to the taxonomy — 0 of 36 turns show the block on a build that attaches it three ways. Investigation only; proposed one-line fix written; belongs to 29.4.
- 00:41 Wiring proved outside the harness: 2 tests in `tests/test_full_records.py` drive a real SQL-route turn on live Postgres and assert the block + the line arithmetic are in the built prompt -> **14 passed in 7.02s**. That test caught a defect in this session's own `_chat_summary_llm()` (reached `utils.llm.build_llm`, bypassing the harness override and any patch of this module — Gap 471's shape); fixed.
- 00:41 Regression batch (8 files incl. the two the 29.12 change touches): **18 failed, 341 passed in 105.37s** — the 18 are `test_chat_sql_quality.py`'s HEAD baseline name-for-name (Gap 477), every other file green. Earlier sweeps: 11 files -> 2 failed/217 passed (both pre-existing, proved by a HEAD swap); 10 files -> 9 failed/417 passed (all in the standing baseline's named ops/agent_eval areas).
- 00:41 **Full suite NOT run.** Justified in the tracker: with the Luna `.env` in place a two-file baseline run took 4,213 s tonight against the throttled dev endpoint (35 s when the endpoint was idle), so `pytest -q --ignore=tests/us` could not be guaranteed to finish before the hard stop, and a checkpoint killed half-way is not a checkpoint. Targeted files were run instead and every failure is accounted for.

## Run 2 final status (2026-09-07 00:43, ended before the 02:30 hard stop)
Ticked and verified this run: **29.1, 29.2, 29.3, 29.6, 29.12**. Advanced to `[~]` with code complete and
unit-verified but a stated blocker: **29.5** (Gap 478 -- the golden harness cannot see the block it attaches)
and **29.9** (Gap 474 -- provenance does not reach the browser until `MessageResponse` is widened, which needs
a founder go). Not started, each with a written reason: **29.4** (its deliverable is a ~95-minute baseline
re-run and its scope grew tonight with Gap 478) and **29.10** (the long fixtures do not exist, and its stated
verification needs a calibrated judge that 29.2 has just shown is not calibrated). Track D/E untouched by scope.

Gaps filed this run: **477** (18 pre-existing `test_chat_sql_quality.py` failures, Gap 471's shape, standing
baseline stale) and **478** (the harness grades a narrower evidence set than the model was given). Both are
investigation-only with proposed fixes, per the discuss-before-fixing rule. One defect in this session's own
code was found and fixed before it shipped (`_chat_summary_llm()` bypassing this module's `build_llm`).

The single most consequential number of the run is **29.2's kappa = 0.151**: the LLM judge fails 20 of the 28
golden answers a reader marks correct against the bank's own reference text, and passes none it would fail. Until
that clears, no pass-rate figure in this feature -- including 29.5's 22.2% -- is a measurement of the product.

Nothing was committed. Every change is uncommitted in the working tree; `git log origin/master..HEAD` is empty.
Status: run 2 complete (Tracks A-C as far as they can go without founder decisions).
- 00:43 machine left consistent: backend + queue worker restarted from `.venv` onto the current (uncommitted) code and healthy (`/health` 200, worker process up); `.env` was never modified this run and still reads luna / luna / gpt-5-mini.
