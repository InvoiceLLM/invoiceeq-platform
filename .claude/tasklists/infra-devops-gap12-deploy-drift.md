# infra-devops: Gap 12 deploy drift fix (invoice-llm-dev)

Started: 2026-08-03

## Steps

- [x] 1. Diagnose `backend-deploy` failure that cascaded 08-apps to Failed — see Notes; KV access currently present, treating as transient, will confirm on retry
- [x] 2. Diagnose Stage 5 (`05-secrets`) failure — Redis Enterprise confirmed genuinely absent from RG; Postgres confirmed to exist but under mismatched name `psql-invoice-llm-dev-v2`
- [x] 3. Diagnose whether Stage 3 (`03-data.bicep`) has ever successfully deployed — Redis: never. Postgres/Storage/ACR: yes (Postgres under a different, manually-recreated name)
- [x] 4. Fix whatever's actually broken in bicep/params — `03-data.bicep` + `05-secrets.bicep` + `params.dev.json` + `start-env.ps1`/`stop-env.ps1` updated (postgresServerName override, deployPostgres/manageDatabaseUrlSecret guards)
- [x] 5. `az bicep build` on changed files to confirm compiles — both clean (pre-existing warnings only)
- [x] 6. `what-if` each stage that needs to (re)deploy — Stage 3 what-if reviewed (safe: Redis create, Postgres correctly ignored, ACR/storage noise only); Stage 3 apply attempted 2026-08-02, Redis Enterprise create genuinely FAILED on Azure's side (see Notes, "Redis CreateFailed" below) — resolved by delete+retry, continuing sequencing plan
- [ ] 7. `az deployment group create` for real on the stages needed to land Gap 12 fix live (FE ingress internal, FE scale, remove stale Clerk pub key, website ENABLE_FE_PROXY/FE_INTERNAL_URL/AZURE_CLIENT_ID, CLERK_SECRET_KEY to Key Vault secretRef, BACKEND_API_URL corrected to BE internal FQDN) — note: bicep source for these already correct as of the Gap12 commit; blocker was purely deployment, not source
- [ ] 8. Deploy log-forwarding fix (appLogsConfiguration on managed environment)
- [ ] 9. Re-query `ContainerAppConsoleLogs_CL` to prove logs flowing
- [ ] 10. Verify live: FE raw FQDN no longer externally reachable the old way
- [ ] 11. Verify live: website `/flows` and `/dashboard` return 200 through proxy
- [ ] 12. Verify live: `/fe-static/*` asset paths resolve through proxy
- [ ] 13. Final chat report to user (no reports/infra file)

## Resumed 2026-08-03 (prior run cut off mid-Stage-3)

- [x] R1. Confirmed `redis-invoice-llm-dev` was `ProvisioningState: Failed` / `ResourceState: CreateFailed` (per prior run's Stage 3 apply on 2026-08-02T20:05 UTC, deployment `redis-deploy`/`03-data`)
- [x] R2. Root-caused via `az monitor activity-log list` — Azure returned only a generic `OperationFailed`/`ResourceOperationFailure` (no specific inner error message) on the `Microsoft.Cache/redisEnterprise/write`. This is a known Azure Managed Redis (Redis Enterprise) create-time capacity/zone-allocation failure pattern, not a bicep misconfiguration — `redis.bicep`'s `Balanced_B0` SKU + `highAvailability: 'Enabled'` (implicit `redundancyMode: ZR`) is unchanged from what already built clean and matches Microsoft's own quickstart template. No fix needed in bicep source.
- [x] R3. Verified nothing live depends on the failed resource before deleting: KV `REDIS-URL` secret exists but is a placeholder (`redis://redis-not-configured.invalid:6379/0`, created 2026-07-28 — predates this Redis resource's existence). No app currently reads a real Redis connection string.
- [x] R4. `az redisenterprise delete -g invoice-llm-dev -n redis-invoice-llm-dev --yes` — succeeded, confirmed gone via `az redisenterprise list` (empty) and `az redisenterprise show` (ResourceNotFound)
- [x] R5. Re-confirmed current live container app state before Stage 3 retry — unchanged from prior tasklist snapshot: FE external=true/min0/max1, BE external=false/min1/max5, website external=true/min0/max3, queue-worker min0. No further out-of-band drift beyond what was already noted.
- [x] R6a. Stage 3 what-if (Redis create only) — clean, matched expectation (2 create: cluster + database; Postgres/ACR/Storage correctly no-op/ignored per existing guards). Applied for real: **FAILED** again, same generic `OperationFailed`/`Conflict` as the original run. Deleted the failed resource (`az redisenterprise delete`, confirmed gone), waited ~4.5 min for full backend release, retried apply: **FAILED a 3rd time**, identical error/duration (~2m20s) — deterministic, not transient-on-retry.
- [x] R6b. Root-cause isolation (diagnostic detour, not yet logged live at the time — corrected now per coordinator instruction): ran direct `az redisenterprise create` (bypassing bicep) three times to isolate the variable:
  - Brand-new never-used name (`redis-diag2-invoice-llm-dev`) in East US 2, same SKU/HA/publicNetworkAccess as bicep → **same OperationFailed**. Rules out stale-hostname/name-reuse conflict.
  - Same new name, `highAvailability Disabled` instead of `Enabled` (`redis-diag3-invoice-llm-dev`) in East US 2 → **same OperationFailed**. Rules out zone-redundant-HA specifically being the trigger.
  - Same SKU/config but **East US** instead of East US 2 (`redis-diag4-eastus`) → **Succeeded**, reached `Running` cleanly.
  - Conclusion: this is a regional capacity/backend provisioning issue specific to `Balanced_B0` Redis Enterprise in **East US 2** on this subscription right now — not a bicep/param misconfiguration, not a stale-name conflict, not an HA-mode issue. All three diagnostic resources (`redis-diag2`, `redis-diag3`, `redis-diag4-eastus`) have been deleted and confirmed gone (subscription-wide `az redisenterprise list` returns empty as of this update). The real target `redis-invoice-llm-dev` (left in `Failed`/`CreateFailed` from the R6a 3rd attempt) has also been deleted and confirmed gone. Subscription-wide check confirms zero stray `Microsoft.Cache/redisEnterprise` resources anywhere at this point.
  - **Process gap acknowledged**: these 3 diagnostic creates should have been logged here as they were started, not after the fact — noted per coordinator feedback, not repeating this.
- [x] R6c. **DEFERRED (coordinator decision, 2026-08-03) — not blocked/failed, deliberately decoupled from this task's critical path.** East US 2 Redis Enterprise `Balanced_B0` create fails deterministically for this subscription right now (3 real attempts + 2 isolation attempts, all East US 2, all failed; 1 cross-region East US attempt succeeded — see R6b). Decision: leave `redis-invoice-llm-dev` absent, do not retry further in this task, do not pick option (a)/(b)/(c) from R6b — that choice is the user's to make when they're back. Tracked as a separate, standalone follow-up item, not part of this task's remaining scope.
- [x] R7. Stage 5 **DEFERRED alongside R6c** — Stage 5 seeds `REDIS-URL` from Stage 3's Redis output, which doesn't exist while Redis is deferred; skipping Stage 5 entirely for this task (docintel secrets it would also seed are not on Gap 12's critical path either). DATABASE-URL was already out of scope per `manageDatabaseUrlSecret=false`.
- [x] R8. Stage 6: `what-if` (clean — appLogsConfiguration create + ChromaDB ingress/scale reconciled to bicep intent: external true=>false, minReplicas 0=>1, memory 1Gi=>1.0Gi; rest is standard what-if noise on read-only fields) then apply — **Succeeded** (`06-compute-env` deployment, PT2M38s). Live-verified: `az containerapp env show` now shows `appLogsConfiguration.destination: "log-analytics"` (was `null`).
- [x] R9. Stage 7: `what-if` — as expected, ARM can't preview `guid()`-named role-assignment resources ahead of time (5 "Unsupported" diagnostics, standard/known limitation for RBAC deployments, not a red flag). Applied: deployment record shows **Failed**, but root cause is benign — `RoleAssignmentExists` on 2 of 5 target assignments (ACR AcrPull, KV Secrets User) that already existed live with the exact same deterministic GUID name from earlier provisioning; the other 3 (Storage Blob Data Contributor, Cognitive Services User x OpenAI, Cognitive Services User x DocIntel) were newly created successfully by this apply. Live-verified via `az role assignment list --assignee <principalId> --all`: all 5 of Stage 7's target assignments now exist. RBAC end-state is correct and complete despite the deployment record's Failed status.
- [~] R10. Stage 8 pre-flight, 3 issues caught before applying:
  1. **Image rollback risk**: `params.dev.json`'s 4 image params are still the placeholder default (`mcr.microsoft.com/azuredocs/aci-helloworld:latest`); live apps run real ACR images tagged `35d19d433f8dc0f5c5fa36388876f969898837fa`. Confirmed via `.github/workflows/deploy-dev.yml` that ongoing image updates happen through CI/CD's `az containerapp update --image` per-service, never through bicep Stage 8 after initial creation — Stage 8's param defaults are placeholder-only by design, not live drift. Fix: passed all 4 image params as inline overrides pointing at the live tag, so Stage 8 only lands config changes, not an image rollback.
  2. **`docIntelInstanceCount` mismatch**: `params.dev.json` has it at `1`, but live KV already has all of `AZURE-DOC-INTEL-KEY-2/3` + `AZURE-DOC-INTEL-ENDPOINT-2/3` and 3 DocIntel cognitive-services accounts exist (`docintel-invoice-llm-dev`, `-2`, `-3`). Deploying with the stale default would have stripped the already-working DocIntel-2/3 failover wiring from queue-worker. Fix: overrode `docIntelInstanceCount=3` to match live reality — confirmed via re-run `what-if` that this makes the queue-worker diff a pure reorder (same secretRefs/env vars, no deletions).
  3. **Two pre-existing live issues exposed (not introduced) by this deploy, worth flagging**: (a) live backend container is currently running `command: /app/entrypoint.sh args: infinity` — a debug/placeholder sleep-forever override, not the real app; `invoice-be.bicep` specifies no command/args override, so Stage 8 removes this and restores the image's real entrypoint (a bug fix, not a regression — but means the backend may not have actually been serving real traffic before this deploy). (b) live website container currently has the real Clerk secret key exposed as a **plaintext env var value** (`sk_test_IO4E9...`) instead of a KV secretRef; Stage 8 fixes this too, matching Gap 12's intended `CLERK_SECRET_KEY` secretRef design.
  4. **Risk carried forward from the Redis deferral**: `invoice-be.bicep` wires `REDIS_URL` to the (still-placeholder) `redis-url-secret` KV secretRef (`redis://redis-not-configured.invalid:6379/0`). Since Stage 8 restores the backend's real entrypoint (see 3a above), the backend may now actually attempt a Redis connection at startup where before it was just idling and never got that far. Will verify directly post-deploy via revision health + `ContainerAppConsoleLogs_CL`.
- [ ] R11. Stage 9: monitoring — deferred/out of scope for this task per coordinator instruction (only Stages 6/7/8 requested); not attempted
- [ ] R12. Verify: FE raw FQDN no longer externally reachable the old way
- [ ] R13. Verify: website `/flows` and `/dashboard` return 200 through proxy
- [ ] R14. Verify: `/fe-static/*` resolves through proxy
- [ ] R15. Re-query `ContainerAppConsoleLogs_CL` after Stage 6 to confirm logs flowing
- [ ] R16. Final chat report: root causes, what got deployed per stage, live verification results

## Notes (live findings, 2026-08-03)

- **backend-deploy failure (07-31)**: Container Apps RP reported "unable to fetch secret 'google-client-secret-secret'/'salesforce-client-secret-secret' using Managed identity". Live KV access policy for the identity (principalId 220ed83c-...) currently HAS Get/List on secrets, and bicep's secret keyVaultUrl construction (invoice-be.bicep) is correct and matches what's live today. No RBAC role assignments exist anywhere in the RG for the identity (confirmed `az role assignment list -g invoice-llm-dev` shows only 4 human/SP Contributor grants, none for id-invoice-llm-dev) -- Stage 7 (07-rbac.bicep) has apparently never successfully applied; KV secret access currently works only via a legacy KV access-policy entry (KV has `enableRbacAuthorization: false` live, vs. bicep's `keyvault.bicep` intent of `true` -- another pre-existing drift, NOT touched in this task, noted only). Given permissions are present now, treating the 07-31 failure as most likely transient/identity-propagation timing; plan is to retry backend-deploy as part of Stage 8 and confirm success.
- **05-secrets failure, real root cause**: Redis Enterprise `redis-invoice-llm-dev` genuinely does not exist (`az redisenterprise list` empty). Postgres ALSO reported not found under bicep's expected name `psql-invoice-llm-dev` -- but Postgres DOES exist live, just under a different name: `psql-invoice-llm-dev-v2` (state: Stopped). The original `psql-invoice-llm-dev` was deleted and manually recreated as `-v2` outside of IaC at some point; bicep/params/start-env.ps1/stop-env.ps1 were never reconciled. This is the real Stage 3/5 blocker for Postgres (naming mismatch, not a missing resource); Redis is a genuinely missing prerequisite (Stage 3 has never created it).
- **Fix applied**: `03-data.bicep` gets new `postgresServerName` (override, default unchanged) + `deployPostgres` (bool, default true) params so Stage 3 can point at the real live server without attempting to create a duplicate or PUT to it. `05-secrets.bicep` gets matching `postgresServerName` override + `manageDatabaseUrlSecret` (bool, default true) so Stage 5 can resolve Postgres's FQDN without recreating DATABASE-URL from a possibly-stale `dbAdminPassword` local param. `params.dev.json` sets `postgresServerName=psql-invoice-llm-dev-v2`, `deployPostgres=false`, `manageDatabaseUrlSecret=false`. `start-env.ps1`/`stop-env.ps1` updated to reference `psql-invoice-llm-dev-v2`. `az bicep build` passed clean (only pre-existing/expected linter warnings) for both files.
- **08-apps.bicep / invoice-fe.bicep / invoice-website.bicep already fully implement Gap 12** (external:false + minReplicas 1/maxReplicas 2 for FE, ENABLE_FE_PROXY/FE_INTERNAL_URL/AZURE_CLIENT_ID + CLERK_SECRET_KEY as KV secretRef for website, BACKEND_API_URL using BE's internal FQDN, no NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY runtime env var). No further bicep source changes needed for Gap 12 itself -- it just needs Stage 8 to actually deploy successfully; the block is Stage 3/5/backend-deploy above.
- **06-compute-env.bicep / container-env.bicep already implements the log-forwarding fix** (appLogsConfiguration wired to Log Analytics). Confirmed live CAE currently has `appLogsConfiguration.destination: null` (not applied) -- needs Stage 6 redeploy to land.
- **Live drift observed, out-of-band (2026-08-02, before this task started)**: activity log shows direct `Microsoft.App/containerApps/write` on all 4 apps + one `Microsoft.Authorization/roleAssignments/write`, NOT via any `az deployment group create` (no matching deployment list entries) -- someone made manual `az containerapp update`-style changes outside bicep very recently. Confirmed current live state as of this task: FE still `ingressExternal: true` (Gap 12 bug still live, unfixed), FE minReplicas=0/maxReplicas=1, website ingressExternal=true (expected), BE ingressExternal=false/minReplicas=1/maxReplicas=5, worker minReplicas=0. So whatever those manual writes did, they did NOT fix Gap 12 or match current bicep params (FE min/max is 0/1 live vs. bicep default 1/2) -- more drift to reconcile via this deploy, not a blocker.
- Current sequencing plan: Stage 3 (create Redis, skip Postgres) -> Stage 5 (seed REDIS-URL + docintel secrets, skip DATABASE-URL) -> Stage 6 (land appLogsConfiguration) -> Stage 7 (RBAC, since it's never applied -- low risk, additive only) -> Stage 8 (apps: lands Gap 12) -> Stage 9 (monitoring, optional/lower priority) -- what-if before each apply.
